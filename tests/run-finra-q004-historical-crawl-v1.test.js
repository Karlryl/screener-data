'use strict';const assert=require('node:assert/strict'),cp=require('node:child_process'),path=require('node:path'),test=require('node:test');const R=path.resolve(__dirname,'..'),S=path.join(R,'scripts','run-finra-q004-historical-crawl-v1.py');function run(args,opt){return cp.spawnSync('python',[...(opt?['-O']:[]),S,...args],{cwd:R,encoding:'utf8',windowsHide:true});}test('FINRA historical crawl is resumable, private, and fail-closed',()=>{for(const opt of [false,true]){let p=run(['verify-contract'],opt);assert.equal(p.status,0,p.stderr);p=run(['self-test'],opt);assert.equal(p.status,0,p.stderr);const x=JSON.parse(p.stdout);assert.ok(Object.values(x.kills).every(Boolean));assert.equal(x.outcomesAccessed,false);}});
