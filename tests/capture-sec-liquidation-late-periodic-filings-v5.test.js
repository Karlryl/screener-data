#!/usr/bin/env node
'use strict';const assert=require('assert'),path=require('path'),{spawnSync}=require('child_process');const root=path.resolve(__dirname,'..'),runner=path.join(root,'scripts','capture-sec-liquidation-late-periodic-filings-v5.py');function run(c,o=false,remote=true,code=0){const a=[...(o?['-O']:[]),'-B',runner,c,...(remote?['--remote']:[])],r=spawnSync('python',a,{cwd:root,encoding:'utf8'});assert.strictEqual(r.status,code,`${a.join(' ')}\n${r.stdout}\n${r.stderr}`);return code?null:JSON.parse(r.stdout)}for(const o of [false,true]){const r=run('dry-run',o);assert.strictEqual(r.capturedCandidates,119);assert.strictEqual(r.remainingCandidates,3);assert.strictEqual(r.incidentCount,6);assert.strictEqual(r.pendingIncidentRequestSequence,126);assert.strictEqual(r.nextRetryRequestSequence,127);assert.strictEqual(r.maximumResponseBytes,500000000);assert.strictEqual(r.networkRequests,0);assert.strictEqual(r.writes,0);assert.strictEqual(r.outcomesAccessed,false);const s=run('self-test',o);assert.strictEqual(Object.keys(s.mutationKills).length,7);assert.ok(Object.values(s.mutationKills).every(Boolean));run('dry-run',o,false,2)}console.log(JSON.stringify({status:'PASS',capturedCandidates:119,pendingIncidentRequestSequence:126,outcomesAccessed:false}));
