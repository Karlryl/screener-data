# 5.2 Small-Cap-Board — PRÄREGISTRIERUNG (eingefroren 2026-07-21, VOR dem Bau)

> DIAGNOSTIC-Debüt (kein Score-Gewicht bei Start). Friert die Design-Weichen VOR
> dem Bau ein (Anti-Fudge). Grundlage: Gauntlet 21.07. — Council (2 Linsen) → Court
> DENIED-für-autonom (Codex-Mitrichter 10/10) → Karl-Entscheid E-20260721-4 „Voll
> ausstatten" (A1 echte Verwässerungs-Warnung + B revisionsMomentum raus).
> Akten: Vault `_5.2-DUELL-AKTE-2026-07-21.md`, Auflagen `_KARL-ENTSCHEIDE.md` E-20260717-2.

## Board-Definition (eingefroren)
- **Universe:** US-Operating-Companies, Marktkapital-Floor **$300M**, Small/Micro-Cap.
  Operating-Company-Filter + US-Filter wie 5.1-Probe (`probe-smallcap-*.js` R1–R6).
- **Kohorten:** EIGENE Small-Cap-Perzentile (nicht die All-Cap-Kohorten). `MIN_COHORT_N=15`
  **beibehalten** (Court-Auflage 3): bestehender Live-Wert, SE-gematcht mit `SHRINK_K=2`
  (score.js:193-213) → am Schwellwert kein Sprung; KEINE neue Magic Number, kein Absenken.
  1-Stufen-Fallback (Kohorte→Eltern, bestehend) fürs Debüt; 3-Stufen-Kaskade ERST später
  (ist neuer Code, nicht nulltouch).
- **Achsen: 7** — die HG-Achsen OHNE `revisionsMomentum` (Court-Auflage/Karl-Nod E-20260721-4 B:
  Yahoo-only, 0% SEC-Abdeckung, nicht querverifizierbar, kollidiert mit dem Selektionsbias den
  Auflage 4 misst). Achsen-Set-Änderung ist präregistriert (GG7) — hier dokumentiert + Karl-genickt.
- **Coverage-Floors:** data-abgeleitet je Achse aus der Verteilung (renorm-on-drop, bestehende
  `coverageWeight`/`weightedScore`-Maschinerie). KEIN Uniform-Floor (freie Konstante = GG4-Bruch).
- **Liquidität:** NUR Anzeige (Float/Volumen), nie Score (GG1).
- **Start als DIAGNOSTIC**, Fail-loud bei Coverage-Ampel-Abweichung, NO-GO-Ausweg bei dünnen Daten (GG2a).

## Verwässerungs-Warnung (Karl E-20260721-4 A1 — neue Datenquelle autorisiert)
- **Lampe B `shareCountDilution`:** Aktienzahl-WACHSTUM über Zeit aus SEC-XBRL
  (`dei:EntityCommonStockSharesOutstanding`, Vorlage `lib/sec-pit.js:sharesHistory`).
- **Datenweg:** `build-secannual.js` um `annualShares` erweitern → `sec-secannual.json`
  regenerieren (companyfacts.zip lokal, 1.3G) → `mergeSecIntoUniverse` assignt `annualShares`.
  Die SEC-annual-Schicht ist bereits netzfrei-verdrahtet (kein neuer Live-Mechanismus, nur neues Feld).
- **Schwelle data-abgeleitet** (Kohorten-Perzentil des shares-Wachstums), KEINE handgesetzte Konstante (GG4).
- Die bestehende SBC/Rev-Lampe (`highDilution`) bleibt als Display; `highDilutionSharp` wird NICHT gebaut
  (wäre 2. Magic Number). Vorzeichen/Reverse-Split sauber behandeln (Split verzerrt die Roh-Aktienzahl).

## Karls 5 Hardcore-Auflagen (bindend, VOR Board-an-Karl; Court-präzisiert)
1. **Stichprobe ≥10 Namen**, jede der 7 Achsen gegen echtes 10-Q/10-K nachgerechnet (Muster E-20260714-2).
2. **Getrennt (Court-Auflage 4):** (a) **Verwässerung** — Lampe B an echten Aktienzahl-Dilution-Fällen
   bewiesen (Positiv-Fixture leuchtet, sauberer Wert nicht); (b) **Manipulation** — separat definierte
   Lampe (z.B. AR-Divergenz/Channel-Stuffing) mit eigenem Positiv/Negativ-Fixture. Beide Mechanismen
   + Fälle sind VOR dem Test zu benennen.
3. **Coverage-Gate live belegt:** Zähl-Beleg im Run-Log (Firmen unter Achsen-Mindest-Coverage fliegen
   nachweislich raus, `exclude`+`reason`-Pfad).
4. **Out-of-sample mit bestanden/nicht-bestanden-Maß (Court-Auflage 4):** erster Board-Vintage OHNE
   Re-Tuning; Kriterium = rankIC-Vorzeichen-Konsistenz OOS (nicht nur „angesehen"); die 5.1-Yahoo-
   Selektionsbias (BH-014) explizit gegengemessen (Rejection-Sampling `probe-smallcap-messlauf3.js`).
5. **Kreuz-Review-Lane (§4c) PFLICHT** für den Board-Commit (scoring-nah = harter Trigger).
Fällt eine der fünf: Board geht NICHT an Karl, Befund in die Queue.

## Bau-Reihenfolge (startbereit; jeder Schritt einzeln testbar)
1. `build-secannual.js` + `annualShares` (Extraktion via companyfacts.zip) → sec-secannual.json regen.
2. `mergeSecIntoUniverse` assignt annualShares; `lamps.js` Lampe B (data-abgeleitete Schwelle) + Fixtures.
3. 5.2-Board: Universe-Filter ($300M/US/Operating), eigene Kohorten, 7 Achsen, Coverage-Gate + Run-Log.
4. `board-status.js` DIAGNOSTIC-Registrierung, `outputs/findash-export/v1/`, eigene Vintage-Reihe.
5. Die 5 Auflagen fahren; Kreuz-Review; erst dann Court-Freigabe an Karl.

## Nicht-Ziele
Kein core-Gewicht bei Start. Keine 3-Stufen-Kaskade, kein highDilutionSharp, kein Uniform-Floor,
kein revisionsMomentum im Track. Kein Board-an-Karl vor allen 5 Auflagen + Kreuz-Review.
