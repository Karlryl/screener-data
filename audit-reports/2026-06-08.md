# Pipeline Audit 2026-06-08

Multi-agent audit (5 specialists: pull+SEC, scoring-methods, generate/r40-latest, CI, data-quality).
~40 raw findings → triaged below. **Fixed & pushed** in commit `2c96e40a9` (Tag 236);
all 184 fixture-hash assertions + 10/10 anchor tests stay green.

## FIXED (verified, pushed — Tag 236)

| ID | Sev | File | Bug |
|----|-----|------|-----|
| F-001 | CRITICAL | pull-yahoo.js | `_balanceUsable(null)` threw on FTS null-placeholder balance rows → `[mapper-bug]` silently dropped the whole ticker snapshot (observed live in the refresh pull). Added `r!=null` guard. |
| F-02 | HIGH | generate-screener.js | r40-latest merge never removed a ticker newly hard-gated **this** run → poisoned entry survived 60 days. Now pruned (removed 20 stale rows live). |
| F-03 | MED | generate-screener.js | prune `cutoffStr` from a 2nd `new Date()` → off-by-one vs `generatedAt` across UTC midnight. Now derived from `generatedAt`. |
| F-01 | HIGH | generate-screener.js + build-r40-latest.js | `b.r40 - a.r40` → NaN/undefined order if a kept entry has null r40. Null-safe sort + ticker tiebreak. |
| F-04 | HIGH | build-r40-latest.js | missing `generatedAt` → `asOf=''` → next daily run prunes every entry it wrote. Now hard-fails. |

## REAL bugs — DEFERRED (touch fixture-tested scoring/pull logic; need per-fix anchor verification)

| ID | Sev | File:line | Bug | Suggested fix |
|----|-----|-----------|-----|---------------|
| BUG-02 | HIGH | methods/altman-z-score.js:88 | reads `totalLiab` only; post-Tag-211l snapshots write `totalLiabilities` → always falls back to a liabilities **heuristic**, understating Z by 0.3-0.5 (false DISTRESS in TURNAROUND mode). Sibling ohlson-o-score already coalesces. | `_balanceVal(0,'totalLiabilities') ?? _balanceVal(0,'totalLiab')` |
| F-002/F-010 | HIGH | pull-yahoo.js:1127-1141,1815 | `mapFTSToQuarterly` skips `revenueQ` push on null-rev rows but pushes null to opIncQ/grossProfitQ → arrays misalign; opIncQ override fires even when revenueQ came from quoteSummary → cross-source length mismatch. Quarterly-acceleration methods zip wrong quarters. | push null to all 3; gate opIncQ/grossProfitQ override on same source as revenueQ |
| F-004 | HIGH | pull-yahoo.js:516-520 | bank/insurance OpInc fallback `.filter(Boolean)` compresses array → misaligns with `annualRev` (JPM/BAC/NU/SOFI). | return unfiltered (keep null placeholders) like the FTS path |
| F-NY-001 | HIGH | pull-yahoo.js:1805 | `netIncomeQ` wraps null as `{value:null}` → length-based "computable" checks see data that's all-null. | filter null before wrap |
| F-NY-002 | HIGH | pull-yahoo.js:619-626,1086 | `totalDebt = (std||0)+(ltd||0)` coerces a missing debt component to 0 → understates leverage; `_debtPartial` flag is written but never read. | null when either component missing |
| F-NY-003/004 | HIGH | pull-yahoo.js:183,743-745 | FX: a `delete FX_TO_USD[ccy]` (stale, no fallback) or null `financialCurrency` (OTC ADRs) leaves financials in **local currency** while marketCap is USD — silent mixed-currency snapshot. `ccyAmbiguous` written but unread. | set `fxConversionFailed` when rate absent / ambiguous on non-US exchange |
| BUG-11 | HIGH | methods/deceleration-guard.js:38-40 | `prev3Avg` `.filter(isFinite)` skips null quarters → averages **non-consecutive** quarters (Q-1 & Q-3) → false HARD_DECEL. It's a DATAGUARD → excludes the stock entirely. | break at first null, only consecutive quarters |
| BUG-01/09 | HIGH | q-spike-dataguard.js:142, hypergrowth-quality-class.js:55 | `metricValue('revenueTTM') || annualFallback` treats a legit `revenueTTM===0` as falsy → uses annual fallback (materiality flip). | explicit `!= null` check |
| F-NY-005 | HIGH | methods/data-quality.js:109-113 | `_hasMetric` counts a non-numeric `.value` (string `'N/A'`) as present → inflates DQ grade. | require `typeof === 'number' && isFinite` |
| F-NY-012 | HIGH | methods/data-quality.js:191-199 | `tierCapForGrade` default → `null` (no cap = fail-**open**) for unknown grade strings. | default `'REJECT'` (fail-closed) |
| F-NY-011 | HIGH | lib/atomic-write.js:161-165 | `_fsyncParentDirBestEffort` `openSync(dir)` outside its try → rethrows on Windows EACCES even though the write **succeeded** → caller treats success as failure. | move `openSync` inside the try (truly best-effort) |
| BUG-06 | MED | methods/premium-compounder-proof.js:88-92 | check #3 ROIC uses legacy `TotalAssets−Cash` IC; quality-compounder-roic uses Damodaran `Debt+Equity−Cash` (Tag 232d-4) → same stock passes one, fails the other. | adopt the Damodaran IC lookup |
| BUG-08 | MED | methods/net-debt-ebitda.js:30 | `EBITDA≈OpInc×1.2` when D&A null inflates EBITDA 10-30%; score-aggregator HIGH_DEBT red-flag ignores `approximationFlag`. | lower red-flag threshold when approximated |
| F-06 | MED | methods/rule-of-40.js:65-73 | TTM→annual FCF-margin fallback fires for **structurally** negative FCF (not just WC noise) → inflates R40, can flip FAIL→PASS. | require improving FCF trend, or cap to |TTM|<20pp |
| F-05 | MED | generate-screener.js:639 | R40 tab + r40-latest admit `dqGrade==='C'` rows that are blocked from HG/QC/BF/PRE_BREAKOUT — asymmetry (decide if intentional). | add `r.dqGrade!=='C'` or document |
| F-008 | MED | scripts/pull-13f-institutional.js | `BOOTSTRAP_INSTITUTIONS` has duplicate CIKs (incl. ExxonMobil as an "institution"); de-dupe keeps the wrong first entry → Two Sigma/AQR/Millennium never fetched. | curate list (verify each CIK) |
| F-007/F-011 | MED | scripts/pull-insider-form4.js | submissions pagination (`filings.files[]`) not followed → Form-4s older than the `recent[]` block dropped; primary-doc XML guess can fetch viewer XML. | follow pagination within lookback window |
| F-NY-009 | MED | snapshot-methods-history.js / snapshot-picks.js | `methods-history/` (+picks) grow unbounded in git (~14 MB/day) → repo-size risk. | prune vintages >90d |
| F-006 (sec) | MED | pull-sec-xbrl.js:147-153 | HTTP 429 not distinguished from 500 → a rate-limit burst burns the 50-error abort budget → pulled=0. | detect 429, back off, don't count toward abort |

## REJECTED (false positives / self-retracted by the agent)

- **CI F-06 "freshness gate always fails"** — FALSE: CI `snapshots/` is gitignored & ephemeral per run (only the day's ~3.3k exist → ~100% fresh → gate passes). Proven by the June 5/6/7 commits. (This is also why the r40-latest fix used a **merge**, not a snapshot cache that *would* break this gate.)
- BUG-07 (prof-state persistent-loss), BUG-09 (prof-state cyclical), BUG-10 (piotroski F5), F-NY-010 (ebitda scaling), BUG-04 (score-aggregator thresholdOp) — agents confirmed "no active bug / latent only" on close reading.

## Notes
- Deferred items are real but change anchor-tuned outputs → each needs: apply → `node tag28-tests.js` (fixture hash) + `node tests/integration-anchor-test.js` (anchors) → confirm green → commit. They were NOT applied blind.
- F-003 worker-pool vs yahoo-finance2 internal queue concurrency (pull-yahoo.js:65) is a plausible contributor to the Yahoo 429 storms / pull instability — worth a focused reliability pass separate from correctness.
