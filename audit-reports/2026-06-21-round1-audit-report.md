# Screener Code-Audit — Round 1 (2026-06-21)

_448 agents, file-partitioned, adversarially verified. Spend-limit cut synthesis; this is the deterministic salvage._

**Confirmed-real: 222** of 411 raw. fix_safe: 49.

## CRITICAL (1)

- **Fixture-hash golden test is RED on the committed tree — daily-pull workflow gate is currently broken** — `C:/Users/Karlr/screener-wt/audit-hardening/tag28-tests.js:2788-2809` [bug]
  - tag28-tests.js gates the daily-pull workflow with exit 1. With the golden test failing, the entire ~14.6h pull/screen pipeline is blocked at the test step on every run. Either commit ce8e33432 introduced an unintended behavior change in score-aggregator/methods (a real regression), or it was an intentional formula change that nobody re-baselined. Both are pipeline-stopping. workflows/scripts are entrypoints, so this is not a dormant test — it is actively breaking CI now.
  - _fix:_ Triage commit ce8e33432 against the projection in _computeFixtureHash (HG/QC pass+weight+computable+score). If the output change is the intended 'purity + correctness fix', regenerate the baseline with `ALLOW_FIXTURE_CHANGE=1 "C:\Program Files\nodejs\node.exe" tag28-tests.js` and commit tests/fixture-hash.txt in the SAME commit as the behavior change (the workflow comment at daily-pull.yml:423 documents this discipline). If the change was unintended, revert the offending method delta. Do not touch FTS_CACHE_VERSION. Add a CI assertion that fixture-hash.txt was modified in any commit that changes SCORE_WEIGHTS-bearing methods, so the baseline can never drift silently again.

## HIGH (12)

- **Five price methods each JSON.parse the 70MB prices/history.json into a separate module-level cache — up to 5x parse + 5 resident copies per process** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/above-200d-ma.js:12-19 (and identical in high-proximity-52w.js:12-19, drawdown-52w.js:12-19, volatility-annualized.js:12-19, price-momentum-12-1.js:78-89)` [performance]
  - On first stock evaluated, every one of these methods parses the full 70MB file independently, holding 5 deserialized copies (~hundreds of MB) for the whole pull. This is concrete wasted recompute/memory in the per-stock scoring hot path and contributes to the 14.6h pull / memory pressure.
  - _fix:_ Extract a single shared price-history loader (e.g. methods/_price-history.js with one memoized parse) and have all five methods require it. Keeps FTS_CACHE_VERSION untouched and methods/index.js unmodified. Comment in each method: // prevents: redundant 70MB JSON parse — shares the single memoized price-history loader.
- **Metric-divergence-guard is structurally blind on 'computed-margin' snapshots (annual margin is tautologically equal to TTM margin)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/metric-divergence-guard.js:57-72` [data-integrity, auto-safe]
  - This guard's whole purpose (header lines 4-9) is to catch financial/treasury entities like MSTR where TTM diverges from annual op income. Those are exactly the snapshots most likely to hit the Financial-Services-gated 'computed-margin' fallback, where annualOpInc is synthesized FROM the TTM margin — so the very names the guard targets get a guaranteed divergence of 0 and silently PASS. It is a DATAGUARD (method-types.js:74), so a false pass lets a data-anomalous stock into picks.
  - _fix:_ Read `stock.meta.opIncSource`; when it is 'computed-margin' the annual-vs-TTM comparison is non-independent — return computable:false with reason 'annualOpInc derived from TTM margin (opIncSource=computed-margin) — divergence undefined'. Add comment: 'prevents tautological pass when annual op-income was back-derived from the TTM operating margin'.
- **Annual-backfill emits Q4-only labels (YYYY-Q4) that interleave with live quarterly TTM entries under string sort, corrupting the 'last 4 quarters' R40 durability gate** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/backfill-r40rx-history.js:110-111, 188-190; consumed at generate-screener.js:721` [bug]
  - `slice(-4)` is documented/intended as 'R40 >=40 in >=3 of the last 4 QUARTERS' (a within-year durability signal that McKinsey-style only ~16% of firms sustain). But after backfill the tail is dominated by ANNUAL points one year apart (…2023-Q4, 2024-Q4, 2025-Q4) plus maybe one live quarter. The gate therefore measures durability across 3-4 YEARS, not 4 quarters, and mixes two different FCF-margin definitions (annual-backfill vs TTM, tagged fcfMarginSource). A stock that printed R40>=40 in three separate annual snapshots wrongly earns r40Durable and enters the trusted R40 tab (which is exported to findash via r40-latest.json). This is a look-ahead/semantics defect feeding a live screening gate.
  - _fix:_ Make the durability gate quarter-aware instead of position-aware: in generate-screener.js filter `hist` to entries whose fcfMarginSource==='TTM' (exclude 'annual-backfill') before `slice(-4)`, OR require the 4 entries to be 4 CONSECUTIVE calendar quarters (parse YYYY-Qn, assert no gaps). Backfill entries should populate the long-range chart but not the consecutive-quarter durability decision. Each gate change carries a comment naming the failure mode: 'prevents annual-approximation rows from satisfying the consecutive-quarter R40 durability gate (look-ahead/semantics mismatch)'.
- **Candidate pull bypasses USD/FX normalization — apples-to-oranges scoring vs the watchlist** — `C:/Users/Karlr/screener-wt/audit-hardening/suggest-watchlist-additions.js:31-93, 117-121` [data-integrity]
  - pull-yahoo.js:271-318 exists precisely because leaving annual/quarterly series in reporting-currency while marketCap is USD 'silently corrupts every ratio (fcf-yield, ev/ebitda, etc.) for non-USD stocks' (its own comment). This script re-introduces that exact defect for every candidate: a Japanese/Korean/EU name is scored with JPY/KRW/EUR revenue magnitudes against USD-calibrated method thresholds, so mcap-gated and absolute-magnitude methods pass/fail essentially at random. Since universe-candidates can contain foreign tickers (live watchlist already holds e.g. 000270.KS), the '≥N/17 pass' suggestions for non-USD names are not comparable to the watchlist members they are meant to augment. This is a direct cap on the screener's discovery quality.
  - _fix:_ Reuse the real puller instead of the divergent minimal copy: import pull-yahoo's mapper + _convertSnapshotToUSD (or refactor that mapper into a shared lib) and run candidates through the identical normalization path before Runner.evaluateStock. At minimum, stop hardcoding currency:'USD' — read pr.financialCurrency / pr.currency, look up FX_TO_USD, and multiply series + marketCap to USD. Comment the failure mode: 'prevents non-USD candidates being scored on un-normalized magnitudes (mirrors pull-yahoo _convertSnapshotToUSD; see Tag 134 mixed-currency corruption)'.
- **13F tests mutate the REAL on-disk cache (external-data/sec-13f-by-ticker.json) with non-atomic write + non-finally-safe restore against process kill** — `C:/Users/Karlr/screener-wt/audit-hardening/tag28-tests.js:1849-1926` [data-integrity]
  - The test harness `test()` (line 10-13) only catches thrown Errors per-test, but a hard crash, SIGINT, OOM, or a throw inside _restoreCache itself between unlink and write leaves the user's real 13F cache truncated or deleted. Because institutional-ownership-13f reads this exact file at runtime, a clobbered cache silently degrades every subsequent screen for that signal (institutionsHolding drops to incomputable) with no alarm. Tests should never write to production data paths.
  - _fix:_ Point the 13F tests at a temp fixture path instead of the production cache: have the method accept an injectable cache path (mirror the already-existing injectable pattern used by insider-conviction-score `_evaluateWithTxns`, lines 2418-2452), or set CACHE_PATH_13F to `path.join(os.tmpdir(), 'test-13f-'+process.pid+'.json')`. If the on-disk round-trip must stay, route writes through lib/atomic-write.js (the same writeFileAtomic used by walk-forward-perf.js:24) so an interrupted write can't truncate the real file.
- **court-buckets.json is read but produced by NO script in the repo — score step (and the whole regression gate) has a missing, undeclared dependency** — `court-score.js:131-133` [data-integrity]
  - court-score.js cannot run from a clean tree: readJson(BUCK) throws ENOENT and the pipeline dies. The regression harness court-score-tests.js (line 38-39) runs court-screen + court-score fresh into isolated temp outputs but NEVER regenerates court-buckets.json, so the gate depends on a stale, hand-placed, env-default-path file silently persisting in outputs/. If that file is absent or stale, every court test crashes at load (not a clean FAIL). The classifier output (the thing that decides which bucket each ticker lands in — the most consequential input to the cross-sectional score) is an untracked, unreproducible artefact. This is exactly the kind of silent coupling that caps the screener: results are non-deterministic w.r.t. an input nobody regenerates.
  - _fix:_ Make the bucket dependency explicit and reproducible: (a) have the classification workflow write outputs/court-buckets.json as a committed pipeline step, OR (b) add a COURT_BUCK env override (mirroring COURT_CAND_OUT/COURT_OUT) and have court-score-tests.js generate a deterministic test buckets file into the isolated temp dir before invoking court-score, so the gate exercises the full chain without leaning on a stale shared artefact. Either way, fail loudly with a named error ('court-buckets.json missing — run classification workflow first') instead of a raw ENOENT.
- **classify() conflates true delisting with mere series-end staleness, and accepts a stale-but-within-7-days close as a valid exit — survivorship attrition is mislabelled** — `fitness/lib/forward-returns.js:37-46, 28-30` [data-integrity]
  - The entire naive-vs-conservative survivorship lever (the stated primary value of this harness, freeze-full-baseline.js:92-95) depends on 'delisted' meaning a name actually died (conservative assigns it -1.0 / -100%). Mislabelling end-of-history names as delisted means the conservative variant punishes alive-but-untracked foreign/illiquid names with a fabricated -100% return, biasing conservative IC and cohortSpread downward by an unknown amount. Conversely, accepting a 7-day-stale close as 'ok' injects stale-price returns into the naive variant. Both corrupt the headline survivorship-bias quantification.
  - _fix:_ Split the status: distinguish 'delisted' (series has dates AFTER exitDate region but a gap, OR an explicit delist flag) from 'no_exit_price'/'series_ended' (newest date for that ticker < exitDate - stale window). Only the former should map to -1.0 in the conservative variant; series-ended-but-universe-still-running names should be dropped from both. Record the staleness gap (days between chosen close and exitDate) and flag exits beyond a tighter threshold. Comment the failure mode: '// prevents alive-but-untracked foreign names being booked as -100% delistings'.
- **measure.js anchors entry at frozenAt 00:00:00Z and bypasses getEntryDate's pre-market next-day shift — reintroduces the same-day look-ahead walk-forward-perf.js was hardened against** — `fitness/measure.js:78-82, 89, 92` [bug]
  - If the picks-history vintage was generated from a pre-close (pre-21:00 UTC) snapshot, measure.js's entry window opens on the SAME calendar day the screener ran, capturing intraday price movement the strategy could not have traded at — a classic backtest look-ahead bias that inflates measured IC and cohortSpread. The harness exists specifically to be the 'honest signal' (freeze-full-baseline.js:5-7); silently using a more optimistic entry convention than the production walk-forward scorer makes the two non-comparable and over-states fitness.
  - _fix:_ Import getEntryDate from walk-forward-perf.js and derive the entry from the vintage's asOf timestamp (read picks-history asOf, not just the date) before anchoring: `const entryDate = getEntryDate(vintage.asOf)`. Thread that through resolveWindow. Failing that, document loudly that measure.js entries are date-only and may include same-day look-ahead, and gate the metric. Comment: '// prevents same-day look-ahead: mirror walk-forward getEntryDate pre-close shift'. (Note: requires reading asOf from the baseline; baselines currently only store frozenAt — see A-fitness-08.)
- **Ohlson SIZE term uses raw-dollar TotalAssets with GNP index = 1.0, biasing O downward by ~5-9 points and effectively disabling the DATAGUARD hard-gate** — `methods/ohlson-o-score.js:173-175, 80` [bug]
  - Ohlson is a promoted DATAGUARD (header Tag 224b) whose pass gate is O < 0. The un-deflated size term injects a constant ~ -8 to -11 into O for every firm, so O is almost always far below 0 regardless of actual distress. The header's celebrated '7-sigma moat / every anchor O <= -7.05' is an artifact of this scaling error, not bankruptcy-distance. The hard-gate can essentially never fire (a genuinely distressed firm would need its other 8 terms to overcome an -8 offset), so a DATAGUARD that exists to catch FTX-style distress is silently inert. Tightening to Begley-1996 (O<-3.23), as the header muses, is meaningless while the offset stands.
  - _fix:_ Deflate TotalAssets to the scale Ohlson calibrated on: divide ta_t by 1e6 (express in $millions) before the log, or introduce a documented SIZE_SCALE constant (e.g. GNP_PRICE_INDEX in $millions of 2010 dollars). Re-validate the O<0 boundary and anchor pass-rates after rescaling. Add comment: 'prevents failure mode: un-deflated size term makes the bankruptcy hard-gate permanently non-firing'. Do NOT add ticker exclusions; fix the size scale only.
- **Piotroski uses `!= null` (not isFinite) for balance fields, so a NaN balance value adds a phantom always-failing signal that corrupts both the F-score and the scaled threshold (0.15-weight TURNAROUND input)** — `methods/piotroski-f-score.js:33-37, 93-106` [bug, auto-safe]
  - Two effects compound: (1) phantom signals are counted as failed (depressing score), and (2) `signals.length` is inflated, so `scaledThreshold = round(6*length/8)` (line 130) demands more passes than the data supports — a stock with genuinely 6 computable signals can be judged against an 8-signal denominator. piotroski-f-score is 0.15 weight in TURNAROUND. NaN balance fields are plausible from partial Yahoo parses (pull-yahoo trims trailing nulls but mid-array bad values survive).
  - _fix:_ Make `_balanceVal` finite-guarded (`Number.isFinite(v) ? v : null`) exactly like beneish/ohlson, and/or change every signal guard from `x != null` to `Number.isFinite(x)`. This both excludes non-finite inputs from `signals.length` and prevents the NaN-comparison-false trap. Comment: 'prevents failure mode: NaN balance field added as phantom failed signal, corrupting score and scaled threshold'.
- **listing-age QC multiplier treats annual-history DEPTH as company AGE — penalizes long-history compounders whose snapshot stores few annual entries** — `methods/score-aggregator.js:382, 337-390` [data-integrity]
  - Yahoo frequently stores only 4 annual revenue rows. A 20-year compounder (MSFT/COST/V — the exact QC anchors) with 4 stored annual entries gets cleanYears=4 → multiplier 4/5 = 0.80, silently shaving 20% off the score of the highest-quality names, pushing A-tier (>=80) to B. This is LIVE: daily-pull.yml runs the dashboard + score-history steps with AUDIT_SCORE_MULTIPLIERS=1 (lines 395/407/413), so the multiplied score is what Karl sees and what is persisted to score-history. The cap is invisible (no flag, no red-flag) and systematically biases against durable compounders — the opposite of QC intent.
  - _fix:_ Drive the multiplier from a true age signal, not data depth: use components.ipoAgeYears (already computed at listing-age.js:69) when finite, falling back to cleanYears only when ipoYear is absent; clamp to [0,1]. Comment: 'prevents history-depth being mistaken for company age — Yahoo stores ~4 annual rows even for 20y compounders, which falsely pro-rated QC anchors below A-tier.' Do not hardcode tickers.
- **Asymmetric component fallback inflates RPO/deferred-revenue YoY when current/noncurrent coverage differs between years** — `scripts/extract-ma-rpo.js:375-381, 406-410` [data-integrity]
  - If the latest 10-K reports both Current+Noncurrent contract liability but the prior year's filing (or the stale-cutoff filter at line 154-164) only yields the Current component, `rpoLatest` is a 2-part sum while `rpoPrior` is a 1-part sum. `rpoGrowthYoY` (line 429) then reports a fabricated jump that is purely a coverage artifact, not real growth. Net debt / RPO-growth signals feeding the screener get a phantom acceleration. The treat-missing-as-zero on a *sum of two balance-sheet lines* is the core defect.
  - _fix:_ Require symmetric components: only compute YoY when the SAME set of components is non-null in both years (e.g. compute `rpoLatest`/`rpoPrior` only from components present in both, or set `rpoPrior=null` unless every component used in `rpoLatest` also resolved for the prior period). Comment: '// prevents phantom YoY from asymmetric Curr/Noncurr coverage'.

## MEDIUM (80)

- **res.json() on the 429 path runs with no timeout — a hung response body can block CI, the exact failure this module was built to prevent** — `C:/Users/Karlr/screener-wt/audit-hardening/lib/discord.js:101-104` [bug]
  - The module's stated reason for existing (header lines 11-13) is 'a hung Discord webhook can't block CI'. The headers arrive (status 429) but a stalled/slow body stream on res.json() will hang indefinitely with the timer gone. Discord 429 bodies are tiny so this is rare, but it reopens precisely the hang vector the AbortController was added to close.
  - _fix:_ Read the body under its own timeout, or skip the JSON body entirely and rely on the retry-after header (header is in seconds and already handled at line 99-100). Simplest: wrap the res.json() in Promise.race with a short timeout, or move the body read inside _postOnce before the timer is cleared. Comment: '// prevents CI hang — body read must stay under the same hard timeout as the request'.
- **Asset-growth-divergence compares revenue and asset CAGRs over potentially different fiscal-year spans (positional [0]/[2] across two independently-built arrays)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/asset-growth-divergence.js:26-33, 65-66` [bug]
  - If revenue has a gap year that balance does not (or vice-versa), index [2] points to different fiscal years in the two series, so `spread = assetCAGR - revCAGR` (line 84) subtracts CAGRs measured over mismatched windows. The M&A-divergence DATAGUARD/softGuard (8-point QC penalty, score-aggregator.js:204) then fires or clears on a miscomputed spread — a silent false signal with no diagnostic that the periods didn't line up.
  - _fix:_ Align on a shared fiscal-year index (e.g. carry period labels from the snapshot and intersect, or require both annualRev[i] and annualBalance[i] non-null for the same i before using that i for the CAGR endpoints). At minimum, only use indices where both series are non-null and identical years. Comment: 'prevents CAGR spread computed across mismatched fiscal windows when one series has a gap year the other lacks'.
- **Closed-end-trust S3: revenue-volatility sub-clause is dead code (fully subsumed by hasNonPositive)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/closed-end-trust-guard.js:155` [dead-code]
  - The documented intent was almost certainly a lumpy-revenue detector that fires on extreme peak/trough swings; as written, S3 collapses to 'any rev year <= 0'. Trusts with wildly volatile but never-quite-negative reported 'revenue' (gain years + near-zero years, ratio >3x) do not get the S3 signal, so they may stay below the 2-of-N fail count and leak into the R40 tab — the exact noise this DATAGUARD exists to remove.
  - _fix:_ Make volatility fire independently of the negative-year test, e.g. `s3 = hasNonPositive || (revVolRatio != null && revVolRatio > REV_VOL_RATIO_MAX && hasZeroOrCollapse)` where hasZeroOrCollapse detects a near-zero/collapsing year — or, if the negative-year coupling is truly intended, delete revVolRatio/REV_VOL_RATIO_MAX as dead code. Comment the chosen branch with the failure mode (lumpy gain/loss revenue without a strictly negative print).
- **DCF + owner-earnings claim 'not in SCORE_WEIGHTS / fixture-hash safe by construction' but BOTH are weighted in the BUFFETT mode (0.30 / 0.20)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/dcf-intrinsic-value.js:260-261, 425-435; cross-ref methods/index.js:256-261 and methods/score-aggregator.js:68-72` [data-integrity]
  - The 'fixture-hash safe by construction' guarantee is false: any change to DCF or owner-earnings math directly moves the BUFFETT-mode score. If the fixture-hash test exercises BUFFETT mode, every edit to these files silently drifts the hash and breaks the pre-pull gate (the exact failure class documented in _helpers.js:155-175 for medians). If the test does NOT exercise BUFFETT mode, then real scoring regressions in two 0.20/0.30-weight methods go completely untested. Either way the safety claim that protects edits to these files is wrong and misleads future maintainers into editing them freely.
  - _fix:_ Decide and make consistent: either (a) confirm BUFFETT mode is covered by the fixture-hash test and DELETE the 'not in SCORE_WEIGHTS / fixture-hash safe' comments from both files + index.js entries (replacing with 'WEIGHTED in BUFFETT mode — edits move the score'), or (b) if BUFFETT is intentionally excluded from the hash fixture, add an explicit BUFFETT-mode case to the fixture so these weights are regression-tested. Do not change SCORE_WEIGHTS values. Each touched method comment must name the failure mode (silent BUFFETT-score drift / untested scoring path).
- **surprisePercent units mismatch: method assumes percent (5.0), real Yahoo delivers a ratio (0.05) -> mean-floor gate never fires in production** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/earnings-surprise-momentum.js:26-27, 63, 68-69, 100-103, 108` [data-integrity]
  - On real pulled data, mean surprise is ~0.02-0.08 (ratio scale), so `mean >= 3` is essentially always false. magnitudePass is permanently false -> the method's pass condition `breadthPass && magnitudePass` can effectively never be true in production, even for a stock that beat estimates every quarter. The entire PEAD diagnostic silently emits a dead signal; `largePositiveCount = surprises.filter(v => v > 5)` (line 101) likewise never counts anything. The green fixture hides it because the fixture data is internally inconsistent with what pull-yahoo actually writes. This is the classic Yahoo {raw,fmt} ratio-vs-percent trap.
  - _fix:_ Decide the canonical scale and make pull + method + fixtures agree. Cleanest: keep Yahoo's native ratio in the snapshot and convert inside the method: in _validSurprise return the ratio, then compute `const pct = v * 100` per row before comparing to MEAN_FLOOR_PCT / the >5 and >0 tests, OR normalize MEAN_FLOOR_PCT to ratio (0.03) and the large-beat bar to 0.05. Add a comment naming the failure mode: '// prevents: Yahoo surprisePercent ratio (0.04) silently failing a percent-scaled mean floor (3)'. Then rewrite the tag28 fixtures so surprisePercent matches epsDifference/epsEstimate (e.g. 0.04 not 4) — the fixture is currently the thing certifying the bug as correct. Re-run tag28-tests after; expect a fixture-hash change for THIS diagnostic only (it is not in SCORE_WEIGHTS so the score path hash is unaffected).
- **EV/EBITDA accepts Yahoo's enterpriseToEbitda whenever it is >0, silently ignoring negative-EBITDA firms that Yahoo encodes as a negative ratio** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/ev-ebitda.js:27` [bug, auto-safe]
  - EV/EBITDA is a `lte` valuation gate; the whole point is to reject expensive names. A near-breakeven company can flip between 'Yahoo negative ratio → ignored' and 'fallback opInc*1.2 positive → cheap PASS', producing a valuation verdict that contradicts the native ratio. The branch silently prefers the heuristic over Yahoo's own (more accurate) number for exactly the marginal-profitability names where the answer matters most.
  - _fix:_ Treat a non-null Yahoo ratio as authoritative regardless of sign: when `yahooRatio != null` and `yahooRatio <= 0`, return computable:false with reason 'negative/zero EBITDA per Yahoo native ratio — EV/EBITDA not meaningful' instead of falling through to the heuristic. Only fall back when `yahooRatio == null`. Add comment naming the failure mode: 'prevents heuristic opInc*1.2 PASS overriding Yahoo's negative-EBITDA signal'.
- **Forecast-contamination uses null-compressed annualRev[0], so 'latest annual' may be a stale prior year when Yahoo returns a leading null** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/forecast-contamination-guard.js:43, 54` [bug]
  - The guard's ratio annualLatest/q4Sum (line 73) is meant to compare the most recent annual figure against the last four quarters. A leading-null on annualRev silently shifts annualLatest to an older, smaller year, deflating the ratio and masking genuine forecast-contamination (the look-ahead bias this DATAGUARD exists to catch, header lines 4-12). Inconsistent with the same file's own F-ME-006 alignment fix applied only to the quarterly array.
  - _fix:_ Use a raw (non-compressed) extractor for annualRev and take the first finite leading entry while recording its index, OR require annualRev[0] (raw) to be finite (return computable:false on a leading null). Comment: 'prevents using a stale prior-year annual revenue as annualLatest when Yahoo has not yet populated the most recent fiscal year (leading-null compression)'.
- **Annual fallback path yields REAL_HYPERGROWTH_ACCELERATING from a 1-factor model — spike-concentration (Faktor 3) is silently skipped** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/hypergrowth-quality-class.js:87-106, 126-135, 150-169` [architecture]
  - Annual YoY is far coarser than quarterly breadth and cannot detect a single-quarter spike (the core fake-hypergrowth signal). A name passing on annual data alone gets the same top rank (5) and 0.20-weight credit as one validated on 4 strong quarters with no spike — diluting the very discrimination the method exists to provide.
  - _fix:_ When breadthSource==='y', cap the max class below REAL_HYPERGROWTH_ACCELERATING (e.g. to REAL_HYPERGROWTH_BUT_LOSSY or HYPERGROWTH_REVIEW) and surface components.spikeShare=null as a reduced-confidence flag. Comment: // prevents: annual-only breadth earning full ACCELERATING rank without the quarterly spike-concentration check.
- **Owner-earnings pass-gate uses MIN_YEARS=4 but the spec, JSDoc and module description all claim '>=5y' — and the description still advertises '+ SBC' that the code removed** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/owner-earnings.js:43, 137-138, 311, 349` [bug, auto-safe]
  - Two correctness-of-record problems on a BUFFETT-weighted method (0.20). (1) The 4-vs-5 year gate: a stock with exactly 4 computable OE years PASSES the quality gate while every human-facing string says it needs 5 — so the verdict disagrees with its own stated rule, and downstream DCF (which requires MIN_OE_YEARS=4, line 56) is consistent with 4 but inconsistent with the prose. (2) The description shipped in module.exports advertises a '+ SBC' formula the code deliberately does NOT compute, so any report/UI surfacing `description` tells users the opposite of what the method does. rawSBC (line 156) is dead-read.
  - _fix:_ Pick the intended gate and make it singular: if 5y is intended, set the comparison to a named MIN_YEARS_FOR_PASS=5 (keeping MIN_OE_YEARS=4 alignment for DCF separately) ; if 4y is intended, fix JSDoc (137-138), the 245 reason string, and the 349 description to say 4y. Remove '+ SBC' from the line-349 description string to match the SBC-less formula. Delete the unused rawSBC read at line 156 (or comment why it is retained). Each change carries a comment: 'prevents pass-gate/description disagreement on a BUFFETT-scored method'. No SCORE_WEIGHTS / fixture change (description/string + gate constant only).
- **Check #4 ND/EBITDA still uses the OpInc x 1.2 approximation while net-debt-ebitda.js moved to real D&A — same metric computed two inconsistent ways** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/premium-compounder-proof.js:115-117` [data-integrity]
  - This is the same divergence class that BUG-06 (premium-compounder check #3 ROIC) and Tag 232d-4 already fixed: a single stock can read net-cash/low-leverage on premium-proof check #4 (EBITDA inflated by the 1.2x fudge -> ND/EBITDA understated ~17%) while the dedicated net-debt-ebitda must-gate, using real D&A, fails it. For Industrials/REITs where D&A is 30-50% of OpInc the 1.2x is badly wrong. Inconsistent leverage verdicts across methods on identical data erode trust and can let an over-levered name keep the 'Premium-Compounder' tag.
  - _fix:_ Mirror net-debt-ebitda.js: pull `const rawDA = H.latestAnnual(stock, 'annualDepreciation');` and use `ebitda = (rawDA != null && Number.isFinite(rawDA)) ? rawOpIncs[0] + Math.abs(rawDA) : rawOpIncs[0] * 1.2;` in check #4. Add comment '// prevents: ND/EBITDA divergence vs net-debt-ebitda.js (1.2x understates leverage ~17%, esp. Industrials/REITs)'. Do NOT edit methods/index.js. Re-run tag28 fixture-hash; if it shifts, it is a legitimate scoring-path change for premium-proof and the hash should be bumped intentionally (premium-proof is only a 0.05 'prefer' weight, low blast radius).
- **Degraded path reports value=position-in-range [0,1] against threshold=0.10 (a return), unit mismatch latent if ever promoted to scoring** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/price-momentum-12-1.js:253-278` [bug]
  - Currently DIAGNOSTIC-only so harmless to scoring, but _helpers _inferConfidence/_autoFlags and any future SCORE_WEIGHTS promotion compute dist=(value-threshold)/threshold. A degraded value of 1.0 vs threshold 0.10 yields ratio 10x — a promoted method would award full credit to any stock merely in the top of its short range. The header's documented contract (degraded pass = pos>=0.6) is correct, but the emitted value/threshold pair is internally inconsistent.
  - _fix:_ Emit threshold:DEGRADED_RANGE_PASS (0.6) and thresholdOp consistent with the positional metric on the degraded branch (it already stores thresholdsDegraded.rangePass in components), so value and threshold share units. Comment: // prevents: positional degraded value being graded against the 12-1 return floor.
- **R&D-cut guard is blind to the most extreme real-EM case: R&D slashed to zero** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/rd-cut-guard.js:90-96` [bug]
  - Zeroing the R&D line while op-margin expands >2pp is the textbook Roychowdhury (2006) real-earnings-management pattern the method exists to catch (header lines 5-9). The guard self-disables on precisely its most severe trigger. It also can't distinguish 'R&D genuinely absent from data' from 'R&D deliberately cut to zero'.
  - _fix:_ Require only rd_p > 0 (a valid prior baseline) and accept rd_t >= 0 (allow zero), computing rdYoY = (rd_t - rd_p)/rd_p which is well-defined for rd_t=0. Keep the rd_p>0 guard for the denominator. Comment: 'allow rd_t==0 so a full R&D elimination (the strongest real-EM signal) is detected rather than silently dropped as missing data'.
- **Read/write fiscal-year asymmetry: median built from _firstNonNull (any year) but consumed against latestAnnual (strictly year 0)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/sector-medians-compute.js:79-97 (write side) vs sector-relative-roic.js:83-92 / _helpers.js:31-47 (read side)` [data-integrity]
  - A stock whose latest fiscal year (index 0) has a null netIncome contributes an OLDER-year ROIC to the sector percentile distribution, but is itself ranked using the strictly-latest value (which is null → incomputable, so it drops out). The percentile distribution is therefore built from a mix of fiscal years while every ranked stock is forced to year 0. The Tag 232c-13 comment claims _firstNonNull fixes a survivorship bias, but it introduces a definitional read/write mismatch: the median a stock is compared against was computed on different-vintage data than the stock's own metric. sector-relative-roic's pass gate (rank>=75) becomes subtly miscalibrated.
  - _fix:_ Make write and read agree. Either change the compute side to use position-0-only (matching latestAnnual/latestBalance) — accepting the survivorship trade-off the comment worried about — OR change sector-relative-roic._computeRoic (and roic.js) to also use a first-non-null walk. Pick one definition and route both through a shared helper. Add comment naming the failure mode: '// prevents read/write fiscal-year skew between sector median and the metric ranked against it'.
- **adjclose backfill silently REWRITES historical closes (fetched wins on collision) — injects retroactive split/dividend restatement into the series backtests read, a look-ahead bias** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/backfill-prices.js:138` [data-integrity]
  - adjclose is recomputed by Yahoo every time a split or dividend occurs, so re-running backfill silently rewrites the price a stock 'had' on a past date to its today-adjusted value. A backtest that later reads history.json sees prices that embed information (future splits/dividends) not knowable on that date — textbook look-ahead bias — and historical close values silently change between runs, making backtest results non-reproducible. pull-historical-prices.js is cited as the 'same pattern' but the OVERWRITE-on-collision policy is what makes a backfill (vs an append) corrupt the past.
  - _fix:_ Decide and document one consistent price basis. Either (a) store raw `close` only (drop adjclose) so historical values are immutable, or (b) keep adjclose but make EXISTING entries win on collision (swap the two merge loops so `for (const e of fetched)` runs first, then `for (const e of existing)`), preserving the value originally recorded for each past date. Add a comment naming the failure mode: 'prevents retroactive split/dividend restatement (look-ahead bias) in prices/history.json consumed by backtests'.
- **Non-atomic fs.writeFileSync into fundamentals-cache bypasses the repo's writeFileAtomic — SIGKILL/CI-timeout mid-write truncates a court-screen input file** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/enrich-q-revenue.js:180` [data-integrity, auto-safe]
  - fundamentals-cache/{TICKER}.json is consumed by court-screen.js (durability v3, confirmed: grep revQYoYsec → court-screen.js). A crash between the open-for-write truncation and full flush leaves the WHOLE fundamentals payload (not just revQYoYsec) as a half-written/zero-byte file. The next court-screen run then JSON.parse-throws or silently loses every fundamental for that ticker. Worse than the form4 scripts because here the truncation destroys pre-existing data the script only meant to augment.
  - _fix:_ Import writeFileAtomic from ../lib/atomic-write.js (same one-line require the sibling scripts use) and replace `fs.writeFileSync(cacheFile, JSON.stringify(j))` with `writeFileAtomic(cacheFile, JSON.stringify(j))`. No FTS_CACHE_VERSION touched; this is a scripts/ file, not methods/index.js.
- **Q4 discrete-quarter revenue is systematically missing (duration heuristic excludes the ~365-day 10-K YTD figure) → YoY series has an annual hole, thinning the durability metric this script exists to thicken** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/enrich-q-revenue.js:108` [data-integrity]
  - The script's entire stated purpose (header lines 5-11) is to give court-screen.js durability v3 MORE than the ~5 quarters Yahoo provides. But dropping every Q4 means only ~3 of 4 quarters per year produce YoY points, so a filer with 5 years of 10-Ks yields ~15 not ~20 YoY points, and the most recent Q4 (often the freshest data) is absent — directly weakening the 'freshness-first' selection on line 167 (bestLatestEnd can be stale by a quarter).
  - _fix:_ Compute the discrete Q4 by subtraction: Q4 = FY-annual (the 10-K Revenues value, period ~365d) minus the sum of that fiscal year's Q1+Q2+Q3 discrete points, when all three priors exist for the same fiscal year. Keep it concept-consistent (only subtract within the single chosen concept). If the three priors are absent, leave the Q4 hole rather than guess. Comment the failure mode it prevents.
- **Benchmark column is hardcoded to 'SPY' even when walk-forward used QQQ/IWM — the bug the 232c-17 rename was meant to fix is still live** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/methodology-report.js:60-63, 67, 84` [data-integrity]
  - The whole point of the 232c-17 work (cited in the comment) was to stop mislabeling alpha-vs-benchmark as 'alpha vs SPY' when the benchmark fell back to QQQ/IWM. Because walk-forward never actually emits benchmarkSymbol, the report still prints 'alpha vs SPY' unconditionally — Karl reads a decision-ready report that asserts the wrong benchmark, which can flip a method's retire/keep call.
  - _fix:_ Add `benchmarkSymbol` (and per-vintage benchmark) to walk-forward-perf.js's output object, then this fallback resolves correctly. Until that ships, label the column 'alpha vs Benchmark' (no symbol) rather than the misleading 'SPY' so the report never claims a benchmark it cannot verify.
- **Health gate covers only 4 snapshot scripts; the entire reporting/diagnostic tier fails silently** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pipeline-health-check.js:19-27, 65` [data-integrity]
  - A crash, throw, or empty-output in ANY of the 8 audited reporting/diagnostic scripts is completely invisible: the step swallows the error (continue-on-error or the script's own try/catch->exit 0), and the only hard gate (pipeline-health-check) does not even know those scripts exist. The dashboard/methodology/pick-diff artifacts can silently freeze at yesterday's content for weeks. This is the structural cap on the diagnostic tier's trustworthiness.
  - _fix:_ Extend EXPECTED_SCRIPTS to include each reporting script that is supposed to produce an artifact each run (methodology-report, pick-diff, compute-method-drift, data-quality-report, walk-forward-perf, method-effectiveness, picks-regression-check). Have each script write a tiny pipeline-health/<script>.json {script, n_total, n_ok, n_failed, failure_rate} in its finally/exit path (even the catch->exit 0 path) so the missing-report=100%-failure rule (lines 49-63) can catch a hard crash.
- **Soft-error pulls (404 / no-13f / parse-fail) are written with a fresh `fetchedAt` and cached for the full 100-day TTL** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-13f-institutional.js:324-336, 626-636` [bug]
  - A transient SEC 404/rate-limit, a temporarily missing info-table, or a wrong/placeholder CIK is cached as a successful empty pull. The institution silently contributes 0 positions to the by-ticker view for a full quarter, with no retry. This is exactly the 'errored-pull written as fresh' failure the Tag-211j pattern was meant to prevent — but the pattern only fires on thrown exceptions, and these paths return instead of throwing. Asymmetric survivorship: big/healthy filers refresh, transiently-failing ones rot.
  - _fix:_ Treat a returned `r.error` the same as a thrown error for cache-freshness: when `r.error` is set, write `failedAt` (NOT `fetchedAt`) while preserving any prior successful `positions`/`fetchedAt`, mirroring the catch block at line 648. Distinguish permanent (no-13f-hr-filings for a genuine non-13F CIK) from transient by allowing a short retry TTL. Comment must name the failure mode: 'prevents soft-error (return-not-throw) pulls being cached fresh and starving the by-ticker view for a quarter'.
- **Latest-filing picker can select a 13F-HR/A amendment that partially restates holdings, silently dropping most positions** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-13f-institutional.js:333, 338-339` [data-integrity]
  - If the most recent filing by date is a narrow /A, the puller replaces the full ~5000-position book with the handful of amended rows. The institution then appears to hold only those names, corrupting the by-ticker view and the institutions-holding count for every other name it actually owns. Berkshire/BlackRock-class filers amend regularly, so this hits the highest-signal institutions hardest.
  - _fix:_ Prefer the latest 13F-HR (original); only use a /A when it is the newest AND its position count is within a sane ratio of the prior 13F-HR (e.g. >= 50% of it), otherwise merge the /A onto the base filing's positions. At minimum, when picking a /A, record `amendmentOf` + `form` and flag low-position-count amendments. Comment: 'prevents a partial 13F-HR/A restatement from replacing the full holdings book'.
- **Daily index regex /^4\s+/ silently drops Form 4/A amendments — daily cache diverges from quarterly backfill, which keeps them** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-insider-form4-daily.js:212` [data-integrity]
  - Form 4/A is the correction/late-filing amendment. A late or corrected insider purchase that arrives as a 4/A is invisible to the daily puller (the cache insider-conviction-score.js reads via methods/index.js), but present in the quarterly history cache — so the two caches that feed the same scorer disagree, and conviction scores shift when the quarterly backfill later fills in an amendment the daily run missed. Genuinely amended buys (e.g. corrected share counts) never reach the live screener.
  - _fix:_ Broaden the FormType match to accept the amendment suffix: anchor on `/^4(\/A)?\s+/` in both the `line.charAt(0)` fast-path (keep it, it's a cheap prefilter) and the full regex. Carry a comment: 'prevents silent loss of Form 4/A amendments that the quarterly backfill keeps (daily-vs-history cache divergence)'.
- **txnKey omits price/owner → two real same-day same-share buys at different prices (multi-lot) collapse to one; identical key in backfill-form345 means the bug is repo-wide** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-insider-form4-daily.js:229` [data-integrity, auto-safe]
  - Insiders routinely split one filing into several transaction lines at different prices on the same day. The dedupe in mergeTransactions (line 251-259) and backfill-form345 (line 307-311) drops every line after the first with the same share count, understating aggBuyValue (Σ shares*price) in insider-conviction-score.js line 257. Conviction/value gates (MIN_BUY_VALUE) and the topBuy selection are computed on incomplete data.
  - _fix:_ Add the price (and for the history cache, rptOwnerCik) to the key: `[t.accessionNumber, t.transactionDate, t.transactionCode, t.transactionShares, t.transactionPricePerShare].join('|')`. Apply in both files so daily and history dedupe identically. Comment: 'prevents collapsing distinct same-day multi-lot insider buys (understated aggregate buy value)'.
- **Form-4 soft-error returns (submissions-404 / submissions-parse) cached with fresh `fetchedAt`, suppressing retry for 24h and masking the failure as '0 transactions'** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-insider-form4.js:354-359, 462-470` [bug]
  - Form-4 consumer insider-conviction-score.js is in the live registry (methods/index.js:113). A ticker whose CIK 404s (stale map entry, delisting, share-class mismatch) is cached as a clean empty pull, so the method sees `transactions: []`, returns computable-but-empty, and the INSIDER_BUYING gate silently fails closed. No alarm, no retry next run. The header advertises 'failedAt (NOT fetchedAt) so the next run retries' (lines 478-491) but that guarantee does not hold for the return-path errors, which are the common ones.
  - _fix:_ Have main inspect `result.error`: if set, write `failedAt` (not `fetchedAt`) and preserve prior good data, identical to the catch path. Alternatively make pullTickerForm4 THROW on submissions-404/parse so the existing catch handles it. Add comment: 'prevents return-path soft errors being cached fresh, which hides 404/parse failures as empty insider history and blocks retry for 24h'.
- **isTenB5One footnote regex tests only the FIRST <footnotes> block and matches over-broadly; 10b5-1 sells may be mis-flagged or missed** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-insider-form4.js:228-233` [bug]
  - isTenB5One is filing-level but applied per-transaction, and the footnote scan only sees the first footnotes container. A filing with a 10b5-1 plan noted in a later/second footnote block is not flagged → a programmatic sale is counted against net-buying (gate3 fails spuriously, suppressing a real INSIDER_BUYING signal). Conversely the loose `10b5-?\s?1` substring can match footnote prose like 'rule 10b5-1(c)... NOT pursuant to a plan' and over-suppress. Either way the load-bearing net-buying gate is driven by a fragile filing-wide heuristic.
  - _fix:_ Scan ALL footnote blocks (`_extractAll(xml,'footnotes')`) and, better, resolve per-transaction footnoteId references so the 10b5-1 flag is attached only to the transactions that cite the plan footnote. Tighten the regex to require plan-affirming context. Comment: 'prevents filing-wide 10b5-1 misflag from corrupting the per-transaction net-buying gate'.
- **snapshot-r40rx-history + backfill use raw `ticker + '.json'` while the READER uses safeSnapshotFilename() — write/read filename divergence for Windows-reserved & special-char tickers** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/snapshot-r40rx-history.js:60-62, 123` [data-integrity, auto-safe]
  - For any ticker whose safe filename differs from raw (Windows-reserved stems CON/PRN/AUX/NUL/COM1-9/LPT1-9, or tickers with characters outside [A-Z0-9.-]), the r40rx history is WRITTEN to one path and READ from another. The writer never updates the file the reader opens, so generate-screener silently gets stale/empty r40rx history for those tickers -> the multi-quarter R40 durability gate (generate-screener.js:721 `hist.slice(-4)`) sees <4 entries and routes the row through the weaker TTM-only fallback (r40InsufficientHistory). The audit note F-SM-004 / F-218b-01 documents this exact drift class as P1; score-history was fixed but the two r40rx writers were missed.
  - _fix:_ Import `const { safeSnapshotFilename } = require('../lib/snapshot-fs.js');` in both snapshot-r40rx-history.js and backfill-r40rx-history.js and replace every `ticker + '.json'` (read in readHistoryFile, write in main) with `safeSnapshotFilename(ticker)`, exactly as snapshot-score-history.js does. No method/ files touched; FTS_CACHE_VERSION untouched.
- **pbScore formula is a hand-copied fork of generate-screener.js — duplicated magic constants drift silently because nothing ties them together** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/snapshot-score-history.js:38-69` [architecture]
  - The script's own header warns that any drift between stored history and the dashboard 'produces a permanent fake score uplift today artifact'. Two independent copies of a 6-term weighted formula with eight hardcoded weights is exactly how that drift happens: a future tweak to the dashboard's omaBonus or the revAccel divisor will not be reflected here, and the score-history sparkline/ΔScore badge will diverge from the live pbScore with no test catching it. The formula is currently identical, so this is structural risk, not a present miscompute.
  - _fix:_ Extract pbScore into a shared module (e.g. lib/pb-score.js exporting computePbScore(stock, allResults)) and require it from both generate-screener.js and snapshot-score-history.js. Do NOT touch methods/index.js. This collapses the fork to one source of truth and lets the existing fixture tests guard it.
- **Discipline audit only catches top-level `const NAME = <number>` changes — misses object-property and exported thresholds, the dominant pattern** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/threshold-audit.js:55-68` [bug]
  - This script enforces docs/threshold-discipline.md — the guardrail against single-ticker threshold tuning, which is precisely the failure mode that caps a screener's robustness. If the most common threshold-definition shape isn't matched, the audit reports 'clean' while undisciplined tunes slip through. False sense of governance.
  - _fix:_ Broaden the matcher to also catch `(\w+)\s*:\s*<number>` property assignments and `const NAME = <number>` regardless of indentation, and/or compare the parsed numeric tokens on changed lines within methods/*.js hunks generally. Add a unit case for the `threshold:` property form.
- **Pipeline-health failure_rate understates eval failures 3x — denominator is stock×mode but eval failures counted once** — `C:/Users/Karlr/screener-wt/audit-hardening/snapshot-picks.js:215 vs 305-308` [data-integrity]
  - A stock that throws in Runner.evaluateStock contributes 1 to n_failed but 3 to n_total (it is skipped in all 3 mode loops via `if (!results) continue` at line 242, so it never adds the other 2). The 5% CI gate therefore divides apples (per-stock failures) by oranges (stock×mode slots), understating the true evaluation-failure rate by up to 3x. If 10% of the universe fails to evaluate, the reported rate is ~3.3% and the gate passes — masking a real Yahoo/engine breakage the gate exists to catch.
  - _fix:_ Make numerator and denominator consistent. Simplest: count eval failures against a universe denominator (n_total = stocks.length) for the eval stage, and track per-mode pick failures separately; or multiply each eval failure by 3 when using the stock×mode denominator. Document which semantic the 5% threshold is calibrated to.
- **--threshold parses with no isFinite guard → bad input silently yields zero suggestions** — `C:/Users/Karlr/screener-wt/audit-hardening/suggest-watchlist-additions.js:19-27, 23` [bug, auto-safe]
  - A typo'd threshold (e.g. `--threshold 1O` with a letter O, or a stray flag) produces a clean 'Keine Kandidaten erfüllen die Schwelle' run with exit 0 — indistinguishable from a legitimately empty result. No error, no signal. NaN-propagation with no isFinite guard is exactly the class flagged in the brief.
  - _fix:_ After parsing: `args.threshold = parseInt(...); if (!Number.isFinite(args.threshold)) { console.error('--threshold must be a number'); process.exit(1); }`. Apply the same guard in suggest-watchlist-cleanup.js parseArgs for --threshold-pct/--min-runs (line 15-16).
- **One corrupt/truncated methods-history file aborts the entire cleanup run (no per-file guard)** — `C:/Users/Karlr/screener-wt/audit-hardening/suggest-watchlist-cleanup.js:33-39` [data-integrity, auto-safe]
  - A single bad file in the trailing min-runs window throws SyntaxError (or TypeError on Object.entries(undefined)) and kills the whole suggestion run with a cryptic stack trace — inconsistent with the rest of the pipeline, which the prune/refresh scripts deliberately made resilient (prune-watchlist.js:130-134 wraps the watchlist parse; refresh-fx.js:74-94 backs up corrupt input and continues). Karl loses cleanup suggestions entirely instead of degrading to 'skipped 1 corrupt vintage'.
  - _fix:_ Wrap the read in try/catch, `continue` on parse failure with a console.warn naming the file, and skip files where `!data || typeof data.stocks !== 'object'`. Add a comment: 'prevents one truncated history vintage from aborting the whole cleanup scan'.
- **Vacuous-pass: five 'fail-case' tests assert only !r.pass without guarding r.computable** — `C:/Users/Karlr/screener-wt/audit-hardening/tag28-tests.js:64-68, 84-88, 121-125, 133-137, 149-156` [bug, auto-safe]
  - A result with computable:false carries pass:false (verified across the suite's contract). If a refactor accidentally makes any of these methods return incomputable for valid input (a missing-field guard regressing, an envelope-unwrap break, a thrown error swallowed to a default), the method is silently broken but these tests stay GREEN — they cannot distinguish 'correctly failed the gate' from 'never evaluated'. This is exactly the silent-method-drop failure class the suite elsewhere (line 160) tries to defend against.
  - _fix:_ Add `if (!r.computable) throw new Error('should be computable')` before the `if (r.pass)` line in each of the five fail-case tests, OR add a positive value assertion (e.g. ROIC fail should also assert `approx(r.value, 0.10)`). Cheapest: insert the computable guard. This pins the fail-case to a real computed FAIL, not a vacuous incomputable.
- **cmdAdd never writes added_at → prune-watchlist's no-snapshot grace period can never fire** — `C:/Users/Karlr/screener-wt/audit-hardening/watchlist-cli.js:50-57` [bug, auto-safe]
  - F-DP-022 (the 'no-snapshot-after-Nd' prune rule) is dead for every ticker this CLI adds, because the timestamp it keys on is never set. Tickers that never produce a snapshot (bad symbol, delisted-on-arrival, Yahoo never returns data) accumulate forever unless --prune-orphans is run (which the workflow never passes, and which the code itself warns is irreversible mass-deletion). This is the upstream root cause of the orphan buildup the prune script's own comments lament ('audit found 12 207 orphans').
  - _fix:_ In cmdAdd and cmdImport set `added_at: new Date().toISOString()` on the new stock object. This is the guard-side fix prune already expects — no hardcoded exclusions needed.
- **AGE-NEUTRAL durability test is a tautology for the exact short-history regime the metric claims to fix** — `court-score-tests.js:156-164` [bug]
  - The metric's headline claim is 'strikt alters-/längen-neutral' and the test file (line 12-15, 105-111) sells age-neutrality as the fix for the Youth-Bias that got v2 Court-DENIED. But young IPO leaders (ALAB has only 2 annual rows per the fixtures; CRDO 3) are precisely the names with <12 quarters, where durV3 falls back through `gYoYDur = (revQYoYsec.length >= 4 ? revQYoYsec : gSeriesA).slice(0, DUR_W)` to a short window — and there the metric is NOT length-neutral. The regression gate that is supposed to lock in the fix never tests that regime, so a real age/length leak for short-history names ships green. The companion LÄNGEN-NEUTRAL test (line 149-153) has the same blind spot (12-element `recent`).
  - _fix:_ Add an assertion over a <12-element base, e.g. assert that durV3 of a 6-quarter young name is computed ONLY from those 6 quarters and document that names with winN<12 are NOT length-comparable (the code already lamps 'short-durability-window' at winN<12 in court-score.js:236 — the test should assert that lamp fires and that cross-sectional ranking treats short-window names as not-fully-comparable, rather than asserting a neutrality the metric does not have for them).
- **Unguarded fs.readdirSync(CACHE) throws raw ENOENT when fundamentals-cache is absent — no actionable error, and a partial/empty cache produces an empty candidate set with no alarm** — `court-screen.js:63` [data-integrity]
  - Two failure modes: (1) missing cache -> cryptic ENOENT that the regression gate reports as a generic 'Command failed: node court-screen.js' with no hint that the cache is the problem (I hit exactly this). (2) Worse — a PARTIALLY pulled or empty cache (a real risk given the 14.6h pull can be interrupted) yields `candidates: []` and the script writes a valid-looking JSON with count 0 and exits 0. court-score then ranks an empty universe and the daily report shows nothing, with no error. Silent empty-output on a truncated upstream pull is a classic data-integrity hole for this pipeline.
  - _fix:_ Wrap the readdir in a check: if (!fs.existsSync(CACHE)) throw new Error('fundamentals-cache missing — run the pull first'); and after the loop assert a sane floor (e.g. stats.parsed >= expectedMin or stats.passed > 0) before writeFileSync, exiting non-zero with a clear message otherwise. This converts a silent empty screen into a loud, gated failure. Comment: 'guards truncated/empty cache from producing a silent empty candidate set'.
- **Singapore exchange code mapped wrong: EXCHANGES key 'SP' with Yahoo suffix .SI, but Finnhub's Singapore code is 'SI' — the API call requests a non-existent exchange** — `discovery/finnhub.js:27` [bug, auto-safe]
  - The entire Singapore exchange yields zero tickers every run — a whole market silently missing from the global universe, masked as a benign 'unexpected response' log line. The comment confuses the Finnhub request code (left side, must be Finnhub's code) with the Yahoo suffix (right side).
  - _fix:_ Change the key to Finnhub's real code: `'SI': '.SI'`. Verify each other key against Finnhub's /stock/symbol exchange list (several others — e.g. 'F' Frankfurt vs Finnhub 'F', 'SW' SIX) should be spot-checked the same way. Comment: 'Finnhub exchange code, not Yahoo suffix — prevents whole-exchange silent miss'.
- **Finnhub only short-circuits on HTTP 401; any other persistent error (403/429/timeout) silently burns the full 16-exchange * (1.1s+30s) budget and discards all data** — `discovery/finnhub.js:94-105, 106` [data-integrity]
  - On a 429 storm, the loop wastes up to 16 * 31s ≈ 8 min doing nothing useful, and because each exchange independently fails, fetchFinnhubUniverse returns whatever partial Map it built (possibly empty) with no signal to the aggregator that Finnhub effectively contributed nothing. Inconsistent with the codebase's own Tag-215i retry hardening applied to the sibling pullers.
  - _fix:_ Mirror the nasdaq-api/otc-markets Tag-215i pattern: add timeout/429 retry with exponential backoff, and broaden the early-bail to 403/429-on-first-call as well as 401. Comment naming failure mode: 'prevents 8-min wasted budget on rate-limit/auth storm and aligns retry policy with sibling pullers'.
- **JUNK_SUFFIX_RE applied to whole symbol drops legitimate common stocks ending in W/R/U (shared triplicated bug)** — `discovery/nasdaq-all.js:25, 70, 97` [bug]
  - Direct loss of real, listed common stocks from the authoritative NASDAQ-Trader feed — the highest-confidence US source. Same root regex bug as A-discovery-02 (nasdaq-api) and otc-markets:27. Triplicated, so a fix must touch all three (or be centralized).
  - _fix:_ Replace `[WRU]$` with delimiter-anchored alternatives only (e.g. require a preceding `.`/`-` or a known 5th-char pattern), or rely on the structured columns the file already parses: nasdaqlisted.txt has explicit ETF (parts[6]) and Test Issue (parts[3]) columns and NextShares; warrants/units are better filtered by Security Name containing 'Warrant'/'Unit'/'Right' than by a lossy suffix regex. Centralize into one shared filter. Comment: 'prevents NU/BKU/EW-class common stocks being filtered as warrants'.
- **NASDAQ-API marketCap hint is silently discarded for any ticker already discovered by an earlier source** — `discovery/nasdaq-api.js:173, 175-185` [data-integrity, auto-safe]
  - marketCap is the SOLE ranking key for the universe hard-cap (refresh-universe.js:386-396: withMcap sorted desc, withoutMcap capped at 20% of MAX_UNIVERSE). The one source that provides a free mcap hint is the last to run, so virtually every US large-cap (already in nasdaq-all/sec) is treated as null-mcap and competes only for the 20% 'null bucket' instead of being ranked by its true size. This directly caps screener breadth — exactly the 'survivorship attrition' / silent-shrink failure mode. It also defeats the stated rationale of the module ('Provides company name, sector, and market cap hints').
  - _fix:_ In the merge `else` branch of refresh-universe.js, backfill empty fields from the incoming source: `if (info.marketCap != null && (existing.marketCap == null)) existing.marketCap = info.marketCap;` and likewise for empty `name`/`sector`. (Do NOT edit nasdaq-api.js for this; the bug is in the aggregator contract, not the puller. Carry a comment naming the failure mode: 'prevents last-source mcap/sector hints being dropped for already-seen tickers'.) Reordering nasdaq-api earlier is NOT sufficient because nasdaq-all/sec would then overwrite-skip it later anyway given first-writer-wins semantics.
- **Class-share regex still rejects every class-share ticker the comment claims it fixes (BRK.B, BF.B, BRK-B)** — `discovery/nasdaq-api.js:167` [bug]
  - `/[WRU]$/` is unanchored to a suffix delimiter, so it nukes real common stocks whose ticker merely ends in W, R, or U — e.g. NU (Nu Holdings, ~$60B), BKU, RDW. These are large, liquid names silently excluded from the universe at discovery time. The same JUNK_SUFFIX_RE appears in nasdaq-all.js:25, nasdaq-api.js:37, and otc-markets.js:27 — a triplicated bug.
  - _fix:_ Anchor the warrant/right/unit filter to a delimiter: replace `/[WRU]$|.../` with `/[.\-](WS|WT|WI|RT|UN|U|W|R)$/i` (only treat a trailing single letter as junk when it follows a dot/dash class delimiter, never a bare 'NU'/'EW'). Factor the corrected regex into one shared module imported by all three pullers to kill the triplication. Comment: 'prevents dropping real common stocks ending in W/R/U (NU, BKU, EW)'.
- **nasdaq-api redirect handler does not drain the 3xx body and passes a possibly-relative Location straight to https.get — the exact failure mode sec-tickers.js was hardened against** — `discovery/nasdaq-api.js:52-54` [bug]
  - api.nasdaq.com sits behind a CDN; a relative `Location: /api/...` redirect throws ERR_INVALID_URL, gets swallowed by the per-exchange try/catch (nasdaq-api.js:189), and that exchange contributes nothing — silent universe shrink. nasdaq-api.js additionally never drains the body (a), leaking a socket per redirect under keep-alive. This is the documented sec-tickers.js failure mode (a)/(b)/(c) replicated in 5 sibling files.
  - _fix:_ Factor sec-tickers.js's hardened get() (URL-resolve via `new URL(loc, url)`, res.resume() drain, MAX_REDIRECTS cap, handle 307/308) into a shared http helper and use it in all six pullers. Comment: 'prevents relative-redirect ERR_INVALID_URL silently zeroing an exchange + socket leak'.
- **OTC per-page catch breaks pagination on a single transient error, silently keeping only the pages fetched so far** — `discovery/otc-markets.js:182-186` [data-integrity]
  - A mid-pagination hiccup truncates the OTC universe to whatever was fetched before the error, asymmetrically. Because the aggregator merges whatever Map it gets (refresh-universe.js:336-368) and there is no expected-count assertion, the shrink is invisible. This is the silently-swallowed-error / non-atomic-discovery failure class.
  - _fix:_ Change `break` to `continue` for non-empty intermediate errors (skip the bad page, try the next), or retry the whole page once more before giving up. Track a pageErrors count and surface it to the aggregator so a partial OTC pull is detectable (mirror refresh-universe.js's F-DP-037 zero-quote alert). Comment: 'prevents one bad page truncating the entire OTC tail'.
- **ABSOLUTE_FLOOR (50%) drift alert fires on cold start and for legitimately-sparse fields, re-creating the alert-fatigue this file's comments say it fixed** — `field-coverage.js:151, 156-175` [data-integrity]
  - A HIGH alert that fires every run for fields that are correctly below 50% trains Karl to ignore the drift detector — so when Yahoo actually drops a field (the whole point of this module, per the Nov-2024 incident in :8-9) it is lost in the noise. This structurally caps the screener: silent Yahoo schema regressions are the dominant cause of bad scores, and the detector meant to catch them is desensitized.
  - _fix:_ Make the floor relative to each field's own established baseline, not an absolute 50%: only fire the floor alert when a field that HISTORICALLY sat above the floor drops below it (requires baseline[field] >= ABSOLUTE_FLOOR AND cur < ABSOLUTE_FLOOR). For cold start (no baseline), suppress the floor alert or emit it at INFO, not HIGH. Optionally give each field its own expected-coverage floor. This is a non-method file so no method-comment constraint applies.
- **Baseline omits the vintage asOf timestamp and the per-universe scores, structurally preventing measure.js from ever fixing the entry-look-ahead (A-fitness-03) or computing universe-wide IC (A-fitness-04)** — `fitness/freeze-full-baseline.js:78-99` [data-integrity]
  - This is the structural cap behind two of the higher findings. Because the freeze step discards the asOf timestamp, measure.js cannot apply getEntryDate's pre-close shift (look-ahead, A-fitness-03). Because it discards universe scores, measure.js cannot compute selection-power IC over the full universe (A-fitness-04) — only within-cohort discrimination. The harness's own 'honest signal' ambition is bounded by what the freeze chooses to persist.
  - _fix:_ Persist `asOf: vintage.asOf` and an `evaluatedScores` map (or include score on each evaluatedTicker entry) in the baseline. This is purely additive to the artifact (no scoring change, no FTS_CACHE_VERSION touch, measure.js still read-only). It unblocks A-fitness-03 and A-fitness-04 without violating any binding constraint.
- **"top-decile" cohort is computed from universeSize (5657) but applied to the 81-pick ranking — silently collapses to "all picks"** — `fitness/measure.js:143-149, 156` [bug]
  - cohortSpread_decile is reported as a distinct, more-robust metric next to cohortSpread_top8, but it is mathematically identical to 'mean of all picks minus universe median' for every real baseline (picks always << 10% of a 5657 universe). The dashboard/reader believes they are seeing a genuine top-decile concentration signal; they are seeing the whole cohort. A 10%-of-universe decile only makes sense against the universe's returns, not against an already-top-sliced 81-name pick list. This is exactly the kind of silently-degenerate metric that keeps the screener at '20% of potential' — a headline number that looks like signal but carries none.
  - _fix:_ Decide what the decile is a decile OF and make it explicit. If the intent is top-10%-of-picks, use `Math.max(1, Math.ceil(0.10 * rankingEntries.length))`. If the intent is universe-decile, the metric must be a quantile of universe returns, not a cohortSpread over the 81 picks. Add a comment naming the failure mode: '// prevents decile cohort silently equalling the full pick list when picks << universe'. Also surface cohortN_used in the summary so a collapsed cohort is observable.
- **Ticker injected into onclick JS-string context via fragile manual quote-escaping, not HTML-escaped — breakage + stored-XSS vector** — `generate-dashboard.js:419, 448` [bug]
  - ticker flows from picks-history / methods-history keys (`Object.keys(methodsHistory.stocks)`), which originate from the watchlist / Yahoo symbol list. A symbol containing a double-quote or `<` (foreign tickers, malformed Yahoo rows, or an attacker who can influence the watchlist) breaks out of the onclick attribute and injects arbitrary HTML/JS into the deployed GitHub-Pages dashboard. Even benign tickers with a backslash render a broken button (silent loss of the detail modal for that row). The whole point of the surrounding escaping was XSS-safety; this is the one hole.
  - _fix:_ Stop building inline onclick with string concatenation. Render the ticker into a `data-ticker` attribute via `escapeAttr(...)` and attach a single delegated click listener (like the existing `#tabs` listener) that reads `e.target.closest('button[data-ticker]').dataset.ticker` and calls `openDetail`. This removes the JS-string context entirely. // failure mode: HTML/JS-attribute injection + modal breakage from un-escaped ticker symbols
- **Unguarded JSON.parse + latest.stocks access can throw on truncated/old-schema snapshot; workflow swallows it (continue-on-error) → stale diff-report.html silently deployed** — `generate-diff-report.js:41-42` [data-integrity, auto-safe]
  - methods-history files are large (~14 MB per the workflow comments). A truncated file (CI cancellation mid-write of the upstream snapshot-methods-history.js) or a schema change where `stocks` is renamed/absent throws either a SyntaxError or `Cannot read properties of undefined (reading 'stocks')`. Because the step continues-on-error, the run goes green and GitHub Pages keeps serving the previous day's diff-report.html with no signal that the diff silently stopped updating — exactly the 'silently swallowed error' class in the audit scope.
  - _fix:_ Wrap both parses in try/catch (reuse a safeJSON helper), and after parsing assert `latest && latest.stocks && previous && previous.stocks` are objects; if not, write the same 'need ≥2 snapshots' placeholder and `console.error` a distinct message so the failure is visible in logs rather than masked by continue-on-error.
- **Kennzahl-Rangliste and Metric Deep-Dive rank DataGuard-disqualified stocks; only the Top-N cards exclude them** — `generate-methods-report.js:280-286 (deepDiveLists), 545-552 (leaderboardData), vs 242-249 (discoveryRows)` [data-integrity]
  - A stock that hard-fails a DataGuard (revenue-shock-guard, q-spike-dataguard, ohlson-o-score bankruptcy risk, pre-commerciality-megacap-guard, etc. — see methods/method-types.js DATAGUARD entries) is, by the pipeline's own definition, disqualified. Yet it can rank #1 in 'Kennzahl-Rangliste Top 30' and 'Metric Deep-Dive Top 10' on Rule of 40 / ROIC / FCF-Margin — the exact discovery surfaces Karl reads to pick names. A revenue-spike fake (q-spike) topping the Rule-of-40 leaderboard is precisely the failure these guards exist to prevent, re-introduced one section lower on the page.
  - _fix:_ Build both deepDiveLists and leaderboardData from discoveryRows instead of rows (or add an explicit, visible 'disqualified' badge in those tables). Move the discoveryRows computation above the deep-dive block (it is currently defined at line 242, already before both) and swap the `rows` reference. Comment: '// prevents DataGuard-disqualified stocks (q-spike/revenue-shock/ohlson) ranking #1 on the discovery leaderboards'.
- **All interactive filters/sorts operate on pre-truncated server-side slices; stocks ranked beyond the cutoffs are unreachable through the UI** — `generate-methods-report.js:337 (TOP_PICKS_N=200), 432 (TOP_MATRIX_N=300), 235 (TOP_N=50), 543 (LEADERBOARD_TOP=30); filter JS 857-915, 1014-1118` [architecture]
  - This is a structural cap on the screener's usefulness (the '~20% of potential' symptom). The slices are ranked by global pass-count / single-metric value. A name that is, say, 6th-best on FCF-Yield but 700th on pass-count never appears in any list except the FCF-Yield top-50 card — and a custom multi-method AND filter ('roic AND fcf-yield AND net-debt-ebitda') can only ever match within the 300 matrix rows the generator happened to pick by an unrelated ranking. The truncation was added for output-size reasons (Tag 220 F-GR-001, legitimately), but it conflates 'how much HTML to emit' with 'what the user can filter over'.
  - _fix:_ Decouple data from presentation: emit the full (or a much larger) ranked dataset once as compact JSON (the STOCK_DATA_MAP pattern already exists) and let the client render/paginate/filter from it, instead of pre-slicing the DOM. Even a single shared array of {ticker, passCount, per-method pass bitset, key metrics} for the whole universe is a few MB and removes the cap. This is the highest-leverage structural change in this file.
- **'FCF Margin' columns silently report R40's 3y-annual-median fallback value, not TTM FCF margin** — `generate-methods-report.js:136-142 (growthYoYFromRo40/fcfMarginFromRo40), 510-513 + 275 (FCF-Margin leaderboard/deep-dive)` [data-integrity]
  - Two stocks shown side by side under the same 'FCF Margin' header can be measuring different things (one TTM, one a 3y median), and the value can differ by 40+ percentage points (the MELI repair case in the method header: -12.9% TTM vs ~33% median). Karl compares these columns to pick names; an apples-to-oranges column undermines exactly that comparison. The raw stock.metrics.fcfMarginTTM is available on the row (lastRow.fcfMargin, line 102) if the true TTM value is wanted.
  - _fix:_ Either (a) label the cell with the source (the components carry fcfMarginSource — show a small '~med' marker when it is not TTM), or (b) display the raw TTM lastRow.fcfMargin in the 'FCF Margin' column and reserve the R40-component value for the Rule-of-40 column only. Pick (a) to preserve the R40-consistent ranking while flagging the substitution.
- **--top arg parsed with parseInt and no NaN/radix guard -> bad arg silently empties the entire report** — `generate-modes-report.js:54` [bug, auto-safe]
  - A typo or non-numeric --top value (e.g. `--top all`, `--top 200px`, or an env-substituted empty/garbled value in CI) makes topN=NaN. Every picks/near-miss list becomes empty, the report renders with zero cards, yet the script exits 0 and the workflow treats it as success. The pipeline-health failure-rate gate (l.1309) only counts per-stock evaluation throws, not an empty output, so a silently empty user-facing artifact ships. parseInt also lacks a radix (a leading-zero value like `--top 010` would historically be misread; minor vs the NaN issue).
  - _fix:_ Parse defensively: `const n = Number.parseInt(argv[++i], 10); if (Number.isInteger(n) && n > 0) args.topN = n; else { console.error('invalid --top'); process.exit(1); }`. Add a comment naming the failure mode: 'prevents NaN topN -> slice(0,NaN) -> silently empty report'.
- **Modal KPI mislabels latest annual revenue as 'Rev TTM'** — `generate-modes-report.js:1074` [data-integrity]
  - The newest annual revenue is not TTM unless the fiscal year happens to be complete and current. For any company mid-fiscal-year, or where annualRev[0] is a stale prior FY, the headline number Karl reads in the click-through modal is wrong by up to a year. This is a decision-facing number on the primary artifact; mislabeling fundamentals undermines trust in the whole screener. (Snapshots store revenueGrowthYoY separately, confirming TTM and latest-annual are distinct concepts here.)
  - _fix:_ Either relabel to 'Rev (FY, latest)' to match what is actually shown, or compute true TTM from `timeseries.revenueQ` (sum of last 4 quarters) and label it 'Rev TTM'. Whichever is chosen, the label must match the source series.
- **Per-stock and per-mode failures are caught and silently turned into null/skip; snapshot parse errors return null with no diagnostics** — `generate-modes-report.js:122-125, 127-130, 99` [data-integrity]
  - The health gate (the one safety net, l.1309 process.exit(1) at >5%) only sees outer per-stock throws. A systematic bug that makes, say, every QUALITY_COMPOUNDER evaluation throw would set modeEvals.QUALITY_COMPOUNDER=null for all stocks — QC silently shows zero/garbage picks, failure_rate stays 0%, gate passes, ships green. Likewise a batch of unparseable snapshots silently shrinks the universe with no count discrepancy surfaced. This is the 'silently swallowed errors' class: the screener can be badly degraded while every signal says healthy.
  - _fix:_ Count and surface inner failures: increment a per-mode error counter in the l.123 catch and a parse-error counter in loadStocks, include both in the pipeline-health JSON (l.1305) and factor them into the threshold. At minimum console.warn each swallow with ticker+mode+message so CI logs show it.
- **Catch-and-swallow everywhere: bad snapshots, DQ failures, history parse errors, and FX/field gaps are silently dropped with no run-level error budget** — `generate-screener.js:4280-4289, 403-407, 173, 189, 97, 138-141, 4396` [data-integrity]
  - There is no aggregate guardrail: if a snapshot schema change or a corrupt write makes 30% of files JSON.parse-fail, loadStocks silently returns the surviving 70%, buildRow silently skips more, and the run still 'succeeds' (exit 0) with a quietly decimated universe. Combined with the r40-latest 60-day merge (finding 04), a day where most snapshots fail to parse produces a screener that looks normal but is mostly stale carry-over. Silent attrition is exactly the asymmetric-survivorship failure mode the audit brief calls out.
  - _fix:_ Add a run-level sanity gate in main(): count loaded vs skipped vs parse-failed; if skipped/parse-fail exceeds a threshold (e.g. >5% of files, or loaded count drops >20% vs the prior run's logged count) exit non-zero so CI fails loudly instead of shipping a thinned universe. Keep the per-item swallow for resilience but tally and report it; never let a mass failure pass as exit 0.
- **Altman coalesces NaN/garbage totalDebt and totalCash to 0 via `|| 0`, producing a false-SAFE Z-score on a 0.20-weight TURNAROUND gate** — `methods/altman-z-score.js:42-43, 50-51, 67` [bug]
  - `|| 0` cannot distinguish a real zero (debt-free firm) from NaN/missing/corrupt data, and silently treats missing debt as 'no leverage' — the most favorable possible assumption for a solvency model. altman-z-score carries 0.20 weight in TURNAROUND SCORE_WEIGHTS (score-aggregator.js:63), so a single bad balance field flips a distressed turnaround candidate to a clean SAFE pass and lets it score 1.0 on a fifth of the mode weight. This is the exact '|| 0 falsy bug' class.
  - _fix:_ Replace `_balanceVal` with the finite-guarded variant used in beneish/ohlson/penman (`return Number.isFinite(v) ? v : null;`). Then derive `cashVal`/`debtVal` with explicit presence checks: only coalesce to 0 when the field is present-but-genuinely-zero or the firm is independently confirmed debt-free; otherwise return computable:false('totalDebt/totalCash non-finite'). Comment: 'prevents failure mode: NaN debt masked to zero inflates X1/X4 into false SAFE'.
- **Index-pairing of NI/OpInc/GP with Rev and balance assumes year-aligned arrays, but the quoteSummary mapper compresses each series independently via _arr().filter(Boolean)** — `methods/buffett-criteria.js:153-160, 222-231, 376-386, 567-577` [data-integrity]
  - ROE, ROIC, operating margin, gross margin and EPS are all computed by dividing two values the code believes are the SAME fiscal year but which can be different years. The repeated 'F-004'/'F-DP-003' comments in pull-yahoo (lines 570-573, 593-594) explicitly acknowledge this exact alignment hazard and claim null placeholders fix it — but that fix lives only in the FTS path (line 1095+) and the financials-derived path; the PRIMARY quoteSummary _arr path still filters. Silent cross-year division corrupts the headline 14-point composite for any name with a gap year (banks/insurers/fintech are named offenders).
  - _fix:_ Do not rely on positional alignment across independently-compressed arrays. Either (a) consume year-keyed objects (pair by fiscalYear), or (b) within each method build a single aligned table from arrays that share null placeholders. As a method-local guard, only compute a ratio when both arrays still carry the same length AND neither was compressed — but the real fix is upstream consistency. Add comment: `// prevents cross-fiscal-year ratio (NI[i]/equity[i] where i is not the same year)`.
- **DCF Margin-of-Safety is a HARD pass requirement, but any dcf-intrinsic-value load/throw is swallowed and silently demoted to 'MoS not met' rather than 'not computable'** — `methods/buffett-criteria.js:744-761, 696-703` [data-integrity]
  - A transient require error, a throw inside DCF, or a DCF that is merely not-computable for lack of data all collapse to the same verdict as a real valuation failure: the stock is reported as a genuine Buffett FAIL on valuation grounds. There is no distinction between 'expensive' and 'we could not value it'. In BUFFETT strategy mode this method is a MUST, so swallowed DCF errors quietly remove qualifying names with a misleading reason. This is the classic catch->degrade-silently data-integrity pattern.
  - _fix:_ When dcfResult is null OR not computable, set the overall result computable:false (or carry an explicit `mosUnknown` flag and a distinct reason), instead of treating absent DCF identically to MoS-fail. Add comment: `// prevents swallowed DCF error from masquerading as a valuation FAIL`.
- **Insufficient-data guard uses AND not OR: a stock with 4y net income but zero balance years (or vice versa) is treated as computable and scored on half-missing data** — `methods/buffett-criteria.js:693-703` [bug]
  - The whole premise (comment line 698-699: 'Buffett rules require multi-year track record') is that you need BOTH earnings and balance history. The AND lets through half-blind evaluations that then produce a confident-looking 0-100 score and pass/fail. The insufficientDataReason text even says 'both annualNetIncome AND annualBalance < 5y' — but it is checked against MIN_YEARS_REQUIRED=4, not 5, so the message is also wrong.
  - _fix:_ Use OR for the hard gate (`niValid < MIN || balValid < MIN`) since balance data is required by the majority of tests, and fix the 5y/4y wording. Add comment: `// prevents scoring the 14-pt composite when one required series (NI or balance) is absent`.
- **parseFloat(...) || DEFAULT pattern for treasury yield and pass threshold: a legitimate env value of '0' silently falls back to the default (falsy-zero bug)** — `methods/buffett-criteria.js:423, 755` [bug, auto-safe]
  - This is the '|| 0'-class falsy bug inverted: 0 is a legitimately valid configured value that gets discarded. For the threshold, an intended 0 (diagnostic / let-everything-through run) silently becomes 0.85. NaN from a malformed env string also falls back (acceptable) but masks misconfiguration without warning.
  - _fix:_ Parse then explicitly check Number.isFinite: `const t = parseFloat(env); treasuryYield = Number.isFinite(t) ? t : DEFAULT;`. Add comment: `// prevents env value 0 from being discarded by falsy || default`.
- **marketCap envelope unwrap omits Number.isFinite guard, allowing NaN/Infinity marketCap to propagate into the yield and produce a NaN value that buildResult nulls silently** — `methods/fcf-yield.js:23` [bug]
  - A NaN/Infinity marketCap (FX-conversion edge, missing-data envelope with value:NaN) yields a result that is computable:true, pass:false (NaN>=thresh is false), value:null — i.e. the stock is silently marked as FAILING fcf-yield rather than incomputable. Because fcf-yield is a 'prefer' sort hint in QC core and was a scored weight historically, a NaN slips through as a real fail signal instead of a clean NO_DATA flag. Inconsistent with the isFinite discipline applied everywhere else (helpers.metricValue:28 explicitly returns null for non-finite for exactly this reason).
  - _fix:_ Replace the inline unwrap with a finite-checked helper: `var mcap = _unwrap(stock && stock.marketCap);` using the same `_unwrap` shape as the sibling methods (return null unless Number.isFinite). Then the existing `mcap == null` branch (:26) correctly routes to computable:false. Add comment: 'failure mode prevented: NaN/Infinity marketCap propagating to a false-FAIL instead of clean incomputable.'
- **insider-net-buying nets TRANSACTION COUNTS, not share volume — defeats its own stated failure mode** — `methods/insider-net-buying.js:61-70, 89-97` [bug]
  - The exact scenario the method advertises catching — 2 small buys vs a few huge sells — still passes: 2 buy-transactions minus 1 sell-transaction = net +1 > 0 = PASS, even when the sells dwarf the buys in share count. It nets the wrong quantity. The correct signal (netShares90d) is sitting unused in the same object. The method gives a false 'insiders accumulating' verdict precisely when insiders are dumping in size.
  - _fix:_ When using the insiderActivity fallback, base pass/value on signed share volume when available: prefer `act.netShares90d` (already computed by the puller) over the transaction-count delta. Keep the count delta only as a tiebreak/context. Add comment: '// prevents false-accumulate verdict when few large sells outweigh many tiny buys (transaction-count != share-volume).' Do not touch insiderActivity producer or FTS_CACHE_VERSION.
- **Yahoo fallback applies the curated-CIK threshold (>=3) to Yahoo's broad ~7,000-filer count → signal collapses to near-always-pass** — `methods/institutional-ownership-13f.js:109-128, 56` [data-integrity]
  - A '>=3 of ~40 curated smart-money names' bar is a meaningful conviction signal; a '>=3 of ~7,000 filers' bar is satisfied by essentially every non-microcap on earth, so the fallback turns a selective smart-money gate into a near-constant TRUE. Because the fallback fires whenever the SEC cache is missing or the ticker is absent (the common case until the curated cache is broadly populated), in practice the method almost always reports the trivial pass while masquerading (same id/threshold) as the concentrated 13F signal. components.source flags the regime but the binary pass is corrupted.
  - _fix:_ Use a regime-specific threshold in `_yahooFallback` (e.g. broad institutionsCount needs a much higher floor, or compare institutionsPercentHeld instead) rather than reusing the curated THRESHOLD. Add comment naming the failure mode: '// prevents broad ~7k-filer count from trivially clearing a bar calibrated for a ~40-name curated CIK list.' Do not hardcode ticker exclusions.
- **gmEnd/gmStart and medians derived from positionally-compacted gms[] can mix non-adjacent years and misalign GM vs OpM windows** — `methods/margin-quality.js:70-87, 124` [bug]
  - gms and opMs are pushed independently, so if year 2 has gp=null but opInc present, gms skips it while opMs keeps it: gms[k] and opMs[k] then refer to different fiscal years, and gms.length != opMs.length. gmStart (oldest GM) may be 3 years back while opMargMedian spans 5 — the asymmetric GM-Decline test then compares a TTM to a start year that isn't the window boundary it assumes. Since this gates QC MUST-3, a misaligned decline test can wrongly fail/pass a quality compounder.
  - _fix:_ Build a single aligned per-year record {rev,gp,opInc} keeping only rows where all needed fields are finite (or track per-metric year indices), so gmStart/gmEnd/medians all reference the same contiguous window. Comment the failure mode.
- **Exported threshold/thresholdOp (0.35/gte) misrepresent the method: pass is a multi-branch composite, not value>=0.35** — `methods/margin-quality.js:152-156` [architecture]
  - runner.js getMethods() exposes threshold/thresholdOp to the methods report (generate-methods-report.js) and _helpers _inferConfidence/_autoFlags use threshold to compute NEAR_THRESHOLD distance on value. Because value=gmMedian but the real gate is composite, the confidence/NEAR_THRESHOLD flag and the report's 'pass if value>=0.35' description are wrong for any tier-overridden or OpMargin/decline-driven outcome. Not a scoring bug (runner trusts the method's own pass), but it corrupts the confidence/flag metadata and operator-facing report for a CORE gate.
  - _fix:_ Either set thresholdOp to a sentinel ('composite') that _helpers skips for NEAR_THRESHOLD/confidence, or expose the binding sub-threshold actually used (gmFloorRequired/opMarginRequired) per evaluation. Document that value=gmMedian is descriptive, not the gate.
- **Pairs are formed over the COMPACTED years[] array, so a dropped (null/neg-rev) year silently makes a 'consecutive pair' span two fiscal years, inflating revGrowth and diluting the leverage average** — `methods/operating-leverage-margin-accel.js:114-127` [bug]
  - If a loss year (oi present) or a null-rev year sits between two retained years, the 'consecutive pair' actually spans 2 fiscal years. revGrowth across 2 years is roughly double a 1-year growth, so leverage = marginDelta/revGrowth is understated, and the averaged value can fall below the 0.05 pass floor for a genuinely high-leverage compounder (or the reverse). This is the more-robust sibling method but it inherited the contiguity blind spot; the header even claims it 'explicitly conditions on positive-growth years so contraction years don't pollute the average' — but dropping a contraction year still merges its neighbors into one inflated-growth pair.
  - _fix:_ Track each retained year's source index and only form a pair when the two are adjacent in the original array (index diff == 1); skip pairs that straddle a dropped year. Comment the gap-merge failure mode.
- **ΔOI/ΔRev reads fixed indices [0] and [3] with no contiguity/null-position guard — interior nulls or a short clean window corrupt the 3y leverage** — `methods/operating-leverage.js:52-55, 85-86` [bug]
  - annualOpInc and annualRev are built in separate loops in mapFTSToAnnual (pull-yahoo 1098-1108) and a missing operatingIncome in a year writes null to annualOpInc but a real value to annualRev — so oiArr and revArr can have nulls in DIFFERENT positions. Indexing [0]/[3] blindly then divides a real ΔRev by a phantom/misaligned ΔOI. operating-leverage is defaultActive and surfaces in the dashboard as a quality signal; a corrupted value can flip pass at the 0.25 boundary.
  - _fix:_ Collect aligned finite (rev,oi) tuples (like operating-leverage-margin-accel.js does at lines 96-101), require >=4, and take the newest and the 4th-newest of the ALIGNED set. Confirm the two endpoints are 3 fiscal years apart. Add comment naming the misaligned-interior-null failure mode.
- **PEG 'use earnings growth' fix is dead code — earningsGrowthYoY/epsGrowthYoY are never produced by the pipeline, so PEG ALWAYS uses revenue growth** — `methods/peg.js:16-19` [dead-code]
  - Peter Lynch's PEG is BY DEFINITION P/E divided by EARNINGS growth. The method computes P/E divided by REVENUE growth and labels the result 'PEG (Lynch)'. For any company growing revenue faster than EPS (most hypergrowth SaaS, margin-dilutive scalers) the denominator is overstated, PEG is understated, and overvalued names pass the <=1.5 gate. The Tag-179 'Bug #4 / F-ME-009' fix that the comments celebrate has never once executed in production — it is permanently inert because the upstream field was never wired. This is a silent correctness hole masquerading as a fixed bug.
  - _fix:_ Either (a) wire earningsGrowthYoY/epsGrowthYoY in pull-yahoo.js from Yahoo's earningsQuarterlyGrowth / financialData.earningsGrowth and keep the existing preference chain, or (b) until that field exists, rename the method/label/description to 'P/Rev-Growth' and stop claiming Lynch-PEG semantics. Add a method comment: 'failure mode prevented: mislabeling a revenue-growth ratio as earnings-PEG, which under-prices margin-dilutive growth.' Do not silently keep the inert branch.
- **Volatility DATAGUARD hard-fails on a SINGLE -25% YoY anywhere in the window, including the OLDEST pre-IPO/ramp year — asymmetric survivorship attrition against early hypergrowth** — `methods/revenue-volatility-guard.js:99-103, 116-122` [data-integrity]
  - This is the dominant structural cap on the screener: a hygiene guard built for one pattern (SPHR) silently culls legitimate re-accelerating compounders that had a single down year (COVID air-pockets, divestitures, one weak fiscal year). Survivorship is asymmetric — only clean monotone histories survive — which is exactly the 'stuck at 20% of potential' symptom. Compounding bug A-methods-growth-09 means the same -25% guard ALSO reads non-USD magnitudes incorrectly via the post-conversion currency tag.
  - _fix:_ Require the decline to be RECENT (e.g. only index 0 or 1) OR require >=2 decline years to fail, and exempt the oldest pair when the series is otherwise monotone-increasing toward the latest. Keep it a guard but stop hard-disqualifying on a single stale dip. Comment: 'prevents single stale-year dip from culling re-accelerating compounders (survivorship attrition)'.
- **ROIC-trend pairs annualNetIncome[idx] with annualBalance[idx] assuming positional year-alignment that the quoteSummary path does not guarantee** — `methods/roic-trend.js:49-67, 86` [data-integrity]
  - A one-year offset between NI and invested-capital silently computes ROIC from FY2023 NI over FY2022 balance, then reports a spurious YoY 'improvement' or 'decline'. roic-trend is a QC trajectory signal; a misalignment flips pass/fail (threshold is gt 0) on noisy data with zero error surfaced. The method header (lines 28-31) claims robustness to 2-3y balance coverage but never checks that NI and balance indices refer to the same year.
  - _fix:_ Either (a) key both series by an explicit fiscalYear/period stamp and align on that, or (b) at minimum guard that niArr and balArr have equal length before index-pairing and bail to computable:false otherwise. Comment: '// prevents: NI/balance index drift from independent filter(Boolean) compaction misdating ROIC YoY delta'.
- **Sloan ratio uses FCF (NI - FCF) instead of operating cash flow (NI - CFO), so capex is wrongly counted as an accrual — overstating |Sloan| for capex-heavy firms** — `methods/sloan-ratio.js:35-36, 56-58` [bug]
  - Capex is an investing outflow, not an earnings accrual. Adding it inflates the Sloan magnitude for reinvestment-heavy firms (the TURNAROUND/hypergrowth archetype), pushing them toward the WARNING/REVIEW/EXTREME bands and toward the red-flag rule `val > 0.20` (score-aggregator.js:90-98). A capex-heavy but honestly-accounted recovery can be flagged as accrual-manipulation purely because it is investing. The asymmetric posV logic (lines 79-99) does not fix this — it only changes sign handling, not the capex contamination.
  - _fix:_ Compute accruals as (NI - OCF)/totalAssets, preferring annualOCF and falling back to annualFCF only when OCF is absent (mirror piotroski.js:54-55 `cfo0 = ocf0 != null ? ocf0 : fcf0`). Ideally scale by average totalAssets ((ta_t+ta_{t-1})/2) per the canonical definition. Comment: 'prevents failure mode: capex miscounted as accrual inflates |Sloan| for reinvesting firms'.
- **WC-anomaly guard silently passes any firm whose current-year WC proxy is negative — the growth ratio sign-flips and trivially satisfies `<= 1.3`** — `methods/working-capital-anomaly.js:23-27, 45, 50-51, 57, 60` [bug]
  - working-capital-anomaly is a SoftGuard (score-aggregator.js:205, penalty 6). pass=true means the penalty is NEVER applied, and in graduated lte scoring a negative value would even earn 0.99 (score-aggregator.js:159). So precisely the firms most likely to be manipulating working capital while sitting on a large cash/deferred-revenue balance escape the detector entirely. The negative-ratio reason string ('WC growth -120%') is also nonsense reported to the dashboard.
  - _fix:_ After computing wcT/wcT1, require BOTH to be positive for a meaningful growth ratio; if wcT<=0 (or sign differs between years) return computable:false with reason 'WC proxy non-positive / sign-flip — growth ratio undefined' rather than a misleading pass. Comment: 'prevents failure mode: negative current WC proxy flips growth ratio negative and trivially passes the <=1.3 manipulation gate'.
- **Two scripts write the SAME prices/history.json with DIFFERENT price adjustment bases (raw close vs adjclose) — split-contaminated momentum** — `pull-prices-bulk.js / pull-historical-prices.js:pull-prices-bulk.js:61-67 vs pull-historical-prices.js:99-103` [data-integrity]
  - price-momentum-12-1.js:206-209 computes `priceLastMonth / priceOneYearAgo - 1` across that single series, and walk-forward-perf.js:106/150 computes returns over it. A stock that split 2:1 in the window will have a ~100% phantom return at the raw/adjusted boundary, or a real split silently erased — directly corrupting the momentum factor and every backtest return that reads this file. This is exactly the kind of silent data corruption that caps the screener's reliability.
  - _fix:_ Make adjustment basis a single invariant for prices/history.json. Standardize ALL writers on `q.adjclose ?? q.close` (pull-prices-bulk.js:61-64 must change `close: q.close` -> `close: q.adjclose ?? q.close`). Add a one-line provenance/version key per ticker series or a top-level `{ priceBasis: 'adjclose' }` marker so a future writer mismatch fails loud. Each touched method/consumer change must carry a comment naming the failure mode (split-contaminated mixed-basis series).
- **Quarterly net-income FTS extraction uses raw r.netIncome — misses NetIncome / netIncomeContinuousOperations casing, unlike the annual path** — `pull-yahoo.js:1836-1837` [bug, auto-safe]
  - Yahoo FTS rows for many tickers emit PascalCase ('NetIncome') or the GAAP-continuous variant ('netIncomeContinuousOperations') instead of camelCase 'netIncome' (the file itself documents this drift at lines 1054-1064 and 1099-1102). This map() bypasses the _ftsValue helper, so for every such ticker the entire quarterly-NI series reads all-null. That silently disables the 8-quarter earnings-stability method (Tag-90) — exactly the kind of method that should separate the screener's '20% of potential' from its full power. The bug is masked because nulls look like 'Yahoo didn't provide it' rather than 'we read the wrong key'.
  - _fix:_ Route through the existing helper: `.map(r => { const v = (r != null) ? _ftsValue(r, 'netIncome', 'NetIncome', 'netIncomeContinuousOperations') : null; return v != null ? v : null; });`. No FTS_CACHE_VERSION bump needed (read-shape change only; affects freshly-parsed rows). Comment the failure mode: 'prevents all-null quarterly-NI when Yahoo FTS emits PascalCase/continuous-ops netIncome key'.
- **Schema-stale + currency-stale probes each do a full readFileSync+JSON.parse of every young snapshot — two full parses per ticker on the 80%-hit fast path** — `pull-yahoo.js:1451-1479, 1502-1529` [performance]
  - For a ~15-20k universe where ~80% hit the young path, that is up to 3 full JSON.parse of a ~50KB snapshot per ticker per run (~45-60k parses), the file's own comment (line 1438) already flags one parse as a cost concern but two more were added by Tag 226a-2/230a. This is wasted recompute on the hottest path and a direct contributor to the long pull window. The two probes read the same file; they can share one parse.
  - _fix:_ Read+parse the snapshot ONCE in processOne (or in a combined probe) and pass the parsed object to both checks plus _priceOnlyUpdate. Refactor the two `_existingSnapshot*` functions to accept an already-parsed object. No semantic change. Comment: 'single parse shared across schema/currency probes + price-only — was 3× parse per fast-path ticker'.
- **Schema-validation failures on international tickers are reclassified as 'not-found' and the snapshot is marked delisted — silent survivorship attrition of valid non-US names** — `pull-yahoo.js:2123-2139` [data-integrity]
  - The comment (lines 2106-2119) acknowledges the risk: a multi-module quoteSummary that fails the library's strict-shape validator gets classified not-found and the existing snapshot is permanently flagged delisted. For genuinely-listed Korean/Taiwanese/Japanese/HK/Chinese names that merely returned a partially-populated (but valid) payload, this is asymmetric survivorship attrition — the international leg of the universe quietly shrinks and never gets retried (delisted flag suppresses future attempts). This is precisely the 'silently swallowed errors → mark dead' and 'asymmetric survivorship attrition' pattern from the prompt, and it structurally caps international coverage.
  - _fix:_ Do NOT mark delisted on schema-validation failures — only on literal 404/'no data'/'invalid symbol'. Split errClass: keep a distinct 'schema-fail' class (counted, monitored via pull-stats-check as the comment intends) that does NOT set meta.delisted, so the ticker is retried next run. Reserve the delisted flag for the unambiguous not-found regex (line 2102). Comment: 'prevents valid international tickers with partial-but-valid payloads from being permanently marked delisted (survivorship attrition)'.
- **FTS-vs-quoteSummary merge logic is a 80-line tangle of per-field non-null-count comparisons duplicated 7× — the structural source of the year-alignment bug class** — `pull-yahoo.js:1859-1898, 1932-1937` [architecture]
  - The inconsistency between `> non-null count` (rev/opinc/ni), bare `.length > 0` (GP/FCF/OCF lines 1932/1936/1937), and unconditional triplet replacement (revenueQ siblings) is exactly what spawned the long parade of F-DP-030/031/F-002/F-010 alignment fixes documented inline. annualGP at line 1932 uses `ftsAnnual.annualGP.length > 0` — a length test, so a 4-entry all-null FTS GP array (placeholders) would overwrite a populated QS GP array, reintroducing the very drift the count-based guards elsewhere prevent. This god-merge block is the single biggest structural cap on data correctness in the file.
  - _fix:_ Extract one helper `mergePreferRicher(qsArr, ftsArr, {mode:'count'|'length'})` and a single `mergeQuarterTriplet(...)` that moves rev/opInc/gp as a unit, then call them uniformly. Fix annualGP (line 1932) to use the non-null-count predicate like its siblings, not `.length>0`. Pure refactor preserving the count-based semantics. Comment each call with the failure mode (year-index drift) it prevents.
- **pickPriorAnnual independent fallback can pair Goodwill latest/prior from non-adjacent fiscal years, corrupting deltaGoodwillYoY** — `scripts/extract-ma-rpo.js:168-193, 296-303` [data-integrity]
  - For companies with a fiscal-year change, a restatement (10-K/A), or a gap in filings, prior can be 2 years back or an amended duplicate of the same period. `deltaGoodwillYoY` and `headlineGrowth` (line 325-326) then measure the wrong interval. The fallback at line 191 has no day-gap guard at all, so it silently accepts any spacing.
  - _fix:_ Drop the unconditional `annuals[latestIdx+1]` fallback, or constrain it to a max gap (e.g. <=550 days) and emit the actual gap in the record so downstream can reject mismatched pairs. Return null rather than a wrong-interval pair.
- **byQuality buckets only know A/B/C/D — the dominant 'A+' grade (88% of universe) is silently dropped from every quality split** — `scripts/method-effectiveness.js:195-199, 242-247, 350-355, 457-461` [data-integrity]
  - The Phase-3.5 quality-split — the whole point of recording `_quality.grade` per vintage (snapshot-methods-history.js line 70-89) — is dead for the grade that covers ~9 of every 10 stocks. byQuality['A'] reports on the 493 plain-'A' stocks, NOT the 4180 'A+' ones, so any reader concluding 'method X works best on A-grade names' is reasoning from a 10% non-representative slice. This is exactly the kind of silent cap that holds the screener at '20% of potential': a built feature producing confidently-wrong sub-analysis.
  - _fix:_ Normalize the grade to its bucket before lookup, e.g. derive a base grade `const qBase = quality ? quality.charAt(0) : null;` and key byQuality on qBase (so 'A+','A-','A' all fold into 'A'). Add buckets if finer grades are wanted, but folding is the minimal fix. Comment must name the failure mode: '// prevents: A+/A- grades silently excluded from quality split (byQuality only had A/B/C/D)'. Add a one-line guard that logs once if a grade fails to map to any bucket, so a future grade-scheme change is loud instead of silent.
- **degenerateZero gate kills any method whose pass- or fail-median is exactly 0.0 — a legitimate median, conflated with the manufactured-zero signature** — `scripts/method-effectiveness.js:408-409, 418-421` [data-integrity]
  - A method with genuinely neutral central tendency (median≈0) is the MOST common honest outcome for a weak signal, yet it gets stamped 'degenerate-zero-returns' and suppressed entirely — alpha=null, no CI, excluded from methodology-report's significant/neutral tables. This systematically erases true negatives/neutrals from the effectiveness ranking, biasing the surviving set toward methods that happen to have non-zero medians. Combined with the resolution-level 0-day guard already in place, this second guard now does more harm than good.
  - _fix:_ Since the manufactured-zero source is already eliminated at resolution time (entry===exit drops), tighten this defensive gate so it only fires on the manufactured signature, not on legitimate zeros: require BOTH a zero median AND evidence the cohort is artificial (e.g. canonicalFrac extremely low, or a high fraction of exactly-0 raw returns in group.fail). Or drop the median-equality gate and instead gate on 'fraction of exactly-0.0 raw returns > threshold'. Comment: '// prevents: legitimate median==0 (weak/neutral signal) being misclassified as a 0-day-window manufactured zero'.
- **Look-ahead bias: magic-formula PASS/FAIL is computed from TODAY's market cap/EV, then matched to a PAST vintage's forward return** — `scripts/tag229b-new-method-backtest.js:146-159, 184-187` [bug]
  - magic-formula's value leg depends on price, which moved materially between the vintage dates and 'today'. Using today's cheap/expensive classification to predict a return that already happened weeks earlier injects future information into the PASS bucket. The reported magic-formula spread is contaminated; only asset-growth-anomaly and penman-nissim (balance-sheet-only, confirmed reading `stock.annual.annualBalance`) honor the slow-moving assumption. A spurious 'PASS beats FAIL' for magic-formula could green-light a method that has no real edge — directly relevant to the '20% of potential' framing.
  - _fix:_ Restrict the today's-snapshot shortcut to balance-sheet-only methods. For magic-formula, either (a) reconstruct EV at the vintage date from `prices/history.json` close * shares + net debt, or (b) exclude magic-formula from this smoke test and label it 'not testable without point-in-time EV'. Add a comment naming the failure mode: '// prevents look-ahead: price-derived EV must be as-of vintage, not today'.
- **MIN_SAMPLES gate uses pickReturns count `n` for alphaVsBenchmark/alphaVsFrozenVintage, but the benchmark/frozen leg may itself have <MIN_SAMPLES — alpha published on a thin benchmark** — `scripts/walk-forward-perf.js:514-520, 537, 557` [bug]
  - A frozen-vintage benchmark median built from 2 tickers, or a universe median from a handful, is statistically meaningless, yet alphaVsFrozenVintage / alphaVsUniverse are published whenever the PICK side clears 10. The per-vintage value then flows into the summary medians (the F-BT-004 vintage-count gate only protects the cross-vintage aggregate, not the per-vintage value that pick-diff/dashboard may read). This is the same false-confidence failure the MIN_SAMPLES gate exists to prevent, applied to the wrong leg.
  - _fix:_ Gate each alpha on BOTH legs: e.g. `alphaVsFrozenVintage = (n >= MIN_SAMPLES && frozenVintage.n >= MIN_SAMPLES && ...)`. For universe similarly check univResult.n. Benchmark is a single instrument so document that its 'n' is the pick n only. Comment naming the failure mode on each guarded line.

## LOW (129)

- **eligibleForMode duplicates the sector-exclude + dataGuard reject logic that evaluateMode already performs** — `C:/Users/Karlr/screener-wt/audit-hardening/generate-modes-report.js:232-243` [architecture]
  - Two copies of the hygiene gate can diverge: if evaluateMode's reject conditions change (e.g. the Tag 206l mcap_missing handling, or a new guard), generate-modes-report keeps the old logic and the report shows a different eligible set than the score path. Single-source-of-truth violations like this are how 'fixed in methods/ but still wrong in the report' bugs arise.
  - _fix:_ Derive eligibility from the cached modeEval (e.g. reject when `me.reason` is one of the hygiene reasons, or expose an `eligible` boolean from evaluateMode) instead of re-deriving from raw guards/sector. Keep methods/strategy-modes.js as the single arbiter. (Respect the constraint: this edit is in generate-modes-report.js, not methods/index.js.)
- **Retry budget is shared across 429/5xx/network so a 429 burst can exhaust attempts and drop the alert without honoring the last retry-after** — `C:/Users/Karlr/screener-wt/audit-hardening/lib/discord.js:79-122` [bug]
  - With maxRetries=2 the loop makes at most 3 sends. On the final attempt (attempt===maxRetries) a 429 falls through all the `attempt < maxRetries` guards to the generic 4xx branch at line 119 ('Discord post failed: HTTP 429') and returns false — the alert is dropped even though Discord told us exactly how long to wait. Since the module's own comment says bursts 'often 429', the worst-case (every send 429) silently loses the alert this code exists to guarantee.
  - _fix:_ Track 429 retries separately from a small fixed budget, or on the terminal 429 do one final honored sleep+send instead of treating it as permanent failure. Comment naming the failure mode: '// prevents dropped alert under sustained 429 — terminal rate-limit must still honor retry-after once'.
- **Permanent failures are logged to console.log and swallowed (return false), not surfaced — a dead webhook URL fails silently forever** — `C:/Users/Karlr/screener-wt/audit-hardening/lib/discord.js:90, 119` [data-integrity]
  - A 401/404 (rotated/bad DISCORD_WEBHOOK) is a permanent misconfiguration that disables every alert. It is logged at console.log level (not console.error / not a non-zero exit) and the return value is discarded by all consumers, so the screener's alerting can be silently dead for weeks. This is the 'silently swallowed error -> alert never surfaces' class the module was created to kill (header lines 6-10).
  - _fix:_ Keep postDiscord returning false (callers may legitimately not care), but log permanent 4xx at console.error and consider one self-test/heartbeat in CI that asserts postDiscord returns true. At minimum upgrade the log level so a dead webhook is visible in CI logs.
- **classifySubProfile and classifySubProfileDetailed disagree on banks and on sector-precedence — two public classifiers return different sub-profiles for the same stock** — `C:/Users/Karlr/screener-wt/audit-hardening/lib/sub-profile.js:78-82 vs 116-138` [data-integrity]
  - A bank (industry contains 'bank') is classified BANK by classifySubProfile but FINTECH by classifySubProfileDetailed. Banks have their own peer group for sector-relative medians (sector-medians-compute.js CLI uses classifySubProfile at line 368). If any consumer ever switches to the 'detailed' variant — or if the two are compared in a test/migration — banks silently change peer group, contaminating sector-relative ROIC/medians with fintech peers. Two functions in one file that claim to implement the same precedence ladder must not diverge; this is a latent data-integrity landmine, not just style.
  - _fix:_ Make classifySubProfileDetailed delegate to a single shared classification core (return {profile, confidence, reasonCode}) so the precedence ladder exists once. Add a BANK branch to detailed mirroring line 79. Comment: '// prevents F-classifier-divergence: bank peer-group must be identical across both public entry points'.
- **classifySubProfileDetailed is exported but has zero live consumers (its own comment admits it)** — `C:/Users/Karlr/screener-wt/audit-hardening/lib/sub-profile.js:114-139, 141` [dead-code]
  - It is ~24 lines of a parallel, subtly-divergent classifier (see A-lib-08) that no one runs, so its divergence is invisible and untested — dead code that is also a correctness trap. Keeping a second copy of the precedence ladder guarantees future drift.
  - _fix:_ Either delete classifySubProfileDetailed and its export, or (preferred, since the confidence/reasonCode is genuinely useful) make classifySubProfile a thin wrapper over it so there is one ladder and the detailed version becomes the tested source of truth. Resolving A-lib-08 by delegation also resolves this.
- **looksKeyedMap heuristic is duplicated verbatim in extractStocksArray and detectShape and can drift; a wrapped object missing its stocks array is misread as a ticker-keyed map** — `C:/Users/Karlr/screener-wt/audit-hardening/lib/watchlist-fs.js:38-55 and 60-70` [architecture]
  - The module's stated purpose (header lines 11-16) is to be the single source of truth so 'future schema migrations need to land in exactly one file' — but the core discrimination logic lives in two places inside that very file, so they can silently disagree (one says shape='wrapped', the other extracts via the keyed-map branch). The downstream contract (size used by the drift gate in check-pull-stats.js:36) depends on these agreeing. A misclassified wrapped file yields a bogus synthetic stocks array and a wrong universeSize, which is the exact 'silently disabling the drift gate' failure the header warns about.
  - _fix:_ Extract one private `_classify(raw)` returning {shape, stocks} and have both public functions call it, so the heuristic exists once. Tighten the keyed-map guard to require a MAJORITY of keys (not .some) to look like tickers, and explicitly exclude a present-but-non-array `stocks` key from being treated as keyed-map. Comment: '// prevents drift gate corruption from a wrapped file whose stocks field degraded to non-array'.
- **Price-history loaders swallow JSON.parse errors silently (catch → empty cache), unlike the loud _warnMediansLoad pattern in _helpers.js** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/above-200d-ma.js:16-17 (and high-proximity-52w.js:16-17, drawdown-52w.js:16-17, volatility-annualized.js:16-17, price-momentum-12-1.js:85-87)` [data-integrity]
  - A single corrupt or mid-write history.json would silently zero out every momentum/trend/volatility/drawdown signal across the entire universe with no CI/pipeline-health signal — exactly the regression class the medians fix (F-SM-022) was created to prevent, left unpatched on the price path.
  - _fix:_ Add the same console.warn-on-parse-failure (reuse a shared warn helper) in each price loader; still fall back to {} for liveness. Comment: // prevents: silent universe-wide loss of price signals on a corrupt/partial history.json write (mirrors F-SM-022).
- **Asset-growth-anomaly assumes annualBalance[0] and [1] are consecutive fiscal years; an interior null/gap distorts or disables the 1y growth rate** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/asset-growth-anomaly.js:86-112` [bug]
  - Cooper-Gulen-Schill is specifically a ONE-year total-asset growth predictor (header lines 6-7,12). Treating [0]/[1] as adjacent without confirming consecutive fiscal years means a gap-year snapshot yields either a missed signal or a 2-year growth compared against a 1-year (20%) threshold — systematically over-failing names with a balance-sheet gap. It is a DIAGNOSTIC (method-types.js:108), so blast radius is detail-modal/percentile views, not hard gating — hence low.
  - _fix:_ Confirm balArr[0] and balArr[1] are consecutive reporting periods before computing (carry period dates, or skip null placeholders and verify a 1-year gap). Comment: 'prevents labelling a multi-year asset growth as 1y when annualBalance has an interior gap year'.
- **BORDERLINE flag is buried in components.flag and never reaches result.flags, so the soft-fail signal is invisible downstream** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/betting-against-beta.js:135, 149-151` [bug, auto-safe]
  - The method's header makes the (1.0,1.5] BORDERLINE band a core feature ('prevents the operator from mis-reading this as a strong negative'). Because the flag never surfaces in the standard flags channel, the dashboard/detail-modal cannot distinguish a 1.1-beta soft-fail from a 2.2-beta hard-fail — the documented operator value is silently lost.
  - _fix:_ Return a top-level `flags: borderline ? ['BORDERLINE'] : []` on the result (keep the components copy for debugging). Comment: // prevents: BORDERLINE soft-fail being invisible because it lived only in components, not the wrapEvaluate flags channel.
- **Stale DCF doc comments: header & inline say growth clamp [-5%, 25%] over a '10-year' projection; code uses GROWTH1_CLAMP_MAX=0.15 and S1_YEARS=7** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/dcf-intrinsic-value.js:23-26, 53-54, 257-264` [other, auto-safe]
  - The comments describe the OLD (pre-tightening) model. Anyone reasoning about IV magnitude or reviewing a regression will trust the 25%/10y prose and mis-predict outputs. Given DCF feeds the BUFFETT score (finding 01), stale documented assumptions on the growth cap are a real review hazard.
  - _fix:_ Update header (lines 23-26) and inline (lines 257-264) to state the actual clamp [-5%, 15%] and the 7+5 = 12y explicit horizon. Comment-only; no behavior, no SCORE_WEIGHTS, no fixture change.
- **break-on-first-null walks latest-first annualRev, so one missing/late LATEST fiscal year zeros cleanYears for an established company** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/listing-age.js:56-61` [data-integrity]
  - score-aggregator.js:382 multiplies QC score by min(value/5,1.0); cleanYears=0 → multiplier 0 → established compounder's QC score forced to 0 and pass flips false — a survivorship/attrition artifact driven purely by a single late filing, not by business quality.
  - _fix:_ Skip a leading null (or count total consecutive non-null from the first non-null entry), or count from the oldest end inward. At minimum, if revArr[0]==null but revArr[1..] are clean, start counting at the first non-null index. Comment: // prevents: a single missing latest-FY revenue zeroing the clean-year count for long-established names.
- **Loss-magnitude-guard tests TTM margin re-labelled as annual when opIncSource=='computed-margin'** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/loss-magnitude-guard.js:52-75` [data-integrity]
  - The guard claims to gate on annual operating loss vs annual revenue (header line 8: `annualOpInc[0]/annualRev[0] >= -0.50`). For financial-sector names that fell to the margin fallback the 'annual' figure is just the TTM margin — the guard is not measuring what its reason string and audit trail claim, weakening the SOUN/IONQ-style detection for that cohort and making the components.oi0 value misleading for downstream review.
  - _fix:_ When `stock.meta.opIncSource === 'computed-margin'`, either mark computable:false or set a components flag `opIncDerived:true` so the reason string and Deep-Dive modal disclose the figure is TTM-derived, not an independent annual reading. Comment: 'prevents presenting TTM-margin-derived OpInc as an independent annual loss ratio'.
- **NI-volatility denominator rev0 and NI swing can reference different fiscal years (independent arrays, no alignment check)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/net-income-volatility-guard.js:67-83, 99-106` [bug]
  - The ratio is interpreted as 'max single-year NI swing as a fraction of one year's revenue' (header lines 11-12). If revArr has a leading null (latest year unreported) while annualNetIncome does not, rev0 is a prior, smaller year, inflating the ratio toward the 1.0 hard-fail gate (DATAGUARD, method-types.js:82) and risking a false disqualification — or the reverse. Lower severity than the divergence/forecast cases because both Yahoo arrays usually populate together, but it is the same class of unguarded positional assumption.
  - _fix:_ Require revArr[0] (raw, pre-compression) to be a finite latest-year value aligned with nis[0], or normalize the NI swing against the revenue of the same fiscal index that produced maxDelta. Comment: 'prevents dividing an NI swing by a revenue figure from a different fiscal year when the two annual arrays are misaligned'.
- **_arrVals() is defined but never called in premium-compounder-proof.js (only _rawVals is used)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/premium-compounder-proof.js:21-25` [dead-code, auto-safe]
  - Dead helper invites accidental use: a future edit might call _arrVals (null-collapsing/filtered) where the file deliberately needs _rawVals for positional alignment (see the explicit 'use raw positionally aligned arrays' comment on line 46 and Bug #10 note on line 69). Filtered arrays would silently reintroduce the index-misalignment bug the file was hardened against.
  - _fix:_ Delete lines 21-25 (`function _arrVals`). It is purely local (not exported), so removal is safe and cannot affect any caller. No methods/index.js touch needed; no fixture-hash impact (function is unreferenced).
- **Local _cagr() lacks the latest<=0 guard that _helpers.cagr3y() has — NaN possible if latest revenue is non-positive** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/premium-compounder-proof.js:37-40, 72-74` [bug, auto-safe]
  - If a snapshot carries a negative latest 'revenue' (data-quality artifact: sign-flipped FTS row, or a non-revenue row mis-mapped to annualRev — the file's own comments warn FTS mapping has had alignment issues), `Math.pow(negative, 0.25)` = NaN. rev5yCagr becomes NaN; `NaN >= 0.15` is false so the check fails closed rather than throwing, but NaN then lands in components.checks[].val and the report. Low severity because revenue is rarely negative, but it is an unguarded NaN-propagation path inconsistent with the shared helper.
  - _fix:_ Add the sign guard to match _helpers: `if (oldest == null || oldest <= 0 || latest == null || latest <= 0) return null;`. Comment: '// prevents: NaN from fractional power of non-positive latest revenue (sign-flipped/mis-mapped FTS row)'. Pure internal helper, no index.js/fixture-hash concern unless a fixture currently relies on negative-revenue behavior (none do).
- **Exported value=passing counts ALL checks (incl. N/A) but threshold=6; graduated score in fail-case under-credits stocks with legitimate N/A checks** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/premium-compounder-proof.js:168-190, 184` [bug]
  - A stock with, say, 4 computable checks of which it passes 3 and fails 1, plus 2 genuine N/A (OCF/RnD cache gap), reports value=3 -> ratio 3/6=0.5 -> low partial score, as if it failed 3 of 6. But it only failed 1 of 4 evaluable. The denominator should be evaluable.length, not the fixed 6. The pass=true path short-circuits to 1.0 so this only bites the partial-credit fail case, but premium-proof carries a (small) 0.05 QC weight, so it does feed the score and systematically under-rewards data-incomplete names — the opposite of the file's stated 'N/A is not an implicit failure' intent.
  - _fix:_ Make value/threshold semantics align with the evaluable-only gate. Either export value as a fraction the aggregator can grade against threshold=1 (e.g. `value: evaluable.length ? passing_evaluable/evaluable.length : null`, thresholdOp 'gte', threshold computed to all-pass) or set `value: passingEvaluableCount` and pass a per-result `threshold: evaluable.length` via buildResult so normalizeMethodScore divides by the right denominator (it already prefers result.threshold over meta, score-aggregator.js:118). Comment the failure mode: '// prevents: N/A checks inflating the score denominator (passing/6) and under-crediting data-incomplete compounders'. Verify against tag28 premium-proof fixtures.
- **Decimal-vs-percent unit guard rejects legitimately small-but-percent inputs (|growth|<=1 AND |fcfMargin|<=1) as 'unit error'** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/rule-of-40.js:89-95 (same pattern rule-of-x.js:30-36)` [bug]
  - The guard cannot distinguish a true 'someone passed 0.38 instead of 38' bug from a genuine near-flat company whose percent values happen to both be < 1.0. Such names are silently dropped from the R40 universe (the most permissive tab) — a survivorship/attrition bias against low-growth-but-positive names, and the data-integrity class flagged in the brief (0/small values legitimately valid). Because the pipeline already guarantees percent units upstream, the guard defends against an input shape that cannot occur on the real path while creating false negatives on the path that does.
  - _fix:_ Tighten the heuristic so it only fires on the actual corruption signature: require BOTH |growth|<=1 AND |fcfMargin|<=1 AND at least one of them be non-zero-and-fractional in a way percents are not — e.g. only treat as unit error when the SUM |growth|+|fcfMargin| <= 1 AND both are non-integer-ish, or better, drop the heuristic and rely on the upstream percent guarantee. Minimum safe change: gate on `Math.abs(growth) < 1 && Math.abs(fcfMargin) < 1 && (growth !== 0 || fcfMargin !== 0)` is NOT enough; prefer requiring the combined R40 value < 2 to flag. Add comment: 'prevents false unit-error rejection of genuinely near-flat percent inputs'. Verify against fixture (growth=38/fcfMargin=22) which is unaffected.
- **v1 legacy flat-schema branches in lookupMedian/lookupPercentile are unreachable from production loaders** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/sector-median-lookup.js:56-64, 114-122` [dead-code]
  - Not harmful, but it is latent dead code that implies a v1 file path still flows through here when it does not. It widens the surface a reader must reason about and can mask the fact that a genuinely malformed (non-v2, non-{}) object would silently fall through to the legacy branch and return a value from an unexpected shape.
  - _fix:_ Either drop the v1 branches (tests can wrap fixtures in v2 like the loaders do) or add a comment that they exist solely for direct/test callers and are not reached via _loadAutoMedians. Keep if a documented external caller relies on flat input; otherwise prune.
- **POSITIVE_ONLY_METRICS filter keeps values down to -0.05, contradicting its own 'positive only' contract** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/sector-medians-compute.js:147, 159, 156-164` [bug]
  - ROIC values between -5% and 0 are admitted into the 'positive-only' median. In a loss-heavy sector (e.g. SAAS/biotech-tilted region buckets) a cluster of small-negative ROICs pulls the median down toward/under zero. sector-relative-roic._rankRoic returns null when `p50 <= 0` (sector-relative-roic.js:99), so the whole method silently goes incomputable for that sector instead of ranking — or, if p50 lands just above 0, the percentile gate becomes anti-signal exactly as the comment warns. The -0.05 floor partially defeats the F-ME-004 minN-positive guard right below it.
  - _fix:_ Either make the filter match the contract (`v > 0`) or rename the constant/comment to reflect a deliberate small-loss tolerance and document WHY -0.05. Given _rankRoic already rejects p50<=0, `v > 0` is the consistent choice. Add comment naming the failure mode: '// prevents small-negative ROICs inverting the above/below-median percentile signal in loss-heavy sectors'.
- **Rolling 12m median is an unweighted median-of-daily-medians; ignores per-day sample count n** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/sector-medians-compute.js:320-334, 330` [data-integrity]
  - effectiveThreshold() prefers the rolling12mMedian over the live auto-median once >=12 weekly samples exist (_helpers.js:226-228), so this is the AUTHORITATIVE threshold for mature sub-profiles. A few low-n days (e.g. early in a sector's history, or a partial pull) carry the same weight as full-universe days, so the live pass-gate can drift toward a noisy small-sample estimate. Median-of-medians is also statistically biased vs pooling the underlying observations.
  - _fix:_ Weight by n (e.g. n-weighted percentile of the daily medians) or, better, persist the raw per-day value arrays and pool them for the rolling computation. At minimum drop daily entries below a small n-floor before taking the rolling median. Add comment: '// prevents low-sample days from dragging the authoritative rolling threshold'.
- **ROIC bucket uses invested-capital = totalAssets - cash with no guard against cash > assets or near-zero IC blowups** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/sector-medians-compute.js:116-119, 128-131` [data-integrity]
  - Net-cash software/biotech names (very common in the growth universe Karl targets) can produce ROIC of several hundred percent purely from a denominator artifact. Because p75/p90 drive the sector-relative top-quartile gate, a couple of these inflate the bar so legitimately-strong-but-not-absurd compounders fall below p75 and get filtered. Anti-signal in the high tail.
  - _fix:_ Add a denominator sanity floor (e.g. require `ic >= some fraction of ta`, or cap |ni/ic| at a sane bound before pushing), consistent with however roic.js / sector-relative-roic.js intend to treat net-cash firms. Mirror the exact guard in both compute and the consuming method. Add comment: '// prevents near-zero invested-capital from producing artifact ROIC that inflates sector p75/p90'.
- **SoftGuard failures never surface as human-readable warnings in buildStory (e.g. HYPERGROWTH revenue-shock / deceleration / quarter-concentration)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/strategy-modes.js:325-361, 92, 302-311` [bug]
  - Karl reads the candidate STORY, not the score internals. A HYPERGROWTH stock whose revenue jump looks like a one-off (revenue-shock-guard fail) still passes (soft guard, correctly non-blocking) and gets a score penalty — but the story shows NO warning, so Karl sees a clean candidate with a hidden landmine. The SOFT_WARNING_TEXT table is effectively dead: it can only ever be reached via mode.softWarnings, which is empty everywhere. The whole soft-warning UX is wired to the wrong array.
  - _fix:_ In buildStory, after the dataGuards loop, iterate `modeEval.failedSoftGuards` (already returned by evaluateMode at line 293) and push `SOFT_WARNING_TEXT[sgId] || (sgId + ' auffaellig')` into warnings. Keep the score penalty as-is. Add comment: '// prevents silent soft-guard failures — Karl must see revenue-shock/deceleration warnings in the story, not just a hidden score penalty'.
- **PROFILES registry and applyProfile() are exported but called by nobody (entire QC profile-override feature is dead)** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/strategy-modes.js:390-429, 431-438` [dead-code]
  - M&A-compounders, asset-heavy compounders, and COVID-recovery names are precisely the QUALITY_COMPOUNDER candidates being wrongly rejected today (net-debt-ebitda must, reinvestment-rate must, earnings-stability must). The mechanism to admit them was built and then never wired into the pipeline — a concrete chunk of the 'screener at ~20% of potential' gap. Dead code also misleads future readers into thinking profiles are active.
  - _fix:_ Do NOT delete — wire it in. In generate-modes-report.js / generate-screener.js, evaluate QUALITY_COMPOUNDER candidates that fail exactly one of {net-debt-ebitda, reinvestment-rate, earnings-stability} against the matching applyProfile() variant and surface them in a clearly-labeled sub-tab ('M&A-Compounder', etc.). If a decision is made to keep it dormant, add a `// DORMANT: not yet wired into any report — see Tag NNN` banner so it isn't mistaken for live behavior.
- **Off-by-one between documented MIN_FOR_TREND=3 and effective minimum of 4 — exactly 3 points always returns n/a** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/trend.js:10, 21, 30-34` [bug]
  - The trend column in generate-methods-report.js / detect-changes.js shows n/a for any method with exactly 3 history points, contradicting the stated 3-point minimum. Cosmetic/documentation mismatch (no crash, clean n/a), but misleads anyone reasoning about when trend becomes available and wastes the 3rd data point.
  - _fix:_ Either set MIN_FOR_TREND=4 to match behavior, or change the recent/older split so 3 points compares last-1 vs first-2 (recent=slice(-1), older=slice(0,-1)) when length===3. Comment: // prevents: documented 3-point minimum silently requiring 4 points due to recent=slice(-3) consuming the whole window.
- **Hard 60-bar entry floor (labeled 'daily prices') rejects weekly-frequency stocks with 52-59 weekly bars before frequency is even detected** — `C:/Users/Karlr/screener-wt/audit-hardening/methods/volatility-annualized.js:25-30` [bug]
  - Non-US / weekly-frequency names with ~1 year of weekly history are silently dropped from the volatility signal even though >=30 usable returns exist — coverage attrition concentrated on the weekly cohort (asymmetric across the universe).
  - _fix:_ Move the entry floor below frequency detection and scale it (e.g. require >= max(MIN_RETURNS+1, lookback-aware floor)); the downstream returns.length>=30 check already guarantees statistical validity. Comment: // prevents: the daily-sized 60-bar floor rejecting valid weekly series with >=30 returns.
- **Quintiles mix incompatible value scales (12-1 return ratio vs degraded 0..1 positional score) into one distribution** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/_tag225d_effectiveness_probe.js:102, 132-139, 181-184` [data-integrity]
  - The probe's quintiles drive threshold-calibration judgments (the LOOSE/TIGHT flags at lines 163-164 use passRate, but the quintile columns are what a human eyeballs to set thresholds). Blending a return distribution with a 0..1 range-position distribution produces meaningless percentiles for any method that has a degraded fallback path, leading to mis-set thresholds — directly relevant to the '20% of potential' calibration problem.
  - _fix:_ Segment values by components.pricesUsed / by a method-declared value-semantics tag before quantiling, OR record the degraded-path count separately and exclude degraded values from the proper-path quintiles. At minimum, surface per-method how many values came from a degraded path so the percentiles can be discounted.
- **Sampling rate degenerates when universe < SAMPLE_TARGET and is silently biased by LCG quality, never reaching the 500 target** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/_tag225d_effectiveness_probe.js:44-51` [bug]
  - Low severity because this is a documented one-shot probe, but the reported coverage/pass-rate stats inherit sampling variance the report does not disclose, and a reader treating '500-sample' as authoritative over-trusts the precision.
  - _fix:_ If a fixed-N sample matters, shuffle allFiles deterministically and take the first (SAMPLE_TARGET - anchorsPresent); otherwise document that the sample is approximate. Not worth fixing if the probe stays one-shot.
- **One-shot probe is referenced by no workflow/cron/package script; uses process.exit(1) on missing method and a non-atomic write** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/_tag225d_effectiveness_probe.js:38, 172, 188` [dead-code]
  - Dead/orphaned tooling accumulates: it hardcodes a fixed 13-method list (lines 15-29) and 10 anchors (line 31) that will silently rot as methods are renamed/retired (the process.exit(1) turns a stale list into a hard crash). It is the kind of file that confuses future audits about what is live.
  - _fix:_ Either delete it (the audit it served is closed per the report) or move it under a scripts/oneoffs/ dir and replace process.exit(1) with a skip-and-warn so a renamed method doesn't crash the probe. Do not invest in atomic-write here unless it is promoted to a recurring script.
- **loadTickerFile trusts JSON array contents verbatim — non-string/object elements flow into yf.chart() and history[ticker] keys, corrupting the store** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/backfill-prices.js:55` [bug]
  - A ticker-file like `[{"ticker":"AAA"}, "BBB ", 123]` yields: an object key stringified to '[object Object]' in history.json, a ' BBB ' key with a trailing space that will never match the watchlist symbol later, and a numeric 123 passed to yf.chart. The newline-delimited path is hardened (trim+filter) but the JSON path is not, so the safety is inconsistent and a malformed ticker-file silently pollutes prices/history.json.
  - _fix:_ Normalize both branches identically: after JSON.parse, `return arr.map(t => (typeof t === 'string' ? t : (t && t.ticker) || '').trim()).filter(Boolean);` so object-shaped and stray entries are coerced or dropped the same way as the newline path.
- **Backfill silently resets r40rx history on schema-version mismatch instead of warning — can wipe accumulated TTM quarters** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/backfill-r40rx-history.js:60-69 (readHistoryFile)` [data-integrity]
  - backfill's merge policy (lines 173-190) is designed to PRESERVE live TTM entries while rewriting backfill rows. But if a history file has a future/old schemaVersion or is momentarily corrupt, readHistoryFile returns entries:[] with zero diagnostics, so preservedEntries is empty and the atomic write overwrites the file with backfill-only annual approximations — silently destroying real banked TTM quarters. The reset itself may be defensible, but doing it without a warning means the operator cannot tell a legitimate backfill from a TTM-history wipe. Silent error-swallowing on a write path is the data-integrity class in scope.
  - _fix:_ Mirror snapshot-score-history.js: emit `console.log('::warning::r40rx-history schema/parse reset for ' + ticker + ...)` in both the schema-mismatch branch and the catch before returning empty entries, so a destructive reset is at least observable in CI logs. Optionally skip the write entirely (continue) when the existing file failed to parse, to avoid overwriting a recoverable file.
- **backfill-r40rx-history.js, tag228a, tag229a are not wired into any entrypoint — one-shot tools that read as live code but never run in CI** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/backfill-r40rx-history.js:1-207 (whole file); not referenced by any workflow or package.json` [dead-code]
  - These are post-execution one-shots: tag228a was a historical phantom-vintage migration (its own header says 'retroactive cleanup'), tag229a a Run-#110 projection harness, backfill a manual seed of the r40rx table. None are in the daily pipeline. That's legitimate for migration/verify tooling, but mixed into scripts/ alongside live daily scripts they read as production code and accrue maintenance attention (e.g. finding 01's filename bug in backfill looks live but only bites a manual re-run). build-r40-latest.js IS a documented manual CLI (findash rebuild) so it's intentional, not dead.
  - _fix:_ No code change required, but move retired one-shots (tag228a once its migration is confirmed applied to all committed vintages, tag229a after Run #110) into a scripts/archive/ or scripts/oneshots/ subfolder, or add a top-of-file banner 'ONE-SHOT / not in daily-pull.yml', so the live-vs-tooling boundary is explicit. Do NOT delete without Karl's OK (binding constraint).
- **whyDropped only inspects methods present in the PRIOR vintage; a method newly added since prior that flips a pick out is never reported** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pick-diff.js:58-69` [bug]
  - pick-diff's stated job (line 56-57 comment) is to answer 'why dropped'. When a freshly-added MUST-gate is the actual reason a ticker left the picks, the report shows 'no method-flip detected — likely universe-pruning or score-cap' (line 191), pointing the operator at the wrong cause during exactly the kind of method-rollout that most needs an accurate diff.
  - _fix:_ Iterate over the union of prior and today method ids: `for (const mid of new Set([...Object.keys(prior), ...Object.keys(today)]))`. Classify (pass->fail), (pass->incomputable), and (newly-present-and-failing / newly-absent) as distinct flip reasons.
- **Ticker / method-id values interpolated into HTML without escaping (stored-XSS / markup-corruption if a symbol contains < & ")** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pick-diff.js:185, 192, 96 (compute-method-drift.js)` [bug, auto-safe]
  - Yahoo/exchange symbols and OTC tickers can contain characters like '&' or '^' (e.g. preferred-share or warrant suffixes) that break the rendered table; a fully attacker-controlled symbol would be stored-XSS on the public gh-pages site. Low severity because the symbol universe is curated, but it is a latent correctness/security gap on a public artifact.
  - _fix:_ Add a small escapeHtml(s) helper (& < > ") and wrap every interpolated ticker/method-id/reason before concatenation in both files.
- **jaccard() doc-comment says 'Jaccard distance' but the function returns Jaccard SIMILARITY (intersection/union)** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pick-diff.js:41-49` [other, auto-safe]
  - Mislabeled metric in an audit doc invites a future 'fix' that inverts it (1 - x) to match the word 'distance', which would silently flip every stability threshold (0.7 good becomes 0.3 good). Terminology bugs in shared diagnostics are how downstream regressions get introduced.
  - _fix:_ Change the comment to 'Jaccard similarity (index)' and keep the formula/behavior.
- **drift: Infinity (median=0, today>0 new-mode case) serializes to null in the report JSON, losing the alert signal** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/picks-regression-check.js:5, 72, 79, 123-132` [data-integrity]
  - The JSON report is the durable audit artifact (the Discord message at line 172 uses (a.drift*100) which prints 'Infinity%', also ugly). Anyone reading the committed report later sees a null drift and cannot distinguish 'new mode appeared from nothing' from 'field absent', undermining the post-mortem value of the guard.
  - _fix:_ Represent the unbounded case explicitly: store `drift: null` plus a `driftKind: 'from-zero'` flag (or a large sentinel) and format the Discord/console line as 'new (median was 0)' instead of 'Infinity%'.
- **Doc/behavior mismatch: header says 'median of previous 4 runs', code uses an 8-vintage window and median() of it** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/picks-regression-check.js:5, 39-44, 109-118` [bug, auto-safe]
  - The drift band's statistical meaning (median over 8 daily vintages vs the documented 4) changes the sensitivity of a guard that gates production picks. The 232c-21 comment already patched the pick-diff window label but the top-of-file docstring here still claims '4 runs', so an operator tuning the band reasons from the wrong baseline.
  - _fix:_ Align the header docstring with the actual 8-vintage window (or introduce a named PRIOR_WINDOW constant used by both the slice and the docstring) so the documented and effective windows cannot drift apart again.
- **Malformed health report with missing/non-numeric failure_rate passes the gate (undefined > 0.05 === false)** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pipeline-health-check.js:65` [bug, auto-safe]
  - A script that writes a partial/corrupt health report (e.g. crashed after opening the file, or a schema drift that renames failure_rate) is scored as healthy. The gate that exists specifically to hard-fail on systemic problems silently green-lights the run.
  - _fix:_ Treat a report whose failure_rate is not a finite number as a breach: `const breached = reports.filter(r => !Number.isFinite(r.failure_rate) || r.failure_rate > THRESHOLD);` and surface the malformed entry distinctly in the summary.
- **isInvalidSymbol length>12 heuristic can false-positive-prune legitimate long symbols** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/prune-watchlist.js:60-67` [data-integrity, auto-safe]
  - Some legitimate Yahoo symbols (certain mutual-fund classes, structured products, and crypto pairs) exceed 12 chars without dot/dash/caret. Such an entry would be silently evicted as 'looks-like-company-name' and, once removed from the committed watchlist, only recoverable from git history. Currently 0 live entries trip it, so it's latent — but it's a hardcoded-shape heuristic doing irreversible deletion.
  - _fix:_ Tighten the guard so it only matches the actual failure mode: require it to be all-alpha AND mixed-case (real symbols are upper-case), e.g. `ticker.length > 12 && /[a-z]/.test(ticker) && !/[.\-:^ ]/.test(ticker)`. This still catches 'STMicroelectronics' but spares all-caps long symbols. Comment the precise failure mode it prevents.
- **13F `value` stored in thousands-USD but consumed and rendered as whole dollars (1000x understatement)** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-13f-institutional.js:245, 554` [data-integrity]
  - institutional-ownership-13f.js is wired into the live method registry (methods/index.js:177). Every displayed institutional-ownership dollar figure is understated by exactly 1000x (a $94B Berkshire AAPL stake shows as ~$94M). Any future ranking/threshold on totalValueUSD is silently wrong. This is a contract bug rooted in the puller emitting a raw, unit-ambiguous `value` with only a code comment as the unit marker.
  - _fix:_ In buildByTickerView make the unit explicit at the data boundary: emit `valueThousandsUSD: p.value` AND a derived `valueUSD: (Number.isFinite(p.value) ? p.value * 1000 : null)` in each holding (with a comment: 'prevents the thousands-vs-dollars unit-confusion failure mode that understated totalValueUSD 1000x'). Coordinate the consumer to read valueUSD. Do NOT silently multiply inside the method without renaming, or the next reader re-introduces the ambiguity.
- **MAX_POSITIONS_PER_FILING truncation is silent — large filers (Vanguard/BlackRock 5000+) can be cut with no flag in the cache** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-13f-institutional.js:82, 225-226` [data-integrity]
  - 20000 is above today's largest filers so the cap likely never trips in practice, but if a filer (or a malformed concatenated XML) crosses it, the institution's holdings are silently partial and indistinguishable from a complete pull. Downstream institutions-holding counts and totalValue silently drop for the truncated names. Silent caps are exactly the class flagged in this repo's own audit-reports/2026-05-17-tag227a-silent-caps.md.
  - _fix:_ When the cap trips, set `truncated: true` and `positionsParsed`/`blocksSeen` on the result and persist them in the cache entry; log a WARN. Optionally raise the cap or stream-count blocks first. Comment: 'prevents silent truncation of large 13F books going undetected downstream'.
- **Header claims 'NOT wired into daily-pull.yml' and per-ticker pull is superseded by pull-insider-form4-daily.js; only parseForm4Xml is still live** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/pull-insider-form4.js:1-49, 521` [dead-code]
  - The expensive per-CIK puller (the source of the multi-hour pull concern) is effectively dead for production — the daily index-driven script replaced it — yet it remains as a large, separately-maintained surface that still carries its own soft-error bug (finding 03) and a stale 'not wired' header that misleads the next maintainer. parseForm4Xml being shared also means a change here silently affects the daily path.
  - _fix:_ Either (a) reduce pull-insider-form4.js to the shared parser/util module that pull-insider-form4-daily.js actually imports and delete the dead per-CIK main()/pull path after confirming no other entrypoint calls it, or (b) extract parseForm4Xml into a lib/ module both scripts import and retire the per-ticker puller. Do NOT delete files without Karl's OK — propose and stage. Fix the stale 'NOT wired' header regardless.
- **Three daily snapshot scripts each call Runner.evaluateStock() over the full ~3500-stock universe, recomputing ALL methods when r40rx only needs rule-of-40 / rule-of-x** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/snapshot-r40rx-history.js:83; cross-file with snapshot-score-history.js:140 and snapshot-methods-history.js` [performance]
  - evaluateStock with no opts runs the entire method registry per stock; r40rx needs exactly 2 results. Across ~3500 stocks x 3 scripts that is 3 full universe re-classifications per day on the CI runner, a large slice of wasted compute that compounds as the method registry grows. runner.getMethods already supports `{type, onlyDefault}` filtering, so the machinery to evaluate a subset exists and is unused here. This is the 'per-method re-classify / wasted recompute' performance class in scope.
  - _fix:_ For the r40rx script, evaluate only the two methods it needs (the runner exposes evaluateStockExtended(stock,{...}) and METHODS; add a thin helper to run just rule-of-40 + rule-of-x, or memoize one evaluateStock per stock and share its result across the r40rx and score-history steps by merging the two snapshot scripts into a single pass). Verify equality via the existing fixture-hash tests. methods/index.js stays untouched.
- **isoToQuarter has no validation of the input ISO date — a malformed RUN_DATE_UTC yields a NaN quarter ('YYYY-QNaN') that poisons every history key for the day** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/snapshot-r40rx-history.js:35-39 (isoToQuarter)` [bug, auto-safe]
  - appendAndPrune keys entries by quarter and de-dupes by it (line 75 `e.quarter !== entry.quarter`). A 'QNaN' quarter would never match a real quarter, so it appends a junk entry every run that uses the bad date and never overwrites it — corrupting the durability-gate input (finding 02) and the chart. The workflow currently sets RUN_DATE_UTC via `date -u +%Y-%m-%d` (daily-pull.yml:53) so it's safe in CI, but the script accepts arbitrary env with zero guard.
  - _fix:_ Validate `today` against `/^\d{4}-\d{2}-\d{2}$/` at the top of main() (same regex build-r40-latest.js:45 already uses for asOf) and exit 1 with a clear message if it fails. Comment: 'prevents a malformed RUN_DATE_UTC from minting a QNaN quarter key that never de-dupes'.
- **Phantom-vintage cleanup has NO atomic/try-catch around the per-file parse and writes — one corrupt vintage aborts the whole batch mid-rewrite, and `main()` runs unguarded** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/tag228a-cleanup-phantom-vintages.js:30, 35-36, 94` [data-integrity, auto-safe]
  - This is a one-shot destructive migration that mutates committed methods-history vintages (subtracts summary.totalStocks/anyComputable/allPass and deletes stocks._* rows). If it dies halfway, the operator sees a Node stack trace and an inconsistent on-disk state: some vintages cleaned, some not, no summary of what completed. There's no idempotency hazard (re-run skips already-clean files), but the abort hides which file is corrupt and leaves the batch half-applied. Also `main()` is called unconditionally even though the file is require()-able, so importing it for a test would execute the mutation.
  - _fix:_ Wrap the per-file body in try/catch (log `f` + error, continue) so one bad vintage doesn't abort the batch; guard the entry point with `if (require.main === module) { try { main(); } catch(e){ console.error(e); process.exit(1);} }`. writeFileAtomic is already used (line 81) so torn writes are covered; the gap is batch-abort + unguarded import.
- **Stale-snapshot verify harness INLINES copies of three pull-yahoo probes — verification value silently lapses if the live probes change** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/tag229a-stale-snapshot-verify.js:49-65, 67-106; live source pull-yahoo.js:1481-1530 (_existingSnapshotMissingCurrencyNormalization)` [architecture]
  - A verification harness that copies the code it verifies only proves the COPY is self-consistent; the moment pull-yahoo's locally-scoped probe changes (e.g. tightening the OR to AND, or adding a field to the gate), this harness keeps asserting 'OK: probe wired correctly' against stale logic and gives false assurance about the 14.6h full-pull fraction it projects (Phase 2). The 4th safeSnapshotFilename copy is also the exact drift surface lib/snapshot-fs.js was created to eliminate (F-218b-01).
  - _fix:_ Refactor pull-yahoo.js to export the three probe predicates (or move them to a lib/snapshot-staleness.js) and have tag229a import them, so the harness exercises the REAL functions; at minimum import safeSnapshotFilename from lib/snapshot-fs.js instead of re-inlining it. Fix the misleading comment at line 84 to state these fields are diagnostic-only, not part of the stale gate. Do not parallel-edit methods/index.js (not involved here).
- **sh() swallows all git errors and returns '' — a broken git invocation makes the audit report 'No commits' and pass green** — `C:/Users/Karlr/screener-wt/audit-hardening/scripts/threshold-audit.js:46-49, 71` [data-integrity]
  - In CI --strict mode this is the gate that is supposed to fail the build on undisciplined threshold changes; a silent git failure converts it into an always-pass no-op. The catch->'' pattern hides the difference between 'genuinely no commits' and 'git could not run'.
  - _fix:_ Distinguish failure from empty: have sh() return null on catch and, in main(), if the git log call returns null, print '::error::git log failed' and (in STRICT) return 1 instead of silently reporting no commits.
- **normScore fallback in pickStockForMode is dead code — ev.normScore never exists** — `C:/Users/Karlr/screener-wt/audit-hardening/snapshot-picks.js:102-104` [dead-code, auto-safe]
  - The middle ternary branch can never be taken, so the elaborate Tag 232c-22 fix (multiply normScore*100 to align 0-1 vs 0-100 scales) protects against an input that cannot occur. The real consequence is silent: whenever the score-aggregator is unavailable (methods/strategy-modes.js line 5 wraps `require('./score-aggregator.js')` in try/catch, so scoreResult can be null → ev.score === null), the pick is written with score:null and falls to the bottom of the sort (lines 250-260) and into the primaryMetric tiebreak. There is no normScore safety net at all — the comment claims one exists. picks-history is the walk-forward backtest's source of truth, so a missing aggregator silently nulls every pick.score for that vintage rather than degrading gracefully.
  - _fix:_ Either (a) delete the dead normScore branch and the misleading Tag 232c-22 comment, or (b) if a normalized fallback is actually wanted, have evaluateMode return normScore and source it. Do NOT leave a comment asserting a fallback that the data path never produces. (Does not touch methods/index.js.)
- **dedupePicksByCompany tiebreak uses `|| 0` — a legitimate score of 0 loses to a null-scored duplicate** — `C:/Users/Karlr/screener-wt/audit-hardening/snapshot-picks.js:128` [bug]
  - Targeted '|| 0 falsy' class. score can legitimately be 0 (0-100 scale) and can be null (aggregator unavailable, see finding 01). `(p.score || 0)` collapses both null and 0 to 0, so a real score of 0 never wins the tiebreak and the comparison can't distinguish 'scored 0' from 'unscored'. For two same-listing-status duplicates this picks an arbitrary survivor. Low impact today (ties at exactly 0 are rare) but it is the exact operator-precedence/falsy footgun the audit flags, and it compounds finding 01 (null scores are common when aggregator is missing).
  - _fix:_ Compare with explicit null handling: const ps = p.score == null ? -Infinity : p.score; const es = existing.score == null ? -Infinity : existing.score; if (ps > es) ... — so a scored 0 beats an unscored null and 0-vs-0 is a true tie.
- **getMcap lacks the Number.isFinite guard that strategy-modes uses — NaN marketCap propagates into pick output** — `C:/Users/Karlr/screener-wt/audit-hardening/snapshot-picks.js:61-66` [data-integrity, auto-safe]
  - If marketCap.value is NaN or Infinity, getMcap returns it verbatim into the pick. JSON.stringify(NaN) serializes as null so the on-disk vintage is not corrupted, but the value flows through in-memory consumers and the asymmetry with strategy-modes (which treats NaN mcap as 'missing') means the same stock can be mcap_missing for gating yet carry a non-null in-memory mcap here. Minor NaN-propagation / no-isFinite-guard instance the audit asks for.
  - _fix:_ Mirror strategy-modes: `const v = (typeof m === 'number') ? m : (m && typeof m === 'object' && 'value' in m ? m.value : null); return Number.isFinite(v) ? v : null;`
- **quoteSummary requests 4 heavy modules (income/balance/cashflow history) that are never read** — `C:/Users/Karlr/screener-wt/audit-hardening/suggest-watchlist-additions.js:34-36` [performance, auto-safe]
  - Every candidate pull fetches three large statement-history payloads that are immediately discarded, on top of the three fundamentalsTimeSeries calls that actually feed the data. Over a real universe scan (hundreds–thousands of candidates at rateLimit 1500ms each) this inflates payload size, parse time, and Yahoo throttle exposure for zero benefit — the same wasted-recompute/over-fetch class that caps the pull.
  - _fix:_ Drop the three unused *History modules from the quoteSummary call (keep price, summaryDetail, defaultKeyStatistics, financialData, assetProfile). Comment: 'statement history comes from fundamentalsTimeSeries below; do not double-fetch via quoteSummary'.
- **--rate-limit default is unreachable — defined but parseArgs has no branch for it** — `C:/Users/Karlr/screener-wt/audit-hardening/suggest-watchlist-additions.js:20-26` [bug, auto-safe]
  - Operator cannot slow down/speed up the scan to dodge Yahoo throttling without editing source. Given the repo's hard-won PULL_CONCURRENCY/rate-limit tuning (daily-pull.yml Tag 215f), a hidden fixed 1.5s with no override is a foot-gun for large scans. Low severity because a fixed value still works.
  - _fix:_ Add `else if (argv[i] === '--rate-limit' && argv[i+1]) args.rateLimit = parseInt(argv[++i], 10);` with an isFinite guard, and document the flag in the usage header.
- **Unused `path` require and unused `_val` helper** — `C:/Users/Karlr/screener-wt/audit-hardening/suggest-watchlist-additions.js:10, 47` [dead-code, auto-safe]
  - Dead imports/helpers are noise that suggest a refactor was abandoned mid-way (the divergent puller in A-add-watchlist-01 is the larger symptom). Harmless at runtime but they mislead future readers about what the field-mapping contract is.
  - _fix:_ Delete the unused `path` require and the `_val` arrow. If _val was the intended uniform wrapper, use it consistently instead of the ad-hoc `{ value: v }` literals.
- **FX-conversion tests depend on the mutable live fx-rates.json — non-hermetic, can flip between live rate and stale fallback** — `C:/Users/Karlr/screener-wt/audit-hardening/tag28-tests.js:418-454, 536-556` [data-integrity]
  - A unit test that reads a file refreshed by a separate workflow (refresh-fx.js) is not deterministic: the green/red outcome depends on calendar age of an external artifact, not on code. The wide band masks a real FX-normalization regression (e.g. a 2x scaling bug would still land inside [10e9,50e9] for some inputs). It also means a stale-FX state that production should alarm on is invisible to the test.
  - _fix:_ Make the FX path injectable for tests: have _convertSnapshotToUSD accept an explicit rate map (or expose a `PY._setFxForTest({TWD:0.0316})`), then assert the EXACT converted value (1000e9 * rate) rather than a band. This makes the test hermetic and tightens the tolerance enough to actually catch a scaling bug. Keep the hardcoded fallback table untouched (binding constraint: no FX behavior change in production).
- **'getMethods matches filesystem' guard relies on a hand-maintained NON_METHOD_FILES denylist that silently rots** — `C:/Users/Karlr/screener-wt/audit-hardening/tag28-tests.js:160-182` [architecture]
  - Every future non-method helper added to methods/ (the directory already mixes plugins and helpers like sector-median-lookup.js) breaks this test until someone manually appends to the Set — and the natural 'fix' under deadline pressure is to add the filename to the denylist, which permanently blinds the very drop-detector the test exists to be. It is an inverted guard: maintenance burden grows with the codebase and the failure mode is to weaken the assertion.
  - _fix:_ Invert the contract: instead of denylisting non-methods, require method plugins to self-identify (they already export an `id` and are loaded by runner). Compare runner's loaded id-set against files that export a method-shaped contract (e.g. have `module.exports.evaluate` and an `id`), or move helpers into a methods/_lib/ subfolder excluded by the existing `_`/dir filter so no denylist is needed. Then the count check needs no manual upkeep.
- **SGA-Revenue-Trend test carries a self-contradictory comment that misdescribes its own fixture** — `C:/Users/Karlr/screener-wt/audit-hardening/tag28-tests.js:1986-1994` [other, auto-safe]
  - The next maintainer who edits this fixture will trust the wrong half of the comment and likely break the boundary (THRESHOLD=0.005 in sga-revenue-trend.js:52 is exactly at the +0.5pp/yr edge). A test asserting behavior at a threshold boundary needs an accurate comment, because the margin is one rounding error wide.
  - _fix:_ Delete the rejected-dataset reasoning (the 'Wait,...' block) and keep only the description of the committed fixture: chrono [10, 10.4, 10] → least-squares slope ≈ 0 ≤ 0.005 → PASS. No code change; comment-only correctness fix.
- **CSV export/import use naive comma split with no quoting → names containing commas corrupt round-trips** — `C:/Users/Karlr/screener-wt/audit-hardening/watchlist-cli.js:107-116, 81-105` [data-integrity]
  - Export→edit→import is the documented bulk workflow (header line 'ticker,name,yahoo_symbol,isin,track'). Any company name with a comma (common: 'Company, Inc.') silently shifts columns on re-import, producing a wrong yahoo_symbol/isin/track for that row — bad data injected into the watchlist that then drives the entire pull. Low severity only because most names lack commas.
  - _fix:_ Quote fields on export (wrap any field containing comma/quote/newline in double-quotes, doubling internal quotes) and parse with a minimal CSV reader that honors quotes, or strip commas from names on export. Even a simple `JSON.stringify(field)` per cell on export + JSON-aware parse would be safe.
- **add accepts --position/--track/--name silently; documented flags partly unvalidated and partly ignored** — `C:/Users/Karlr/screener-wt/audit-hardening/watchlist-cli.js:44-62, 118-129` [bug]
  - Two foot-guns: (1) the position command exists but the add-time --position flag is silently dropped, so users who set initial status on add get no error and no effect; (2) a flag given with no value poisons the field with `true`, which later crashes cmdList/cmdExport (name.padEnd / undefined). Both are silent until a downstream command blows up.
  - _fix:_ In cmdAdd, read and validate opts.position against the allowed set (or drop it from the usage text if unsupported). In parseFlags, treat a value-less recognized flag as an error rather than coercing to boolean true for string-valued options; and coerce name/track to String() before storing.
- **list/info assume wrapped {stocks:[...]} shape — crash on the legacy array/object shapes the rest of the pipeline tolerates** — `C:/Users/Karlr/screener-wt/audit-hardening/watchlist-cli.js:36-41, 73-78` [bug]
  - The codebase explicitly treats the three historical watchlist shapes as a live hazard (lib/watchlist-fs.js header; Tag 207a CI fix). watchlist-cli.js is the human-facing editor for that same file but is the one consumer that ignores the contract — a rollback or hand-edit to the legacy shape makes every CLI command crash with a cryptic TypeError instead of degrading. Low severity because current file is wrapped.
  - _fix:_ Replace the hand-rolled load() with `const { loadWatchlist } = require('./lib/watchlist-fs.js')` and operate on the normalized `.stocks` array; on save, write back through the existing wrapped path. Single source of truth, no duplicated shape logic.
- **Dedupe fingerprint `${c.gm}|${c.scaleRevM}` can silently drop legitimately distinct companies (collision) and is order-dependent** — `court-score.js:171` [data-integrity]
  - The intent (comment line 170) is to drop identical foreign-OTC double-listings of the SAME company. But the key is only (gm, revM) — not the issuer/CUSIP. At 4-dp gm and $1M revenue granularity, two distinct mid-caps colliding is plausible across a ~3200-name universe, and whichever the Map iteration reaches second is dropped with no lamp, no log, no record. This is asymmetric survivorship attrition driven by an accidental hash collision — a name vanishes from the rankable set for reasons unrelated to its merits, and it is invisible. Iteration order of `bucketOf` (a Map built from the classification list) determines which survives.
  - _fix:_ Dedupe on a stable identity key (ticker root / CUSIP / SEC CIK) rather than (gm, revM). If only price-like fields are available, at minimum (a) require MORE fields to match before treating as a dup (e.g. gm AND revM AND growth AND fcfMargin), and (b) push a 'deduped-duplicate-listing' lamp / counter so dropped names are auditable. Comment must name the failure mode: 'fp collision on (gm,revM) silently drops a distinct issuer'.
- **INORGANIC_FALLBACK hardcodes 'AVGO' and 'GEN' as a name-level safety net — the kind of hardcoded exclusion/override the data-driven inorganicFlag was built to replace** — `court-score.js:234, 110` [architecture]
  - This is a lamp-only fallback (it doesn't alter score), so it is low-severity, but it re-introduces exactly the name-pinned logic the redesign removed: GEN is already covered data-driven (its snapshot row has paymentsToRev 0.206, rpoGrowth 0.013 -> 'inorganic'), so its presence here is redundant and will go stale if GEN's data changes; AVGO is pinned only because it lacks a snapshot row. Per the audit's binding constraint ('NO hardcoded ticker exclusions — fix the guard instead'), the guard, not the name list, should handle missing-snapshot coverage.
  - _fix:_ Remove the GEN entry (data-driven rule already covers it). For AVGO/missing-snapshot names, replace the name set with a structural guard: when a fabless_semi member has a high headline growth but `maRpoByTicker.get(t)` is null (no M&A/RPO coverage), emit a generic 'M&A-coverage-missing(P_auth-blind)' lamp keyed on coverage absence, not on the ticker. This satisfies the no-hardcoded-ticker constraint and self-maintains as the snapshot universe grows.
- **`generatedFromCacheAt: new Date && undefined` always serializes to nothing — provenance timestamp is silently dropped** — `court-screen.js:191` [dead-code]
  - The candidate artefact carries NO generation timestamp — the field the author intended (a 'when was this cache snapshot scored' provenance marker) is invisible in every output. Anyone diffing or staleness-checking court-candidates.json has no as-of date. It also looks like a half-finished edit (likely was meant to be `new Date().toISOString()` then disabled to keep determinism). Note: a literal timestamp here WOULD break the byte-identical determinism test (court-score-tests.js:180-185), so the fix must be deliberate.
  - _fix:_ If determinism across runs is required (it is — the gate hashes the full chain), drop the field entirely and stop pretending it exists, OR derive the timestamp from the newest cache file's mtime / an explicit COURT_ASOF env so it is stable within a snapshot. Do not emit a wall-clock `new Date()` — that re-breaks the determinism gate.
- **Growth source can silently mix annual and quarterly YoY across the universe, and accel is annual-only — cross-sectional anchors compare apples to oranges** — `court-screen.js:100-101, 155` [bug]
  - court-score.js computes the cross-sectional Growth anchor (median/MAD) over `c.growth` mixing both definitions (court-score.js:185-188), so a quarterly-derived growth number is z-scored against annual-derived peers in the SAME bucket. Annual YoY and trailing-5Q YoY are not the same statistic (different smoothing, different as-of), so the pseudo-z is biased for the fallback names. Separately, accel is null for any name lacking >=2 annual YoY (young IPOs — exactly the high-growth targets), so the Accel axis is structurally absent for the most interesting cohort and they get neutral s=0 on a 15%-weighted axis (Fabless), depressing them relative to older names. Both are silent.
  - _fix:_ Record which growth definition was used (e.g. a `growthSource: 'annual'|'quarterly'` field, already done for durSource) and either (a) compute anchors per-source or (b) prefer a single consistent quarterly-TTM YoY universe-wide. For accel, add a quarterly-YoY-acceleration fallback mirroring the durability source-selection so young names are not silently neutralised. Comment: 'prevents annual-vs-quarterly growth-definition mixing in cross-sectional z and accel-absence demotion of young names'.
- **OTC pagination completeness check uses PAGE_SIZE arithmetic that breaks the moment any page is filtered or short, terminating early and dropping tickers** — `discovery/otc-markets.js:175-179` [bug]
  - Two opposite failures: (1) if totalRecords is reported, `(page-1)*PAGE_SIZE` assumes every prior page returned exactly 500 raw rows; any API hiccup returning a short-but-nonempty page makes `fetched` undercount and the loop keeps going past real end (benign) OR if the API returns MORE than PAGE_SIZE the count overshoots and stops early (drops data). (2) MAX_PAGES=10 * 500 = 5000 hard ceiling while the file's own header says 'Expert Market + OTCQX+OTCQB combined can exceed 5,000' (lines 9, 193-196). The warn at 193 fires but the data is already truncated — asymmetric survivorship attrition against OTC small-caps.
  - _fix:_ Track fetched count from the actual cumulative rows consumed, not `(page-1)*PAGE_SIZE`: `cumulative += rows.length` across iterations. Keep the empty-page stop as the authoritative terminator. Raise MAX_PAGES (or make it `Math.ceil(totalRecords/PAGE_SIZE)` when known) so the documented >5000 case is actually fetched. Comment: 'prevents early pagination stop / 5000-ticker OTC ceiling silently truncating discovery'.
- **OTC rawSym may be a non-string field, and .trim() will throw if symbol is numeric/null in one of the tolerated response shapes** — `discovery/otc-markets.js:146-147` [bug]
  - A single numeric symbol cell in an Expert-Market page would crash the page handler and, via the break, truncate the whole OTC pull. Low likelihood but high blast radius because of the break-on-error pagination.
  - _fix:_ Coerce defensively: `const rawSym = String(row.symbol ?? row.ticker ?? row.Symbol ?? row.tickerSymbol ?? '').trim().toUpperCase();`. Comment: 'prevents non-string symbol field crashing the page and truncating OTC pagination'.
- **Wikipedia ticker extraction is a brittle wikitext heuristic that conflates row-splitting and yields both false positives and missing constituents** — `discovery/wikipedia-indices.js:79-107` [data-integrity]
  - S&P 500 / FTSE 100 / DAX membership is the highest-signal source for index-quality names. A parser that depends on a frozen denylist and a specific wikitext table dialect will (a) miss real constituents whenever Wikipedia editors change table markup, and (b) admit garbage tokens that happen to match. The header claims to 'Cover S&P 500, FTSE 100, DAX 40' but correctness is unverifiable and decays over time. The single-letter whitelist also hardcodes a point-in-time NYSE snapshot that will silently exclude future single-letter listings.
  - _fix:_ Stop scraping raw wikitext. Use the structured data: for S&P 500, parse the rendered HTML table via the action=parse&prop=text endpoint and select the Symbol column by header, or better pull from a structured source (the index provider, or Wikidata SPARQL). At minimum, locate the ticker COLUMN by its header rather than scanning every cell, eliminating the need for NOT_TICKERS/SINGLE_LETTER guesswork. Comment: 'prevents index-membership drift from wikitext dialect changes and frozen denylists'.
- **isGood() only checks picks[0].score on the FIRST pick of SOME mode; the oldest-good-vintage scan JSON.parses every vintage with no try/catch, so one corrupt picks-history file aborts the whole freeze** — `fitness/freeze-full-baseline.js:39-43, 56-60` [bug]
  - The freeze is the deterministic entrypoint for the whole fitness pipeline. A partially-written or corrupt vintage (the snapshot pipeline can produce these on cron failure — walk-forward-perf.js explicitly defends coverage gaps at F-BT-012) will hard-crash freeze-full-baseline.js with an unhandled SyntaxError rather than skipping to the next-oldest good vintage. The isGood weakness is minor (first-pick-only) but means a vintage with a scored first pick and null-scored remainder passes the 'good' gate with a misleadingly sparse ranking.
  - _fix:_ Wrap the per-file parse in try/catch and skip-with-warn on failure (mirror loadJson). Strengthen isGood to require that a mode has at least N picks with numeric scores, or that the median pick carries a score, not just p[0]. No constraint impact.
- **quintileMonotonicity defines monotoneScore as adjacent pairs where Q[i] >= Q[i+1], counting flat/tied quintiles as 'monotone' — a degenerate all-equal model scores a perfect 1.0** — `fitness/lib/metrics.js:139-175` [bug]
  - monotoneScore=1.0 is meant to certify 'top quintile reliably beats bottom quintile' — a strong fitness signal. A flat or near-flat model getting the same 1.0 makes the metric unable to separate a great model from a useless one in exactly the no-signal regime the harness is supposed to detect. Quietly inflates apparent model quality.
  - _fix:_ Use strict > for the monotone count, OR additionally report the Q1-minus-Q5 spread and require it to exceed a threshold before treating monotoneScore as meaningful. Add a tie-rate field. Comment the failure mode: '// strict > so a flat (no-signal) quintile profile does not score as perfectly monotone'.
- **Output directory creation and atomic write are not coordinated; mkdir is non-atomic and the .tmp/rename can cross-device if outputs/ is a mount, leaving no fsync** — `fitness/measure.js:188-202, 36-40` [data-integrity]
  - (1) No fsync means a crash/power-loss between rename and the OS flushing can still surface a zero-length or torn file on some filesystems; the comment 'Atomic write' overstates the guarantee. (2) The relative-path dependency on cwd is fragile: invoked from any other directory, measure.js reads prices/history.json and writes fitness/outputs relative to the wrong root, silently producing or overwriting files in the wrong place or FATAL-exiting on a missing baseline. walk-forward-perf.js by contrast anchors every path with path.join(__dirname, '..', ...).
  - _fix:_ Anchor all paths against __dirname/ROOT like walk-forward-perf.js does, instead of relying on cwd. In atomicWriteJson, fsync the file descriptor before rename and (best-effort) fsync the directory after, or reuse the project's lib/atomic-write.js writeFileAtomic that walk-forward-perf.js already imports (consistency + single source of truth).
- **universeSize falsy-fallback via ternary: a legitimate 0 universe falls through to methods totalStocks instead of showing 0** — `generate-dashboard.js:187` [bug, auto-safe]
  - This is the classic '|| 0 / falsy-truthy' class from the audit scope. A genuinely empty universe (universeSize === 0) would silently display methodsHistory.summary.totalStocks instead of the true 0, masking a pipeline collapse (Yahoo pull returned nothing) behind a stale-looking count. Low severity because 0-universe is rare and the page still renders, but it hides exactly the failure you'd most want to see on the dashboard header.
  - _fix:_ Use explicit null/undefined checks: `const universeSize = (picks && picks.universeSize != null) ? picks.universeSize : ((methodsHistory && methodsHistory.summary && methodsHistory.summary.totalStocks) || 0);`.
- **Quarterly display reverses arr.slice(0,4) but sparkline is passed the un-reversed slice — table reads Q-4→Q-1 while the sparkline trends the opposite direction** — `generate-dashboard.js:525, 528` [bug]
  - When one of the 4 quarters is null (common for freshly-IPO'd or thinly-reported names), the sparkline silently compresses to 3 points across the full width while the table still shows 4 columns with a '—'. The trend line then doesn't line up with the dashes the user is reading, giving a subtly misleading visual. Cosmetic but it is a correctness mismatch between two renderings of the same data.
  - _fix:_ Render the sparkline from the SAME reversed, null-preserving array used for the table (pass `display`), and have renderSparkline keep nulls as gaps (break the polyline) rather than filtering+rescaling, so x-positions match the table columns 1:1.
- **buildDetailIndex re-reads + re-parses one snapshot JSON per detailed ticker (readQuarterly + readSnapshotMeta), O(tickers) synchronous file reads on top of the methods-history load** — `generate-dashboard.js:153, 197` [performance, auto-safe]
  - For a few hundred detailed tickers this is several hundred synchronous readFileSync + JSON.parse calls, and each ticker that needs both quarterly and meta parses its snapshot twice. On the 14.6h-pull-scale universe this is wasted recompute exactly in the 'O(n) re-parse of large JSON' class. Low severity because the detail set is bounded (top-30 modes + top-25 metrics) but it doubles the per-ticker IO needlessly.
  - _fix:_ In buildDetailIndex read each ticker's snapshot once into a local `const snap = safeJSON(tickerToSnapshotPath(ticker))` and derive both quarterly and meta from it (refactor readQuarterly/readSnapshotMeta to accept an already-parsed snapshot object). Eliminates the double parse and is a pure speedup with no behavior change.
- **picks (latest.json) and methods-history are loaded from independent sources with no date-consistency check — dashboard can blend today's picks with a stale methods snapshot** — `generate-dashboard.js:186-188, 588-589` [data-integrity]
  - If a run regenerates picks/latest.json but the methods-history snapshot for that date fails to write (the snapshot step can fail independently; the dashboard step is continue-on-error), the dashboard shows fresh mode picks dated today alongside metric leaderboards and detail values from an older methods snapshot — with the header asserting today's date. A pick can appear with a primaryMetric from picks but a contradictory ROIC/EV-EBITDA from a different day, and nothing flags the mismatch. This is asymmetric-staleness data integrity, masked by the single-date header.
  - _fix:_ After loading both, compare `picks.asOf.slice(0,10)` to `methodsHistory.date`; if they differ, render a visible banner ('Mode-Picks Stand X · Kennzahlen Stand Y') and log a warning, rather than presenting one date as authoritative.
- **Tier thresholds (80/65/50) duplicated inline in two places and divergent from the server-side TIER_STYLE constant — single source of truth missing** — `generate-dashboard.js:416, 499` [architecture]
  - Duplicated business logic: if the screener's tier cutoffs change (and tier boundaries are exactly the kind of thing that gets retuned), one of the two copies will be missed and the leaderboard badge will disagree with the modal badge for the same stock. Also, these thresholds almost certainly should match whatever the upstream score-aggregator/methods use to assign tiers — embedding them in the view layer risks the dashboard inventing its own tiering that contradicts the pipeline.
  - _fix:_ Define a single `function scoreToTier(score)` (server-side, injected into the client payload or duplicated once) and call it in both places; ideally source the thresholds from the same config the score-aggregator uses so the dashboard cannot drift from canonical tiering.
- **Diff-report picks the two lexicographically-last *.json files, not the two most recent by date — also matches non-dated JSON in the dir** — `generate-diff-report.js:34, 41-42` [bug, auto-safe]
  - Any non-dated JSON dropped into methods-history/ (e.g. a `latest.json`, `index.json`, `backup.json`, or a `.tmp.<pid>` leftover that ends in .json) sorts by string and can become `files[length-1]`/`[length-2]`. `latest.json` sorts AFTER `2026-06-12.json` (lowercase 'l' > digit), so the diff would compare `latest.json` against the newest dated file — comparing a file to itself or to the wrong vintage, producing a silently wrong or empty diff. Because the workflow step runs with `continue-on-error: true` (daily-pull.yml:535), a crash or wrong output is never surfaced. The date-format guard the dashboard already uses is the correct contract.
  - _fix:_ Use the same strict regex as generate-dashboard.js:62: `.filter(f => /^\d{4}-\d{2}-\d{2}\.json$/.test(f))` before `.sort()`. ISO date filenames then sort correctly and stray JSON is excluded. // failure mode: wrong-vintage / self-comparison diff from non-dated JSON files in history dir
- **methodValueDiffs is re-sorted inside the template render after passCountDiffs already sorted — redundant, and the .slice(0,50) sort mutates the source array** — `generate-diff-report.js:111` [performance, auto-safe]
  - Minor, but the heading count (full length) disagrees with the visible table (capped at 50) with no '(showing top 50 of N)' note, which is misleading. The in-line sort-then-slice is also slightly wasteful style; harmless functionally since it runs once.
  - _fix:_ Sort + slice into a named const before building the heading, and render the heading as `top ${Math.min(50, methodValueDiffs.length)} of ${methodValueDiffs.length}` so the count matches what is shown.
- **Pass-Count filter silently leaves matrix rows ranked 201-300 visible (map holds only 200, matrix renders 300)** — `generate-methods-report.js:964-987 (applyPassCount), with 336-373 (STOCK_DATA_MAP build) and 432-437 (TOP_MATRIX_N=300)` [bug]
  - The Pass-Count >= N filter is the primary triage control on the matrix. 1/3 of the visible matrix rows (the lowest-ranked 100) ignore it entirely, so a stock with passCount=2 sitting at rank 250 stays on screen when Karl sets the filter to >=8. The on-screen 'X / Y >= N pass' count is also understated because those rows are never counted. The old per-row data-row blob (removed in Tag 221c) covered all rendered rows; the STOCK_DATA_MAP refactor narrowed coverage to 200 without shrinking the matrix to match.
  - _fix:_ Make the rendered set and the data map share one source of truth. Cleanest: drive both the matrix slice and STOCK_DATA_MAP from the same N (e.g. include every ticker that appears in either table in stockDataMap), OR set TOP_MATRIX_N === TOP_PICKS_N. Minimal: when building stockDataMap, also add matrixRanked entries. Add a comment naming the failure mode: '// prevents pass-count filter no-op on matrix rows beyond TOP_PICKS_N (map/render set divergence)'.
- **Header 'disqualified by DataGuards' count is inconsistent with how disqualification is treated everywhere else on the page** — `generate-methods-report.js:675 (header disqualified count) vs 242-249 (discoveryRows)` [bug]
  - Self-contradicting report: the summary line asserts a stock is disqualified, then lists it as a top pick. Erodes trust in the artifact and makes it impossible to reconcile the counts. Low severity because it is a display inconsistency rather than a wrong score, but it is the visible symptom of finding 02.
  - _fix:_ Fix is subsumed by finding 02 (route leaderboard/deep-dive through discoveryRows). Once disqualified stocks are excluded from the ranked tables, the header count and the tables agree.
- **Full ~55MB method-history sidecar is JSON.parsed into memory even though only top-200 trends are emitted** — `generate-methods-report.js:53 (sidecar JSON.parse), 844 (JSON.stringify(stockDataMap)), 79-86 (batched read)` [performance]
  - At the projected 19k-ticker universe this parses a multi-hundred-MB sidecar and builds N x M trend objects, most of which are thrown away — wasted CPU and a large transient allocation on a path already flagged at 14.6h. Not correctness-critical, and the trend compute itself is cheap per call, but the sidecar parse + full-universe trend materialization scales with the wrong quantity (universe size, not output size).
  - _fix:_ Defer trend computation until after the top-picks/matrix slice is chosen, computing trends only for the ~300-500 tickers actually rendered. Alternatively stream/lazy-load the sidecar per-ticker. This also composes with finding 03 (emit-once dataset) if that is adopted.
- **fmtValue is never called; topByMethod and topAllMust are exported but consumed by nobody (and unused internally)** — `generate-modes-report.js:1316, 71-77, 245-264, 266-286` [dead-code]
  - Dead exports/functions are maintenance traps: the prior audit already deleted renderRow for the same reason (l.434-439 comment). fmtValue additionally hides a latent bug — `if (unit === 'ratio' && Math.abs(v) < 1)` (l.74) silently rescales any ratio in (-1,1) to a percent, which would be wrong if it were ever wired up. Carrying three unreferenced symbols (one buggy) inflates the file and misleads future readers into thinking topByMethod/topAllMust are part of a live code path.
  - _fix:_ Delete fmtValue (l.71-77). Remove topByMethod and topAllMust from module.exports (l.1316); if they are not part of any documented public API, delete the function bodies too. Keep evaluateAll/dedupeByCompany only if a test or external caller actually needs them (none found — confirm before trimming).
- **Status-strip 'Sektor-Exclude' count derived from HYPERGROWTH eligibility only, but eligibleForMode never applies mcapFloor** — `generate-modes-report.js:591, 1010-1013` [data-integrity]
  - The headline 'Sektor-Exclude' figure actually folds in HYPERGROWTH dataguard failures (sloan, forecast-contamination, q-spike, revenue-volatility), so the number Karl reads as 'banks/REITs/mining removed' is inflated by earnings-quality rejects. Minor (a status badge, not a ranking), but it is a mislabeled metric on the front page and conflates two very different exclusion reasons.
  - _fix:_ Compute sector exclusion explicitly: `totalStocks - evaluated.filter(ev => !SM.isExcludedBySector(ev.stock, SM.MODES.HYPERGROWTH)).length`, or relabel the cell to 'HG-ineligible'. Keep the dataguard count as a separate cell if useful.
- **Tier grouping silently buckets any non-A/non-B tier (REJECT, null) into NEAR_MISS** — `generate-modes-report.js:458-466, 168-170` [bug]
  - A REJECT-tier or untiered stock with a numeric score outside 50–64 lands under a header that asserts 'Score 50–64', so the section header lies about its contents. The CSS even defines a cb-tier-reject badge (l.927), confirming REJECT is a real tier that this grouping does not handle. Stocks the scorer flagged REJECT can appear in the Near-Miss bucket as if they were borderline-good.
  - _fix:_ Make the buckets exhaustive and honest: add an explicit REJECT group (or drop tier!=='NEAR_MISS' && score not in [50,64] from the near bucket), and gate NEAR_MISS on `me.tier === 'NEAR_MISS'`. Comment: 'prevents REJECT/untiered stocks from being mislabeled as Near-Miss 50–64'.
- **buildRow() does 2 synchronous fs.existsSync+readFileSync per stock — silently defeats the async batched snapshot loader and adds minutes back to the run** — `generate-screener.js:439-444, 160-192, 255-259, 4276-4290` [performance]
  - The header comment at L80-84 (Tag 232c-11 / audit F-PF-003) explicitly converted loadStocks() to 32-way async batched reads precisely to kill 'serial fs.readFileSync over 3-15k+ files' that 'added 2-3 minutes per generator run'. But buildRow then re-introduces TWO serial synchronous reads per stock (score-history + r40rx-history) on the single main thread — i.e. up to ~2×(universe size) blocking syscalls, exactly the pattern that was supposedly fixed. On a 3.5k-tabbed / larger total universe this is the dominant wall-time cost of the build and blocks the event loop the whole time. This is a top structural cap on iteration speed (the screener is at '~20% of potential' partly because each build is slow).
  - _fix:_ Read the score-history/ and r40rx-history/ directories the same way loadStocks() reads snapshots: one async batched pass (Promise.all over fs.promises.readFile with CONCURRENCY=32) into two Maps keyed by safeSnapshotFilename(ticker), built ONCE in main() before the buildRow loop, then passed into buildRow (or looked up from a module Map). The per-ticker memo Maps (_scoreHistoryCache/_r40rxHistoryCache) already exist — just pre-populate them asynchronously instead of lazily-synchronously. No method/scoring change, so fixture hashes are unaffected.
- **Dead helper and unused locals in the modal: yoyDelta() and revPrev are computed but never rendered** — `generate-screener.js:2543, 2544-2545, 2555-2557, 2620-2634` [dead-code, auto-safe]
  - Dead locals that look load-bearing (a YoY-delta helper next to a revenue card) invite a future maintainer to 'fix' the card to use them, or mask the fact that the card's sub-line is hard-wired. Minor, but it is confirmed dead code in a god-file that is already hard to reason about.
  - _fix:_ Delete `yoyDelta` (L2543) and `revPrev` (L2544). If a per-card YoY delta is actually wanted, wire it explicitly; otherwise remove to reduce noise.
- **Column sort is descending-only: aria-sort never renders 'ascending' and clicking an already-sorted header does not toggle direction** — `generate-screener.js:2332-2342, 3628-3640` [bug]
  - A user clicking the MCap or RevGr% header expecting to toggle to ascending (smallest-first, e.g. to find the cheapest or slowest names) gets descending every time, with no way to reverse it, and the screen-reader announcement (aria-sort) can never report ascending. The CSS ascending-caret rule is dead. It's a real usability gap in a tool whose whole value is fast slicing, and it contradicts the accessibility intent the code otherwise invests in.
  - _fix:_ Track a `sortDir` ('asc'|'desc') alongside sortKey; clicking the active sort column toggles it, clicking a new column resets to 'desc'. Make sortList() multiply the comparator by the direction sign. Emit aria-sort='ascending' accordingly so the existing CSS caret works.
- **Insider 'Shares' column formats a share COUNT with the money formatter fmtM (T/B/M suffix), mislabeling raw share quantities** — `generate-screener.js:2909, 497-503` [bug]
  - A purchase of 1,500,000 shares renders as '2M' (rounded, suffixed) in a column a user reads as a share quantity, conflating share counts with dollar magnitudes and dropping precision. The adjacent '$' column already uses fmtM correctly for value; reusing the dollar formatter for the count is a copy-paste slip that misstates insider conviction size.
  - _fix:_ Format share counts with a plain thousands-separated integer (e.g. `b.shares.toLocaleString()`), reserving fmtM for monetary columns. Comment the failure mode (share-count vs dollar-magnitude conflation).
- **Altman X1 working-capital source can mix real current accounts with the cash-minus-debt proxy across firms, making X1 non-comparable and the proxy fallback systematically biased** — `methods/altman-z-score.js:60-69` [data-integrity]
  - On a 0.20-weight TURNAROUND gate, two otherwise-identical firms are graded on structurally different X1 definitions purely based on whether Yahoo persisted current accounts for them (~76% per the comment). The proxy's documented directional bias (cash-rich overstated) systematically pushes cash-rich names toward SAFE and current-liability-heavy names toward DISTRESS, an attrition asymmetry across the universe rather than a per-stock crash.
  - _fix:_ Either gate the method to computable:false when real current accounts are absent (consistent definition for all scored firms), or calibrate a separate threshold for the proxy regime. At minimum emit a NEAR/low-confidence flag when wcSource==='cash-minus-stdebt-proxy'. Comment: 'prevents failure mode: dual X1 definition makes solvency gate non-comparable across the universe'.
- **One-Dollar test misaligns dividends with net income: rawDiv is indexed positionally against ni5 even though the two series are independently filtered/compressed** — `methods/buffett-criteria.js:498-501` [bug]
  - retained5y (NI minus dividends) drives the capital-allocation pass/fail and the 'capital destruction' fail branch (line 521-524). Subtracting a dividend from the wrong year's earnings can flip retained5y sign for borderline payers, producing a false 'capital destruction' fail or a false pass.
  - _fix:_ Build a single aligned [{ni,div}] table over the un-filtered arrays before summing, skipping years where NI is non-finite. Add comment: `// prevents subtracting dividend[year_k] from netIncome[year_j] after independent filtering`.
- **T3 totalLiab fallback can go negative and tlRatio NaN-formats; liabTest passes when tlRatio is null, weakening the debt gate silently** — `methods/buffett-criteria.js:240-241, 257` [bug]
  - For the entire quoteSummary-sourced universe (no equity/liabilities fields), the 'TotalLiab/Assets < 60%' leg is a no-op pass, so T3 collapses to just LTD/NI<5x. The method advertises a two-pronged conservative-debt test (lines 243-251) but one prong is dead for the majority data shape. Leverage that the second prong was meant to catch slips through.
  - _fix:_ Derive totalLiab from totalAssets - totalEquity only when equity exists; otherwise mark the liab leg 'unknown' and require the LTD/NI leg to be stricter, or count the test as not-passed-but-not-failed explicitly. Add comment: `// prevents liab/assets leg becoming a silent always-pass when equity/liab fields absent`.
- **Q1 moat returns inconsistent component key: 'industryMedianPlus5pp' on the insufficient-data path vs 'industryMedianPlus10pp' on the computed path** — `methods/buffett-criteria.js:544-545, 560` [bug, auto-safe]
  - Any consumer (report generator, dashboard, downstream aggregator) that reads `q1.industryMedianPlus10pp` gets undefined when the test was insufficient-data, and one reading the legacy `industryMedianPlus5pp` key gets a value only on the failure path. Inconsistent component schemas across return paths are a quiet source of undefined/NaN in reports.
  - _fix:_ Use the same key on both paths (industryMedianPlus10pp). Add comment: `// prevents component-key drift across return paths (undefined in downstream report)`.
- **T2 ROIC 'not declining' compares latest vs a 5-element slice that, when fewer than 5 valid years exist, equals the full set including latest — making the test near-tautological** — `methods/buffett-criteria.js:200-205` [bug]
  - The 'not declining' guard is meant to reject deteriorating moats, but at the common Yahoo 3-4y depth the comparison includes the very year it is testing, biasing strongly toward pass. The method's own header (line 295-298) notes Yahoo FTS max is 4y, so this short-window degenerate case is the NORM, not an edge case.
  - _fix:_ Exclude the latest year from the comparison baseline (compare valid[0] against mean of valid.slice(1, k)), and require a minimum baseline length before applying the trend leg. Add comment: `// prevents self-comparison: latest year must not be inside its own trend baseline`.
- **_computeAnnualROIC recomputed twice per stock (T2 and Q1) — full balance-row iteration done redundantly across the whole universe** — `methods/buffett-criteria.js:107-137, 187-215, 538-561` [performance, auto-safe]
  - Per the brief, per-method re-classify / wasted recompute over the universe is a named performance dimension. Across ~19k stocks every Buffett evaluation does the ROIC array build twice. Minor per call, but it is pure duplicated work on the hot path and the kind of redundancy that compounds across 36+ methods × universe × the multi-hour pull.
  - _fix:_ Compute the ROIC array once at the top of evaluate() and pass it into both _testT2_ROIC and _testQ1_Moat. Add comment: `// prevents double ROIC recompute per stock (T2 + Q1 shared input)`.
- **components.latestRatio / reason 'latest=' report ratios[0] which is the first VALID year, not fiscal year 0 — mislabels the latest ratio whenever the most recent year was skipped** — `methods/capex-vs-sbc-quality.js:111-115, 127` [bug]
  - The diagnostic claims to show the most-recent-year capex/SBC ratio but can silently show a 1–2 year stale value, misleading anyone drilling into the dashboard component. Low severity (display/diagnostic only, not in SCORE_WEIGHTS) but it is a correctness-of-evidence bug: the number a reviewer trusts as 'this year' may be two years old, and there is no indication the latest year was dropped.
  - _fix:_ Track the source year index alongside each ratio (push {ratio, yearIndex}) and report latestRatio with its true yearIndex, or rename the field to firstValidRatio. Comment: 'failure mode prevented: labeling a skipped-year-adjusted ratio as latest-year.'
- **Composite re-evaluates four sub-methods per stock instead of consuming already-computed runner results — duplicate work across the universe** — `methods/capital-allocation-quality.js:84-87, 103-114` [performance]
  - Across a multi-thousand-name universe each stock pays for 4 redundant sub-method evaluations (array unwrap + arithmetic) every run, on top of the standalone runs. Cheap per call, but it is exactly the 'per-method re-classify / wasted recompute over the universe' pattern that structurally caps throughput; capital-allocation-quality is also one of several composites (premium-compounder-proof, etc.) doing the same, so the pattern compounds. It also creates a correctness coupling risk: if the runner ever applies effectiveThreshold overrides or wrapEvaluate flags to the standalone results, the composite silently uses the RAW evaluate() output instead, diverging from what the dashboard shows.
  - _fix:_ Plumb the runner's allResults map into composite methods (an optional second arg `evaluate(stock, allResults)`), and have the composite read `allResults['buyback-yield']` etc. when present, falling back to _safeEvaluate only when a dependency result is absent. This removes the recompute and guarantees the composite sees the same values the rest of the pipeline does. Add comment: 'failure mode prevented: composite diverging from standalone results + O(n) duplicate sub-evaluations across the universe.'
- **Proportional 'needed' scaling via Math.round can demand more positives than the rounding of 4*obs/5 intends at the 3-year boundary** — `methods/earnings-stability.js:71-72, 77-83` [bug]
  - PLTR/CRDO-type 3-4y-history compounders the Tag 221 fix (lines 66-72) explicitly names as the motivation are still rejected at the >=4 observable gate for OpInc/FCF, so the proportional-scaling 'fix' does nothing for the 3y case it cites. Low severity (no wrong PASS), but it is dead/contradictory logic that masks an unmet requirement — a newly-listed quality compounder with exactly 3 clean years is dropped as incomputable.
  - _fix:_ Decide the real minimum-history policy: if 3y compounders should be scored, lower the computable gate (lines 47) to >=3 and let the proportional needed handle it; otherwise delete the unreachable 3y branch from the comment. Do not hardcode tickers. Comment: '// prevents: contradictory >=4 gate vs 3y-needed scaling silently dropping 3y-history compounders'.
- **GP[0] and totalAssets[0] taken from index 0 of two independently-built arrays with no fiscal-year alignment check — mismatched-period GP/TA ratio** — `methods/gross-profitability.js:82-94` [data-integrity]
  - annualGP[0] comes from mapFTSToAnnual (income statement rows) and annualBalance[0] from mapFTSToBalance (balance rows); these are built from different Yahoo modules with independent trailing-null trims (pull-yahoo 1197-1199 vs 1109). If one module returns one more (or fewer) recent period than the other, gpArr[0] and balArr[0] are different fiscal years, so Novy-Marx GP/TA divides this year's gross profit by last year's assets (or vice versa). The anchor checks in the header (NVDA/MSFT/COST) only validate the happy path. Result: a quality floor (>=0.20) decided on mismatched-period inputs.
  - _fix:_ Match GP and totalAssets by fiscal-year/date key (the balance entries carry a period) rather than by array index, or assert the two arrays' newest periods are equal before dividing; otherwise return incomputable. Comment the cross-module period-alignment failure mode.
- **Filing-lag gate (gate4) has no lower bound — a filingDate before the transactionDate passes via negative lag** — `methods/insider-conviction-score.js:285-291` [bug, auto-safe]
  - SEC Form 4 must be filed AFTER the transaction (2-business-day rule); a filing-before-transaction is a data error, yet it silently satisfies the timeliness gate, letting a malformed record contribute to a pass on the load-bearing INSIDER_BUYING hard gate. Low severity because it requires bad cache data, but the gate exists precisely to validate data sanity.
  - _fix:_ Require `lagDays >= 0 && lagDays <= MAX_FILING_LAG_DAYS`. Add comment: '// prevents filing-before-transaction (negative lag) data errors from clearing the timeliness gate.'
- **Source-1 fields (stock.insider.*) are never produced by the puller — permanently dead branch** — `methods/insider-net-buying.js:47-72` [dead-code]
  - The method's headline claim is a 180d net-buying horizon, but the 180d source is never populated — every live evaluation silently degrades to the 90d transaction-count fallback (which is itself flawed per finding 01). The method's name/label '(180d)' is misleading versus what it actually computes, and the dead branch invites future maintainers to think 180d data exists.
  - _fix:_ Keep the forward-compatible branch but mark it clearly as not-yet-populated, and make the label/value reflect that the live computation is 90d-windowed. Best paired with finding 01 (switch the live path to netShares90d). Comment the failure mode: '// 180d source not yet produced by puller; live path is 90d — label must not over-claim horizon.'
- **Documented formula (percent) disagrees with emitted value (ratio); reason string and value use different units** — `methods/institutional-density.js:19, 86, 94` [bug]
  - Any downstream consumer that trusts the header / treats this method's `value` as the documented percent composite will be off by 100x. Even if no current consumer does, the in-file contradiction is a latent trap and makes the diagnostic table inconsistent with the method's own description (the same divergence that historically caused field-name/scaling regressions elsewhere in this codebase). Self-documenting code is load-bearing here because Karl reads results, not implementation.
  - _fix:_ Make value and the docstring agree — either emit the percent (`pct*100`) to match the documented composite, or correct the header to say value is the ratio. Pick one and add a comment naming the unit explicitly to prevent the 100x consumer bug.
- **SOFT_GUARD penalty / score-weight only fire when the underlying method is actually evaluated — onlyDefault callers and disabled methods silently drop penalties and weight** — `methods/method-types.js:65, 116-118; methods/score-aggregator.js:205, 116-118` [bug]
  - The coupling between (a) SCORE_WEIGHTS / SOFT_GUARD_PENALTY registration and (b) whether the method is in the evaluated set is implicit and has already caused at least two logged bugs (#20, #21). Any future weight/penalty added without flipping defaultActive, or any caller using onlyDefault:true, silently zeroes that contribution with no error — the score quietly drifts. This is a recurring class, not a one-off.
  - _fix:_ Add a startup invariant check (in runner or a test) asserting every id in SCORE_WEIGHTS[*] and SOFT_GUARD_PENALTY is both registered and isDefaultActive(); throw/log loudly on violation. This converts the silent-drop class into a loud failure, consistent with the index.js 'fail LOUDLY' philosophy. Comment: 'prevents a scored/penalized method silently contributing 0 because it was not in the evaluated set.'
- **Positive-only sign filter on OCF/NI silently excludes the most informative low-quality cases (NI>0 while OCF<=0), biasing the coverage mean upward** — `methods/operating-cashflow-coverage.js:104-109` [data-integrity]
  - Header states the failure mode it detects is 'NI persistently exceeds OCF' / 'reported earnings increasingly non-cash'. But the worst manifestation — positive NI, negative OCF — is filtered out as 'sign mismatch', so the 3y mean only averages the years where cash conversion was already positive. A serial accruals-inflator with one heavily negative-OCF year gets that year excluded and can still clear the 0.80 floor on its two good years. Self-defeating for a DIAGNOSTIC quality signal.
  - _fix:_ Treat NI>0 with OCF<=0 as coverage 0 (or a small floor) and include it, rather than skipping; keep skipping only true non-informative cases (NI<=0). Comment that excluding NI>0/OCF<=0 would hide the primary failure mode.
- **Sign-flip 'turnaround' branch uses a fixed $1M absolute swing threshold, mis-scaling for both micro-caps and mega-caps** — `methods/profitability-trend.js:44` [bug]
  - profitability-trend is CORE and feeds the 2-axis Compounder/Turnaround matrix the file documents (lines 13-21). A size-blind absolute floor produces both false-FLATs (missed small-cap turnarounds — the discovery edge Karl wants) and false-IMPROVINGs (noise on large caps). This caps the screener's turnaround-discovery precision.
  - _fix:_ Scale materiality relative to revenue or to |y1| (e.g. swing must exceed max($1M, 2% of revenue)) consistent with profitability-state's MARGINAL_REV_PCT=0.02 pattern. Comment: '// prevents: fixed $1M swing floor mis-scaling sign-flip materiality across micro-/mega-cap'.
- **OpMargin-median for the retail-tier gate is computed from independently-unwrapped Rev/OpInc arrays with no index-alignment or null-skip parity, so a single missing year mispairs revenue and operating income** — `methods/quality-compounder-roic.js:45-49, 119-128` [bug]
  - The retail-tier override (line 140-145) lowers the ROIC floor from 20% to 15% for high-asset-turnover names (COST pattern). A mispaired opMargin median can wrongly satisfy or fail the OPMARGIN_RETAIL_GATE (0.035), flipping the CORE pass for exactly the retail-compounder class the tier was added to catch. The bug is dormant when both arrays come from the same path with no holes, but the method makes no check.
  - _fix:_ Guard `rawRevs.length === rawOpIncs.length` before the paired loop (mirror the alignment guard recommended for roic-trend), and skip the pair when either side is null rather than relying on Number.isFinite of a possibly-misaligned element. Comment: '// prevents: cross-year Rev/OpInc pairing inflating/deflating retail-tier opMargin median'.
- **Concentration DATAGUARD fails any stock whose single largest of the last 4 quarters exceeds 50% of the 4Q sum, with no seasonality exemption — unlike q-spike-dataguard which has Q4-seasonal and launch-inflection suppression** — `methods/quarter-concentration-guard.js:30-37` [data-integrity]
  - Two DATAGUARDs implement the same single-Q-concentration concept with DIFFERENT exemption logic (q-spike has YoY>100% trigger + seasonal/launch suppression; quarter-concentration has none and fires unconditionally at 50%). The stricter, exemption-free one wins (any DATAGUARD fail disqualifies), so q-spike's careful seasonal suppression is defeated by its blunt sibling — culling legitimate seasonal compounders and contributing to the survivorship narrowing.
  - _fix:_ Either consolidate the two concentration guards into one (single source of truth, shared seasonal/launch suppression) or apply the same _isSeasonalQ4Spike/_inLaunchInflection suppression here before failing. Do not parallel-edit methods/index.js. Comment: 'prevents blunt 50% concentration cut from overriding q-spike seasonal/launch suppression'.
- **Acceleration gate requires r1>0 and r2>0 but not r0>0; a swing from negative revenue base is mishandled and a tiny denominator inflates delta without a magnitude floor** — `methods/revenue-acceleration-yoy.js:58, 66-68` [bug]
  - DIAGNOSTIC-only today (not scored), so bounded, but it produces nonsense reason strings ('Δ=+19900pp') for trough-recovery names and would be actively misleading if promoted. The negative-r0 case yields a negative yoyT0 that can still beat an even-more-negative yoyT1 and falsely report 're-acceleration'.
  - _fix:_ Add a base-materiality floor (skip when r1 or r2 below a revenue floor) and require r0>0; cap/flag delta when any denominator is a trough. Comment: 'prevents trough-base denominators from manufacturing fake re-acceleration'.
- **Three growth DIAGNOSTICs (revenue-quality, revenue-quality-cov, revenue-acceleration-yoy, single-quarter-dependency, quarterly-revenue-acceleration) are defaultActive:true but feed no score and no gate — computed every run, consumed only by display** — `methods/revenue-quality-cov.js:61, 168-173` [dead-code]
  - Performance + signal-dilution: five non-trivial array methods recomputed for every stock every pull (the multi-hour pull the brief flags) while contributing nothing to ranking. This is part of why high-quality signals are diluted — the screener computes a lot it never acts on. Several (revenue-quality-cov, single-quarter-dependency) are the BETTER versions of signals that are not scored, while weaker scored proxies remain.
  - _fix:_ Decide per method: promote the strong ones (revenue-quality-cov, revenue-acceleration-yoy) into HYPERGROWTH SCORE_WEIGHTS or a soft-guard, and gate the rest behind an explicit on-demand/detail-modal flag so they are not recomputed for the whole universe. Do not change FTS_CACHE_VERSION or methods/index.js wiring as part of this; adjust method-types defaultActive + aggregator weights only.
- **Annual-fallback z-score uses a magic `99` sentinel when MAD==0 and ratio>5, but `5x` threshold and `99` are unexplained and bypass the materiality jump-check asymmetrically** — `methods/revenue-shock-guard.js:92` [bug]
  - Inconsistent handling of the zero-dispersion case between quarterly and annual paths means the same underlying shock is caught annually but missed quarterly (or vice versa) depending only on which data tier is present. DATAGUARD verdicts should not depend on tier-specific magic numbers.
  - _fix:_ Unify the MAD==0 handling across both branches: when dispersion is zero, fall back to a relative-jump ratio test with a single named constant (e.g. SHOCK_RATIO_WHEN_NO_DISPERSION) instead of a 99 sentinel in one branch and silent 0 in the other. Comment naming the failure mode: 'prevents tier-dependent shock detection when prior-period MAD is zero'.
- **roic.js trusts H.latestBalance without a finite guard; a non-finite balance field yields computable:true with value:null instead of computable:false** — `methods/roic.js:10-12, 22, 30` [data-integrity, auto-safe]
  - A degenerate ROIC silently presents as a computable FAIL (value null, pass false) rather than an honest computable:false. For a CORE/aggregator-weighted method this contaminates the score and the dashboard with a phantom hard-fail. Same latent issue in quality-compounder-roic.js and sector-relative-roic.js, which also read totalAssets/totalCash via H.latestBalance without finite guards.
  - _fix:_ Add `Number.isFinite` guards after reading totalAssets/totalCash (or harden `latestBalance` centrally — but that is a helpers change). At method level: `if (!Number.isFinite(totalAssets)) return computable:false`. Comment: '// prevents: NaN/Infinity balance field producing a phantom computable FAIL instead of computable:false'.
- **Methods loaded once at module-eval (good) but evaluateStock re-derives type/core/coverage sets per stock via repeated Object.keys().filter() over the universe** — `methods/runner.js:40, 13-38` [performance]
  - On a 23,748-name universe (snapshots/_manifest.json) this is tens of millions of redundant hash lookups and array allocations per pull, compounding the 14.6h pull. None of it varies by stock — the method list and their types are fixed once index.js + method-types.js load.
  - _fix:_ Precompute static sets once after _loadAllMethods(): an array of {method, type, isCore, isDefaultActive} and a frozen CORE_IDS list; in evaluateStock iterate the precomputed array and increment coreCount/coreCountPass inline instead of two post-hoc Object.keys().filter() passes. Pure refactor, behavior-identical. Comment: 'prevents per-stock re-classification — method types are process-static; avoids O(universe×methods) redundant REGISTRY lookups.'
- **Largest-weight CORE inputs are categorical (string thresholds) and can only score binary 0/1 — caps score discrimination on the dominant signals** — `methods/score-aggregator.js:32-73, 110-130` [architecture]
  - Half of the TURNAROUND score and 40% of HYPERGROWTH is a coin-flip 0/1 with no gradient, so two stocks with very different underlying profitability trajectories that land in the same state bucket get identical points. The graduated-scoring architecture (Tag 120's whole premise) is bypassed for the heaviest inputs, flattening A/B/NEAR_MISS discrimination precisely where it matters most — a structural cap on screener precision.
  - _fix:_ Have the categorical methods additionally export a numeric ordinal in components (e.g. profitability-state: LOSS=0, TURNAROUND=1, RECENT=2, EMERGING=3, STABLE=4) and teach normalizeMethodScore to graduate on that ordinal vs an ordinal threshold when threshold is a known enum. Keep the binary path as fallback. Comment each touched method: 'prevents heavy categorical CORE inputs collapsing to binary 0/1, flattening score discrimination.'
- **Coverage gate divides by totalWeight which only sums weights of methods present in the loop — a SCORE_WEIGHTS id never returned by the runner is invisible to the coverage denominator** — `methods/score-aggregator.js:248-250, 264-265` [bug]
  - A registry/weights typo doesn't fail loud here — it silently lowers every stock's coverage ratio in that mode, mass-REJECTing names near the 0.30/0.40 boundary. Given the heavy reliance on string-keyed weight maps and the history of registration drift (Bugs #20/#21, Tag 206d), this is a realistic silent-degradation path.
  - _fix:_ Cross-check SCORE_WEIGHTS keys against the live method registry at startup (ties into finding -08's invariant) so an unmatched weight key throws rather than silently inflating totalWeight. Comment: 'prevents a typo'd/de-registered SCORE_WEIGHTS key silently depressing coverage and mass-rejecting borderline stocks.'
- **Sloan validity check is decoupled from per-year zipping: NI/FCF presence is validated globally but the consecutive-year fail rule reads positional indices that can mix non-aligned years** — `methods/sloan-ratio.js:30-48, 100-101` [bug]
  - pull-yahoo.js explicitly warns (comment near line 1147) that annualBalance[i] could reference a different fiscal year than annualRev[i] before the alignment fix; the Sloan code assumes positional alignment across annualNetIncome / annualFCF / annualBalance without verifying a shared fiscalYear key. A misalignment silently computes accruals from year t NI against year t-1 FCF/assets, and the 2-consecutive-year CHRONIC_FAIL gate (the only hard-fail, feeding the red-flag rule) can fire or not-fire on phantom adjacency.
  - _fix:_ If the snapshot exposes a per-row fiscalYear/endDate, key the zip on it and assert equality across the three arrays per year; otherwise document and assert equal lengths + shared origin before positional zipping. Comment: 'prevents failure mode: cross-array positional zip mixes non-aligned fiscal years in accrual + consecutive-fail logic'.
- **Non-SPY tickers only ever get ONE day appended per run, so the 252-day momentum/backtest history takes a year of clean runs to build and any gap is never back-filled** — `pull-historical-prices.js:150, 119-129` [data-integrity]
  - price-momentum-12-1.js needs LOOKBACK_FULL_DAILY=252 entries for the proper path (:205-207) and 84 for even the degraded path; walk-forward-perf.js needs a year of dated points. Until ~252 successful consecutive daily runs accumulate, every non-benchmark stock is stuck on the degraded/incomputable path. Worse, any day the run fails or a ticker errors (:131-134) leaves a permanent hole that is never repaired (only backfill-prices.js, run manually, can fill it). This is a direct structural cap on the momentum factor and backtest coverage.
  - _fix:_ Back-fill all fetched quotes for every ticker the same way SPY is handled (:172-182): build a Set of known dates and insert every missing dated quote, then sort+cap to 400. The data is already fetched — discarding it is pure waste. Carry a comment: 'prevents permanent history gaps / 252-day warm-up — back-fill all fetched bars, not just the latest.'
- **pull-prices-bulk.js is an orphan entrypoint with a dead in-memory accumulator and no error gating** — `pull-prices-bulk.js:42, 50, 66, 75` [dead-code]
  - A second, unmaintained writer of the single-source-of-truth history file is a latent integrity hazard (see A-01): if anyone runs it, it injects raw-close rows. The dead `out` accumulator misleads future maintainers into thinking a full snapshot is persisted. An unhandled rejection in main() exits non-zero with no diagnostic.
  - _fix:_ Decide its fate: if backfill-prices.js supersedes it, delete pull-prices-bulk.js (ask Karl before deleting). If kept as a manual range-runner, (a) fix the adjclose basis per A-01, (b) remove the dead `out` object and its load block, (c) wrap `main().catch(e => { console.error(e); process.exit(1); })` and add a `require.main === module` guard.
- **Redundant Object.fromEntries(...)/Object.values round-trip over the SEC ticker map, and --max slices an arbitrary insertion-ordered subset (no determinism / no prioritization)** — `pull-sec-xbrl.js:124` [bug]
  - Minor perf/allocation waste, but the slice-by-insertion-order means `--max`-limited runs (or the >50-error/>200-429 abort gates at :165/:173) systematically under-serve tail tickers; combined with the 90-day stale skip (:131) the tail may rarely refresh. Low severity because the monthly full run normally has max=Infinity.
  - _fix:_ Replace with `const entries = [...tickers.values()];`. If `--max` is used operationally, sort `todo` by oldest `fetchedAt` first (round-robin freshness) so partial runs make uniform progress instead of re-pulling the head.
- **_withTimeout is dead — fully superseded by _withAbortTimeout (F-PY-102)** — `pull-yahoo.js:1241-1247` [dead-code, auto-safe]
  - The non-aborting variant is exactly the zombie-queue-slot mechanism F-PY-102 was written to eliminate (documented at lines 1249-1258). Leaving it in the file invites a future edit to reintroduce the throughput-collapse bug by grabbing the wrong helper. It is also misleading documentation about the live behaviour.
  - _fix:_ Delete the _withTimeout function (lines 1241-1247). Keep only _withAbortTimeout. Pure removal; no behaviour change.
- **totalDebt absence-as-zero summing is documented as deliberate but the _debtPartial flag is never consumed inside pull-yahoo and depends on every downstream method opting in** — `pull-yahoo.js:687, 1167` [data-integrity]
  - When only one of short/long-term debt is present, totalDebt is understated (the missing leg counted as 0). The decision to keep this is documented (F-NY-002, 12.4% of snapshots) and surfaced via _debtPartial — but the safety of the decision rests entirely on EVERY leverage/EV consumer reading debtPartialFlag. That is an unenforced cross-module contract: a new method using annualBalance[i].totalDebt without checking _debtPartial will silently use an understated number (too-low net-debt/EBITDA → a leveraged name looks safe). Not a bug in this file per se, but a structural fragility this file originates.
  - _fix:_ Out of scope to change the summing (explicitly forbidden by the documented decision). Hardening only: add `_debtPartialReason: std==null?'no-current-debt':'no-long-term-debt'` so consumers can size the gap, and consider emitting a snapshot-level `meta._debtUnderstatedYears` count so score-aggregator can tier-cap. Comment already exists; extend it to name the consumer-contract risk.
- **Top-level catch swallows ALL errors as exit 0, so a broken drift monitor reports green forever** — `scripts/check-pull-stats.js:158-162` [data-integrity]
  - This is the monitoring script itself. If postDiscord throws, or collectStats hits an unexpected shape, the monitor dies silently with exit 0 and Karl believes 'no drift' when in fact the detector never ran. A monitor that fails open defeats its own purpose. It is intentional (workflow non-failure) but the failure should still be surfaced — e.g. a Discord 'monitor crashed' ping.
  - _fix:_ Keep exit 0 for workflow safety, but in the catch attempt a best-effort `postDiscord('check-pull-stats crashed: '+e.message)` so a dead monitor is visible. Do not let the only signal be a console line nobody reads.
- **evaluateStock re-runs ALL methods per pick purely to extract 4 price-derived values; and snapshot-load failure silently degrades the row** — `scripts/elliott-export.js:98, 81-97` [data-integrity]
  - Performance: full re-classification per pick is wasted recompute — exactly the 'per-method re-classify' anti-pattern called out in the audit scope. For a few-hundred-pick export it is tolerable, but it scales O(picks * methods). Data-integrity: silent degradation count would help Karl trust the CSV.
  - _fix:_ Add an opts filter to evaluate only the 4 needed price methods (runner already supports `{type}` filtering, line 44/50), or compute them directly. Print a tally of snapshot-miss rows at the end.
- **Function safe() is dead — defined, never called; duplicate of pct()** — `scripts/extract-ma-rpo.js:227-230` [dead-code, auto-safe]
  - Dead duplicate invites the classic divergence trap: a future edit fixes one twin and not the other. Minor, but it is genuinely unreferenced.
  - _fix:_ Delete `safe()`. Keep `pct()` (the live one).
- **Debug nearby-key search uses cik.slice(4) on a 10-digit zero-padded CIK, matching the wrong substring** — `scripts/extract-ma-rpo.js:278` [bug, auto-safe]
  - Diagnostic-only (runs only when a CIK is not in the zip), so no data corruption — but the hint it prints is misleading when debugging coverage gaps, which is exactly when you rely on it. Zip keys are `CIK<10digits>.json` (line 273), so the search target should be the full padded CIK.
  - _fix:_ Use the full CIK: `k.includes(cik)` or `k.includes(cik.replace(/^0+/,''))` to match the un-padded numeric form actually present in some archives.
- **100-line ZIP central-directory parser is copy-pasted across scripts instead of living in lib/** — `scripts/extract-ma-rpo.js:38-137` [architecture]
  - A ZIP64/deflate edge-case fix (or the cik.slice bug above) must be applied N times. lib/ already holds shared helpers (atomic-write, snapshot-fs, watchlist-fs, discord) — the ZIP reader is the obvious next extraction. This kind of triplicated low-level parsing is a structural cap on maintainability.
  - _fix:_ Extract to lib/zip-reader.js exporting `openCentralDirectory(fd,fileSize)` and `readEntry(fd,entry)`; have extract-ma-rpo.js, enrich-q-revenue.js, and the probe require it. (Does not touch methods/index.js or FTS_CACHE_VERSION.)
- **Unguarded JSON.parse of prices/history.json in a live workflow step throws and fails the daily-pull job** — `scripts/macro-regime.js:69` [data-integrity, auto-safe]
  - A truncated/half-written prices/history.json (e.g. interrupted upstream write) makes macro-regime.js throw an uncaught exception → non-zero exit → the daily-pull workflow step fails. By contrast elliott-export.js (line 138-140) and check-pull-stats.js (line 158-161) wrap main and exit 0 on error precisely to avoid breaking the pipeline. macro-regime is the asymmetric outlier. Note it DOES gracefully handle the empty-series case (line 72-85) — only the parse itself is unguarded.
  - _fix:_ Wrap the read+parse in try/catch (reuse the `loadJson` pattern used in the other scripts) and on parse failure write the same `error: 'no_price_data'` fallback the empty-series branch already produces, then exit 0 — keeping the regime output present-but-empty rather than crashing the job.
- **Asymmetric-attrition (delisting bias) diagnostic only runs on fresh-load; once a vintage is cached the pass/fail-drop imbalance warning vanishes forever** — `scripts/method-effectiveness.js:301-372 (fresh-load attrition block) vs 223-250 (cache-replay block)` [data-integrity]
  - The author explicitly built this to catch the survivorship/delisting bias that 'systematically biases alpha' (comment lines 281-291) — asymmetric exclusion of delisted tickers' returns from one side of the pass/fail cohort. But the signal only appears in CI logs on the single day a vintage is first processed, then disappears. An operator scanning recent logs sees nothing and assumes the cohorts are balanced. The bias itself still occurs (dropped tickers are simply absent from pass/fail arrays on replay too) — only the *visibility* is lost.
  - _fix:_ Persist the per-vintage attrition counts into the cache entry header (e.g. `cache.vintageReturns[cacheKey]` could be `{ entries: [...], droppedPass, droppedFail, droppedTickers }`) and re-emit the imbalance warning on replay. Bump CACHE_SCHEMA_VERSION (allowed — that is not FTS_CACHE_VERSION) so old caches rebuild once. Alternatively aggregate attrition into the final report JSON so it is observable in the artifact, not just transient logs.
- **Maturity pre-gate uses asOf+1 (conservativeEntry) but the actual entry can be asOf+0 when generatedAt is post-close — pre-gate can DROP a mature vintage (over-conservative), shrinking the corpus** — `scripts/method-effectiveness.js:207-219, 262-264` [bug]
  - The comment claims +1 is 'conservative ... so we never include vintages whose exit price isn't yet available' — true for excluding immature ones, but it ALSO excludes a mature vintage for one extra day, and because results are cached per (asOf,key) the first time they're processed, a vintage skipped on its eligibility day is simply processed one day later — minor. The real risk is the divergence between the two date computations: any future change to getEntryDate's threshold (already changed once, Tag 232c-28/F-BT-007 in walk-forward) silently desyncs the pre-gate from the actual maturity check, reintroducing look-ahead (pre-gate too loose) rather than just lag (too tight).
  - _fix:_ Derive the pre-gate from the same getEntryDate logic, or read generatedAt before the horizon loop (one extra cheap stat/read) and gate on the real realFutureDate. At minimum add a comment cross-linking the +1 assumption to getEntryDate's post-close threshold so the two are changed together. Comment: '// prevents: pre-gate desyncing from getEntryDate threshold → look-ahead (too loose) or corpus shrink (too tight)'.
- **canonicalFrac (unmatured gate) is computed from cache-replayed `matured` flags whose value depends on the benchmark series length AT THE TIME THE VINTAGE WAS FIRST PROCESSED, not now** — `scripts/method-effectiveness.js:400, 414, 233-247` [data-integrity]
  - A method can be permanently stuck in 'unmatured-nearest-day-snapped' (alpha+CI nulled) because of a transient benchmark gap on one historical processing day, with no way to recover short of a cache-schema bump. Conversely, the gate's protection is only as good as the benchmark availability on first-touch. This couples a *statistical-validity* gate to a *historical-IO-timing* artifact — fragile and non-reproducible across machines that first-processed vintages on different days.
  - _fix:_ Either recompute `matured` on replay (cheap: re-derive canonical dates for the vintage and check map membership) or document that canonicalFrac reflects first-touch benchmark availability and bump CACHE_SCHEMA_VERSION whenever the benchmark series materially changes. Prefer recomputation so the gate is a pure function of current data.
- **'Median-R40' picks middle of a descending-sorted list with no even-length handling** — `scripts/regional-oos-test.js:80` [bug, auto-safe]
  - Reported 'Median-R40' is the upper-middle element's value (because list is descending, index floor(n/2) is below center) and never averages the two middle values for even n. It is a single-element proxy, not a true median, biasing the regional comparison that the whole report exists to make. Low severity because it is a reporting stat, not a screener gate.
  - _fix:_ Reuse the proper `median()` helper already written in check-pull-stats.js (line 44-49) / tag229b (line 47-52): `median(list.map(p=>p.r40))`. Consider extracting median to lib/ to stop re-implementing it.
- **Forward horizon is calendar days, mislabeled as '5-trading-day' return** — `scripts/tag229b-new-method-backtest.js:32-35, 89, 118, 171` [bug]
  - 5 calendar days starting on a Thursday/Friday spans a weekend = ~3 trading days, while starting Monday = 5 trading days. The horizon silently varies per vintage, so per-vintage spreads are not comparable and the pooled median mixes 3-day and 5-day returns. The dataset here happens to carry daily (incl. weekend) entries so the snap is benign now, but on real trading-day price data the inconsistency is live.
  - _fix:_ Either index forward returns by N entries in the sorted per-ticker price array (true trading days) or rename everywhere to 'calendar days' and document that horizon length varies. Comment the chosen invariant.
- **FATAL missing-prices path exits 0 (success), masking a failed run** — `scripts/tag229b-new-method-backtest.js:122` [data-integrity, auto-safe]
  - If a caller or future CI step ever gates on this script's exit code, a missing price file would be reported as success and the empty/absent JSON output would be silently trusted. Same silent-success anti-pattern the codebase explicitly closed elsewhere (F-SC-007).
  - _fix:_ Use `process.exit(1)` for the genuinely fatal missing-input case. This is a manual analysis tool (not in workflows), so failing loudly is safe.
- **Exported `priceAt` (and its forward-walk staleness path) is referenced by no caller; method-effectiveness uses nearestTradingDay/_priceAtCanonical instead** — `scripts/walk-forward-perf.js:115-131 (priceAt), 836-838 (export)` [dead-code]
  - Two divergent price-lookup implementations with different staleness semantics (priceAt walks FORWARD via setUTCDate(-i)? no — backward; but it lacks the symmetric gate logic of the canonical path) invite future drift: a maintainer 'fixing' lookups may edit priceAt believing it is live. Dead exported surface also enlarges the module's apparent contract for method-effectiveness, which imports WF.
  - _fix:_ Confirm via repo-wide grep for `.priceAt(` and `priceAt,` across all callers/tests; if truly unused outside tests, remove priceAt + its export (and priceAtLegacy if it becomes unreachable), or collapse all lookups onto the canonical helper so there is one resolution path. Do not touch the FTS cache or methods/index.js. If kept for tests only, mark `@internal` and stop exporting from the public surface.

