# Pipeline Audit 2026-05-18 (Tag 232 wave-12)

## Executive Summary
- Total findings: 71 (5 CRITICAL, 22 HIGH, 31 MEDIUM, 13 LOW)
- Deduplicated from: 78 raw findings across 7 agents (collapsed ADR-currency stack F-DP-002/F-DP-005/F-DQ-009 into compound group CG-002; collapsed Tag-230c hand-rolled-atomic-write family F-SM-001..004/009/010 into CG-003; merged year-alignment family F-ME-001/005/010 references into compound group CG-004; removed F-CI-003 Pages-drift since `.nojekyll` was already added this session).
- Top risk: Run #110/#111 will silently mass-corrupt every ADR snapshot (TSM/BABA/NU/9988.HK) via the price-only path multiplying USD quotes by the financial-currency FX factor (~32× too small), then the $1B mcap floor (line 1647) irreversibly unlinks the snapshot — universe loses small/mid-cap ADRs on every refresh, AND simultaneously the Tag 226a-2 schema-stale probe (F-DP-001) re-spends rate-limit budget on the same tickers forever because v2-but-pre-Tag-211l fundamentals caches never invalidate.
- Recommended first action: Before Run #110 fires, ship a one-line patch to `pull-yahoo.js` that bumps `FX_TO_USD[meta.tradingCurrency]` lookup into `_priceOnlyUpdate` (F-DP-002 fix) AND bump `FTS_CACHE_VERSION` from 2 to 3 (F-DP-001 fix). Both edits are mechanical, both stop active silent corruption, and both unblock the Tag 211l field rollout.
- Already-fixed this session: Pages Liquid syntax error (`.nojekyll` added at repo root). Do not re-list.

## TOP 5 ACTION ITEMS
(sorted by severity × evidence-tier × runway-criticality)

1. [CRITICAL] pull-yahoo.js:1336-1356 — _priceOnlyUpdate scales by financial-currency FX rate, but Yahoo quote() returns trading-currency
   - Root cause: After Tag 204, `meta.fxRateApplied` is the financial-currency-to-USD factor (e.g. TWD→USD ≈ 0.031) but `yf.quote().regularMarketPrice` for ADRs returns trading currency (USD for TSM/BABA). Multiplying USD × 0.031 makes the snapshot price 32× too small.
   - Impact: Every ADR with `tradingCurrency !== reportingCurrencyOriginal` has price and mcap silently corrupted on every price-only refresh (~80% of pulls). The $1B mcap-floor unlinks the snapshot. Universe loses TSM/BABA/NU/9988.HK and every small/mid-cap ADR irreversibly on every run.
   - Fix: Look up `FX_TO_USD[existing.meta.tradingCurrency.toUpperCase()]` and use THAT rate for the price/mcap scale; financial-currency rate stays for annual.* series. For US-listed ADR tradingCurrency='USD' → rate=1 → no-op.
   - Compound: CG-002 (with F-DP-005 + F-DQ-009)

2. [CRITICAL] pull-yahoo.js:1240-1268, 1443-1486, 1589-1593 — Schema-stale probe forces full-pull on every run because v2-but-pre-Tag-211l caches never invalidate
   - Root cause: FTS_CACHE_VERSION=2 was introduced BEFORE Tag 211l added annualSGA/annualDepreciation/annualShares. Caches written between those tags satisfy `_cacheVersion === 2` (cache-fresh) yet lack the new fields. The full-pull serves them from cache, the merge guard at line 1589 skips length=0 arrays, snapshot stays missing the fields, probe fires again next run.
   - Impact: ~98% of universe (per Tag 225d probe) is forced into the expensive full-pull every day, defeating the price-only fast-path budget. Adds to the 7,210 rate-limit failures already observed in Run #107. Beneish-m/ohlson-o/buyback-yield/sga-revenue-trend/working-capital-trend stay computable=false for affected tickers indefinitely.
   - Fix: Bump `FTS_CACHE_VERSION` from 2 to 3 in pull-yahoo.js. Document at line 1443: 'v3: added ftsAnnualSGA, ftsAnnualDepreciation, ftsAnnualShares.'
   - Compound: CG-001 (with F-DQ-001 stale-grade)

3. [CRITICAL] detect-changes.js:57-64, 105-139 — Hand-rolled tmp+rename for alert-state.json + method-history-state.json bypasses lib/atomic-write
   - Root cause: Both 21.6 MB alert-state.json (committed) and 8.9 MB method-history-state.json (sidecar) are written with raw `fs.writeFileSync(tmp) + fs.renameSync(...)` — no tmp fsync, no Windows EPERM retry, no parent-dir fsync. The comment on line 135 ('Atomic write via tmp+rename (was already done; preserved from existing code)') actively misleads auditors. Tag 230c-1 hardening never reached either site.
   - Impact: On Karl's Windows + OneDrive box, an OneDrive scan handle during the rename raises EPERM, the entire detect-changes state-delta is lost, the next run re-fires METHOD_PASS_LOST/GAINED Discord events for every flip (potentially hundreds), and orphan-cleanup work is silently discarded. On POSIX power-loss: zero-byte alert-state.json gets committed and pushed.
   - Fix: Replace both writes with `writeFileAtomic(...)` from lib/atomic-write.js. Delete the misleading line-135 comment. Three-line patch.
   - Compound: CG-003 (with F-SM-002, F-SM-003, F-SM-004, F-SM-009, F-SM-010)

4. [CRITICAL] methods/data-quality.js:20-38 — Grader still tracks Tag 134 fields; Tag 211l/219/220c additions are invisible
   - Root cause: CRITICAL_FIELDS enumerates 15 fields, none of which are the post-Tag-211l schema additions (annualSGA, annualDepreciation, annualShares, currentAssets, currentLiabilities, totalLiabilities, accountsReceivable, netPPE, ebitda, enterpriseValue, beta, forwardPE). A snapshot missing every newer field but having all 15 legacy fields grades A+.
   - Impact: With DATAQUALITY_ENFORCE=1, snapshots missing ALL Tag 211l fields still get full evaluation. The schema-stale probe (F-DP-001) drives 98% of universe through full-pull because of these fields — but the grader thinks everything is fine. data-quality-report.md grade distribution is unrelated to method coverage. Compounds with F-DQ-003 (grade D unreachable) to hollow the entire dq-enforcement design.
   - Fix: Extend CRITICAL_FIELDS to add weighted entries for the Tag 211l/219/220c additions (see suggested_fix in F-DQ-001). Recalibrate GRADE_THRESHOLDS so anchors stay A/A+.
   - Compound: CG-005 (with F-DQ-002 + F-DQ-005 + F-DP-001 stale-snapshot)

5. [HIGH] .github/workflows/daily-pull.yml:493-519 + :538-562 — Corrupt alert-state.json blocks the entire commit step + `--strategy-option theirs` can empty pushed commits
   - Root cause: (a) 'Strip methodHistory from alert-state' step has no `continue-on-error: true`, so a JSON.parse failure aborts the entire DAG before the 'Commit Snapshots + Alert-State' step runs — discarding ~4h of fresh pulls. (b) The retry rebase uses `git rebase --strategy-option theirs origin/main` which in rebase semantics PREFERS origin (the opposite of merge's 'theirs'), silently emptying the local commit when SHA verification only checks the commit object survived (not its diff).
   - Impact: Sticky failure: every subsequent day fails the same way because the corrupt alert-state is still in main. Concurrent dispatch + cron run on the 1st of the month can empty the loser's commit silently — workflow reports green.
   - Fix: (a) Add `continue-on-error: true` to the strip step OR on parse failure write a fresh `{}` and exit 0 (preserving the .corrupt-backup audit trail). (b) Drop `--strategy-option theirs` from the rebase OR add a post-rebase diff-empty check before push.
   - Compound: CG-006 (CI commit-blocking family with F-CI-002, F-CI-005, F-SM-004)

## Full Findings

### CRITICAL

#### F-DP-001 — Tag 226a-2 schema-stale probe loops forever when FTS cache is post-v2-but-pre-Tag-211l
- **File**: pull-yahoo.js
- **Lines**: 1240-1268, 1443-1486, 1589-1593
- **Evidence tier**: T1
- **Category**: silent-corruption
- **Description**: FTS_CACHE_VERSION=2 was introduced BEFORE Tag 211l added annualSGA/annualDepreciation/annualShares. The Tag 226a-2 probe detects snapshots that lack annualSGA and forces a full pull, but the full pull then re-uses the same stale FTS cache because `cached._cacheVersion !== FTS_CACHE_VERSION` is FALSE for v2 caches written between v2 introduction and Tag 211l. ftsAnnualSGA defaults to [], the merge guard at line 1589 only sets canonical.annual.annualSGA when length > 0, so the rewritten snapshot stays missing annualSGA and the next pull's schema-stale probe fires again, forever. Tag 225d probe showed 0/100 sampled snapshots had the new fields at one point; ~98% of universe is forced into expensive full-pull every day.
- **Impact**: Burns rate-limit budget (Run #107 already produced 7,210 rate-limit failures), keeps beneish-m-score/ohlson-o-score/buyback-yield/sga-revenue-trend/working-capital-trend computable=false for affected tickers indefinitely. Probe was supposed to solve this; the comment at line 1236 incorrectly claims `_cacheVersion is undefined for pre-Tag-211l caches`.
- **Fix**: Bump FTS_CACHE_VERSION to 3 alongside this fix. Alternative if invalidation cost too high: set useCache=false when staleSchema=true so the schema-stale path always re-fetches FTS.
- **Compound**: CG-001

#### F-DP-002 — _priceOnlyUpdate scales Yahoo quote() by financialCurrency FX rate but Yahoo returns trading-currency
- **File**: pull-yahoo.js
- **Lines**: 1336-1356
- **Evidence tier**: T1
- **Category**: silent-corruption
- **Description**: After Tag 204/219c, meta.reportingCurrencyOriginal is the financial currency (TWD for TSM) and meta.fxRateApplied is the financial→USD factor (~0.031). Price-only multiplies live yf.quote() output by this factor, but Yahoo returns regularMarketPrice and marketCap in TRADING currency (USD for ADRs like TSM/BABA/NU). For TSM at $200 USD live: new snapshot price = 200 × 0.031 = $6.20 (~32× too small). Same defect for marketCap.
- **Impact**: Every ADR with `tradingCurrency !== reportingCurrencyOriginal` gets price and mcap silently corrupted on every price-only refresh (~80% of pulls). MarketCap-floor delete at line 1361-1366 then irreversibly removes the snapshot. Universe silently loses TSM, BABA, NU, 9988.HK, every other major non-US-domiciled ADR.
- **Fix**: Look up `FX_TO_USD[existing.meta.tradingCurrency.toUpperCase()]` and use THAT rate for price/mcap scale in the price-only path. tradingCurrency=USD → rate=1 → no-op for US-listed ADRs. Persist `meta.tradingFxRateApplied` for stable lookup.
- **Compound**: CG-002

#### F-SM-001 — detect-changes.js bypasses lib/atomic-write for BOTH alert-state.json AND method-history-state.json (Tag 230c-1 hardening never applied)
- **File**: detect-changes.js
- **Lines**: 57-64, 105-139
- **Evidence tier**: T1
- **Category**: tag230c-regression
- **Description**: alert-state.json (21.6 MB committed dedup baseline) and method-history-state.json (8.9 MB sidecar) are both written via hand-rolled tmp+rename. No tmp-file fsync, no Windows EPERM retry, no parent-dir fsync, no partial-write loop. The comment on line 135 ('Atomic write via tmp+rename (was already done; preserved from existing code)') actively misleads any future auditor. lib/atomic-write.js exists since Tag 189 but neither write site uses it.
- **Impact**: On Windows + OneDrive (Karl's documented setup, the entire motivation for Tag 230c-1): an OneDrive scan handle during the rename fails EPERM, the run's state-delta is silently discarded, METHOD_PASS_LOST/GAINED re-fires for every flip on the next run (potentially hundreds of duplicate Discord alerts), orphan-cleanup never persists, file grows toward 50 MB. On POSIX power-loss: zero-byte alert-state.json gets committed and pushed.
- **Fix**: Replace lines 57-64 with `const { writeFileAtomic } = require('./lib/atomic-write.js');` and `writeFileAtomic(HISTORY_SIDECAR, JSON.stringify(...))`. Replace lines 136-138 with `writeFileAtomic(statePath, JSON.stringify(committed));`. Delete the misleading line-135 comment.
- **Compound**: CG-003

#### F-DQ-001 — CRITICAL_FIELDS still grades against the Tag 134-era field set
- **File**: methods/data-quality.js
- **Lines**: 20-38
- **Evidence tier**: T1
- **Category**: schema-drift
- **Description**: CRITICAL_FIELDS enumerates 15 fields. None of the Tag 211l/219/220c additions are graded: annualSGA, annualDepreciation, annualShares, annualBalance[i].{currentAssets, currentLiabilities, totalLiabilities, accountsReceivable, netPPE}, metrics.{ebitda, enterpriseValue, beta, forwardPE}, timeseries.netIncomeQ. A snapshot missing every newer field but having all 15 legacy fields grades A+. (Also reported by data-pipeline agent via F-DP-001 cross-reference.)
- **Impact**: With DATAQUALITY_ENFORCE=1, snapshots lacking every Tag 211l field still get full evaluation. The recently-added schema-stale probe (F-DP-001) drives ~98% of universe into full-pull because of these fields, yet the grade never reflects their absence. Anchor stocks (NVDA/MSFT/PLTR/META) all PASS today partly because they have the legacy fields populated, masking that 30%+ of the universe still lacks Tag 211l fields. Combined with F-DQ-003 (D-grade unreachable): the entire dq-enforcement path is a no-op for new fields.
- **Fix**: Extend CRITICAL_FIELDS to weight Tag 211l/219/220c additions, e.g. `{ id: 'annual.annualSGA>=2', weight: 0.5, check: s => _arrLen(s.annual && s.annual.annualSGA) >= 2 }`. Recalibrate GRADE_THRESHOLDS afterward.
- **Compound**: CG-005

#### F-SM-004 — Workflow's 'Strip methodHistory from alert-state' step uses raw fs.writeFileSync inside inline node -e
- **File**: .github/workflows/daily-pull.yml
- **Lines**: 493-519
- **Evidence tier**: T1
- **Category**: tag230c-regression
- **Description**: The 'Strip methodHistory from alert-state' step runs an inline node -e script (lines 513-515) that does hand-rolled tmp+rename: `const tmp = 'alert-state.json.tmp.' + process.pid; fs.writeFileSync(tmp, JSON.stringify(s)); fs.renameSync(tmp, 'alert-state.json');`. This step runs AFTER detect-changes.js and is the LAST mutator of alert-state.json before it is committed and pushed. Inline node -e scripts can't cleanly require('./lib/atomic-write'). Missing every Tag 230c-1 hardening (no fsync, no Windows EPERM retry, no parent-dir fsync).
- **Impact**: alert-state.json on disk after step completion may be empty (zero bytes) on parent-dir fsync gap manifesting. Next 'Commit' step does `git add -A` and commits a zero-byte alert-state.json to main. Next scheduled run hits JSON.parse fail; new RESET_ALERT_STATE=1 guard exits non-zero (good) but the empty file is already in git main, requires manual revert.
- **Fix**: Pull the inline node -e into scripts/strip-method-history.js, require lib/atomic-write.js properly, call from workflow via `node scripts/strip-method-history.js`. Bonus: add integration test asserting the stripper preserves all keys except methodHistory.
- **Compound**: CG-003 + CG-006

### HIGH

#### F-DP-003 — mapFTSToBalance row-skip rule misaligns annualBalance with annualRev
- **File**: pull-yahoo.js, lines 991-1030, 1564-1566
- **Evidence tier**: T1, Category: silent-corruption
- **Description**: mapFTSToBalance only pushes a row when cash/totalDebt/totalAssets is present (line 1016) but mapFTSToAnnual has its own (different) skip rule. Independent FTS fetches → year-coverage mismatch → annualBalance[i] silently references a different fiscal year than annualRev[i]. F-DP-030/031 caught the rev-side; the balance side uses its own skip-rule and was missed.
- **Impact**: ROIC, net-debt-EBITDA, asset-turnover, working-capital-trend, ohlson-o-score, beneish-m-score all consume annualBalance[i] positionally. A 1-year mis-alignment shifts every ratio by one fiscal year. Silent — no warning emitted.
- **Fix**: Apply F-DP-030/031 fix pattern to mapFTSToBalance: push null/empty placeholder row when all balance fields null, OR build year-indexed dict first, OR tag every row with fiscal year and align in consumers.
- **Compound**: CG-004 (year-alignment family)

#### F-DP-004 — _convertSnapshotToUSD does not scale targetMeanPrice / targetMedianPrice
- **File**: pull-yahoo.js, lines 316-328, 789-790
- **Evidence tier**: T2, Category: schema-drift
- **Description**: Tag 220c surfaced targetMeanPrice/targetMedianPrice into metrics from yahoo.financialData. These are per-share prices in TRADING currency. CCY_DENOMINATED_METRICS omits them. After _convertSnapshotToUSD price/mcap are USD-normalized but targets remain trading-currency. Comment at line 781-783 marking them as 'ratios + counts + analyst price targets, so no FX implication' is incorrect.
- **Impact**: Currently low (no shipped method directly combines targetMeanPrice with marketCap). Becomes HIGH for any future analyst-upside method.
- **Fix**: Add 'targetMeanPrice' and 'targetMedianPrice' to a TRADING-currency-scaled variant of CCY_DENOMINATED_METRICS; requires the meta.tradingFxRateApplied addition discussed in F-DP-002.
- **Compound**: CG-002

#### F-DP-005 — marketCap from quoteSummary.summaryDetail may already be USD for ADRs — _convertSnapshotToUSD double-converts
- **File**: pull-yahoo.js, lines 247-347, 756
- **Evidence tier**: T2, Category: silent-corruption
- **Description**: Yahoo's summaryDetail.marketCap for ADRs is in trading currency (USD for TSM/BABA/NU), but mapper sets reportingCurrency=TWD/CNY/HKD and _convertSnapshotToUSD multiplies marketCap by the financial-currency factor. TSM real $1.1T × 0.031 = $34B post-conversion. Mcap floor at $1B (line 1647) deletes any ADR whose real USD mcap < ~$32B (1B / 0.031 for TWD).
- **Impact**: ADRs in the $1B–$30B real-USD range have their snapshot irrecoverably deleted on every pull. Compounds with F-DP-002 (price-only path scales the same wrong way).
- **Fix**: Add a guard in _convertSnapshotToUSD that detects `tradingCurrency !== reportingCurrencyOriginal` and uses the TRADING fx rate for marketCap. Add regression test BABA/TSM/NU snapshot marketCap in $50B-$1T range after pull.
- **Compound**: CG-002

#### F-DQ-009 — Inside a single ADR snapshot, price.regularMarketPrice is FX-scaled while targetMeanPrice/targetMedianPrice stay in trading currency
- **File**: pull-yahoo.js, lines 316-328, 789-790, 1336-1356
- **Evidence tier**: T1, Category: currency-assumption
- **Description**: Within a single ADR snapshot for TSM (financial=TWD, trading=USD): after price-only refresh existing.price.regularMarketPrice = USD-quote × 0.031 ~ 32× too small; existing.metrics.targetMeanPrice.value = unchanged USD analyst target. Any cross-field method divides USD price-target by 32×-too-small current price → 32× upside computed from a ratio of two numbers in inconsistent units.
- **Impact**: F-DP-002 already flagged corruption per se; this is the second-order downstream — same snapshot has internally inconsistent units. ADRs: TSM, BABA, BIDU, JD, NU, 9988.HK, ASML, NVO.
- **Fix**: Store `meta.tradingFxRateApplied` and split CCY_DENOMINATED_METRICS into financial-ccy and trading-ccy buckets. Subsumed by the F-DP-002 unification.
- **Compound**: CG-002 (also reported by data-pipeline agent via F-DP-002)

#### F-SM-002 — snapshot-methods-history.js still uses hand-rolled tmp+rename for the vintage file
- **File**: snapshot-methods-history.js, lines 147-150
- **Evidence tier**: T1, Category: tag230c-regression
- **Description**: The 14.4 MB methods-history/YYYY-MM-DD.json (canonical source of truth for every backtest) is written via `fs.writeFileSync(tmpFile); fs.renameSync(tmpFile, outFile);`. Same three Tag 230c-1 gaps as F-SM-001. The file requires writeFileAtomic at line 14 and uses it for the small pipeline-health write at line 165 but missed the much bigger vintage write 15 lines earlier. tmpFile suffix is just `.tmp` (no pid), so concurrent invocations stomp each other.
- **Impact**: Silent vintage loss on partial-write events. method-effectiveness silently skips zero-byte vintage; pipeline-health-check counts it as success because pipeline-health JSON survived.
- **Fix**: Replace lines 147-150 with `writeFileAtomic(outFile, JSON.stringify(data));`. Require already present at line 14.
- **Compound**: CG-003

#### F-SM-003 — methods/sector-medians-compute.js has its OWN private _atomicWrite that bypasses lib/atomic-write
- **File**: methods/sector-medians-compute.js, lines 207-214, 221, 230, 303
- **Evidence tier**: T1, Category: tag230c-regression
- **Description**: Private `_atomicWrite` helper writes three files driving the effectiveThreshold lookup priority chain: sector-medians-auto.json, sector-medians-auto-legacy.json, sector-medians-rolling.json. None get the Tag 230c-1 hardening. Rolling-medians wipe is especially expensive — re-maturity requires 12 weeks of weekly samples (ROLLING_MIN_WEEKS=12).
- **Impact**: A silent threshold-source regression flips PASS/FAIL flags across the entire universe, propagating to a wave of false METHOD_PASS_LOST events the next run.
- **Fix**: Replace _atomicWrite with `require('../lib/atomic-write.js').writeFileAtomic`. Drop-in.
- **Compound**: CG-003

#### F-SM-005 — saveState writes sidecar BEFORE alert-state — SIGKILL between them yields desynchronized methodHistory vs methodState
- **File**: detect-changes.js, lines 133-138
- **Evidence tier**: T1, Category: split-state-inconsistency
- **Description**: Two-file atomicity gap. Comment 'F-SM-008: write sidecar first' rationalizes the order but a SIGKILL/EPERM between leaves sidecar with TODAY's methodHistory but alert-state with YESTERDAY's methodState + lastRun. On next run: every method×ticker that already flipped today re-fires as a fresh METHOD_PASS_LOST event.
- **Impact**: Duplicate Discord alerts (potentially hundreds on a Yahoo schema-change run). Trend signals include a phantom datapoint. lastRun regresses backward.
- **Fix**: Option A (simplest): swap order — write alert-state FIRST, then sidecar. If alert-state succeeds and sidecar fails, next run's loadState gets empty methodHistory (re-builds harmlessly). Option B: write both as a transactional pair with verify-after.
- **Compound**: CG-003

#### F-SM-006 — methodHistory in sidecar grows unbounded across delisted tickers AND orphan method-ids
- **File**: detect-changes.js, lines 302-321, 332-338, 379-388
- **Evidence tier**: T1, Category: state-growth
- **Description**: Three asymmetric pruning gaps vs methodState: orphan-method-cleanup mutates only methodState; 30-day pruning only operates on methodState; shallow-clone at line 335 preserves stale entries by reference. method-history-state.json is 8.9 MB today with ~6 MB dead weight. Extrapolated at current rate it crosses GitHub's 50 MB hard limit in 24-36 months.
- **Impact**: Sidecar bloat → GitHub 50 MB push rejection. JSON.parse latency grows linearly. Trend output is unaffected (8-entry arrays handled correctly), bug is purely growth-side.
- **Fix**: Mirror orphan-method cleanup and 30-day stale-ticker prune onto methodHistory in saveState.
- **Compound**: none

#### F-SM-007 — Workflow concurrency:main-push group serializes pushes but cron + workflow_dispatch overlap can silently lose detect-changes work
- **File**: .github/workflows/daily-pull.yml, lines 15-22, 540-562
- **Evidence tier**: T1, Category: concurrency
- **Description**: workflow_dispatch gets per-run group ID (manual-{run_id}), bypassing main-push serialization. Two concurrent runs proceed in parallel on two runners; `git rebase --strategy-option theirs` (Tag 179 F-CI-001 v2) silently overwrites the first push's alert-state with the second push's. Hundreds of duplicate Discord events possible on the lost-run's flips.
- **Impact**: Silent state-write loss on cron + dispatch overlap. picks-history skips a day; fieldCoverage.history gaps; score-history's daily entry lost for half the tickers.
- **Fix**: Make BOTH cron and dispatch share the same concurrency group, OR refuse dispatch when cron is in flight, OR build real state-merger.
- **Compound**: CG-006

#### F-SM-008 — archive-old-snapshots.js uses raw fs.writeFileSync + fs.appendFileSync; first-line-only verify before unlink loses tail-corruption to permanent data loss
- **File**: scripts/archive-old-snapshots.js, lines 100-119
- **Evidence tier**: T1, Category: state-atomicity
- **Description**: appendFileSync + writeFileSync without lib/atomic-write. First-line-only verify at lines 105-114 passes on append-corrupted tail. Then lines 116-119 unlinkSync the original methods-history files — permanent data loss. With `--methods-keep-days 7`, vintages beyond 7 days live ONLY in the archive.
- **Impact**: Permanent vintage loss per partial-write event. method-effectiveness then sees shrinking vintage counts and the evidenceGate trips silently to 'unevaluable'.
- **Fix**: Switch to build-then-rename-then-fsync via writeFileAtomic. Verify EVERY line, not just the first. Keep originals around for one full run cycle before unlinking (move to .pending-delete on first pass, unlink on second pass).
- **Compound**: none

#### F-CI-001 — Corrupt alert-state.json blocks the snapshot commit for the entire day
- **File**: .github/workflows/daily-pull.yml, lines 493-519
- **Evidence tier**: T1, Category: data-loss/silent-failure
- **Description**: 'Strip methodHistory from alert-state' step has no `continue-on-error: true`. Corrupt JSON causes exit 1; subsequent default steps (incl. 'Commit Snapshots + Alert-State') skip. ~4h of fresh snapshots/picks-history/methods-history/walk-forward lost. Failure is sticky because the corrupt file is still on main.
- **Impact**: Up to 4h of compute and ~15k snapshot files lost; vintage continuity gap breaks the 84-day walk-forward window.
- **Fix**: (a) Add `continue-on-error: true` to the strip step, OR (b) on parse failure write a fresh `{}` with .corrupt-backup and exit 0.
- **Compound**: CG-006

#### F-CI-002 — `git rebase --strategy-option theirs` against origin/main can silently drop snapshots' file content
- **File**: .github/workflows/daily-pull.yml, lines 538-562
- **Evidence tier**: T2, Category: data-loss/git-strategy
- **Description**: In rebase, 'theirs' = the branch being rebased ONTO (origin/main) — opposite of merge. Path-level conflict between local commit and remote → LOCAL discarded. The post-rebase SHA-existence check passes but the rebased commit can have an empty diff. Push succeeds with empty commit.
- **Impact**: Daily snapshots lost on rebase conflict — the EXACT failure F-CI-001 v2 meant to prevent. Probability low but non-zero on 1st of month (daily+monthly overlap) and on workflow_dispatch.
- **Fix**: Drop `--strategy-option theirs` (let rebase fail noisily; cherry-pick recovery path is battle-tested), OR add post-rebase `git diff HEAD~1 HEAD --quiet` and reset on empty.
- **Compound**: CG-006

### MEDIUM

#### F-DP-006 — sortByStaleness regex matches fetchedAt before asOf
- pull-yahoo.js:1081, 1328-1329, T1. Sort-order inefficiency only — freshness decisions remain correct since age-check uses asOf. Original Tag 218 intent partially defeated.
- Fix: Update meta.fetchedAt alongside meta.asOf in _priceOnlyUpdate (one line).

#### F-DP-007 — FTS cache write race during retry; safeSnapshotFilename collision risk
- pull-yahoo.js:1487-1538, T2. Two tickers sanitizing to the same filename share FTS cache → silent cross-pollination of annual.*.
- Fix: Disambiguate cache filename via crc32 hash or use yahoo_symbol directly.

#### F-DP-008 — errClass 'mapper-bug' classification triggers no special handling
- pull-yahoo.js:1733-1741, T1. Mapper bugs at 5-30% failure rate emit buried MAPPER-BUG lines but don't fire the 75% fail-ratio exit-1 gate.
- Fix: Add dedicated exit-2 gate for any mapper-bug count > 0; emit `::error::` GitHub-Actions annotation.

#### F-DP-009 — refresh-universe.js fetchScreener silently returns [] on any exception
- refresh-universe.js:125-133, T1. 25 regions × 13 screeners = 325 chances for silent failure. New IPOs may never enter watchlist.
- Fix: Apply F-DP-037/Tag 190 pattern: return {quotes, error}, accumulate per-bucket stats, alert on >10% failure.

#### F-DP-010 — FX fallback rate of 1.0 missing for USD-key absence path
- pull-yahoo.js:203-208, 266-274, T2. Corrupt fx-rates.json silently falls back to 2024-stale hardcoded rates; only per-snapshot WARN, no run-summary CI gate.
- Fix: Add hard CI gate counting `meta.fxRateSource === 'hardcoded-fallback'` snapshots; exit 1 if > 5% of non-USD universe.
- Compound: CG-002

#### F-ME-001 — sga-revenue-trend / working-capital-trend treat non-consecutive valid years as consecutive when computing slope
- methods/sga-revenue-trend.js:98-132; methods/working-capital-trend.js:97-121, T1. _slope uses x=[0..n-1] sequential indices, not actual year offsets. One gap year doubles apparent severity.
- Fix: Track per-ratio year-offset OR hard-require 3 consecutive valid years.
- Compound: CG-004

#### F-ME-002 — fcf-conversion-stability description says '>= 0.85' but THRESHOLD is 0.75
- methods/fcf-conversion-stability.js:189, 83, T1. Description drift; tooltip-vs-gate mismatch.
- Fix: Update description to '>= 0.75' or interpolate THRESHOLD.

#### F-ME-003 — score-aggregator normalizeMethodScore does not handle 'lt'/'gt' threshold ops
- methods/score-aggregator.js:111-137, T1. Latent — currently no SCORE_WEIGHTS entry uses lt/gt. Today degrades NEAR_THRESHOLD signal for ohlson/op-leverage/rev-quality-cov.
- Fix: Extend normalizeMethodScore to handle 'lt'/'gt'; mirror in _helpers.js.

#### F-ME-005 — revenue-shock-guard quarterly path: filter() collapses null gaps
- methods/revenue-shock-guard.js:60-77, T1. filter() reindexes; prior window mixes non-peer quarters. MAD inflated, shock missed.
- Fix: Walk qrev[1..k] forward for N CONSECUTIVE finite values; don't filter the slice.
- Compound: CG-004

#### F-ME-006 — earnings-surprise-momentum can pass on only 3 valid surprises but description says '>= 3 of last 4'
- methods/earnings-surprise-momentum.js:61-110, T1. POSITIVE_COUNT_FLOOR is absolute count; sample denominator varies 3→4.
- Fix: Compare breadthPass against FIXED denominator WINDOW (4), not surprises.length.

#### F-ME-010 — hypergrowth-quality-class annual fallback scaling assumes all 3 annual pairs evaluated
- methods/hypergrowth-quality-class.js:84-99, T1. Hard-coded scaling (strongQ===3 → 4) over-/under-counts when middle year null. In SCORE_WEIGHTS.HYPERGROWTH at weight 0.15.
- Fix: Scale by ACTUAL valid pair count (4 / pairsEvaluated) OR require all 3 pairs valid.
- Compound: CG-004

#### F-ME-012 — magic-formula EV synthesis treats null totalDebt as 0
- methods/magic-formula.js:162-170, T1. Yahoo balance-sheet gaps default debt to zero, understating EV → overstating earningsYield → false-positive passes.
- Fix: When totalDebt null AND enterpriseValue unavailable, emit computable=false with 'cannot determine debt'.

#### F-SM-009 — scripts/method-effectiveness.js uses raw fs.writeFileSync for cache + outputs
- scripts/method-effectiveness.js:298, 379, 414, T1. Three raw writes (cache 50-100k entries, .json/.md outputs). Cache corruption forces 50-min full replay; .md deploys truncated to gh-pages.
- Fix: Import writeFileAtomic, replace all three calls.
- Compound: CG-003

#### F-SM-010 — walk-forward-perf.js writes both walk-forward.json AND walk-forward.md via raw fs.writeFileSync
- scripts/walk-forward-perf.js:559, 590, T1. Same pattern as F-SM-009.
- Fix: Same — import writeFileAtomic and replace.
- Compound: CG-003

#### F-SM-011 — score-history appendAndPrune resets schemaVersion on every call
- scripts/snapshot-score-history.js:127-133, T1. Object.assign overwrites history.schemaVersion with SCHEMA_VERSION=1 — any future migration would be silently downgraded.
- Fix: Preserve schemaVersion from history; only set when missing.

#### F-SM-012 — snapshot-score-history per-ticker writeFileAtomic correct, but _meta.json is single late write
- scripts/snapshot-score-history.js:164-193, T1. SIGKILL between per-ticker writes and _meta write leaves stale lastRun while per-ticker files have today's entry.
- Fix: Write _meta.json with 'pending' marker first; flip to 'complete' atomically at end.

#### F-SM-013 — score-history/ documented as committed but absent from working tree
- .gitignore:57-67 + scripts/snapshot-score-history.js:152, T1. Fresh clone has no score-history dir; dashboard ΔScore/sparkline shows no data until first cron run completes and commits.
- Fix: Commit score-history/.gitkeep; OR make snapshot-score-history.js no-op outside CI.

#### F-SM-014 — _loadMethodHistory silently returns {} on parse-fail, swallowing the same corruption class loadState defends against
- detect-changes.js:46-55, 66-103, T1. Asymmetric error contracts. Sidecar corruption → 8.9 MB trend history silently wiped.
- Fix: Mirror loadState pattern: backup .corrupt.<ts>, log ERROR, exit non-zero unless RESET_METHOD_HISTORY=1.

#### F-CI-004 — FINNHUB_API_KEY missing only warns transitively
- .github/workflows/daily-pull.yml:108-114, T2. Key rotation → refresh-universe runs degraded with continue-on-error:true → universe silently stagnates.
- Fix: Add pre-flight existence check; add sanity step asserting watchlist GREW at least weekly.

#### F-CI-005 — workflow_dispatch concurrency bypass + monthly-sec-xbrl main-push group creates 1st-of-month race
- daily-pull.yml:21-23, monthly-sec-xbrl.yml:21-23, T2. Three concurrent pushers possible. With `--strategy-option theirs` makes commit-order race a data-loss hazard.
- Fix: Mirror concurrency groups OR block monthly from running concurrently with manual daily-pull.
- Compound: CG-006

#### F-CI-006 — monthly-sec-xbrl.yml lacks fetch-depth:0, npm cache, step timeouts, continue-on-error guards
- .github/workflows/monthly-sec-xbrl.yml:30-61, T1. Shallow clone breaks the Tag 218 F-218c-02 cherry-pick-back logic; soft Verify SEC Coverage commits useless manifests.
- Fix: Mirror daily-pull hardening; hard-fail Verify SEC Coverage when n_ok < 100 && n_total > 1000.

#### F-DQ-002 — TRACKED_FIELDS in field-coverage.js misses Tag 211l/Tag 219 fields
- field-coverage.js:19-42, T1. Drift detector cannot observe Yahoo dropping newer schema. Same failure mode as Nov 2024 incomeStatementHistoryQuarterly regression Tag 22 was designed to catch.
- Fix: Add annual.annualSGA, annual.annualDepreciation, annual.annualShares, metrics.ebitda.value, metrics.enterpriseValue.value, metrics.beta.value. Add anchor-fixture regression test.
- Compound: CG-005

#### F-DQ-003 — Grade D is mathematically unreachable
- methods/data-quality.js:60-66, 114-118, 136-145, T1. nanRatio = missingWeight/TOTAL_WEIGHT ≤ 1.0; C threshold is `<= 1.0`. tierCapForGrade('D') === 'REJECT' is dead code.
- Fix: Change GRADE_THRESHOLDS.C to <1.0 (e.g. 0.85); let D capture the everything-missing case.
- Compound: CG-005

#### F-DQ-004 — DATAQUALITY_ENFORCE silently fails open on price-only path (~80% of pulls)
- pull-yahoo.js:1370-1379 + methods/score-aggregator.js:365, T1. Tag 166 nulls _quality.grade after price-only; score-aggregator skips tier-cap when grade is null. 80% of universe unprotected.
- Fix: Keep _quality.grade populated through price-only (just bump staleSincePriceOnly), OR preserve lastFullPullGrade.
- Compound: CG-005

#### F-DQ-005 — data-quality-report.js and field-coverage.js track metrics.insiderOwnerPercent — a field that doesn't exist
- scripts/data-quality-report.js:46, field-coverage.js:18,41, T1. Mapper writes metrics.insidersOwnership. Permanent false-positive HIGH alert in drift detection.
- Fix: Rename to metrics.insidersOwnership.value in both files; add anchor-fixture regression test.
- Compound: CG-005

#### F-DQ-006 — data-quality-report.js array-presence uses naive `x != null` filter — counts {value: null} envelopes as present
- scripts/data-quality-report.js:39-44, T1. Tag 206f {value:null} positional placeholders pass the filter. Two reports disagree on the same snapshot.
- Fix: Export and reuse _arrLen from methods/data-quality.js.

#### F-DQ-007 — sector-medians-compute reads only annualBalance[0]/annualNetIncome[0]; Tag 206f null placeholders silently drop stocks
- methods/sector-medians-compute.js:67-71, T1. Latest-year-null pattern silently excludes stocks from sector buckets, biasing medians toward names with cleaner latest-year fundamentals.
- Fix: Replace [0] reads with _firstFinite helper.

#### F-DQ-008 — data-quality-report.js gradeSnapshot loader fallback returns grade='unknown' but the table has no 'unknown' row
- scripts/data-quality-report.js:21-28, 100-110, 150-156, T1. If grader fails to require, report shows all-zero across grade tiers — indistinguishable from 'every snapshot grade unknown'.
- Fix: Add 'unknown' row to table when count > 0; write inline WARNING to markdown.

#### F-BT-001 — nearestTradingDay backward-first regression: T+1 entry shift silently undone for weekend snapshots
- scripts/walk-forward-perf.js:114-158, T1. Tag 231a-1 backward-first scan walks back across the weekend to the same trading day → look-ahead p0. All Friday-pre-market vintages affected.
- Fix: Split into nearestTradingDayForEntry (forward-only) and nearestTradingDayForExit (backward-only). Update all 4 call sites.
- Compound: CG-007 (tag-231a-incomplete)

#### F-BT-002 — method-effectiveness skips canonical-date anchoring → per-ticker window drift survives in pass/fail cohorts
- scripts/method-effectiveness.js:237-275, T1. Tag 231a-2 only fixed walk-forward-perf, not method-effectiveness. Per-ticker drift up to ±5 calendar days injects ~1.5pp window noise into every alpha estimate.
- Fix: Mirror walk-forward's canonical-date pattern via WF._priceAtCanonical. Invalidate method-effectiveness-cache.json after the change.
- Compound: CG-007

#### F-BT-003 — method-effectiveness drops delisted tickers via priceIndex absence; no snapshot-of-record fallback
- scripts/method-effectiveness.js:237-260, T1. Classic survivor bias — delisted tickers (typically negative forward returns) excluded from both cohorts. Asymmetric attrition inflates/deflates alpha.
- Fix: Worst-case substitute (e.g. -50% or -100% symmetric), OR augment prices/history.json with delisted-snapshot last-known-price archive.

#### F-BT-004 — 84-day walk-forward window silently truncates on holidays; no horizon-actual-days emitted
- scripts/walk-forward-perf.js:27, 305-455, T2. Backward-snap shaves up to 5 calendar days off the 84d horizon on US holidays. No diagnostic.
- Fix: Emit horizon-actual-days per vintage; skip vintages where deviation > 10%.
- Compound: CG-007

#### F-BT-005 — method-effectiveness always assumes pre-market timestamp because generatedAt is never written
- scripts/method-effectiveness.js:209-211 + snapshot-methods-history.js:67, T1. method-effectiveness uses midnight UTC fallback, walk-forward uses real picksFile.asOf. Systematic 1-trading-day window mismatch between the two reports for after-close runs.
- Fix: Write generatedAt = new Date().toISOString() in snapshot-methods-history.js.
- Compound: CG-007

#### F-BT-006 — methodology-report labels QQQ/IWM fallback alpha as 'alpha vs SPY'
- scripts/methodology-report.js:57-62, 74-78, T1. Tag 139 regime breakdown also mislabeled. Reader draws wrong conclusions about absolute alpha attribution.
- Fix: Change header to 'alpha vs Benchmark'; surface benchmarkTicker per vintage.

#### F-BT-007 — method-effectiveness cache retention (90d) >> methods-history retention (7d)
- scripts/method-effectiveness.js:120-144, 317, T2. Cache becomes sole record after 8 days. Cache corruption silently loses months of effectiveness history that cannot be reconstructed.
- Fix: Raise methods-history retention to 90d (preferred), OR reduce cache retention to 7d.

#### F-BT-008 — macro-regime SMA window keyed by calendar date but indexed by trading days
- scripts/macro-regime.js:44-65, T2. Gap-handling produces stale regimes on vintages within 30 days of a gap. Tag 139 regimeAlpha may show spurious cross-regime asymmetry from data-gap artifacts.
- Fix: Gate SMA emission on calendar-day window < 320; pre-fill gaps in history.json.

#### F-PF-001 — Runner.getMethods() rebuilt per-stock (× 2) inside evaluateAllStocks() loop
- generate-methods-report.js:66-125 (L88, L98), T1. Previously flagged in 2026-05-15 audit; fix never applied. ~7.9M wasted ops + transient object arrays at 15k tickers.
- Fix: Hoist `const ALL_METHODS = Runner.getMethods();` above the for-loop.

#### F-PF-002 — Snapshots written with pretty-print JSON (null, 2) on every full + price-only pull
- pull-yahoo.js:1383, 1711, 1768, T1. ~30-40% larger files, 1.5-2× slower stringify. At 15k tickers: ~250MB extra storage, slows the 80% fast-path.
- Fix: Drop `, null, 2` from all three call sites. Same fix as Tag 222 historical-prices OOM.

#### F-PF-003 — loadStocks() in generate-modes-report / generate-screener does serial sync readFileSync over snapshots dir
- generate-modes-report.js:79-87; generate-screener.js:65-77, T1. Pure serial blocking I/O over ~15k files. ~130-160s per generator at 15k tickers; 3 generators × 3min = ~9 minutes daily.
- Fix: Mirror snapshot-methods-history.js loadFilesAsync() pattern: BATCH=200, Promise.all over fs.promises.readFile.

#### F-PF-004 — Runner.evaluateStock() does 4-5 redundant full scans over results map per stock
- methods/runner.js:47-63, T1. coreCount/coreCountPass/disqualified computed and discarded by every caller via evaluateStockLegacy wrapper. ~20M wasted ops at 15k × 5 generators.
- Fix: Lazy getters, OR split into evaluateStockSimple + evaluateStockExtended.

#### F-PF-005 — writeManifestIncremental scans the growing results array on every 100-ticker checkpoint
- pull-yahoo.js:1158-1202 (called from L1796), T1. O(N²/100). At 15.7k: ~1.24M filter ops + 157 transient arrays of growing size.
- Fix: Maintain running counters (n_ok, n_skipped_mcap) updated in-place inside processOne().

#### F-PF-006 — method-history-state.json (14MB) loaded twice — once in generate-methods-report, once in detect-changes
- generate-methods-report.js:46-64 + detect-changes.js:46-55, T2. ~150-300ms duplicate JSON.parse today, ~3 sec at 70MB projected. Contributes to OOM risk Tag 226b warned about.
- Fix: Share parsed object via temp file/memoized loader module/single-process pipeline.

#### F-PF-007 — generate-screener.js per-stock readScoreHistory does synchronous file read inside buildRow loop
- generate-screener.js:85-100 + 320, T1. Same sync-readFileSync-in-hot-loop pattern as F-PF-003. ~15-30s blocking I/O at 15k tickers.
- Fix: Pre-load all score-history files via async batch loader at main() start.

#### F-PF-008 — eligibleForMode/topAllMust recomputed for each mode in main() diagnostic loop after buildHtml already did the same work
- generate-modes-report.js:1219-1223 vs 524-525, T1. ~78,670 extra filter operations + 5 extra sorts just for console logging.
- Fix: Return stats side-channel from buildHtml().

#### F-PF-009 — Methods-report HTML stringifies & deep-walks all 15k rows × 83 methods for sector-distribution + per-method Top-N
- generate-methods-report.js:176-243, T2. O(M × N log N). At N=15k × M=83 × log2(15k) ≈ 18M comparisons + ~1.2M transient elements.
- Fix: Bounded TOP_N heap per method (O(N log TOP_N)), OR amortize filter via single-pass per-method arrays.

### LOW

#### F-DP-011 — pull-historical-prices.js uses simple batched Promise.all
- pull-historical-prices.js:137-144, T1. Single hung Yahoo socket blocks all 10 slots. No per-call timeout.
- Fix: Migrate to worker-pool pattern with _withTimeout wrapper.

#### F-DP-012 — discovery/finnhub.js redirect handler uses unbounded recursion and may follow non-absolute Location
- discovery/finnhub.js:34-49 (also: wikipedia-indices.js:27-28, nasdaq-all.js:35-36, otc-markets.js:40-41, nasdaq-api.js:52-53), T1. Sec-tickers was hardened in Tag 229c-2; the other five share the same defect.
- Fix: Lift hardened get() into shared lib/http-fetch.js and import from all five.

#### F-ME-004 — rule-of-x does not apply rule-of-40's Tag-201c 3y annual median fcfMargin fallback
- methods/rule-of-x.js vs methods/rule-of-40.js, T1. MELI-pattern compounders score inconsistently across the two HG-core methods (weights 0.25 and 0.10).
- Fix: Move _annualFcfMarginMedian to shared helper; apply in both.

#### F-ME-007 — betting-against-beta stores BORDERLINE at components.flag (singular) but wrapEvaluate reads result.flags (plural array)
- methods/betting-against-beta.js:142-152 + methods/_helpers.js:265, T1. Borderline marker is dead-information; operators can't distinguish 1.2 BORDERLINE from 2.5 FAIL.
- Fix: Emit `flags: borderline ? ['BORDERLINE'] : []` at top-level result.

#### F-ME-008 — reinvestment-rate substitutes 0 for missing annualRnD[j] while asset-light fallback gates on rnd.length>0
- methods/reinvestment-rate.js:120-129, 159, T1. Partial R&D coverage triggers strict 20% threshold with deflated denominator → false-fails R&D-heavy QC-MUST methods.
- Fix: Track per-year R&D availability; either skip year (year-alignment), trailing-median imputation, or per-year asset-light trigger.
- Compound: CG-004

#### F-ME-009 — normalizeMethodScore early threshold===0 guard returns 0.99 for any val>0 — wrong for lte/lt/lte_abs
- methods/score-aggregator.js:114, T1. Latent risk. Activates with F-ME-003 fix (when 'lt' support added).
- Fix: Move threshold===0 guard INSIDE per-op branches.
- Compound: F-ME-003

#### F-ME-011 — profitability-state stores stringly-typed value at components.confidence
- methods/profitability-state.js:150-159, T1. Key collision with _helpers.js numeric override contract. Polymorphic data under refactor.
- Fix: Rename to components.sourceConfirmation.

#### F-ME-013 — data-quality grading uses 'metrics.fcfMarginTTM' — verify pull-yahoo writer key matches
- methods/data-quality.js:31, T2. If writer/reader names disagree, every snapshot loses 1.0 weight silently.
- Fix: Define shared METRIC_KEYS constant module; add anchor-fixture regression test.

#### F-ME-014 — sector-medians-compute: OTHER sub-profile stocks → no operator-visible diagnostic when region falls back to _GLOBAL
- methods/sector-medians-compute.js:58-103, 117-150, T2. Behavior correct, debuggability poor.
- Fix: Add `_diagnostics: { otherStocksByRegion: { ... } }` to output.

#### F-SM-015 — pull-prices-bulk.js _safeMergeAndWrite silently resets history.json to {} on parse-fail
- pull-prices-bulk.js:16-26, T1. CLI-only script but mutates committed state. F-SM-015 (Tag 187) hardening never propagated.
- Fix: Mirror loadState pattern: backup, log ERROR, exit non-zero unless RESET_HISTORY=1.

#### F-CI-007 — No fallback path if GitHub silently skips a scheduled cron run entirely
- .github/workflows/daily-pull.yml:3-6, T2. Cron-firing-3-4h-late symptom suggests this is already active. Vintage continuity gaps break 84-day walk-forward window invisibly.
- Fix: Daily-heartbeat external monitor that Discord-alerts if no successful daily-pull run > 36h, OR schedule daily-pull twice with early-exit guard.

#### F-CI-008 — Only Yahoo Pull has a per-step timeout
- .github/workflows/daily-pull.yml:35, 178-183, 316-486, T2. PRICE_CONCURRENCY=20 and EARNINGS_CONCURRENCY=20 still hit the same Yahoo CDN Tag 215f lowered to 8. Hung HTTP can eat 75min job budget.
- Fix: Add per-step `timeout-minutes` to Pull Historical Prices (25), Pull Earnings-Calendar (15), Refresh Universe (10), Refresh FX-Rates (5). Lower PRICE_CONCURRENCY and EARNINGS_CONCURRENCY to 8.

#### F-BT-009 — picks-regression-check claims 'last 8 weeks' but slices last 8 daily snapshots
- scripts/picks-regression-check.js:102-106, T1. Stale comment from weekly-cadence era.
- Fix: Update comment to '8 daily snapshots' (35% threshold is empirically tuned around 8-day baseline).

#### F-BT-010 — pickStockForMode falls back to normScore when score is null
- snapshot-picks.js:82-105, T2. Cross-vintage scores mix [0,1] vs [0,100] scales. Walk-forward picks top-100 per vintage so sort-order is preserved, but absolute score values incomparable.
- Fix: Drop normScore fallback OR write scoreScale metadata per vintage.

#### F-PF-010 — loadFiles in generate-modes-report uses filter().map() — minor double iteration
- generate-modes-report.js:82-87, T1. Subsumed by F-PF-003 fix.
- Fix: When refactoring to async batches per F-PF-003, validate+push in single pass.

## Compound Bug Groups

### CG-001: Tag 211l field-rollout stack (loop + stale grade)
- **Findings**: F-DP-001 + F-DQ-001 + F-DQ-002
- **Interaction**: F-DP-001 forces ~98% of universe through full-pull every day because the schema-stale probe fires forever on v2-but-pre-Tag-211l caches, yet the snapshot field stays missing because the merge guard skips empty arrays. F-DQ-001 / F-DQ-002 then keep the grade at A/A+ and the drift-detector silent for exactly those fields, so neither the grader nor the drift alerts surface the systemic field gap.
- **Combined impact**: The probe burns rate-limit budget on every run (compounds Run #107's 7,210 failures), the affected methods (beneish-m/ohlson-o/buyback-yield/sga-revenue-trend/working-capital-trend) stay computable=false indefinitely, AND the data-quality report claims everything is fine — operators have no signal of degradation. Universe coverage of newer-method evidence rolls out at glacial pace because each daily run mostly re-spends budget rather than maturing new fields.

### CG-002: ADR-currency stack (3 layers)
- **Findings**: F-DP-002 + F-DP-005 + F-DQ-009 + F-DP-004 + F-DP-010
- **Interaction**: F-DP-005 corrupts marketCap on full-pull (Yahoo returns ADR mcap in trading currency, but _convertSnapshotToUSD scales by financial-currency factor → 32× too small). F-DP-002 then re-corrupts price + mcap on every subsequent price-only refresh by the same wrong factor. F-DQ-009 is the second-order downstream: within the corrupted snapshot, price.regularMarketPrice (32× too small) and targetMeanPrice (uncorrupted, in trading currency) now live in mutually inconsistent units. F-DP-004 confirms targetMeanPrice was never added to CCY_DENOMINATED_METRICS. F-DP-010 (FX-load failure) amplifies all of the above when fx-rates.json is being written.
- **Combined impact**: Every ADR snapshot (TSM/BABA/NU/9988.HK/BIDU/JD/ASML/NVO/etc.) silently corrupted on every refresh. MIN_MCAP=$1B floor at line 1647 unlinks small/mid-cap ADRs irreversibly. Any future analyst-upside / fair-value-vs-cap method emits nonsense (32×-too-large upside reads). This is the single most active silent-corruption flow in the pipeline today.

### CG-003: Tag 230c-1 hand-rolled atomic-write family
- **Findings**: F-SM-001 + F-SM-002 + F-SM-003 + F-SM-004 + F-SM-005 + F-SM-009 + F-SM-010
- **Interaction**: Tag 189 introduced lib/atomic-write.js; Tag 230c-1 hardened it (tmp fsync, Windows EPERM retry, parent-dir fsync, partial-write loop). Seven critical state-write sites never adopted it: detect-changes.js (alert-state + sidecar), snapshot-methods-history.js (vintage), methods/sector-medians-compute.js (three medians files), the workflow's inline node -e strip step, method-effectiveness.js (cache + JSON + MD), walk-forward-perf.js (JSON + MD). The comment on detect-changes.js:135 ('Atomic write via tmp+rename (was already done)') actively misled the Tag 230c-1 author into thinking the file was covered. F-SM-005 layers split-state inconsistency on top: even if both writes are atomic individually, the two-file pair is not transactional.
- **Combined impact**: On Karl's Windows + OneDrive machine, any of seven write sites can EPERM-fail silently, losing today's state. On POSIX CI runner, parent-dir fsync gap leaves zero-byte files that get committed and pushed. detect-changes corruption is the worst: a sticky failure that re-fires hundreds of duplicate Discord events, eventually pushes a zero-byte alert-state.json to main, requires manual revert.

### CG-004: Year-alignment family (slope/median/scaling over null gaps)
- **Findings**: F-ME-001 + F-ME-005 + F-ME-010 + F-ME-008 + F-DP-003
- **Interaction**: F-DP-030/031 fixed the writer-side year-alignment for annualRev/SBC/Capex/RnD, but consumer methods (sga-revenue-trend, working-capital-trend, revenue-shock-guard, hypergrowth-quality-class, reinvestment-rate) and one writer (mapFTSToBalance, F-DP-003) still implement their own ad-hoc handling: filter() over null gaps in revenue-shock-guard, sequential x-indices in _slope, hard-coded scaling factors in hypergrowth-quality-class, per-year 0-substitution in reinvestment-rate, independent skip-rule in mapFTSToBalance.
- **Combined impact**: One missing year silently shifts ratio computations by one fiscal year (F-DP-003), inflates MAD windows so shocks slip past (F-ME-005), doubles apparent slope severity (F-ME-001), flips REAL_HYPERGROWTH classification on 1-of-2 vs 2-of-2 evidence (F-ME-010), and false-fails R&D-heavy reinvestment ratios (F-ME-008). hypergrowth-quality-class is in SCORE_WEIGHTS at weight 0.15 so this has real composite-score impact.

### CG-005: Data-quality grader dead-code stack
- **Findings**: F-DQ-001 + F-DQ-002 + F-DQ-003 + F-DQ-004 + F-DQ-005 + F-DQ-008
- **Interaction**: F-DQ-001 (newer fields untracked) + F-DQ-002 (drift detector blind to same fields) + F-DQ-003 (grade D unreachable) + F-DQ-004 (price-only path skips grading entirely, ~80% of pulls) + F-DQ-005 (insiderOwnerPercent permanent false-positive HIGH alert) + F-DQ-008 (grader-load-failure shows all-zeros). Each finding individually is a bug; together they hollow out the entire DATAQUALITY_ENFORCE design: it doesn't track newer fields, can't reach D, doesn't apply to 80% of universe, emits noise alerts the operator learns to ignore, and silently shows all-zeros on grader failure.
- **Combined impact**: Karl's belief that DATAQUALITY_ENFORCE provides a protective gate against ungraded snapshots is unwarranted. The current implementation provides NO effective coverage of Tag 211l/219/220c fields, NO effective tier-cap on the worst grade, NO effective gating of price-only-refreshed snapshots, AND no reliable diagnostic when the grader breaks. The data-quality-report.md gradeCounts table is decorative.

### CG-006: CI commit-blocking family
- **Findings**: F-CI-001 + F-CI-002 + F-CI-005 + F-SM-004 + F-SM-007
- **Interaction**: F-CI-001 (strip step blocks commit on corrupt alert-state) + F-CI-002 (`--strategy-option theirs` can empty pushed commits silently) + F-CI-005 (1st-of-month dispatch + cron + monthly race) + F-SM-004 (inline node -e strip step hand-rolled write) + F-SM-007 (dispatch concurrency bypass). Each is a separate failure mode but they share: any of them can drop a day's pulls; F-SM-004 can write a zero-byte alert-state.json that triggers F-CI-001; F-CI-005 races multiplied with F-CI-002's silent-empty result.
- **Combined impact**: Multiple independent paths to a day's snapshot loss with no operator-visible signal except 'green ✓'. On the 1st of every month, the daily+monthly overlap window is open. picks-history continuity gaps then break the 84-day walk-forward window silently — and the cache-vs-source-files retention mismatch (F-BT-007) means lost vintages may never be reconstructible.

### CG-007: Tag 231a backtest-stack incomplete
- **Findings**: F-BT-001 + F-BT-002 + F-BT-004 + F-BT-005
- **Interaction**: Tag 231a-1 (backward-snap) and Tag 231a-2 (canonical-date anchoring) landed in walk-forward-perf.js but did NOT propagate to method-effectiveness.js (F-BT-002) or fix the weekend-entry edge case (F-BT-001) or address horizon-actual-days emission (F-BT-004). F-BT-005 (generatedAt never written) means method-effectiveness uses midnight-UTC fallback while walk-forward uses real timestamps — the two backtest reports measure different windows on the same vintage.
- **Combined impact**: methodology-report.js renders walk-forward + method-effectiveness side-by-side, but their alpha estimates are computed on different entry-date semantics and (for method-effectiveness) different per-ticker windows. CI bootstrap evidenceGate sign can flip incorrectly. Alpha attribution for tech-heavy modes is contaminated by ~1.5pp window noise. Friday-pre-market vintages silently use look-ahead p0.

## Agent Coverage

| Agent | Files Examined | Findings | Confidence |
|-------|---------------|----------|------------|
| data-pipeline | pull-yahoo.js, refresh-universe.js, pull-historical-prices.js, refresh-fx.js, prune-watchlist.js, discovery/* (6 files), lib/atomic-write.js, lib/snapshot-fs.js | 12 (F-DP-001..012) | 0.82 |
| methods-engine | runner.js, index.js, _helpers.js, score-aggregator.js, strategy-modes.js, sector-medians-compute.js, sector-median-lookup.js, data-quality.js, sga-revenue-trend.js, working-capital-trend.js, ohlson-o-score.js, magic-formula.js, penman-nissim-decomposition.js, ~25 other methods, generate-modes-report.js | 14 (F-ME-001..014) | 0.78 |
| state-management | detect-changes.js, snapshot-picks.js, snapshot-methods-history.js, snapshot-score-history.js, archive-old-snapshots.js, _helpers.js, lib/atomic-write.js, macro-regime.js, picks-regression-check.js, sector-medians-compute.js, method-effectiveness.js, walk-forward-perf.js, tag228a/tag229b/compute-method-drift, pick-diff.js, refresh-fx.js, prune-watchlist.js, check-pull-stats.js, pull-13f-institutional.js, pull-insider-form4.js, methodology-report.js, data-quality-report.js, elliott-export.js, snapshot-fs.js, watchlist-fs.js, trend.js, field-coverage.js, pull-prices-bulk.js, watchlist-cli.js, daily-pull.yml, .gitignore, alert-state.json (21.6 MB), method-history-state.json (8.9 MB), methods-history/, picks-history/ | 15 (F-SM-001..015) | 0.88 |
| ci-workflow | .github/workflows/daily-pull.yml, .github/workflows/monthly-sec-xbrl.yml | 8 (F-CI-001..008) | 0.82 |
| data-quality | methods/data-quality.js, field-coverage.js, scripts/data-quality-report.js, methods/sector-medians-compute.js, methods/score-aggregator.js, pull-yahoo.js (currency + mapper sections) | 9 (F-DQ-001..009) | 0.86 |
| backtest | walk-forward-perf.js, method-effectiveness.js, methodology-report.js, pick-diff.js, macro-regime.js, compute-method-drift.js, picks-regression-check.js, elliott-export.js, pull-prices-bulk.js, snapshot-picks.js, snapshot-methods-history.js, pull-historical-prices.js, picks-history/ + methods-history/ vintages | 10 (F-BT-001..010) | 0.82 |
| performance | pull-yahoo.js, generate-modes-report.js, generate-dashboard.js, generate-screener.js, generate-methods-report.js, methods/runner.js, _helpers.js, method-types.js, sector-median-lookup.js, sector-medians-compute.js, snapshot-methods-history.js, detect-changes.js, walk-forward-perf.js | 10 (F-PF-001..010) | 0.88 |

## Next-Session Pickup

1. **Ship the ADR-currency unification before Run #110/#111 fires** (CG-002): patch _priceOnlyUpdate to use `FX_TO_USD[meta.tradingCurrency.toUpperCase()]`, persist `meta.tradingFxRateApplied` from _convertSnapshotToUSD, split CCY_DENOMINATED_METRICS into financial-ccy and trading-ccy sub-lists, add a BABA/TSM/NU regression test asserting marketCap in [$50B, $1T] post-pull. Verify by inspecting one anchor ADR snapshot before and after the next pull.

2. **Bump FTS_CACHE_VERSION 2 → 3 and adopt lib/atomic-write across the seven hand-rolled sites** (CG-001 + CG-003): single commit with FTS_CACHE_VERSION=3 and `writeFileAtomic` swaps in detect-changes.js (alert-state + sidecar), snapshot-methods-history.js (vintage), methods/sector-medians-compute.js (replace private _atomicWrite), the workflow's strip-method-history inline (lift to scripts/strip-method-history.js), method-effectiveness.js (3 calls), walk-forward-perf.js (2 calls). Delete the misleading 'Atomic write via tmp+rename (was already done)' comment at detect-changes.js:135. Verify by triggering a one-off run on Windows with OneDrive open over snapshots/ and confirming no EPERM lossage.

3. **Add `continue-on-error: true` to the strip-methodHistory step + drop `--strategy-option theirs`** (CG-006): two-line workflow edit blocks F-CI-001 sticky-failure cascade and F-CI-002 silent-empty-push. After applying, re-trigger the broken alert-state recovery path manually (truncate alert-state to '{') to confirm the day's snapshots still commit.

4. **Extend CRITICAL_FIELDS + TRACKED_FIELDS to Tag 211l/219/220c additions** (CG-005): the dq-enforcement design is non-functional without this. After extending, recalibrate GRADE_THRESHOLDS so anchors stay A/A+, AND change GRADE_THRESHOLDS.C from `<=1.0` to `<=0.85` so grade D becomes reachable. Add an anchor-fixture unit test that every CRITICAL_FIELDS / TRACKED_FIELDS entry resolves on NVDA/MSFT/META/PLTR/COST snapshots.
