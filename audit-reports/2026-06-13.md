# Pipeline Audit 2026-06-13

## Executive Summary
- Total findings: 50 (3 CRITICAL, 14 HIGH, 21 MEDIUM, 12 LOW)
- Deduplicated from: 51 raw findings across 7 agents (1 merged: F-SM-002 ≡ F-CI-005)
- Top risk: The backtest/method-effectiveness stack is making keep/retire and score-weight decisions on essentially fabricated evidence — `prices/history.json` froze at 2026-05-17 and the SPY benchmark series has 3 data points, so every "alpha" number in `outputs/` is computed from ≤4 stale overlapping vintages while reporting healthy.
- Recommended first action: Root-cause why `pull-historical-prices` stopped updating after 2026-05-17 and backfill SPY/QQQ/IWM to ≥120d; add a freshness assertion (`max(price date) < today-3bd → ::error:: + skip`) at the top of `walk-forward-perf.js` / `method-effectiveness.js` so stale-price runs fail loudly instead of silently emitting noise.

## TOP 5 ACTION ITEMS
(sorted by priority × confidence × user-priority weighting: wrong-results and slow-screener weighted highest)

1. **[CRITICAL] prices/history.json — Forward-price series frozen at 2026-05-17 starves the entire backtest stack** (F-BT-001, also reported via F-BT-002/004/010)
   - Root cause: `pull-historical-prices` stopped updating after 05-17; `nearestTradingDay` scans only ±5–7d, so any vintage exit beyond 05-22 resolves null and is silently dropped.
   - Impact: All alpha/effectiveness numbers reflect ≤4 overlapping early-May vintages; method keep/retire and score-weight decisions rest on near-zero forward-price coverage, with no error raised.
   - Fix: Backfill prices; add `max(priceDate) < today-3bd → ::error:: + skip/fail CI` freshness gate at the head of walk-forward-perf.js and method-effectiveness.js.
   - Compound: **CG-001** root. Directly enables F-BT-002 (empty SPY benchmark), F-BT-004 (ungated benchmark/frozen alpha shown from 1–2 vintages), F-BT-010 (zero-eval early vintages are the only mature ones).

2. **[CRITICAL] monthly-sec-xbrl.yml — Monthly SEC XBRL workflow writes 6GB to a gitignored dir on an ephemeral runner; nothing consumes it** (F-CI-001, also reported by F-CI-002)
   - Root cause: Data lands in `external-data/sec-xbrl/*` (gitignored) on a runner that is destroyed at job end; the planned snapshot-merge consumer (Tag 133k) was never built; zero JS consumers of xbrl/companyfacts/manifest exist.
   - Impact: Months of monthly runner-time + SEC bandwidth spent on pure dead work; the committed manifest fabricates the illusion that XBRL fundamentals coverage exists, and the slice(0,1500)+304 logic means even a future consumer would never see >15% of the universe.
   - Fix: Either build + persist the consumer (actions/cache or data branch) or disable the workflow until one exists. Stop paying the monthly no-op cost.
   - Compound: **CG-002** with F-CI-002 (slice+304 coverage illusion).

3. **[HIGH] pull-yahoo.js:2130-2143 / 59-65 / 1776-1786 — Concurrency knob, tail-throttle, and FTS-retry triad inflates pull time and 429 storms** (F-DP-001, also reported by F-DP-002, F-DP-007)
   - Root cause: Worker count AND the yahoo-finance2 internal queue concurrency both derive from one env (`PULL_CONCURRENCY`), so they cannot be tuned independently; the tail throttle gates on the shared `idx` (line 2140) not `myIdx`, letting the last CONCURRENCY-1 tickers race; the FTS retry (line 1781) only catches timeout/ECONNRESET, never 429, so a throttle on the FTS leg discards good quoteSummary work and fails the ticker.
   - Impact: Cannot raise parallelism without proportionally raising the global HTTP cap and 429 exposure; pacing degrades at every run tail; FTS-leg 429s inflate n_failed exactly when the system should back off. A primary contributor to 163-min pulls.
   - Fix: Decouple `YF_QUEUE_CONCURRENCY` from worker count; drop the `idx<length` guard (sleep unconditionally); add 429 detection + the same backoff ladder to the FTS retry, plus a no-op `.catch` on the aborted underlying promise.
   - Compound: **CG-003**.

4. **[HIGH] daily-pull.yml / pull-yahoo.js:1265 — Staleness rotation + price-only fast-path are no-ops in CI; every run does a full fundamentals pull of the whole universe** (F-CI-003, also reported by F-CI-006, F-PF-007)
   - Root cause: `snapshots/` and `fundamentals-cache/` are gitignored and no actions/cache restore exists, so on a fresh runner `sortByStaleness` sees age=0 for every ticker (stable sort = alphabetical watchlist order) and `FUNDAMENTALS_MAX_AGE_DAYS` never triggers the price-only path. Tag 151 (stop committing snapshots) silently broke Tag 164 (staleness rotation).
   - Impact: ~2h of the 240-min budget wasted re-downloading full fundamentals universe-wide → rate-limit storms; on timeout-truncated runs the dropped tail is *always* late-alphabet (S–Z), deterministic coverage holes with no catch-up. This is the user's #2 priority (slow/non-optimal) and amplifies the wrong-results risk.
   - Fix: Persist a tiny committed per-ticker `lastPulled` index (or actions/cache for fundamentals-cache keyed by week), restore before pull so rotation + price-only actually engage; then re-validate the 13% coverage gate.
   - Compound: **CG-004** with F-CI-006 (single end-of-job commit → ~19% of days lose the whole run) and F-CI-004 (repo bloat slows the full-history checkout each run).

5. **[HIGH] methods/score-aggregator.js:143-145 — earnings-stability scores 0.99 when it FAILS on positive-years (maxDecline=0)** (F-ME-201)
   - Root cause: `normalizeMethodScore`'s `lte` branch executes `if (val <= 0) return 0.99;` before considering *why* the method failed. A loss-maker / flat-OpInc stock fails earnings-stability on the positive-year floor while carrying `value=maxDecline=0`, so it gets near-full credit on a 0.20-weight QC input.
   - Impact: Genuine non-compounders get inflated QUALITY_COMPOUNDER scores and can be pushed into B/NEAR_MISS tiers — silent wrong screener results (user's #1 priority), invisible because the method correctly reports `pass:false`.
   - Fix: In `normalizeMethodScore` require `val > 0` before the 0.99 `lte` shortcut (fall through to the generic curve otherwise), or have earnings-stability export a composite pass-distance as `value` rather than a magnitude where 0 means "best".
   - Compound: none.

## Full Findings

### CRITICAL

**F-BT-001 [CRITICAL · data-quality · T1]** prices/history.json frozen at 2026-05-17 — backtest stack starved of forward prices. file: prices/history.json. Global max date 2026-05-17 vs today 2026-06-13; picks vintages run through 06-12. Vintages after ~05-17 have no exit price in the 28d/84d horizon → pickReturn/universeMedian/benchmark resolve null and silently drop. nearestTradingDay scans only ±5–7d so any target beyond 05-22 returns null; vintageCount/totalPicks shrink invisibly. Impact: all alpha/effectiveness numbers computed from ≤4 stale vintages; keep/retire decisions rest on near-zero coverage. Fix: freshness assertion at top of walk-forward-perf.js/method-effectiveness.js (max price date < today-3bd → ::error:: + skip/fail); root-cause pull-historical-prices stall after 05-17. compound: CG-001.

**F-CI-001 [CRITICAL · concept-flaw · T1]** Monthly SEC XBRL workflow produces output nothing consumes and nothing stores. file: .github/workflows/monthly-sec-xbrl.yml (1-174); pull-sec-xbrl.js:9-17,36-37. Pulls ~1500 companyfacts files (~6GB transient) into gitignored `external-data/sec-xbrl/` on an ephemeral runner; only `_manifest.json` committed; repo-wide grep shows zero consumers. The Tag 133k follow-up merge was never built (now Tag 246). Impact: months of dead runner/bandwidth work; manifest fabricates a coverage illusion. Fix: build+persist the consumer or disable the workflow. compound: CG-002.

**F-BT-002 [CRITICAL · data-quality · T1]** Benchmark series SPY/QQQ/IWM nearly empty (SPY=3 points) — alphaVsBenchmark unusable. file: scripts/walk-forward-perf.js:275-307. SPY=3, IWM=3, QQQ=8 points. computeBenchmarkReturn picks first present (SPY) and anchors ALL per-vintage canonical entry/exit dates to it; with 3 points the benchmark is null for nearly every vintage and the canonical anchoring starves pick/universe lookups too. Impact: alphaVsBenchmark (labeled "most honest" by methodology-report) is null or computed from a 3-day window — noise. Fix: backfill ≥120d SPY/QQQ/IWM; assert benchmark series length ≥ horizon+buffer before anchoring, else per-ticker fallback + flag. compound: CG-001 (also reported alongside F-BT-001).

### HIGH

**F-DP-001 [HIGH · performance · T1]** Double concurrency cap: yf internal queue AND worker pool both = PULL_CONCURRENCY. file: pull-yahoo.js:59-65,1297,2130-2143. Worker count and yf queue concurrency both derive from PULL_CONCURRENCY; per-ticker fan-out (1 quoteSummary + 1 quote + ≤4 sequential FTS) saturates the shared queue while each worker also sleeps rateLimitMs. Raising the env raises worker count, global HTTP cap, and 429 exposure together — no independent throttle. Fix: separate YF_QUEUE_CONCURRENCY; make the worker-pool sleep the single rate governor. compound: CG-003.

**F-CI-002 [HIGH · logic-bug · T1]** SEC pull processes the SAME first 1500 CIKs every month; 304s counted as coverage of data that does not exist. file: monthly-sec-xbrl.yml:58-114; pull-sec-xbrl.js:124-141. `slice(0,max)` with no rotation offset (CIKs 1501..~9900 never touched); If-Modified-Since from committed manifest → SEC 304 → no file written, yet the coverage gate counts `pulled+c304+fresh` as ok. On an ephemeral runner a 304 = zero bytes anywhere. Fix: persist a rotation cursor (slice(cursor,cursor+max) w/ wraparound); count only files actually written when cache is ephemeral. compound: CG-002.

**F-CI-003 [HIGH · concept-flaw · T1]** Staleness rotation is a no-op in CI: snapshots/ and fundamentals-cache/ gitignored, runner starts empty. file: daily-pull.yml:201-211,346-354; pull-yahoo.js:1265-1292; refresh-universe.js:444. sortByStaleness reads snapshot mtimes from a dir that is regenerated fresh → all age=0 → stable sort preserves alphabetical watchlist order. FUNDAMENTALS_MAX_AGE_DAYS=7 is dead config (no cache restore) → full fundamentals pull every run for every ticker. Impact: ~2h wasted, 429 storms, deterministic S–Z drop on truncated runs. Fix: persist a committed per-ticker lastPulled index or actions/cache; re-validate the 13% gate. compound: CG-004.

**F-CI-004 [HIGH · performance · T1]** Repo bloat: ~125MB committed artifacts churned per run; .git 553MB after ~1 month. file: daily-pull.yml:553-555,580-636,57-61. Daily `git add -A` rewrites alert-state.json (44MB) + method-history-state.json (55.8MB, already past 50MB push-warning) and adds a new 25.7MB methods-history/<date>.json; --methods-keep-days 90 → ~2.3GB working tree; fetch-depth:0 clones full history every run; Archive step is continue-on-error with no size gate. Eventual hard 100MB block kills the pipeline. Fix: shard/gzip state off main, write vintages to gitignored ndjson/data branch, add a size gate after Archive. compound: CG-004.

**F-CI-005 [HIGH · race-condition · T1]** Manual-dispatch concurrency escape hatch guarantees unresolvable conflict when overlapping the cron run. file: daily-pull.yml:19-21,596-635. Also reported by **F-SM-002** (state-management agent, MEDIUM) — same root cause, higher evidence tier kept. workflow_dispatch gets `manual-{run_id}` so it does not serialize against the cron run; both rewrite the same monolithic state files frozen to the same RUN_DATE_UTC. The loser hits a rebase content conflict; the cherry-pick fallback replays the identical conflict and exits 1, discarding ~4h of runner-local pull. Fix: share the 'main-push' group for dispatch, or gate the manual Commit step on a lock, or shard state files so conflicts are disjoint. compound: CG-004.

**F-CI-006 [HIGH · state-atomicity · T2]** Single end-of-job commit + over-committed step budgets: ~19% of days produce NO data commit. file: daily-pull.yml:33,112-211,463-478,580-636. Everything commits in one step at ~min 200 of a 240-min job; pre-commit worst case (~225 min) leaves ~15 min for a >80-min downstream chain. A timeout SIGKILL before Commit discards the whole validated pull (runner-local snapshots). Empirics: bot commits missing 6 of last 31 days. Fix: intermediate Commit Pull Data step after Verify Pull Coverage, or move the report chain to a separate job with artifact handoff. compound: CG-004.

**F-DP-003 [HIGH · schema-drift · T1]** Legacy incomeStatementHistory empty since Nov 2024; FTS is sole source with no backstop. file: pull-yahoo.js:587-675,1002-1004,1836-1839. The QS annual/quarterly block is near-empty for most tickers; the "QS strictly more non-null wins" gates reduce to "FTS always wins"; the Tag 202 "QS is primary RnD source" path reads the dead submodule. No working fallback if FTS field names drift. Fix: per-run sentinel counting tickers with non-empty annualRev post-merge; assert it stays >~80% of attempted, fail/alert on collapse. compound: CG-005.

**F-DQ-001 [HIGH · silent-corruption · T2]** targetMeanPrice/targetMedianPrice scaled by financial FX factor, not trading factor. file: pull-yahoo.js:412-428. CCY_DENOMINATED_METRICS includes the analyst price targets and FX-scales them via scale() using the financial-reporting factor, but targets are quoted in the TRADING currency (like price, which is left unscaled). For ADRs (e.g. TSM, reporting TWD) the USD target is multiplied by the TWD factor (~0.031) → ~32x too small; pence LSE names up to ~100x. analyst-upside = target/price silently wrong for all non-USD reporters. Fix: route targets through scaleTrading() (tradingFactor) like marketCap. compound: none.

**F-SM-001 [HIGH · performance · T1]** methods-history vintage files (~26MB) approach GitHub 50MB hard limit at universe growth. file: snapshot-methods-history.js:117-160. Each YYYY-MM-DD.json is one monolithic object of every ticker×method; measured 26.7MB at ~6200 tickers; at ~12k tickers a single vintage crosses GitHub's 50MB per-file push-reject limit → the daily commit fails to push that blob, blocking ALL state for the run, after the script exits 0 (gate can't catch it). Fix: gzip vintages (.json.gz, ~10x), lower keep-days, add a pre-push >45MB size guard; long-term per-ticker history files. compound: none (related to CG-004 bloat class).

**F-ME-201 [HIGH · silent-corruption · T1]** earnings-stability value=maxDecline=0 yields 0.99 when it fails on positive-years. file: methods/score-aggregator.js:143-145. The `lte` branch returns 0.99 for val≤0 before pass-reason consideration; a loss-maker failing the positive-year floor with maxDecline=0 gets ~0.99 on a 0.20-weight QC input, inflating its QC score and pushing non-compounders into B/NEAR_MISS. Invisible because pass:false is reported correctly. Fix: require val>0 before the 0.99 shortcut, or export composite pass-distance as value. compound: none.

**F-BT-003 [HIGH · look-ahead-bias · T1]** backtest-from-history.js ignores HORIZON_DAYS — measures full available history span. file: backtest-from-history.js:27-31. Return = (series[last]-series[0])/series[0] over the whole series, not a 70d window; AAPL's series spans ~110 calendar days mislabeled "70-day". Heterogeneous per-stock windows → invalid cross-stock comparison. Fix: snap start to endDate-HORIZON_DAYS via nearestTradingDay, or delete in favor of walk-forward-perf.js. compound: CG-006.

**F-BT-004 [HIGH · logic-bug · T1]** Vintage-count gate applied only to alphaVsUniverse; benchmark/frozen alpha ungated. file: scripts/walk-forward-perf.js:560-588. MIN_VINTAGES_UNIVERSE (4) gates only medianAlphaVsUniverse; medianAlphaVsBenchmark/Spy/FrozenVintage are raw medians over however many vintages contributed (1–2 given F-BT-001/002). methodology-report surfaces benchmark alpha as the headline with no min-vintage guard. Fix: apply the same gate to benchmark + frozen-vintage medians; emit a *Note when gated. compound: CG-001.

**F-BT-005 [HIGH · look-ahead-bias · T2]** Pseudo-replication: overlapping daily vintages pooled as i.i.d. inflates significance. file: scripts/method-effectiveness.js:82-109. 28d/84d windows of adjacent daily vintages overlap ~96%, same tickers recur, yet bootstrapAlphaCI resamples every (vintage,ticker,return) row as independent → CIs shrink toward false significance. Fix: non-overlapping vintages per horizon or block/cluster bootstrap resampling whole vintages with within-vintage ticker dedup; report effective sample size. compound: CG-007.

**F-BT-006 [HIGH · logic-bug · T2]** Multiple-comparison: ~30 methods × 2 horizons × 4 grades, no FWER/FDR control. file: scripts/method-effectiveness.js:388-406. ~300 simultaneous 95% CI tests at nominal 5%, each crossing of zero treated as actionable (promote/retire). ~15 methods appear significant by chance. Fix: BH-FDR or Bonferroni across the full grid; require persistence across consecutive reports for promotion (symmetric with the 3-month retirement bar). compound: CG-007.

**F-PF-001 [HIGH · performance · T1]** methods-report: per-method filter+sort of full universe run 3x (top-N, leaderboard, deep-dive). file: generate-methods-report.js:233-269,525-532. Three independent O(M·N·logN) sweeps with comparators that re-walk nested r.results[m.id].value each comparison; ~83 methods × N rows, ~22M comparisons at 19k universe just for sweep 1, structurally repeated. Fix: pre-extract flat numeric columns per metric once, sort plain {ticker,val} tuples; bucket per-method valid lists in one forward pass. compound: none.

**F-PF-002 [HIGH · performance · T1]** modes-report main() recomputes eligibleForMode + topAllMust for every mode just to log counts. file: generate-modes-report.js:1248-1252. buildHtml already computes eligibleByMode and topAllMust internally; main() re-runs the full O(N) filter + O(N·logN) sort per mode purely for console.log. Fix: have buildHtml return the counts it already computed and log those. compound: CG-008.

### MEDIUM

**F-DP-002 [MEDIUM · logic-bug · T1]** Worker-pool inter-ticker sleep keyed on shared idx, not myIdx — tail workers skip throttle. file: pull-yahoo.js:2130-2143. The throttle tests shared `idx<stocks.length`; once idx hits length the last CONCURRENCY-1 in-flight tickers skip rateLimitMs and race back-to-back, spiking 429 pressure at every run tail. Fix: drop the guard and sleep unconditionally, or gate on `myIdx+concurrency<length`. compound: CG-003.

**F-DP-004 [MEDIUM · schema-drift · T2]** FTS field-name lists are hardcoded live-probe guesses; rename yields silent null. file: pull-yahoo.js:1031-1044,1077-1080,1140-1154,1799-1811. _ftsValue matches an exact camelCase list then a generated snake_case variant; a Yahoo rename returns valid-looking null (no error) and the field vanishes (SGA, D&A, dilutedAverageShares, OCF, FCF at risk). Fix: emit a per-run FTS field-coverage report into the manifest; gate on sharp drops. compound: CG-005.

**F-DP-005 [MEDIUM · logic-bug · T1]** Schema-validation errors classified as not-found, auto-marking live intl tickers delisted. file: pull-yahoo.js:2084-2117. The errClass switch maps schema-validation failures to 'not-found', whose branch sets meta.delisted=true. Run #107 produced 637 such failures, all international. A yahoo-finance2 version/schema change could silently delist whole regions of live tickers (delisted snapshots are excluded downstream). Fix: give schema-validation its own 'schema-reject' class that does NOT set delisted; CI gate on schema-reject count/region concentration. compound: none.

**F-DP-006 [MEDIUM · state-atomicity · T2]** Manifest flush cadence keyed on per-worker myIdx; SIGKILL can leave n_ok undercounted. file: pull-yahoo.js:1352-1391,2139-2147. Flushes fire at `myIdx % 100` (irregular under concurrency); only the post-loop final flush is guaranteed. A SIGKILL at the 165-min cap between the last %100 flush and the final flush leaves the on-disk manifest undercounting n_ok by up to ~100+CONCURRENCY, so Verify Pull Coverage reads stale-low. Fix: drive cadence off a shared processed counter; trap SIGTERM to flush once before exit. compound: none.

**F-DP-007 [MEDIUM · logic-bug · T2]** FTS fetch leg has no 429 retry; rate-limit there fails ticker despite quoteSummary backoff. file: pull-yahoo.js:1237-1247,1314-1345,1776-1786. quoteSummaryWithRetry retries on 429+timeout (15/45/90s); the FTS retry (line 1781) only continues on timeout/ECONNRESET at attempt 0 — no 429 check — so a throttle on the FTS leg throws 'FTS fetch failed' and discards good quoteSummary work. Also the aborted underlying promise rejects later unawaited (unhandled-rejection candidate). Fix: add 429 detection + backoff ladder to FTS retry; attach no-op .catch to the aborted promise. compound: CG-003.

**F-DP-008 [MEDIUM · data-quality · T2]** FX_TO_USD seeded from raw.rates verbatim casing but always read uppercased; stale-revert gap. file: pull-yahoo.js:171,185-219,230-235,307-309. loadFx Object.assigns raw.rates verbatim while reads use currency.toUpperCase(); the stale-revert deletes only FX_TO_USD[up], so a lowercase/mixed-case provider key leaves a stale duplicate bypassing the staleness gate while provenance reports fallback. Fix: normalize keys to uppercase at load; assert all keys uppercase. compound: none.

**F-DP-009 [MEDIUM · performance · T1]** refresh-universe screener sweep fully sequential and untimed — hang risk, slow. file: refresh-universe.js:125-144,155-185,207-298. 25 regions × 13 screeners = 325 sequential fetchScreener + 300ms sleeps, then 29 exchanges paginated with 400ms sleeps, no per-call timeout; one hung yf.screener socket stalls the whole refresh. Fix: per-call timeout race + bounded (4–6) worker pool, keep sleep as governor. compound: CG-009.

**F-DP-011 [MEDIUM · data-quality · T2]** price-only floor skips when quote omits marketCap; decayed sub-$1B names linger. file: pull-yahoo.js:1570-1583,1633-1656,1986-1991. _priceOnlyUpdate only updates mcap when q.marketCap!=null and only floors when mcapNow!=null; a missing quote mcap keeps the stale full-pull mcap, the young asOf prevents a corrective full pull, so a collapsed sub-floor ticker persists indefinitely. The full-pull path treats missing mcap as below-floor; price-only does not mirror it. Fix: in price-only, force a full pull or mark stale mcap untrusted when q.marketCap is null for USD-trading tickers. compound: none.

**F-SM-003 [MEDIUM · silent-corruption · T1]** Corrupt method-history-state.json sidecar silently resets to {} (no backup, no fail) unlike alert-state. file: detect-changes.js:53-62. alert-state was hardened (F-SM-015) to back up + exit non-zero on corruption; the parallel 8.9MB methodHistory sidecar logs WARN and returns {}, then saveState overwrites it with only today's data — permanent loss of multi-day trend series, no backup. Fix: mirror the alert-state guard (copy to .corrupt.<ts>, refuse to overwrite / exit non-zero gated by RESET env). compound: none.

**F-SM-006 [MEDIUM · logic-bug · T2]** methodHistory sidecar pruned one run later than methodState; cleanup swallows errors silently. file: detect-changes.js:111-157. saveState prunes methodState before writing alert-state but _saveMethodHistory writes the sidecar without the same prune; orphan cleanup runs at the START of the next run inside a swallowing try/catch. So the two committed files diverge by one run and the sidecar grows with orphaned/delisted-ticker history when cleanup throws. Fix: apply the live-methodState filter inside _saveMethodHistory at write time. compound: CG-004 (bloat class).

**F-ME-202 [MEDIUM · logic-bug · T1]** fcf-yield uses sector MEDIAN as pass gate (no positive-only guard) — ~half of each sector passes. file: methods/_helpers.js:198-228. effectiveThreshold returns the sector median fcf-yield as the threshold, so by construction ~50% of the sector clears it and the gate can sit far below the intended 5% floor (HARDWARE median 0.0108 → pass at ≥1.1%). fcf-yield is NOT in POSITIVE_ONLY_METRICS, so a negative median would pass everything. Carries 0.05 QC weight → leaks into scoring/sort. Fix: clamp eff.threshold ≥ 0.05 static floor, or add fcf-yield to POSITIVE_ONLY_METRICS and use p75 not median. compound: CG-010.

**F-ME-203 [MEDIUM · data-quality · T1]** fcf-yield sector median built from raw FCF but applied to SBC-adjusted yield (write/read mismatch). file: methods/sector-medians-compute.js:98-113. The bucket pushes raw annualFCF/marketCap (line 112) while fcf-yield.evaluate compares (annualFCF-|SBC|)/marketCap against that median — the threshold is calibrated on a systematically higher distribution than the value gated, penalizing SBC-heavy SAAS/HARDWARE against a benchmark that didn't pay the SBC penalty. Fix: subtract |annualSBC| when building the bucket, or compute both sites on the same definition. compound: CG-010.

**F-DQ-002 [MEDIUM · data-quality · T2]** Universe shrinkage (skipped-mcap / fx-unknown) only logged per-ticker, no aggregate survivor-bias alarm. file: pull-yahoo.js:1570-2017. fxConversionFailed / MIN_MCAP-floor / price-only-floor deletions unlinkSync the snapshot with only INFO logs; no run-level counter vs threshold/prior run, so a Yahoo schema/FX change or mis-set MIN_MCAP_USD silently drops hundreds of tickers. Fix: aggregate deletion counts; HIGH alert when ratio >5% of universe or >2x prior run. compound: CG-011.

**F-DQ-003 [MEDIUM · schema-drift · T2]** Field-coverage drift detector blinded ~7d by price-only persistence of stale fundamentals. file: pull-yahoo.js:1585-1620. On the ~80% price-only path the old annual/timeseries arrays are written back unchanged; computeCoverage reads persisted fields so a live Yahoo schema drop is masked until each ticker's 7d full-pull cadence cycles it. Fix: tag coverage-history with pullMode and compute drift only over status:'ok' fresh-fundamentals snapshots. compound: CG-011.

**F-DQ-004 [MEDIUM · data-quality · T1]** data-quality-report scalar KEY_FIELDS use `.value != null`, diverging from grader's finite-number test. file: scripts/data-quality-report.js:52-68. Scalar checks use `!= null` (passes NaN and 'N/A') while the grader's _hasMetric requires Number.isFinite — the report overstates per-field coverage vs the grade distribution, the inconsistency Tag 232c-23 only fixed for array fields. Fix: export _hasMetric from methods/data-quality.js and reuse it for scalar KEY_FIELDS. compound: none.

**F-BT-007 [MEDIUM · look-ahead-bias · T1]** DST window in getEntryDate misclassifies early-March/late-Nov as EDT → same-day look-ahead. file: scripts/walk-forward-perf.js:119-136. isEDT ≈ month 3..11, but US DST is 2nd-Sun-Mar..1st-Sun-Nov; for ~35 days/yr the market is on EST (close 21:00 UTC) yet the code uses 20:00, so a 20:00–21:00Z snapshot is booked same-day (no T+1 shift). Fix: compute the real DST transition or always use the conservative 21:00 threshold. compound: CG-006.

**F-BT-008 [MEDIUM · silent-corruption · T1]** horizonActualDays exposed but never used — summary still pools 79-84d windows. file: scripts/walk-forward-perf.js:292-306,544-590. Tag 232c-27 stores horizonActualDays per vintage but the summary median never reads it; backward-snap one-sidedly shortens pre-holiday exits to 79–82d, pooled un-normalized with 84d windows → one-sided ~1pp bias. Fix: rescale return by nominal/actual before aggregating, or drop |actual-nominal|>3d vintages. compound: none.

**F-BT-009 [MEDIUM · look-ahead-bias · T2]** Entry backward-snap books untradable weekend/holiday gap for Fri & pre-holiday vintages. file: scripts/walk-forward-perf.js:154-170,287. nearestTradingDay scans backward-first and is used for the ENTRY date; a weekend/holiday entry snaps back to the prior Friday close, capturing the Fri→Mon gap the screener couldn't transact. Fix: snap ENTRY forward to the next tradable day while keeping EXIT backward-snap; document the asymmetry. compound: CG-006.

**F-BT-010 [MEDIUM · data-quality · T1]** Survivor bias: early vintages have evaluatedTickers=0 and live universe swings 3300-5657. file: snapshot-picks.js:226-230,275. The first 5 vintages (the only ones with mature forward prices under F-BT-001) have evaluatedTickers=0 so alphaVsUniverse is suppressed; later vintages swing 50% week-to-week and delisted names vanish from prices/history.json with no exit price, so even the corrected universe over-weights survivors. Fix: persist delisted final prices; report universe-size per vintage and gate on >X% deviation. compound: CG-001.

**F-BT-011 [MEDIUM · logic-bug · T1]** score-scale fallback: early vintages lack score; pick.score mixes 0-100 and absent. file: snapshot-picks.js:97-104,250-260. pickStockForMode falls back ev.score→normScore*100→null; the first 5 vintages have no score, so sort collapses to primaryMetric and dedupePicksByCompany tie-break (p.score||0) collapses to insertion order → pick SET composition differs between scored and unscored vintages for non-strategy reasons. Fix: backfill score or tag scoreSource; have walk-forward/method-effectiveness bucket/exclude scoreSource!=='score' vintages. compound: CG-001.

**F-BT-012 [MEDIUM · logic-bug · T1]** Vintage date gaps (7 missing days in 5 weeks) silently shorten N-day window coverage. file: picks-history. Missing 05-12/05-16/05-18/05-27/06-03/06-09/06-11; listVintages globs whatever exists, MIN_VINTAGES counts distinct dates so gaps silently reduce counts; readers can't distinguish gating from pipeline outage. Fix: emit a coverage diagnostic (expected business days vs present) in walk-forward.json; warn on >X% missing. compound: CG-001 (gaps trace to F-CI-006 lost-commit days).

**F-BT-013 [MEDIUM · look-ahead-bias · T1]** Backtest scripts use TODAY's snapshot fundamentals against past prices (self-flagged). file: backtest-10-weeks.js:10-14,79-95. Both backtest-10-weeks and backtest-from-history evaluate current snapshot vs historical returns (look-ahead, now stamped _bias=true). Residual: both write the SAME backtest-10w-result.json with different schemas, so last-run wins and a stale pre-_bias committed copy could be consumed as unbiased. Fix: distinct output filenames; always emit _bias; consider deleting backtest-from-history.js. compound: CG-006.

**F-BT-014 [MEDIUM · logic-bug · T2]** Benchmark choice is availability-ordered (SPY>QQQ>IWM), not strategy-matched. file: scripts/walk-forward-perf.js:275-284. First-present-of-[SPY,QQQ,IWM] is the benchmark even for HYPERGROWTH/TURNAROUND (growth/small-cap), and the resolved benchmark can change across vintages by data availability, so alphaVsBenchmark conflates style/beta with skill and isn't comparable across vintages. Fix: map each mode to a fixed style-appropriate benchmark; record benchmarkTicker per vintage and refuse to aggregate across differing benchmarks. compound: CG-001.

**F-PF-003 [MEDIUM · performance · T2]** modes-report: topAllMust(...,9999).length sorts entire passing set only to count it. file: generate-modes-report.js:159-186,428-455,554-555. Per mode the eligible set is filtered+sorted several times; the topN=9999 calls sort the whole passing set only to read .length. Fix: replace with plain filter().length; cache modeEvals[modeId].score into a local before sorting. compound: CG-008.

**F-PF-004 [MEDIUM · performance · T2]** methods-report: discoveryRows DataGuard filter walks Object.entries(results) per row. file: generate-methods-report.js:226-232. Object.entries materializes ~83 [k,v] pairs per row + isDataGuard per method (~1.6M calls/allocations at 19k×83). Fix: precompute MT.dataGuardIds once and iterate only that small list per row. compound: none.

**F-PF-005 [MEDIUM · performance · T2]** methods-report: snapshot load uses serial blocking readFileSync loop (not migrated to async batches). file: generate-methods-report.js:74-133. evaluateAllStocks reads+parses every snapshot synchronously, blocking the event loop, while modes-report (32-way) and snapshot-methods-history (200-batch) already use batched fs.promises.readFile. ~1–3 min serial I/O at 15k+ files. Fix: mirror the loadStocks batched (CONCURRENCY=32) loader. compound: CG-012.

**F-PF-009 [MEDIUM · performance · T3]** wrapEvaluate + effectiveThreshold re-classify same stock M times per evaluateStock (no sub-profile memo). file: methods/_helpers.js:198-228,289-313. classifySubProfile(stock) runs once per threshold-using method (up to M times/stock); wrapEvaluate also runs _dataAsOfFromStock/_dataAgeDays per method×stock though both depend only on the stock. Highest-multiplicity hot path (runs in methods-report, modes-report, history AND pull). Fix: memoize sub-profile on stock.__sp per evaluateStock; hoist dataAsOf/ageDays to once-per-stock. compound: none.

### LOW

**F-DP-010 [LOW · logic-bug · T1]** fetchScreener documents 'retry up to 3 times' but only backs off twice (off-by-one). file: refresh-universe.js:127-144. Loop 0..2 with guard `is429 && attempt<2` → a 429 on the final attempt returns [] with no third retry; sustained 429 silently drops a region/bucket's tickers. Fix: align code with intent or fix the comment; warn distinguishing 429-exhausted from other failures. compound: CG-009.

**F-DP-012 [LOW · silent-corruption · T3]** Quarterly series order assumed (QS latest-first, FTS reverse) with no date-based guard. file: pull-yahoo.js:672-675,1181-1209. FTS quarterly is slice().reverse()'d to oldest→latest with no validation against row period/endDate; a Yahoo ordering flip would invert every quarterly series and break same-quarter YoY (q[i] vs q[i+4]) with no error. Fix: sort each quarterly array by period/endDate explicitly; log if incoming order disagreed. compound: CG-005.

**F-DP-013 [LOW · data-quality · T2]** refresh-universe rewrites watchlist + fresh timestamp even on a Yahoo-side blackout. file: refresh-universe.js:194-214,300-313,411-450. main() is purely additive and unconditionally writes watchlist.json + bumps lastUniverseRefresh with no floor on sources succeeded; an all-screener/all-exchange 429 looks identical to a healthy refresh in the artifact. Fix: track per-source-class contribution; ::warning:: or exit non-zero when Yahoo screener+exchange contribution is zero/below floor. compound: CG-009.

**F-SM-004 [LOW · logic-bug · T2]** score-history/r40rx-history write raw ticker+'.json', bypassing safeSnapshotFilename helper. file: scripts/snapshot-score-history.js:99-123. Both scripts build filenames as path.join(dir, ticker+'.json') instead of safeSnapshotFilename(), re-introducing the drift surface snapshot-fs.js was created to eliminate; latent for reserved-name ('CON') or path-significant ('/') tickers and Windows runs. Fix: route write+read through safeSnapshotFilename(). compound: CG-013.

**F-SM-005 [LOW · logic-bug · T3]** score-history/r40rx-history mix _meta.json control file with per-ticker payload (dir-walk footgun). file: scripts/snapshot-score-history.js:203-210. A leading-underscore control file shares the per-ticker dir; any future readdir consumer without the !startsWith('_') guard gets a phantom '_meta' ticker — the same class as the methods-history _manifest-full.json pollution. Fix: document the convention / require the guard, or move _meta.json out of the payload dir. compound: CG-013.

**F-ME-204 [LOW · data-quality · T1]** _n_<metric> reports full array length while median/percentile use positive-filtered subset. file: methods/sector-medians-compute.js:154-176. For POSITIVE_ONLY_METRICS the median/percentiles use the v>-0.05 filtered array but _n_<mid>=arr.length (full), so a percentile from 5 positive values can report n=200, overstating reliability in lookupPercentile/UI. Fix: report _n as useArr.length or add _nUsed_<mid> and gate on it. compound: none.

**F-ME-205 [LOW · performance · T1]** roce sector medians/percentiles computed and written but never consumed by any method. file: methods/sector-medians-compute.js:102-129. roce medians+percentiles are computed every pull and written to sector-medians-auto.json but no method id 'roce' or lookup consumes them — dead output bloating the file. Fix: wire a roce-based sector-relative method or drop roce from gather/compute. compound: none.

**F-ME-206 [LOW · logic-bug · T2]** sector-relative-roic degraded mode ranks 90 (not 75) for roic in [p50, 1.5*p50). file: methods/sector-relative-roic.js:94-105. When p75 is absent it's synthesized = p50, so the roic≥p75(==p50) branch (90) shadows the roic≥p50 branch (75) for the whole [p50,1.5p50) range; surfaced sectorRank overstates the signal (pass gate unaffected). Fix: cap rank at 75 for roic≥p50 in degraded mode, or skip p75 buckets when degraded. compound: none.

**F-DQ-005 [LOW · schema-drift · T2]** Coverage tracker omits several consumed metrics (analyst targets, ROE/ROA, EV ratios, sharesOutstanding). file: field-coverage.js:25-62. TRACKED_FIELDS omits targetMeanPrice, returnOnEquity/Assets, enterpriseToEbitda/Revenue, debtToEquity/currentRatio/quickRatio, earningsHistory, meta.sharesOutstanding — ~10 consumed fields whose Yahoo deprecation would be invisible to drift detection. Fix: add the consumed fields to TRACKED_FIELDS. compound: none.

**F-DQ-006 [LOW · data-quality · T3]** Large-scale FX fallback (stale 2024 rates) has no run-level alarm threshold. file: pull-yahoo.js:181-329. loadFx reverts stale per-currency rates to the hardcoded 2024 FX_FALLBACK and logs staleCount only via console.log; a multi-week refresh-fx outage silently prices EM/UK legs off 2024 rates with no HIGH alert. Fix: count snapshots on hardcoded-fallback per run; HIGH alert above a threshold. compound: CG-011.

**F-DQ-007 [LOW · logic-bug · T1]** Dead _convertToUSD helper fails open on unknown currency. file: pull-yahoo.js:230-235. _convertToUSD returns the raw value unchanged when rate==null with no flag; currently unused (live path _convertSnapshotToUSD fail-closes), but a latent fail-open footgun if wired in. Fix: delete it, or return a sentinel on missing rate. compound: none.

**F-BT-015 [LOW · data-quality · T2]** Transaction costs, spread, slippage, FX ignored across entire backtest stack. file: scripts/walk-forward-perf.js:105-108. returnPct is raw close-to-close; no cost model anywhere, and non-USD picks (e.g. 6160.HK) are compared to USD SPY without FX normalization, mixing currency moves into alpha. Fix: document gross-return assumption; convert cross-currency picks to USD before benchmark comparison or restrict to same-currency cohorts. compound: CG-001.

**F-BT-016 [LOW · data-quality · T2]** compute-picks-lookback: short-history stocks mislabeled by lookback year + duplicate fetches. file: compute-picks-lookback.js:44-56,99-107. totalReturn uses hist[0]..hist[last] without checking hist[0] is ~N years back (a 2y-old IPO returns 2y under the 5y label); each ticker's 1/3/5y return is fetched twice (median block + per-pick), ~2x runtime/quota. Fix: reject/relabel series whose first date isn't within ~30d of period1; compute per-pick returns once and reuse. compound: none.

**F-PF-006 [LOW · performance · T2]** methods-report: methodSummary forward pass does lazy-init branch per row*method pair. file: generate-methods-report.js:459-466. Inner loop does `_methodCounts[m.id] || (assign)` lazy-init per N×M pair; short-circuits after row 1 but still a property read + truthiness check each pair. Fix: pre-initialize _methodCounts once before the rows loop. compound: none.

**F-PF-007 [LOW · performance · T2]** pull-yahoo reads each snapshot header twice (sortByStaleness + _getExistingSnapshotAge), 4 syscalls each. file: pull-yahoo.js:1264-1290,1394-1407. existsSync(stat)+openSync+readSync+closeSync per ticker in sortByStaleness, then the same 4-syscall probe again in _getExistingSnapshotAge — header read twice/run (~120k blocking syscalls at 15k tickers). Fix: drop existsSync (rely on openSync ENOENT); stash parsed asOf age in a Map and reuse in processOne. compound: CG-004 (note: moot in CI until F-CI-003 makes snapshots present).

**F-PF-008 [LOW · performance · T2]** sector-medians CLI loads all snapshots with serial readFileSync loop. file: methods/sector-medians-compute.js:352-360. The require.main CLI reads every snapshot synchronously (same serial pattern modes-report/history already replaced); ~1–3 min serial I/O at full universe as the Compute Sector-Medians workflow step. Fix: reuse the batched fs.promises.readFile loader (CONCURRENCY 32). compound: CG-012.

**F-PF-010 [LOW · performance · T3]** modes-report dedupeByCompany norm() runs ~5 full-string regex scans per stock name. file: generate-modes-report.js:191-216. norm() chains accent-class regexes (re-created as literals each call) + suffix + non-alnum over every name; ~5 full-string scans × N. Fix: combine accent replaces into one map-based pass or String.normalize('NFD'); ASCII fast-path. compound: none.

**F-CI-007 [MEDIUM · silent-corruption · T1]** Health-check allowlist misses generate-screener: crash deploys stale screener.html with 404ing detail payload. file: daily-pull.yml:393-397,660-697; scripts/pipeline-health-check.js:19-27. EXPECTED_SCRIPTS omits generate-screener (and several others); a crash (continue-on-error) still passes health-check → Deploy publishes yesterday's committed screener.html with no staleness banner, and screener-detail.json (gitignored) is absent so every detail-modal fetch 404s. Fix: add generate-screener to EXPECTED_SCRIPTS; hard-fail Deploy on stale screener.html timestamp or missing detail JSON. compound: CG-014. (filed MEDIUM)

**F-CI-008 [MEDIUM · data-quality · T2]** detect-changes soft-fail leaves alert/trend state stale with no health-check coverage. file: daily-pull.yml:362-366. detect-changes runs continue-on-error and isn't in EXPECTED_SCRIPTS; a crash (e.g. on corrupt alert-state) keeps the run green, Discord alerts silently stop, and the next run double-reports every intervening change as "new today". Fix: emit a pipeline-health report from detect-changes + add to EXPECTED_SCRIPTS, or add an alert-state freshness probe. compound: CG-014. (filed MEDIUM)

**F-CI-009 [LOW · ci-config · T1]** Stale operational documentation inside live gates (18% vs 13%, 180 vs 240 min). file: daily-pull.yml:304,355-358; monthly-sec-xbrl.yml:8-9. The coverage warning says 18% but the gate is 13%; the monthly comment justifies its cron by a 180-min daily timeout that's been 240 since Tag 219 — the shared concurrency group, not cron spacing, is the only real overlap protection. Fix: correct the warning to 13%; update the monthly comment. compound: none.

**F-CI-010 [LOW · ci-config · T2]** Watchlist sanity floor of 200 is 0.8% of the actual universe — gate cannot catch real corruption. file: daily-pull.yml:134-151. Verify Watchlist Sanity hard-fails only if size<200 while the watchlist is 23,748; a refresh/prune mishap leaving 1,200 tickers sails through and every downstream artifact is computed on the remnant. Fix: relative gate, fail if size < max(10000, 0.6×previous committed size). compound: none.

**F-CI-011 [LOW · concept-flaw · T3]** Daily cron runs 7 days/week incl. weekends/holidays; top-of-hour 02:00 slot adds delay. file: daily-pull.yml:4-5. Cron has no weekday/holiday filter — weekends burn ~8h re-pulling unchanged quotes and produce near-duplicate Sat/Sun vintages that dilute walk-forward independence; '0 2 * * *' is a congested slot. Fix: '17 2 * * 2-6' (Mon–Fri US) off-peak; optionally short-circuit on market holidays. compound: none.

**F-CI-012 [LOW · ci-config · T3]** Actions pinned to mutable major tags (@v4), not commit SHAs. file: daily-pull.yml:58,64; monthly-sec-xbrl.yml:37,42. checkout@v4 / setup-node@v4 are mutable tags in a scheduled workflow holding contents:write; a yanked/compromised tag executes with write access on the next cron. Fix: pin to full commit SHAs with a tag comment, let Dependabot bump. compound: none.

## Compound Bug Groups

### Group CG-001: Backtest evidence collapse — F-BT-001, F-BT-002, F-BT-004, F-BT-010, F-BT-011, F-BT-012, F-BT-014, F-BT-015
Interaction: F-BT-001 (prices frozen at 05-17) is the root that empties the entire forward-return supply; F-BT-002 (3-point SPY series) is the same starvation on the benchmark leg, and because computeBenchmarkReturn anchors every vintage's canonical entry/exit dates to that one sparse series (verified at walk-forward-perf.js:275-307), a null benchmark window cascades into null pick/universe lookups for the whole vintage. F-BT-004's missing min-vintage gate then lets methodology-report surface a confident benchmark alpha from the 1–2 surviving vintages, while F-BT-010/011/014/015 mean those few surviving vintages are themselves survivor-tilted, score-inconsistent, style-mismatched, and gross-of-cost. Combined impact: every alpha/effectiveness number driving method keep/retire and score-weight changes is fabricated from a tiny, biased, mislabeled slice — the single highest-confidence "wrong results" risk in the audit.

### Group CG-002: SEC XBRL dead workflow — F-CI-001, F-CI-002
Interaction: F-CI-001 (output written to a gitignored dir on an ephemeral runner, no consumer) means the data product evaporates seconds after each run; F-CI-002 (slice(0,1500) with no rotation + manifest-driven 304s counted as coverage) means that even if a consumer were built, scheduled runs would deliver ≤15% of the universe and after month 1 mostly nothing while the gate reports healthy. Combined impact: a monthly job that is simultaneously a no-op and architecturally incapable of ever not being a no-op, paying real runner/bandwidth cost while fabricating a coverage signal.

### Group CG-003: Yahoo pull throttle triad — F-DP-001, F-DP-002, F-DP-007
Interaction: F-DP-001 couples worker count and the shared yf HTTP queue to one env so parallelism can't rise without raising 429 exposure; F-DP-002's shared-`idx` tail throttle (line 2140) removes pacing on the last CONCURRENCY-1 tickers exactly when the queue is most saturated; F-DP-007's FTS leg then 429s with no retry (line 1781 catches only timeout/ECONNRESET) and discards the already-paid quoteSummary work. Combined impact: 429 storms concentrate at run tails and on the FTS leg, inflating n_failed and runtime precisely when the system should be backing off — a direct contributor to 163-min pulls (user's slow-screener priority).

### Group CG-004: CI state/commit/bloat coupling — F-CI-003, F-CI-004, F-CI-005, F-CI-006, F-SM-006, F-PF-007
Interaction: F-CI-003 (gitignored snapshots make staleness rotation + price-only a no-op) forces a full-universe fundamentals pull every run, pushing runtime toward the 240-min cap; F-CI-006's single end-of-job commit then converts any cap-timeout into total loss of that day's runner-local data (~19% of days, the source of F-BT-012's vintage gaps); F-CI-004's ever-growing full-history checkout and 100MB-bound state blobs make each run slower and threaten a hard push-block; F-CI-005's manual-dispatch race produces unresolvable rebase conflicts on those same monolithic state files. Combined impact: a self-reinforcing slow/fragile commit pipeline where one defect (no persisted rotation state) inflates runtime, which triggers timeouts, which lose data, which appear downstream as silent backtest gaps.

### Group CG-005: FTS single-point-of-failure — F-DP-003, F-DP-004, F-DP-012
Interaction: F-DP-003 establishes that legacy quoteSummary fundamentals are dead and FTS is the sole source with no backstop; F-DP-004 shows FTS extraction is exact-string matching against an evolving schema (a rename → silent universe-wide null); F-DP-012 shows the FTS quarterly ordering is trusted without date validation. Combined impact: a single FTS field rename or ordering flip silently empties or inverts fundamentals across the whole universe with no error and no working fallback — the failure mode this file's history keeps re-discovering by manual probe.

### Group CG-006: Backtest look-ahead/window family — F-BT-003, F-BT-007, F-BT-009, F-BT-013
Interaction: F-BT-003 (full-span return mislabeled as a fixed horizon) and F-BT-013 (today's fundamentals vs past prices) corrupt the legacy backtest scripts' windows, while F-BT-007 (DST misclassification) and F-BT-009 (entry backward-snap) leak small same-day/weekend-gap advantages into walk-forward entries. Combined impact: measured returns are biased upward through several independent window/look-ahead leaks; mild individually but compounding in the same optimistic direction.

### Group CG-007: Statistical over-confidence — F-BT-005, F-BT-006
Interaction: F-BT-005 (overlapping daily vintages resampled as i.i.d.) shrinks every bootstrap CI, and F-BT-006 (~300 simultaneous uncorrected CI tests) multiplies the false-positive rate on top of those already-too-tight intervals. Combined impact: ciLo95>0 "significant" verdicts feeding method promotion/retirement are doubly anti-conservative — narrow CIs on many uncorrected tests guarantee spurious significance.

### Group CG-008: modes-report redundant recompute — F-PF-002, F-PF-003
Interaction: F-PF-002 re-runs the full per-mode eligibility+sort after buildHtml already computed it (just to log counts), and F-PF-003's topAllMust(...,9999).length sorts the entire passing set only to count it. Combined impact: the most expensive per-mode work is performed two-to-three times per render for output that is either already available or needs no sort.

### Group CG-009: refresh-universe degradation — F-DP-009, F-DP-010, F-DP-013
Interaction: F-DP-009 (serial untimed sweep) makes the refresh slow and hang-prone; F-DP-010 (off-by-one 429 retry) silently drops a region/bucket under sustained 429; F-DP-013 (unconditional watchlist rewrite + fresh timestamp) makes the resulting degraded refresh indistinguishable from a healthy one. Combined impact: a Yahoo-side throttle can slowly starve universe growth while every run reports success, stagnating the universe undetected.

### Group CG-010: fcf-yield gate mismatch — F-ME-202, F-ME-203
Interaction: F-ME-202 turns the fcf-yield pass gate into "beat ~half your sector" (median threshold, no positive-only guard), and F-ME-203 calibrates that median on raw FCF while gating SBC-adjusted yield. Combined impact: the fcf-yield gate is both economically un-anchored (≈50% pass, can go negative) and systematically biased against SBC-heavy names — wrong pass/fail and wrong 0.05-weight score contribution in QC.

### Group CG-011: Missing run-level data-quality alarms — F-DQ-002, F-DQ-003, F-DQ-006
Interaction: F-DQ-002 (universe-shrink deletions only INFO-logged), F-DQ-003 (price-only persistence blinds drift detection ~7d), and F-DQ-006 (FX-fallback has no run-level threshold) are three independent silent-degradation channels, none of which feeds an aggregate alarm. Combined impact: a Yahoo schema/FX change can shrink the universe, mask the coverage drop, and misprice non-USD legs simultaneously while the run reports healthy — survivor bias plus mispricing with no signal.

### Group CG-012: Serial snapshot loaders un-migrated — F-PF-005, F-PF-008
Interaction: Both generate-methods-report and sector-medians-compute still load the full snapshot universe with serial readFileSync loops the modes/history loaders already replaced with 32-way async batches. Combined impact: ~1–3 min of avoidable event-loop-blocking I/O each, on two separate workflow steps, scaling with universe size.

### Group CG-013: score-history/r40rx-history filename-helper drift — F-SM-004, F-SM-005
Interaction: F-SM-004 (raw ticker+'.json' bypassing safeSnapshotFilename) and F-SM-005 (_meta.json mixed into the per-ticker dir) re-introduce in the two newer history scripts exactly the filename-safety and dir-walk footguns that snapshot-fs.js / the methods-history _manifest fix were created to eliminate. Combined impact: latent today, but a future reserved-name/special-char ticker or a dir-enumerating consumer hits a write failure or phantom ticker.

### Group CG-014: Soft-fail report steps invisible to health-check — F-CI-007, F-CI-008
Interaction: Both generate-screener and detect-changes run continue-on-error and are absent from EXPECTED_SCRIPTS, so a crash in either keeps the run green; F-CI-007 then deploys a stale screener.html with a 404ing detail payload while F-CI-008 silently freezes the alert/trend stream and double-reports on recovery. Combined impact: the health-check allowlist's gaps let two user-facing failure modes (stale dashboard, dropped alerts) ship inside a green run.

## Agent Coverage

| Agent | Files Examined | Findings | Confidence |
|---|---|---|---|
| data-pipeline | 11 (pull-yahoo.js, refresh-universe.js, refresh-fx.js, discovery/*, lib/atomic-write.js) | 13 (2H / 7M / 4L) | 0.71 |
| methods-engine | 22 (runner, _helpers, score-aggregator, sector-medians-compute/lookup, strategy-modes, ~14 method plugins, report gens) | 6 (1H / 2M / 3L) | 0.72 |
| state-management | 13 (detect-changes, snapshot-*, archive, atomic-write, snapshot-fs, generate-screener, daily-pull.yml) | 6 (1H / 3M / 2L) | 0.72 |
| ci-workflow | 11 (daily-pull.yml, monthly-sec-xbrl.yml, pull-yahoo, pull-sec-xbrl, detect-changes, refresh-universe, pipeline-health-check, trend, watchlist, git history) | 12 (1C / 5H / 2M / 4L) | 0.85 |
| data-quality | 4 (data-quality.js, field-coverage.js, pull-yahoo.js, data-quality-report.js) | 7 (1H / 3M / 3L) | 0.72 |
| backtest | 11 (walk-forward-perf, method-effectiveness, snapshot-picks, methodology-report, backtest-*, compute-picks-lookback, picks-history, prices/history.json) | 16 (2C / 4H / 8M / 2L) | 0.82 |
| performance | 8 (pull-yahoo, generate-methods-report, generate-modes-report, sector-medians-compute, runner, snapshot-methods-history, _helpers, strategy-modes) | 10 (2H / 3M / 5L) | 0.78 |

Note on dedup: F-SM-002 (state-management, MEDIUM) and F-CI-005 (ci-workflow, HIGH, T1) describe the same manual-dispatch-vs-cron race on the same shared state files; merged under F-CI-005 (higher tier), noted "also reported by state-management". Total after dedup: 50.
