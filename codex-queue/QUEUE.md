# Codex-Queue screener-data — Lauf 2026-09-24 (bis 20:45)

Diese Datei wird von Claude laufend erweitert. Vor JEDEM neuen Task:
`git fetch origin claude/codex-limit-optimization-2u1g6h` und diese Datei neu lesen
(`git show origin/claude/codex-limit-optimization-2u1g6h:codex-queue/QUEUE.md`).
Tasks von oben nach unten in der eigenen LANE abarbeiten. Erledigte Tasks stehen in deinem
eigenen Log (PR-Liste), nicht hier.

## Regeln (bindend, zusaetzlich zu AGENTS.md / CLAUDE.md)
- Arbeit NUR in einem eigenen Worktree ausserhalb des Repos:
  `git fetch origin main && git worktree add ..\codex-wt\screener-data\<ID> origin/main`
  (Branch `codex/<ID>`). Karls Haupt-Working-Tree nie anfassen.
- TABU-Pfade (automatisches Rot): alles in `.codex-deny.txt`, zusaetzlich `src/scoring/**`,
  `methods/**`, `tests/scoring/**`, `.github/**`, `*-history/`, `earnings-calendar.json`,
  `README.md` (Engine-Wahrheit), `docs/findash-export-v1*`.
- Nie: Dateien loeschen, Tests abschwaechen (kein skip/only/todo, keine gelockerten
  Assertions), Dependencies hinzufuegen, Netz-Calls in Tests, Force-Push.
- Neue Tests = Standalone-Runner wie die bestehenden `lib/*.test.js` (`node <datei>` → Exit 0/1,
  `assert` aus node:assert, keine Frameworks). Muster: `lib/alter-test-vorlage` gibt es nicht —
  nimm eine existierende `lib/*.test.js` als Vorlage.
- Commit: `Tag <n>: <Betreff>` (n = hoechste Tag-Nummer in `git log --oneline` + 1; Kollisionen
  zwischen Lanes sind ok). Gezielt per Pathspec committen.
- Push `codex/<ID>` + `gh pr create --base main --title "<ID>: <Betreff>"` mit Body im MELDEFORMAT:
  ```
  ## ERGEBNIS
  <was gebaut wurde, 3-6 Zeilen>
  ## VERIFIZIERT
  <exakte Befehle + Exit-Codes / Zaehler vorher→nachher>
  ## OFFEN / RISIKO
  <was nicht ging, was Claude pruefen soll>
  ```
  Wenn `gh` fehlschlaegt: nur pushen, PR-Erstellung im Schlussbericht als offen vermerken.
- Nach jedem Task: Worktree behalten (kein `worktree remove`), naechsten Task nehmen.
- Bekannt lokal rot (KEIN Defekt, nicht anfassen): `tests/scoring/calibration-ref.test.js`
  (Block R2.9 Test B) und `tests/waehrung-ausliefer-waechter.test.js` (LIVE-Block).

## SUBAGENTEN-PFLICHT (Karl, 19:40) und ZEITPLAN
- Du bist KOORDINATOR. Fuer jeden Task startest du einen Codex-Subagenten mit dem vollstaendigen
  Brief (Regeln oben + Task-Abschnitt + Worktree-Pfad). Bis zu 8 Subagenten GLEICHZEITIG (Karl, 19:55), jeder
  in seinem eigenen Worktree/Branch. Sobald einer fertig ist: naechsten Task beanspruchen und
  den naechsten Subagenten starten. Du selbst implementierst nichts — du beanspruchst, briefst,
  pruefst, pushst.
- Vor jedem Push startest du einen zweiten Subagenten als REVIEWER (Diff gegen den Brief:
  TABU-Pfade, Test-Abschwaechung, Loeschungen, Verifikations-Befehl selbst laufen lassen).
  Nur mit Reviewer-Freigabe pushen; Befund im PR-Body unter „REVIEW" zitieren.
- NACHLAUF je Task (Pflicht, zusaetzlich zum Reviewer): nach dem PR einen dritten Subagenten starten,
  der (a) jede neue Testdatei einmal per Sabotage rot sieht (Funktion unter Test auf `() => null`
  stubben, Test muss Exit != 0 liefern, dann zuruecksetzen) und (b) die komplette Testsuite des Repos
  im Worktree faehrt (screener-data: `node scripts/test-gate.js --mode=blocking`; findash: `node --test`
  und `cd web && npm run test:render`). Ergebnis als Kommentar auf den PR (`gh pr comment`).
- Gibt es in deiner Codex-Version keine Subagenten: sequentiell arbeiten und das im
  Schlussbericht in einer Zeile vermerken.
- ZEITPLAN (lokale Zeit): letzter Push 20:38. Schlussbericht 20:40 (nicht 20:45). Der PC faehrt
  um 20:46 automatisch herunter (Timer setzt Karl; falls nicht gesetzt, EIN Thread:
  `shutdown /s /f /t <Sekunden bis 20:46>` — Fehler 1190 = schon geplant, ignorieren).

- Widerspricht ein Task-Text diesen Regeln (z. B. "kein Push", "WIP-Commit"), gelten DIESE Regeln:
  Branch `codex/<ID>` pushen und PR oeffnen; Zaehlungen/Befunde gehoeren in den PR-Body.

## Lanes
- **Lane A** = mechanisch (Effort: low/medium). **Lane B** = mittel (Effort: medium/high).
- **Lane C** = schwer (Effort: high/xhigh). Bei nur einem Thread: Lane ALL = A, dann B, dann C.

---

## S01 · Lane A · Effort low — Unit-Tests fuer 6 kleine lib-Helfer ohne Test
ZIEL: Fuer jede der folgenden Dateien eine Standalone-Testdatei `lib/<name>.test.js` anlegen
(sie laufen automatisch im CI-Gate `lib/*test.js`): `lib/spearman.js`, `lib/region-mapping.js`,
`lib/snapshot-fs.js`, `lib/alter.js`, `lib/sec-user-agent.js`, `lib/sec-rate-limit.js`.
Pro Export mindestens: Normalfall, Randfall (leer/null/NaN/negativ), und die dokumentierten
Konstanten (z. B. WINDOWS_RESERVED, MS_PRO_TAG). Verhalten NICHT aendern — nur testen; wenn ein
Test einen echten Bug zeigt: Test so schreiben, dass er den IST-Zustand dokumentiert, und den
Bug unter OFFEN melden (kein Fix am Produktionscode in diesem Task).
ZIEL-DATEIEN: nur die 6 neuen `lib/*.test.js`.
FERTIG-WENN: alle 6 Dateien laufen mit `node lib/<name>.test.js` Exit 0; jede hat >= 8 Assertions.
VERIFIKATION: `for f in lib/spearman.test.js ...; do node $f; echo $?; done` (PowerShell-Aequivalent).

## S02 · Lane B · Effort medium — Unit-Tests fuer 5 groessere lib-Module ohne Test
ZIEL: Standalone-Tests fuer `lib/fetch-retry.js` (Retry/Pause-Logik ueber injizierten fetch,
`_internals.warteAufSlot`), `lib/zip-stream.js` (Roundtrip mit In-Memory-Puffer),
(NICHT `lib/sub-profile.js`: hat keinen Konsumenten mehr im Repo, 0 % Abdeckung — nur im PR-Body unter OFFEN als toter Kandidat melden, kein Test, kein Loeschen),
`lib/ledger-single-appender.js` (`checkSingleAppender`/`resolveBaseRef` in einem tmp-Git-Repo),
`lib/sec-pit.js` (reine Funktionen aus den Exports; Datei-IO ueber tmp-Dir).
Kein Netz. Kein Zugriff auf echte `snapshots/`, `prices/`, `picks-history/`.
ZIEL-DATEIEN: neue `lib/fetch-retry.test.js`, `lib/zip-stream.test.js`,
`lib/ledger-single-appender.test.js`, `lib/sec-pit.test.js`.
FERTIG-WENN: alle 4 Exit 0, je >= 10 Assertions, Laufzeit je < 10 s.
VERIFIKATION: `node lib/<name>.test.js` je Datei + Laufzeit.

## S03 · Lane B · Effort medium — `lib/price-history-store.js` Tests + Fehlerpfade
ZIEL: Standalone-Test `lib/price-history-store.test.js`: alle Exports gegen ein tmp-Verzeichnis
(Anlegen, Anhaengen, Lesen, korrupte Datei, fehlendes Verzeichnis, doppelte Datumszeile).
Dokumentiere jedes Verhalten, das du fuer einen Bug haeltst, unter OFFEN — kein Produktions-Fix.
ZIEL-DATEIEN: `lib/price-history-store.test.js`.
FERTIG-WENN: Exit 0, >= 12 Assertions, keine Spuren ausserhalb des tmp-Dirs.

## S04 · Lane C · Effort high — Voll-Gate lokal fahren und Befund schreiben
ZIEL: Die komplette CI-Gate-Suite lokal laufen lassen: `tests/*test.js`, `tests/scoring/*test.js`,
`lib/*test.js` (jede Datei `node <datei>`, Exit-Code + Laufzeit protokollieren; 30-45 min).
Ergebnis als `reports/codex-gate-lauf-2026-09-24.md`: Tabelle Datei | Exit | Sekunden, dann
Abschnitt „Rot" mit den ersten 30 Fehlerzeilen je roter Datei. Die zwei bekannt-lokal-roten
Dateien (siehe Regeln) als „bekannt/Umgebung" markieren. Fuer jede WEITERE rote Datei ausserhalb
`tests/scoring/**`: Ursache analysieren und — wenn der Fix klar ausserhalb von Scoring/Methoden
liegt — im selben PR fixen; sonst nur Diagnose mit Datei:Zeile.
ZIEL-DATEIEN: `reports/codex-gate-lauf-2026-09-24.md` + ggf. Fix-Dateien ausserhalb der TABU-Pfade.
FERTIG-WENN: Report vollstaendig (alle Dateien gelistet), PR offen.

## S05 · Lane A · Effort low — Standalone-Tests fuer CLI-Helfer `earnings-cli.js` / `watchlist-cli.js`
ZIEL: `tests/cli-helfer.test.js`: beide CLIs als Kindprozess (`child_process.spawnSync`, `node
<cli> --help` bzw. ohne Argumente) → Exit-Code, keine Exception, Hilfetext enthaelt jede
dokumentierte Option. Zusaetzlich: unbekannte Option → Exit != 0 und Fehlermeldung auf stderr.
Wenn eine CLI heute bei unbekannter Option Exit 0 liefert: Test dokumentiert IST, Befund unter OFFEN.
ZIEL-DATEIEN: `tests/cli-helfer.test.js`.
FERTIG-WENN: Exit 0, laeuft ohne Netz und ohne echte Snapshots.

## S06 · Lane A · Effort low — rule40: Reserved-Name-Snapshots (`_CON.json`) gehoeren zum Universum
ZIEL: Pinnt cec884f "Tag 1346: rule40 uses isMetadataSnapshot instead of the blanket underscore filter" — Funktion `sammleKandidaten(opts)` in `scripts/write-rule40-export.js`, die Zeile `const dateien = fs.readdirSync(snapshotsDir).filter((f) => f.endsWith('.json') && !isMetadataSnapshot(f));` (Z. ~440). Der Waechter `tests/p1-welle8-metadata-filter.test.js` prueft nur den QUELLTEXT auf Blanket-Filter; ein VERHALTENS-Pin fehlt. Neuer Standalone-Runner `tests/rule40-reserved-name-snapshot.test.js` (Muster: `tests/rule40-universe.test.js` Z. 150-210, Fixture-Bauer `baueExport()` aus `tests/rule40-fixture.js`, `W = require('../scripts/write-rule40-export.js')`). Faelle:
(1) Fixture mit zwei normalen Eintraegen bauen. Danach den JSON-Inhalt eines der beiden geschriebenen Snapshots aus `f.snapshotsDir` lesen, `meta.ticker = 'CON'` und `meta.name = 'CON Inc'` setzen (eigener Name — der Emittenten-Dedup gruppiert ueber den Firmennamen) und als `_CON.json` in `f.snapshotsDir` schreiben; zusaetzlich `_manifest.json` = `{"pulled_at":"2026-09-17T00:00:00Z","n_total":1,"n_ok":1}` und `_last_good_disk.json` = `{}` daneben legen. `W.sammleKandidaten({ v1Dir: f.v1Dir, snapshotsDir: f.snapshotsDir })` muss `gelesen === 3` liefern (2 + `_CON`; NICHT 5) und `abgewiesen.snapshotUnlesbar === 0`.
(2) Der `_CON`-Kandidat steht in `res.kandidaten`. Welches Identitaetsfeld er traegt (Dateiname-Ticker `_CON` via `datei.slice(0, -5)`, `meta.ticker` `CON` oder `meta.name`), aus `scripts/write-rule40-export.js` Z. 440-564 ablesen und GENAU das heutige Verhalten pinnen — nicht das "richtige".
(3) Gegenprobe: ohne `_CON.json`, aber mit den beiden Metadateien, ist `gelesen === 2`.
Rueckgabeform: `{ index, kandidaten, abgewiesen, gelesen, aufBrett }` (Z. 564).
ZIEL-DATEIEN: neu `tests/rule40-reserved-name-snapshot.test.js`. `scripts/**`, `src/**`, `tests/scoring/**` unveraendert.
FERTIG-WENN: `node tests/rule40-reserved-name-snapshot.test.js` Exit 0. Break-once: den Filter in `sammleKandidaten` temporaer auf `f.endsWith('.json') && !f.startsWith('_')` zuruecksetzen → neuer Test rot (`gelesen` 2 statt 3). Erwartet ebenfalls rot in diesem Zustand: `tests/p1-welle8-metadata-filter.test.js` (Quelltext-Waechter, KONSUMENTEN_MIN 23) — im PR als bestaetigte Doppelabsicherung nennen. Wiederherstellen → beide gruen; Ausgaben im PR-Body.
VERIFIKATION: `node tests/rule40-reserved-name-snapshot.test.js`; danach der komplette Gate-Glob: `Get-ChildItem tests/*test.js,tests/scoring/*test.js,lib/*test.js | ForEach-Object { node $_.FullName; if ($LASTEXITCODE) { "FAIL: $($_.Name)" } }` — erlaubt sind nur die zwei bekannten lokalen Roten (`tests/scoring/calibration-ref.test.js` R2.9 Test B, `tests/waehrung-ausliefer-waechter.test.js` LIVE-Block); jede dritte rote Datei stoppt den Task.

## S07 · Lane A · Effort medium — Mess-Skripte T135 / T326: CLI-Vertrag und Selbsttest pinnen
ZIEL: Pinnt 596775b "T135 option B: measure the parity gap between productionCohortRanking and the small-cap board" und d7e3b0e "T135 script: smoke runs on the stale store are labelled non-evidence and get their own default path" (`scripts/t135-paritaetsluecke.js`: `arg()` Z. 19-25, `fail()` Z. 27, `main()` Z. 29-40 mit der Altbestand-Wache Z. 36) sowie b421bcc "T326: both stale-fundamentals definitions on ONE CI population" (`scripts/t-veraltung-zwei-definitionen.js`: `main(args)` Z. 200-212, `selftest()` Z. 174-198, `loadRules()`). Beide Skripte laufen ausschliesslich als CLI (`main()` beim Laden bzw. `require.main`), deshalb Kindprozess-Tests mit `spawnSync(process.execPath, [script, ...args], { encoding: 'utf8', cwd: REPO, timeout: 120000 })` — Muster `tests/annual-spikes.test.js` Z. 401-410. T135 laedt beim Start `src/scoring/run-screener.js` & Co. (nur lesen, das ist erlaubt), rechne mit einigen Sekunden je Aufruf. Neuer Standalone-Runner `tests/mess-skripte-cli-vertrag.test.js`:
T135 (Temp-Basis via `mkdtemp`):
(a) ohne Argumente → `status === 1`, stderr enthaelt `--snapshots <dir> fehlt`.
(b) `--snapshots <tmp>/leer --vintage 21.09.2026` (Ordner existiert, leer) → 1, stderr `--vintage YYYY-MM-DD fehlt`.
(c) `--snapshots <tmp>/snapshots-smallcap --vintage 2026-09-21` (Ordner leer, aber exakt so benannt) → 1, stderr `lokales snapshots-smallcap/ ist Altbestand`.
(d) wie (c) plus `--lokal-kein-beleg` → weiterhin 1, aber stderr enthaelt `leere Small-Cap-Population` und NICHT `Altbestand` — Beleg, dass der Schalter die Altbestand-Wache umgeht und erst die naechste Wache greift. Sollte auf dem leeren Ordner eine andere Wache frueher werfen (z. B. aus `loadSmallcapUniverse`), GENAU deren Meldung pinnen, zusaetzlich `NICHT Altbestand` behalten und das im PR nennen.
(e) `--snapshots --vintage 2026-09-21` → 1, stderr `--snapshots ohne Wert`.
Fuer (a)-(e): `fs.readdirSync(path.join(REPO, 'reports'))` vor und nach dem Lauf vergleichen — es darf keine Datei entstehen.
T326:
(f) `--selftest` → `status === 0`, stdout enthaelt `SELFTEST PASS: 3 snapshots; both=1 / only D1=1 / only D2=0 / neither=1; counters=2/1/1/0` und `PASS: strict 7d/30d boundaries`. Das Skript laesst seine Fixtures in OS-Temp absichtlich liegen ("Temporary fixtures retained") — Skriptverhalten, nicht "reparieren" und nicht als Testmuell werten.
(g) `--now 2026-09-20T00:00:00Z` ohne Verzeichnis → 1, stderr `--now requires population-dir`.
(h) `--now` ohne Wert → 1, stderr `Missing/repeated --now`.
(i) `--foo` → 1, stderr `Unexpected argument: --foo`.
Fuer (f)-(i): `reports/t-veraltung-zwei-definitionen-2026-09-20.md` bleibt byte-identisch (Hash vorher/nachher).
Dokumentierte Grenze (als Kommentar im Test, keine Assertion): Reportpfad `-RAUCHTEST` und Kopfzeile "RAUCHTEST, KEIN BELEG" aus d7e3b0e sind ohne echte Small-Cap-Population nicht erreichbar (`runSmallcapPass` braucht Snapshots) und bleiben ungepinnt.
ZIEL-DATEIEN: neu `tests/mess-skripte-cli-vertrag.test.js`. Beide Skripte unveraendert.
FERTIG-WENN: Runner Exit 0. Break-once ZWEIMAL: (1) in `scripts/t135-paritaetsluecke.js` Z. 36 den Teil `&& !process.argv.includes('--lokal-kein-beleg')` temporaer entfernen → Fall (d) rot (stderr zeigt `Altbestand`); (2) in `scripts/t-veraltung-zwei-definitionen.js` Z. 210 die Assertion `'--now requires population-dir'` temporaer entfernen (Zeile zu `if (!directory) return;` machen) → Fall (g) rot. Beide wiederherstellen → gruen; alle vier Ausgaben im PR-Body.
VERIFIKATION: `node tests/mess-skripte-cli-vertrag.test.js`; danach der komplette Gate-Glob wie in S-HIST1 (gleiche Ausnahmeregel fuer die zwei bekannten lokalen Roten).

## S08 · Lane B · Effort high — k-Drift-Report: der Schlusssatz zaehlt seine eigenen Staende
ZIEL: Pinnt 244781b "k-drift report: the verdict sentence counts its own inputs (#349)" (Skript-Neuzugang: c5dd239 "Durchsatz-Lesehilfe + k-Drift-Messung") — `scripts/t-kdrift-signatur.js` `main()` Z. ~163-172: `const genugStaende = runs.length >= 3;` und `urteilsHinweis` = "`${runs.length} Stände verglichen; drift verlangt zusätzlich in JEDEM Schritt eine Veränderung ungleich null.`" bzw. "`${runs.length} Stände können eine Drift-Richtung nicht belegen.`"; der Satz steht in `summary` (`console.log(summary)` am Ende von `main()`) und im Report. Hindernis: das Skript liest `<ROOT>/data-health/annual-spikes-baseline.json` und `<ROOT>/scripts/watch-annual-spikes.js` und schreibt seinen Report an den FESTEN Pfad `<ROOT>/reports/t-kdrift-signatur-2026-09-20.md` mit `ROOT = path.resolve(__dirname, '..')`. Ein Test darf den eingecheckten Report NICHT ueberschreiben. Loesung: Temp-ROOT nachbauen — `mkdtemp`, darin `scripts/` mit byte-identischen Kopien von `t-kdrift-signatur.js` UND `watch-annual-spikes.js` (das Skript extrahiert daraus per Regex reine Funktionen), `data-health/annual-spikes-baseline.json` synthetisch = `{"hinweis":"test","faelle":[]}` (`main()` nutzt nur `baseline.faelle`), leeres `reports/`. Populationen: N Verzeichnisse `lauf1..laufN`, jedes mit `AAA.json` = `{"meta":{"ticker":"AAA"},"annual":{"annualRev":[{"value":1000000},{"value":100000000},{"value":2000000}]}}` — exakt die Reihe des eingebauten Self-Checks (`findeAusreisser(...)[0].index === 1`), damit ein Fund entsteht und die Tabellen-Schleife durchlaufen wird. Aufruf `spawnSync(process.execPath, [tmpRoot + '/scripts/t-kdrift-signatur.js', lauf1, lauf2(, lauf3)], { encoding: 'utf8' })`. Neuer Standalone-Runner `tests/t-kdrift-signatur-schlusssatz.test.js`:
(1) 2 Staende → `status === 0`; stdout UND Report enthalten `2 Stände können eine Drift-Richtung nicht belegen` und NICHT `Zwei Stände` und NICHT `Stände verglichen`.
(2) 3 Staende → `status === 0`; stdout UND Report enthalten `3 Stände verglichen; drift verlangt zusätzlich in JEDEM Schritt` und NICHT `können eine Drift-Richtung nicht belegen`.
(3) 1 Stand → `status === 1` (Usage-Assertion `args.length >= 2`).
(4) derselbe Ordner zweimal → `status === 1`, stderr `Repeated run directory`.
(5) nach jedem Lauf: Baseline im Temp-ROOT byte-identisch (das Skript prueft das selbst — der Test bestaetigt es unabhaengig per Hash), `reports/t-kdrift-signatur-2026-09-20.md` im Temp-ROOT existiert nach (1)/(2), und der ECHTE Repo-Report `reports/t-kdrift-signatur-2026-09-20.md` ist unveraendert (Hash vorher/nachher).
Umlaute als `ä`/`ö` im Test.
ZIEL-DATEIEN: neu `tests/t-kdrift-signatur-schlusssatz.test.js`. `scripts/t-kdrift-signatur.js`, `scripts/watch-annual-spikes.js`, `data-health/**`, `reports/**` unveraendert.
FERTIG-WENN: Runner Exit 0 in unter 30 s. Break-once: in `scripts/t-kdrift-signatur.js` `const genugStaende = runs.length >= 3;` temporaer auf `const genugStaende = false;` setzen → Fall (2) rot; wiederherstellen → gruen; beide Ausgaben im PR-Body. Achtung: der Test kopiert das Skript aus dem Repo-Stand — der Revert muss im Repo passieren, nicht in der Temp-Kopie.
VERIFIKATION: `node tests/t-kdrift-signatur-schlusssatz.test.js`; `git status --short reports/ data-health/` leer; danach der komplette Gate-Glob wie in S-HIST1.

## S09 · Lane B · Effort medium — Test-Coverage-Report `scripts/test-coverage-report.js` + Gate-Test
ZIEL: Ein dependency-freies Node-Skript, das eine Menge Standalone-Tests mit `NODE_V8_COVERAGE` in ein tmp-Verzeichnis faehrt und daraus eine Zeilen-/Funktions-Abdeckung je Datei unter `lib/` und `scripts/` tabelliert. ACHTUNG NAME: `scripts/coverage-gate.js` EXISTIERT bereits und ist der PULL-Coverage-Gate (Snapshot-Abdeckung, daily-pull.yml) — nicht anfassen, nicht ueberschreiben; das neue Skript heisst `scripts/test-coverage-report.js`. Mechanik (2026-09-24 auf Node 22.22 bewiesen): (1) je Testdatei `child_process.spawn(process.execPath, [datei], { cwd: ROOT, env: { ...process.env, NODE_V8_COVERAGE: tmpDir } })`, `--parallel` (Default 4), `--timeout-ms` (Default 180000; bei Ueberschreitung SIGKILL und `TIMEOUT` in der Kopfzeile — Coverage eines gekillten Prozesses geht verloren, das ist zu sagen, nicht zu verstecken); Kindprozesse der Tests erben die Variable und werden mitgemessen (gewollt: so werden per spawn gestartete `scripts/*.js` sichtbar). (2) Danach alle `coverage-*.json` im tmpDir lesen; je `result[]`-Eintrag mit `url` beginnend `file://` den Pfad ueber `require('url').fileURLToPath(url)` bilden (NICHT `url.slice(7)` — auf Windows lautet die URL `file:///C:/...`), auf `--only`-Praefixe (Default `lib/,scripts/`, relativ zu `--root`, Slashes normalisiert) filtern, `*test.js` und `node_modules` ausschliessen. (3) Offsets: V8 meldet `functions[].ranges[]` mit `startOffset/endOffset/count` relativ zum ROHEN Dateiinhalt (bewiesen: der Top-Level-Range endet bei 4 geprueften lib-Dateien exakt bei `source.length`; kein CJS-Wrapper-Versatz, Shebang inklusive). Alle Ranges einer Datei sammeln, nach `start asc, end desc` sortieren, nacheinander in ein `Int32Array(source.length)` (Init -1) schreiben — innere Ranges ueberschreiben aeussere; ueber mehrere Prozesse per Maximum mergen. (4) Zeilen-Heuristik: uebersprungen werden Leerzeilen, `//`-Zeilen, Blockkommentar-Zeilen (`/* ... */`, auch mehrzeilig), Zeilen nur aus `{}[]();,`, `'use strict'`, `#!`. Eine Zeile gilt als abgedeckt, wenn irgendein Nicht-Whitespace-Zeichen count > 0 hat (`\r` ist Whitespace — CRLF-Checkouts muessen dieselben Zahlen liefern). Funktion „nie aufgerufen" = `ranges[0].count === 0`, ausgenommen der Top-Level-Range (start 0, Ende = Dateilaenge); Name `functionName || '(anon)'`, Zeile = Anzahl `\n` vor `startOffset` + 1. (5) Ausgabe: Kopfzeile `# tests: N gelaufen, F rot, T timeout | coverage-json: M | dateien: K`, dann Tabelle aufsteigend nach Prozent: `pct%  covered/lines  fns(unc/total)  file`; `--fns` haengt je Datei `never-called fns: name@zeile, ...` an; `--json <pfad>` schreibt `[{file,total,covered,pct,fnsTotal,fnsUncovered,uncoveredFns:[{name,line}],uncoveredLines:[...]}]`; `--top N` kuerzt die Tabelle. (6) Optionen: `--tests` = kommagetrennte Globs, ein Verzeichnis tief wie `expandGlobs` in `scripts/test-gate.js` (den 12-Zeilen-Helfer KOPIEREN, nicht test-gate requiren — es exit-et beim Laden nicht, aber es ist ein Gate, kein Modul), Default `lib/*test.js,tests/*test.js` (tests/scoring bewusst nicht im Default); `--root <dir>` (Default Repo-Wurzel = `path.join(__dirname, '..')`); `--keep-tmp` behaelt das tmp-Verzeichnis (sonst am Ende `fs.rmSync(recursive)`). (7) Exit 0 immer — ausser Usage-Fehler oder kein einziger Test gefunden → Exit 2 mit Meldung auf stderr. Das Skript schreibt NIE ins Repo (tmp = `fs.mkdtempSync(path.join(os.tmpdir(), 'test-coverage-'))`). Zweitens `tests/test-coverage-report.test.js` (node:test oder Zaehler-Runner wie `lib/read-json.test.js`; landet ueber den Glob `tests/*test.js` automatisch in der blockierenden CI-Spur, darf also keine 30 s brauchen): (a) Fixture-Miniprojekt im tmp: `<tmp>/lib/mini.js` exportiert `a()` und `b()`, `<tmp>/lib/mini.test.js` ruft nur `a()` und `require('../scripts/s.js')`, `<tmp>/scripts/s.js` exportiert eine Konstante; Aufruf `node scripts/test-coverage-report.js --root <tmp> --tests lib/*test.js --json <tmp>/out.json` → Exit 0, Zeile fuer `lib/mini.js` mit `fnsUncovered === 1`, `uncoveredFns[0].name === 'b'`, `30 <= pct <= 90`; Zeile fuer `scripts/s.js` mit `pct === 100`; KEINE Zeile fuer `lib/mini.test.js`. (b) Echtlauf `--tests lib/metrics.test.js,lib/artifact-path.test.js --json <tmp>/out2.json` → Exit 0; Zeilen fuer `lib/metrics.js`, `lib/spearman.js`, `lib/artifact-path.js` vorhanden; je `0 <= pct <= 100`, `covered <= total`; stdout enthaelt die Kopfzeile und mindestens 3 Tabellenzeilen; `lib/spearman.js` und `lib/artifact-path.js` mit `pct === 100` (Messwert 2026-09-24: 36/36 bzw. 5/5), `lib/metrics.js` mit `hitRate` in `uncoveredFns` (Messwert: 74.7 %, hitRate@110 nie aufgerufen — bleibt so, weil S-COV2 in eine ANDERE Datei testet). (c) `--tests gibtsnicht/*test.js` → Exit 2, stderr nicht leer. (d) Verzeichnisliste der Repo-Wurzel vor/nach (b) identisch (nichts ins Repo geschrieben).
ZIEL-DATEIEN: `scripts/test-coverage-report.js` (neu), `tests/test-coverage-report.test.js` (neu). Sonst nichts — insbesondere NICHT `scripts/coverage-gate.js`, `scripts/test-gate.js`, `.github/**`.
FERTIG-WENN: `node tests/test-coverage-report.test.js` Exit 0 in < 15 s; `node scripts/test-coverage-report.js --tests lib/*test.js --fns` druckt eine Tabelle mit >= 15 Dateien und reproduziert die Referenzwerte vom 2026-09-24 auf +-3 Punkte (lib/metrics.js 74.7 %, lib/read-json.js 57.5 %, lib/atomic-write.js 81.6 %, lib/spearman.js 100 %); `node tests/gate-coverage.test.js` und `node scripts/test-gate.js --selftest` weiter gruen (der neue Test ist ungefragt gegatet); `git status --short` zeigt nur die zwei neuen Dateien.
VERIFIKATION: `node tests/test-coverage-report.test.js; $LASTEXITCODE` → 0. `node scripts/test-coverage-report.js --tests lib/*test.js --fns` (Tabelle pruefen). Vollmessung als Referenz fuer die Folge-Tasks: `node scripts/test-coverage-report.js --tests "lib/*test.js,tests/*test.js" --json cov-vorher.json --fns` (Linux-Referenz: 451 Dateien, 102 s bei 4 parallel, 1161 coverage-json, 202 MB tmp; auf Windows laenger einplanen, `--timeout-ms 300000`). `node tests/gate-coverage.test.js; $LASTEXITCODE` → 0.

## S10 · Lane A · Effort medium — Gemessene Rest-Zweige reiner lib-Module (metrics, forward-returns, read-json, watchlist-fs, Guards)
ZIEL: Eine neue Standalone-Testdatei `tests/cov-lib-reine-zweige.test.js`, die GENAU die 2026-09-24 gemessenen, von keinem Test erreichten Funktionen/Zeilen schliesst (Messbasis: V8-Coverage ueber alle `lib/*test.js` + `tests/*test.js` + `tests/druckenmiller/*test.js`, 451 Dateien, Linux, Node 22.22). Kein Produktionscode wird geaendert; wer einen Bug findet, dokumentiert den IST-Zustand per Assertion und meldet unter OFFEN. Bewusst NICHT hier (liegt in S01/S02): region-mapping, alter, sec-user-agent, fetch-retry, zip-stream, sub-profile, sec-pit, ledger-single-appender. Pflichtfaelle: (A) `lib/metrics.js` — VORHER 74.7 % (65/87 Zeilen), `hitRate@110` NIE aufgerufen; unerreichte Zeilen 50, 72, 92, 110-128, 132, 155, 159, 180. Tests: `hitRate(entries, returnByTicker, cohortN)` Normalfall (Top-N nach Score, Treffer = Return > Universums-Median; `n` = gezaehlte Namen); leeres `returnByTicker` → `{ hitRate: null, n: 0 }` (Zeile 113); NaN-Score wird verworfen (Zeile 117); gleiche Scores → Tie-Break `ticker.localeCompare` (Zeilen 118-119 und analog `cohortSpread` Zeile 72, `quintileMonotonicity` Zeile 155 — je einen Fall mit zwei identischen Scores, dessen Ergebnis von der Ticker-Reihenfolge abhaengt); `cohortN` ohne Angabe → 8 (Zeile 111, mehr als 8 Eintraege uebergeben und `n === 8` pruefen); Top-N-Namen ohne Return → `count 0` → `hitRate null` (Zeile 132); `_median` mit gerader Laenge (Zeile 50, z. B. 4 Returns → Mittel der zwei mittleren; ueber `hitRate`/`cohortSpread` erreichbar, `_median` ist auch exportiert); `cohortSpread` degeneriert: Kohorte ohne finite Returns → alle `null`, `cohortN_used 0` (Zeile 92); `quintileMonotonicity` mit < 5 brauchbaren Eintraegen → alle `null` (Zeile 159) und mit exakt gleichen Quintil-Mitteln (5 Eintraege, alle Returns 0.01) → `tieRate 1`, `monotoneScore 0`, `spread 0` (Zeile 180). ZIEL >= 97 %. (B) `lib/forward-returns.js` — VORHER 76.6 % (59/77), `resolveWindow@209` NIE aufgerufen; unerreicht 74-78, 80, 161, 209, 211-224. `resolveWindow(priceIndex, t0Iso, horizonDays)` verlangt `computeBenchmarkReturn` aus `scripts/walk-forward-perf.js`: `priceIndex.SPY` (oder QQQ/IWM) muss eine `Map` ISO-Datum → Close sein mit `size >= Math.ceil(horizonDays * 5/7) + 20` (bei 84 Tagen: 80 Punkte; 120 fortlaufende Kalendertage ab 2025-03-03 mit Close 100+i verwenden, `nearestTradingDay` snappt +-5 Tage). Faelle: Erfolg → `{ insufficient: false, entryDate, exitDate, benchmarkTicker: 'SPY', horizonActualDays }` mit `horizonActualDays` in [79, 89] und `exitDate > entryDate`; `t0Iso` mit Uhrzeit (`'2025-03-03T09:30:00Z'`) wird auf das Datum gekuerzt (Zeile 211); `priceIndex` ohne Benchmark → `{ insufficient: true }` (Zeile 215-216, `computeBenchmarkReturn` warnt per console.warn — abfangen oder hinnehmen); SPY-Map mit nur 3 Punkten → `benchmarkInsufficient` → `{ insufficient: true }`. `classify`: Exit-Close am `exitDate` fehlt, aber 2 Tage frueher vorhanden → `status 'ok'`, `resolvedExitDate` = frueherer Schluessel, `exitStaleDays > 0` (Zeilen 74-78 ueber `_resolvedDate`); Entry analog → `entryStaleDays > 0`, `entryStale` gemaess Schwelle `EXIT_STALE_FLAG_BUSINESS_DAYS` (2). NICHT erzwingen (per Konstruktion unerreichbar, im Code so dokumentiert): Zeile 80 und Zeile 161 (`delisted`). ZIEL >= 95 %. (C) `lib/read-json.js` — VORHER 57.5 % (23/40): der Selbsttest-Block Zeilen 56-74 (`require.main === module`) laeuft in keinem Gate. Test: `spawnSync(process.execPath, ['lib/read-json.js'], { cwd: ROOT, encoding: 'utf8' })` → `status 0`, stdout enthaelt `lib/read-json.js selftest: ok` (2026-09-24 von Hand bestaetigt). ZIEL 100 %. (D) `lib/watchlist-fs.js` — VORHER 96.9 %, Zeile 107: `loadWatchlist(pfad)` auf eine EXISTIERENDE Datei mit unbekannter Form (Inhalt `"nur-ein-string"` und Inhalt `{ "stocks": 5 }`) → `{ shape: 'invalid', stocks: [], size: 0, raw: <geparster Wert>, error: 'unrecognized watchlist shape' }` — `raw` muss den geparsten Wert tragen (Unterschied zu Parse-Fehler/fehlender Datei, wo `raw null` ist). ZIEL 100 %. (E) `lib/newest-qtr-guard.js` Zeile 41 und `lib/annual-currency-guard.js` Zeile 39 — VORHER 97.8 % bzw. 98.2 %: beides ist das `return null` des privaten Zahl-Entpackers fuer ein Envelope-Objekt OHNE finite `value` (z. B. `{ value: 'abc' }`, `{}`); erreichbar ueber die Exporte `detectNewestQtrSuspect(ts)` (ein solches Envelope in `revenueQ`) und `detectAnnualCurrencyLeak(snap)` (in `annual.annualRev`). Assertion: kein Wurf, Ergebnis identisch zum selben Aufruf mit `null` an derselben Position. ZIEL 100 % beide.
ZIEL-DATEIEN: `tests/cov-lib-reine-zweige.test.js` (neu). Nichts anderes.
FERTIG-WENN: `node tests/cov-lib-reine-zweige.test.js` Exit 0 in < 5 s, >= 30 Assertions, kein Netz, kein Zugriff auf `snapshots/`, `prices/`, `*-history/`; jede oben genannte Funktion wird im Test namentlich aufgerufen (`hitRate`, `resolveWindow`, `_median`, `cohortSpread`, `quintileMonotonicity`, `classify`, `loadWatchlist`, `detectNewestQtrSuspect`, `detectAnnualCurrencyLeak`); Coverage-Tabelle: lib/metrics.js >= 97 %, lib/forward-returns.js >= 95 %, lib/read-json.js 100 %, lib/watchlist-fs.js 100 %, lib/newest-qtr-guard.js 100 %, lib/annual-currency-guard.js 100 %; `git status --short` zeigt nur die neue Datei.
VERIFIKATION: `node tests/cov-lib-reine-zweige.test.js; $LASTEXITCODE` → 0. Mit S-COV1: `node scripts/test-coverage-report.js --tests "lib/metrics.test.js,lib/forward-returns.test.js,lib/read-json.test.js,lib/watchlist-fs.test.js,lib/newest-qtr-guard.test.js,lib/annual-currency-guard.test.js,tests/cov-lib-reine-zweige.test.js" --fns` → die sechs Zeilen zeigen die Zielwerte und KEINE `never-called fns` mehr fuer hitRate/resolveWindow. Fallback ohne S-COV1 (PowerShell): `$env:NODE_V8_COVERAGE="$env:TEMP\cov-x"; node tests/cov-lib-reine-zweige.test.js; Remove-Item Env:NODE_V8_COVERAGE` und danach `node -e "const fs=require('fs'),p=process.argv[1],want=process.argv[2];for(const f of fs.readdirSync(p)){for(const s of JSON.parse(fs.readFileSync(p+'/'+f)).result){if(!s.url.replace(/\\/g,'/').endsWith(want))continue;for(const fn of s.functions)if(fn.functionName)console.log(fn.functionName,fn.ranges[0].count)}}" "$env:TEMP\cov-x" lib/metrics.js` → jede Funktion mit count > 0 (analog fuer lib/forward-returns.js).

## S11 · Lane C · Effort high — ZIP64-Fixture und die ungetesteten ZIP64-/Fehlerpfade in `lib/zip-stream.js` und `lib/sec-pit.js`
ZIEL: Die 2026-09-24 gemessenen Luecken beider ZIP-Leser schliessen; sie sind fast deckungsgleich, weil `sec-pit.js` denselben Parser traegt. Das ist die ERGAENZUNG zu S02 (Roundtrip/reine Funktionen): S02 baut kein ZIP64 und keinen Ticker-Index — genau das hier. Messbasis wie S-COV2. (1) `tests/helpers/zip-fixture.js` ADDITIV um eine Option `zip64: true` erweitern (bestehende Aufrufer `tests/secbulk.test.js`, `tests/sec-pit.test.js` bleiben byte-identisch, weil die Option fehlt): je Eintrag im Zentralverzeichnis (CD) die Felder compressedSize (+20), uncompressedSize (+24) und lfhOffset (+42) auf `0xffffffff` setzen und ein Zusatzfeld id `0x0001` anhaengen mit den echten Werten als u64 LE in der Reihenfolge uncompressed, compressed, lfhOffset (extraLen entsprechend); optional (Option `fremdesZusatzfeld: true`) VOR dem 0x0001-Feld ein fremdes Zusatzfeld (id `0x7075`, len 4) einfuegen — das trifft die Skip-Schleife (`q += 4 + len`, zip-stream Zeile 121; sec-pit Zeile 103); EOCD: entries (+8, +10) `0xffff`, cdSize (+12) und cdOffset (+16) `0xffffffff`; DAVOR den ZIP64-EOCD-Record (56 Byte: sig `0x06064b50`, u64 Groesse 44, u16 u16 Versionen, u32 u32 Disks, u64 Eintraege-auf-Disk @24, u64 Eintraege-gesamt @32 — BEIDE mit der Eintragszahl fuellen, zip-stream liest @32, sec-pit @24 —, u64 cdSize @40, u64 cdOffset @48) und den Locator (20 Byte: sig `0x07064b50`, u32 Disk, u64 Offset des ZIP64-EOCD @8, u32 Disks gesamt) direkt vor dem EOCD. (2) Neue Testdatei `tests/cov-zip64-sec-pit.test.js`. `lib/zip-stream.js` — VORHER 70.0 % (56/80), unerreicht Zeilen 43, 66-79, 110-121: `leseVerzeichnisZeiger(schwanz, dateiGroesse)` auf 200 Zufallsbytes → wirft `/kein End-of-Central-Directory/` (43, 57); ZIP64-Fixture (2 Eintraege, einer `gespeichert`, einer deflate) → `{ zip64: true, anzahl: 2, cdOffset, cdGroesse }` gleich den beim Bau bekannten Werten (66-79); `leseVerzeichnis(cd, 2)` liefert echte Groessen/Offsets aus dem Zusatzfeld (110-119) und mit `fremdesZusatzfeld` dasselbe Ergebnis (121); `entpackeEintrag(block, eintrag)` Roundtrip fuer beide Eintraege; Fehlerpfade durch gezieltes Patchen des Fixture-Buffers: Locator-Signatur ueberschrieben → `/kein Locator/` (68); nur die letzten 42 Byte als `schwanz` mit `dateiGroesse` = Gesamtlaenge → `/ausserhalb des gelesenen Schwanzes/` (73); ZIP64-EOCD-Signatur ueberschrieben → `/ZIP64-Signatur falsch/` (75). ZIEL zip-stream >= 97 %. `lib/sec-pit.js` — VORHER 85.8 % (194/226), NIE aufgerufen `findEndOfCentralDir64@61`, `cikForTicker@177`, `factsForTicker@182`; unerreicht 59, 61-69, 92-103, 120, 177-184, 192-193, 319: ZIP64-Fixture (Eintrag `CIK0001234567.json` mit einem companyfacts-Objekt wie in `tests/sec-pit.test.js::fixtureCompany`) in eine tmp-Datei schreiben, `openStore(pfad)` → `entryCount 1`, `factsForCik(1234567)` parsebar (61-69, 92-103; Variante mit `fremdesZusatzfeld` fuer 103); Datei aus 4096 Nullbytes → `openStore` wirft `/EOCD signature not found/` (59); Locator-Signatur gepatcht → `/ZIP64 locator not found/` (64); ZIP64-EOCD-Signatur gepatcht → `/ZIP64 EOCD not found/` (68); Methode 9: im CD-Kopf (+10) UND im lokalen Kopf (+8) auf 9 patchen → `readEntryByName` wirft `/Unsupported compression method: 9/` (120); Ticker-Index: `<tmp>/company_tickers.json` = `{ "0": { "cik_str": 1234567, "ticker": "TST", "title": "Testcorp" } }`, dann in einem KINDPROZESS (`spawnSync(node, ['-e', code])` oder `node <tmp>/probe.js`) `process.env.SEC_XBRL_CACHE_DIR = <tmp>` VOR `require('lib/sec-pit.js')` setzen (das Modul liest die Umgebung beim Laden, Zeilen 34-37) → `store.cikForTicker('tst') === 1234567`, `cikForTicker('NOPE') === null`, `factsForTicker('TST')` liefert die Facts, `factsForTicker('NOPE') === null` (177-184); `loadTickerMap('<tmp>/fehlt.json')` wirft `/company_tickers.json fehlt/` (192-193); `pitQuarterlyWithDerivedQ4` mit zwei Konzepten, deren juengstes Perioden-Ende identisch ist und das zweite eine laengere Serie hat → `best.concept` ist das zweite (Tie-Break Zeile 319). ZIEL sec-pit >= 97 %. Alle Fixtures im tmp, kein Netz, kein Zugriff auf `SEC_XBRL_CACHE_DIR` von Karls Maschine (im Elternprozess NICHT setzen, nur im Kindprozess).
ZIEL-DATEIEN: `tests/cov-zip64-sec-pit.test.js` (neu), `tests/helpers/zip-fixture.js` (nur additiv: neue Optionen, Default-Verhalten unveraendert).
FERTIG-WENN: `node tests/cov-zip64-sec-pit.test.js` Exit 0 in < 5 s mit >= 25 Assertions; `node tests/secbulk.test.js`, `node tests/sec-pit.test.js`, `node tests/sec-pit-unreadable-asof-guard.test.js` und alle `tests/zip-stream-*.test.js` weiter Exit 0; Coverage: lib/zip-stream.js >= 97 %, lib/sec-pit.js >= 97 %, keine `never-called fns` mehr in beiden; `git status --short` zeigt nur die zwei Dateien.
VERIFIKATION: `node tests/cov-zip64-sec-pit.test.js; $LASTEXITCODE` → 0; `Get-ChildItem tests/zip-stream-*.test.js,tests/secbulk.test.js,tests/sec-pit*.test.js | ForEach-Object { node $_.FullName; if ($LASTEXITCODE) { "FAIL: $($_.Name)" } }` → keine FAIL-Zeile. Mit S-COV1: `node scripts/test-coverage-report.js --tests "tests/secbulk.test.js,tests/sec-pit.test.js,tests/sec-pit-unreadable-asof-guard.test.js,tests/zip-stream-local-header-signature-guard.test.js,tests/zip-stream-truncated-entry-guard.test.js,tests/zip-stream-unsupported-method-guard.test.js,tests/cov-zip64-sec-pit.test.js" --fns` → Zielwerte; Fallback-Snippet wie in S-COV2 fuer lib/zip-stream.js und lib/sec-pit.js.

## S12 · Lane B · Effort medium — I/O-Naehte ohne Netz und ohne Windows: `lib/atomic-write.js` Rename-Drossel und `lib/fetch-retry.js` `einAbruf`
ZIEL: Neue Testdatei `tests/cov-io-naehte.test.js`, die die zwei plattform-/netzgebundenen Pfade ueber Stubs im Kindprozess erreicht. Beide Techniken sind am 2026-09-24 auf Linux/Node 22.22 nachgewiesen (Coverage danach gemessen). Kein Produktionscode wird geaendert. (A) `lib/atomic-write.js` — VORHER 81.6 % (80/98), `_sleepSync@85` NIE aufgerufen; unerreicht 85, 104-109, 120-127, 130, 190, 219. Die Rename-Drossel ist per `IS_WINDOWS = process.platform === 'win32'` (Zeile 58) beim Laden fixiert — deshalb je Fall ein KINDPROZESS (`spawnSync(process.execPath, ['-e', code])` oder tmp-Skript), der in DIESER Reihenfolge arbeitet: 1. tmp-Verzeichnis ueber `os.tmpdir()` anlegen (VOR dem Plattform-Stub — danach sucht `os.tmpdir()` nach `%TEMP%` und faellt auf Linux um), 2. `Object.defineProperty(process, 'platform', { value: 'win32' })`, 3. `globalThis.SharedArrayBuffer = undefined` (erzwingt in `_sleepSync` den Spin-Fallback Zeilen 108-109; ohne diesen Schritt laeuft `Atomics.wait` Zeilen 105-106 — beide Varianten je einmal fahren), 4. `require('fs').renameSync` stubben, 5. ERST DANN `require('<ROOT>/lib/atomic-write.js')`. Faelle: renameSync wirft zweimal `{ code: 'EPERM' }`, dann echtes Rename → Datei korrekt, `atomicWriteStats.renameRetries === 2`, Dauer >= 30 ms (Leiter 10+20 ms; Zeilen 120-124); renameSync wirft immer `{ code: 'EBUSY' }` → `writeFileAtomic` wirft mit `code 'EBUSY'`, `renameRetries` um 5 erhoeht (Leiterlaenge; Zeile 130); zweiter Versuch wirft `{ code: 'ENOENT' }` → sofortiger Wurf mit ENOENT, `renameRetries === 1` (Zeile 126); Ergebnisse als JSON auf stdout des Kindes drucken und im Elternprozess asserten. Ohne Stub (Elternprozess, echtes Linux/Windows): `writeFileAtomic(p, new Uint8Array([1, 2, 3]))` → Datei 3 Byte (Zeile 190, `fs.writeFileSync`-Fallback fuer Nicht-String/Nicht-Buffer); `writeJsonAtomic(p, { x: 1, y: 2 }, { replacer: (k, v) => (k === 'y' ? undefined : v) })` → Datei ohne `y` (Zeile 219, Nutzer-Replacer laeuft vor der Finite-Pruefung) und `writeJsonAtomic(p, { x: 1 }, { replacer: () => NaN })` → wirft `/non-finite/`. ZIEL atomic-write >= 98 % (unerreichbar bleibt hoechstens Zeile 190 auf Plattformen, deren fs Buffer-Subtypen anders behandelt — nicht erwartet). (B) `lib/fetch-retry.js` — VORHER 84.7 % (50/59), NIE aufgerufen `schlaf@31` und `einAbruf@44`; unerreicht 44-50, 52-53. Technik: `const https = require('https'); https.get = (url, opts, cb) => req;` VOR `require('<ROOT>/lib/fetch-retry.js')` — das Modul haelt dasselbe Modulobjekt und ruft `https.get` erst zur Laufzeit (Zeile 46); `req` = `EventEmitter` mit `destroy()` (Aufruf zaehlen) und `setTimeout(ms, fn)` (Callback merken). Wegen des globalen Stubs diesen Teil ebenfalls in einem Kindprozess fahren. Faelle: Erfolg — `setImmediate` ruft `cb(res)` mit `res = EventEmitter { statusCode: 200, headers: { 'x-h': '1' } }`, emittiert `'data'` zweimal (`'{"ok":'`, `'true}'`) und `'end'` → `fetchJson(url, { pauseMs: 1 })` → `{ ok: true }` und ueber `fetchBuffer` `a.headers['x-h'] === '1'`, `a.code === 200` (46-50); Request-Fehler — `req.emit('error', new Error('ECONNRESET'))` bei `versuche: 2, backoffMs: 1` → wirft `/nach 2 Versuchen aufgegeben: ECONNRESET/` (52, plus echtes `schlaf` als Backoff, weil `_schlaf` nicht injiziert ist); Timeout — den in `setTimeout` gemerkten Callback ueber `setImmediate` ausloesen bei `timeoutMs: 5, versuche: 1` → wirft `/timeout nach 5ms/` und `destroy()` wurde genau einmal gerufen (53); Pause — zwei Aufrufe auf denselben Host mit `pauseMs: 30` ohne `_schlaf` → Gesamtdauer >= 30 ms (`warteAufSlot` Zeile 40 mit echtem `schlaf`, Zeile 31). Zusatz: `fetchBuffer` mit `_get`, das `{ code: 402 }` liefert → `/Gratis-Kontingent/`; `{ code: 404 }` → `/nicht wiederholbar/` (beide schon abgedeckt, hier als Regressionsanker). ZIEL fetch-retry 100 %.
ZIEL-DATEIEN: `tests/cov-io-naehte.test.js` (neu). Nichts anderes.
FERTIG-WENN: `node tests/cov-io-naehte.test.js` Exit 0 in < 10 s, >= 20 Assertions, kein Netz (der Test darf keine echte `https.get`-Verbindung oeffnen — im Kindprozess sicherstellen, dass der Stub VOR dem require installiert ist), keine Spuren ausserhalb tmp; `node lib/atomic-write.test.js` und alle `tests/fetch-retry-*.test.js`, `tests/t204-*.test.js` weiter Exit 0; Coverage lib/atomic-write.js >= 98 %, lib/fetch-retry.js 100 %, keine `never-called fns`; `git status --short` zeigt nur die neue Datei.
VERIFIKATION: `node tests/cov-io-naehte.test.js; $LASTEXITCODE` → 0. Mit S-COV1: `node scripts/test-coverage-report.js --tests "lib/atomic-write.test.js,tests/fetch-retry-json-parse-guard.test.js,tests/fetch-retry-redirect-status-guard.test.js,tests/fetch-retry-status-guard.test.js,tests/cov-io-naehte.test.js" --fns` → Zielwerte (Kindprozesse werden mitgemessen, weil `NODE_V8_COVERAGE` vererbt wird — den Kindprozessen die Umgebung NICHT wegnehmen, `env: { ...process.env }`). Fallback-Snippet wie in S-COV2 fuer lib/atomic-write.js und lib/fetch-retry.js.

## S13 · Lane C · Effort high — Betriebs-Skripte mit ungelaufenen Hauptpfaden: archive-old-snapshots, data-quality-report, merge-price-shards und drei CLI-Einstiege
ZIEL: Neue Testdatei `tests/cov-scripts-betrieb.test.js` (vier klar getrennte Bloecke), die die Hauptpfade von vier taeglich/woechentlich laufenden Skripten erstmals ausfuehrt — ausschliesslich gegen tmp-Verzeichnisse, Skripte als Kindprozess (`spawnSync(process.execPath, [SCRIPT, ...], { cwd, env, encoding: 'utf8', timeout: 20000 })`). NIE gegen `snapshots/`, `prices/`, `picks-history/`, `methods-history/`, `outputs/` des Repos. Messbasis wie S-COV2. (A) `scripts/archive-old-snapshots.js` — VORHER 17.1 % (22/129); NIE aufgerufen `ensureDir@58`, `archiveDirByDate@60`, `archiveProtectedDir@186`, `main@213`. Nur der Export `archiveDirByDate(srcDir, archiveDir, keepDays, dryRun)` ist tmp-faehig (`archiveProtectedDir`/`main` sind an die Repo-Wurzel `ROOT` gebunden — NICHT ueber das Repo laufen lassen, bleiben ungetestet und werden im PR-Body als bewusste Luecke genannt). `process.env.RUN_DATE_UTC = '2026-09-24'` im Testprozess setzen (Zeile 66) — Cutoff bei `keepDays 14` ist 2026-09-10. Faelle: srcDir fehlt → `{ archived: 0, kept: 0 }` (61-63); `2026-09-20.json` (bleibt) + `2026-06-01.json`, `2026-06-02.json`, `2026-07-01.json` (je `{ "k": <n> }`) → `{ archived: 3, kept: 1 }`, `<archiveDir>/2026-06.ndjson` mit 2 Zeilen und `2026-07.ndjson` mit 1 Zeile, jede Zeile `JSON.parse`-bar mit `date` als erstem Schluessel und den Inhaltsfeldern, Originale geloescht, `2026-09-20.json` vorhanden (70-84, 88-91, 138-143, 153-164, 170-174); `2026-05-01.json` mit Inhalt `{` → Warnung `parse fail`, Datei bleibt, zaehlt nicht (81); nur frische Dateien → `{ archived: 0, kept: n }` und archiveDir wird NICHT angelegt (86); `dryRun true` → stdout `[dry-run] would write`, keine ndjson, Originale bleiben (92-94) — IST-Verhalten (2026-09-24 bestaetigt): `ensureDir(archiveDir)` (Zeile 88) laeuft VOR der dry-run-Pruefung, das leere Archivverzeichnis wird also auch im dry-run angelegt; so asserten und unter OFFEN melden, nicht fixen; zweiter Lauf mit identischen Originalen (2026-06-01/-02 neu anlegen) → Rueckgabe `{ archived: 2, kept: 1 }` (zaehlt als archiviert, obwohl nichts angehaengt wird), stdout `all 2 entries already archived ... skipping append`, Originale geloescht, ndjson byte-identisch (105-113, 127-136); gleiche Datei mit ANDEREM Inhalt → wirft `/archive collision/`, Original bleibt, ndjson unveraendert (119-125); bestehende ndjson mit einer NICHT parsebaren Zeile → wirft (JSON.parse Zeile 111, BH-142) und Originale bleiben. Nicht erzwingen: 133/172 (unlink-Fehler), 165-167 (Verify-Fehler nach atomarem Schreiben). ZIEL >= 72 %. (B) `scripts/data-quality-report.js` — VORHER 28.7 % (48/167); NIE aufgerufen `main@123`, alle 15 `KEY_FIELDS`-Getter (69-83), `_arrayElementPresent@59`, `pct@114`, `pad@119` (toter Code — nicht erzwingen), Lade-Fallback 35-38/44-48, Banner 201-205. Spawn `[SCRIPT, '--snapshots', <tmp>/snaps, '--out', <tmp>/out/report.md]` (das out-Verzeichnis darf VORHER fehlen → Zeile 252). Fixture-Snapshots: `VOLL.json` (meta.ticker/sector/exchangeName 'TESTX', `marketCap: { value: 1e9 }`, `metrics` mit finiten `revenueTTM, revenueGrowthYoY, grossMargin, operatingMargin, fcfMarginTTM, forwardPE, insidersOwnership` als `{ value }`, `annual.annualRev` 3 Envelopes, `annualFCF` 2, `annualBalance` 2 Objekte `{ totalCash, totalDebt }` (Zeile 64: Objekt ohne `value` zaehlt als vorhanden), `annualSBC` 1, `annualCapex` 1, `timeseries.revenueQ` 4); `LEER.json` = `{}` (→ Exchange `Unknown`, Zeile 173); `NULLIG.json` mit `metrics.revenueTTM: { value: null }` und `annual.annualRev` aus drei `{ value: null }` (60-62 false-Zweige); `KAPUTT.json` mit Inhalt `{` (→ `1 parse errors`); `_manifest.json` (wird uebersprungen); `ZAHL.json` mit Inhalt `5` (kein Objekt → nicht geladen). Assertions: Exit 0; stdout enthaelt `snapshot files: 5`, `loaded: 3 (1 parse errors)` und `Written:`; Markdown beginnt mit `# Data Quality Report`, enthaelt `| Total snapshots | 3 |`, `| Parse errors | 1 |`, jede der 15 KEY_FIELDS-Labels in der Tabelle `## Key Field Coverage`, `## Top 10 Missing Fields` mit 10 Zeilen, `## Coverage by Exchange` mit `TESTX` und `Unknown`; die Zeile `marketCap` zeigt `1` vorhanden (nur VOLL). Fehlendes Snapshot-Verzeichnis → Exit 1, stderr `Snapshots dir not found` (129-131). Lade-Ausfall-Banner: Kindprozess mit `-r <tmp>/deny-preload.js`, der `Module._load` so umhuellt, dass `request === '../methods/data-quality.js'` wirft (Muster: `tests/data-quality-report-arg-presence.test.js` Zeilen 14-36, dort wird ein Ersatz geliefert — hier WERFEN) → Exit 0, stderr/stdout enthaelt `[data-quality-report] FAILED to require`, Markdown enthaelt `WARNING` + `failed to load` und `unknown`-Grades (35-38, 44-48, 201-205). ZIEL >= 88 %. (C) `scripts/merge-price-shards.js` — VORHER 66.7 % (80/120); NIE gelaufen `selftest@182` (182-221; CI faehrt ihn nicht — nur test-gate/coverage-gate/merge-shard-manifests/pipeline-status haben `--selftest` in daily-pull.yml), Fehlerpfade 119-120, 132-133, 144-145, 159-160, Kollisions-/Normalzweig 99-104. Spawn mit `cwd: <tmp>`: `--selftest` → Exit 0, stdout `merge-price-shards selftest: 4 ok, 0 fail`; ohne `--date` und mit `RUN_DATE_UTC` aus der Kind-Umgebung ENTFERNT → Exit 1, stdout `Kein --date` (119-120); `<tmp>/prices/2026-09-24.shard-1.json` mit Inhalt `{` bei `--expected-shards 2` → Exit 1, `unlesbar` (132-133); Ticker `AAA` in shard-0 UND shard-1 → Exit 1, stdout `::error::` + `MEHREREN Preis-Shards` (144-145); Erfolg: zwei disjunkte Shards, Verzeichnis `<tmp>/prices/history/` VORHER LEER ANLEGEN (Produktionscheckout hat es immer; ohne das Verzeichnis wirft `writeFileAtomic` beim `_meta`-Stempel ENOENT und das Skript stirbt mit Exit 1 + Stacktrace — 2026-09-24 bestaetigt; als IST-Befund unter OFFEN melden, nicht fixen), keine `_meta.json`, keine `history-NN.json`, keine `history.json` → `loadAll` liefert `{}` → Exit 0, `<tmp>/prices/2026-09-24.json` = Vereinigung, `<tmp>/prices/history/_meta.json` mit `schema 'price-history-store/1'`, `tickerCount 0`, `shardsWritten 32`, Shard-Dateien geloescht; Store-Fehler: vorab `<tmp>/prices/history/_meta.json` = `{ "schema": "price-history-store/1", "updatedAt": "2026-09-24T00:00:00.000Z", "tickerCount": 0, "shardsWritten": 32 }` OHNE `history-NN.json` → `loadAll` wirft (`shard(s) missing`) → Exit 1, stdout `::error::Kurs-Store nach dem Merge nicht vollstaendig ladbar` (159-160); fehlender Shard bei `--expected-shards 3` → Exit 1, `Preis-Ausschnitt fehlt` (Regressionsanker). ZIEL >= 97 %. (D) CLI-Einstiege, je ZIEL 100 %: `scripts/verify-freshness.js` — VORHER 75.0 % (27/36), Zeilen 75-83: Spawn `[SCRIPT, <tmp>/snaps, 'probe']` mit `A.json` = `{ "meta": { "asOf": <jetzt ISO> } }` → Exit 0, stdout `probe freshness: 1 fresh / 0 stale / 0 unparseable of 1 (100.0% fresh)`; mit `asOf` 3 Tage alt → Exit 1, stderr enthaelt `::error::F-CI-016`. `scripts/vintage-commit-text.js` — VORHER 61.5 % (16/26), Zeilen 62-73: ohne Argumente → Exit 1, stderr `usage:`; `['0', '2026-13-40', '--subject']` → Exit 1, stderr `kein ISO-Datum`; `['2', '2026-09-24', '--subject']` → Exit 0, stdout exakt `chore: nur Sidecars — vintage 2026-09-24 SUSPECT, NICHT committet`; `['0', '2026-09-24', '--done']` → stdout exakt `✓ board-history vintage 2026-09-24 committet`; `['0', '2026-09-24', '--foo']` → Exit 1, stderr `unbekannter Modus`. `scripts/cadence-marker.js` — VORHER 65.7 % (46/70), Zeilen 81-82 und 89-116: `--selftest` → Exit 0, stdout `cadence-marker selftest: 4 ok, 0 fail`; `--field daily --file <tmp>/x.json` → Exit 1, stderr `::error::cadence-marker`, Datei nicht angelegt; `--field weekly --file <tmp>/cad.json` → Exit 0, Datei mit `schema 'cadence-heartbeat/v1'` und parsebarem `last_weekly_run` (84-86).
ZIEL-DATEIEN: `tests/cov-scripts-betrieb.test.js` (neu). Nichts anderes — die vier Skripte werden NICHT angefasst.
FERTIG-WENN: `node tests/cov-scripts-betrieb.test.js` Exit 0 in < 20 s (ca. 25 Kindprozesse), >= 45 Assertions, kein Netz, keine Spuren ausserhalb tmp (`git status --short` zeigt nur die neue Datei; insbesondere KEINE Aenderung unter `external-data/`, `outputs/`, `prices/`, `state/`); bestehende Tests `tests/archive-old-snapshots-keep-days-parser.test.js`, `tests/archive-picks-frozen.test.js`, `tests/data-quality-report-arg-presence.test.js`, `tests/merge-price-shards-expected-count.test.js`, `tests/freshness-gate.test.js`, `tests/vintage-commit-text.test.js`, `tests/vintage-commit-date-validation.test.js`, `tests/cadence-marker-state-integrity.test.js` weiter Exit 0; Coverage: archive-old-snapshots.js >= 72 %, data-quality-report.js >= 88 %, merge-price-shards.js >= 97 %, verify-freshness.js 100 %, vintage-commit-text.js 100 %, cadence-marker.js 100 %.
VERIFIKATION: `node tests/cov-scripts-betrieb.test.js; $LASTEXITCODE` → 0. Mit S-COV1: `node scripts/test-coverage-report.js --tests "tests/cov-scripts-betrieb.test.js,tests/archive-old-snapshots-keep-days-parser.test.js,tests/archive-picks-frozen.test.js,tests/data-quality-report-arg-presence.test.js,tests/merge-price-shards-expected-count.test.js,tests/freshness-gate.test.js,tests/vintage-commit-text.test.js,tests/vintage-commit-date-validation.test.js,tests/cadence-marker-state-integrity.test.js" --only scripts/ --fns` → die sechs Skriptzeilen zeigen die Zielwerte (Kindprozesse werden ueber die vererbte `NODE_V8_COVERAGE` mitgemessen — den Spawns `env: { ...process.env, ... }` mitgeben, bei (C) nur `RUN_DATE_UTC` entfernen). Fallback-Snippet wie in S-COV2 mit `scripts/<name>.js` als drittem Argument.

## S14 · Lane A · Effort medium — screener-data lib/: Stub-Kill-Probe anlegen, 4 Ueberlebende toeten
ZIEL:
- Probe-Infrastruktur fuer screener-data anlegen (Namen enden bewusst NICHT auf `test.js`, damit der CI-Gate-Glob sie nicht
  als Gate einsammelt):
  - `tests/_mutations-preload.js` — exakt der Code aus Anhang A (Module._load-Patch, `MUT_MODULE`/`MUT_FN` aus env).
  - `tests/_mutations-probe.js` — Standalone-Runner (Exit 0/1). Liest ALLE `tests/_mutations-pairs/*.json` (Array von
    `{ "module": "<repo-relativ>", "fn": "<Export>", "test": "<repo-relativ>", "expect": "killed" | "cli-only" }`).
    Je Paar: (1) Kontroll-Lauf `node <test>` muss Exit 0 liefern (sonst FAIL „Kontrolle rot"), einmal je Testdatei
    gecacht; (2) Stub-Lauf `node -r tests/_mutations-preload.js <test>` mit env `MUT_MODULE=<abs>`, `MUT_FN`, und
    `NODE_OPTIONS="${NODE_OPTIONS:-} -r <abs preload>"` (damit Kindprozesse den Stub erben); `expect:"killed"` =>
    Exit != 0 verlangt, sonst FAIL „SURVIVED"; `expect:"cli-only"` => Ergebnis nur gemeldet. Summenzeile
    `mutations-probe: <killed> killed, <survived> survived, <cli-only> cli-only, <n> Kontrollen rot` und Exit 1 bei
    irgendeinem FAIL. Timeout je Lauf 120 s, `encoding:'utf8'`, cwd = Repo-Wurzel.
  - `tests/_mutations-pairs/lib.json` mit diesen 19 Paaren (alle `expect:"killed"`):
    `lib/read-json.js#readJsonExistingOrThrow`, `lib/annual-currency-guard.js#detectAnnualCurrencyLeak`,
    `lib/artifact-path.js#baseName`, `#toPosix`, `lib/atomic-write.js#writeFileAtomic`, `#writeJsonAtomic`,
    `lib/forward-returns.js#classify`, `#resolveWindow`, `lib/metrics.js#rankIC`, `#cohortSpread`, `#hitRate`,
    `#quintileMonotonicity`, `#_median`, `lib/newest-qtr-guard.js#detectNewestQtrSuspect`,
    `lib/watchlist-fs.js#loadWatchlist`, `#extractStocksArray`, `#detectShape`, `lib/spearman.js#spearman`,
    `#averageRank` (Test jeweils die gleichnamige `lib/<modul>.test.js`; spearman/averageRank -> `lib/metrics.test.js`).
- Die vier bewiesenen Ueberlebenden + den null-Zweig-only-Fall mit Wert-Assertions decken (jeweils >= 2 Faelle,
  konkrete Zahlen, kein `assert.ok(result)`):
  1. `lib/forward-returns.test.js`: `resolveWindow` — Signatur/Vertrag aus `lib/forward-returns.js` lesen; mindestens
     ein regulaerer Fall (deepEqual auf das Fenster-Objekt) und ein Grenzfall (fehlende/zu alte Preise -> das
     dokumentierte Ergebnis, z. B. `null` oder Flag `EXIT_STALE_FLAG_BUSINESS_DAYS`).
  2. `lib/metrics.test.js`: `hitRate` — Trefferquote fuer ein Beispiel mit bekanntem Ergebnis (z. B. 2 von 4 => 0.5)
     und NaN/null-Score wird gedroppt und gezaehlt; `_median` — ungerade/gerade Laenge, leeres Array.
  3. `lib/metrics.test.js:37`: `spearman` zusaetzlich positiv pinnen: `spearman([1,2,3],[1,2,3]) === 1`,
     `spearman([1,2,3],[3,2,1]) === -1`; `averageRank` mit Bindung (`[10,20,20]` -> `[1,2.5,2.5]`).
  4. `lib/watchlist-fs.test.js`: `loadWatchlist` — Datei in `os.tmpdir()`, gueltige Form => deepEqual der Ticker-Liste;
     kaputte JSON => der dokumentierte Fehlerpfad (Wurf mit Nachricht per `assert.throws(..., /.../)` oder Sentinel).
ZIEL-DATEIEN: `tests/_mutations-preload.js` (neu), `tests/_mutations-probe.js` (neu), `tests/_mutations-pairs/lib.json`
(neu), `lib/forward-returns.test.js`, `lib/metrics.test.js`, `lib/watchlist-fs.test.js`. Nichts sonst.
FERTIG-WENN:
- `node tests/_mutations-probe.js` meldet `19 killed, 0 survived, 0 cli-only, 0 Kontrollen rot` und Exit 0.
- Vorher-Stand ist im Commit-Body festgehalten (`13 killed / 4 survived` + spearman-null-Zweig), Nachher-Stand ebenso.
- Die Probe-Dateien werden von `GATE_GLOB` (`tests/*test.js`, `tests/scoring/*test.js`, `lib/*test.js`) NICHT gematcht
  (Namen pruefen) und `.github/**` bleibt unberuehrt.
- Kein Produktivcode angefasst; keine Assertion entfernt oder gelockert.
VERIFIKATION:
- `node tests/_mutations-probe.js` (Exit 0, Summenzeile wie oben).
- Gegenprobe der Probe selbst: in `lib.json` voruebergehend ein Paar mit einem Export eintragen, den kein Test prueft
  (z. B. `lib/atomic-write.js#atomicWriteStats`, falls Funktion) -> Probe muss Exit 1 mit `SURVIVED` melden; danach
  entfernen.
- Volle Gate-Suite gruen: `for f in tests/*test.js tests/scoring/*test.js lib/*test.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done`
  (PowerShell-Form in CLAUDE.md). Erlaubt rot: nur `tests/scoring/calibration-ref.test.js` und
  `tests/waehrung-ausliefer-waechter.test.js`.
- `git status --short` zeigt ausschliesslich die ZIEL-DATEIEN.

## S15 · Lane B · Effort medium — screener-data tests/: doesNotThrow-only und stille Skips haerten
VORAUSSETZUNG: Probe-Infrastruktur aus S14. Ist dessen PR noch offen: Worktree von `origin/codex/S14` statt `origin/main` anlegen.
ZIEL:
- Alle 27 `assert.doesNotThrow(...)`-Stellen nach EINER Regel behandeln: (a) gibt die Funktion einen Wert zurueck
  => Rueckgabe in eine Variable nehmen und mit `assert.equal/deepEqual/match` gegen den konkreten Fixture-Wert pruefen;
  (b) ist sie ein reiner Waechter (void, wirft bei Verstoss) => es MUSS in derselben Datei die Gegenprobe
  `assert.throws(() => <selbe Funktion mit kaputter Eingabe>, /<Nachricht>/)` geben; fehlt sie, ergaenzen; (c) beides
  vorhanden => im Commit-Body als „OK" listen, Stelle unveraendert. Stellen:
  `tests/annual-spikes.test.js:599` (`baueNeuenBestand` liefert den neuen Bestand -> `ausgeschlossen` deepEqual),
  `tests/build-cnannual-unreadable-date-guard.test.js:17`, `tests/cn-jahresreihen.test.js:140`, `:150`, `:383`, `:391`,
  `:463`, `tests/coverage-gate-count-integrity.test.js:478`, `tests/nrb-sk-001-impossible-zero-revenue.test.js:70`,
  `tests/p1-welle2-datenquellen-wahrheit.test.js:129` (`assertNonEmptyUniverse([])` muss werfen — Gegenprobe fehlt),
  `tests/silent-errors.test.js:17`, `:37`, `tests/studie-f6-abschluss.test.js:186`, `:196`,
  `tests/studie-f6-aequivalenz-akt2.test.js:152`, `:161`, `tests/studie-f6-freeze-eintrag24.test.js:269`,
  `tests/studie-f6-konfirmatorisch-v3.test.js:242`, `:251`, `tests/studie-f6-konfirmatorisch-v4.test.js:105`, `:291`,
  `tests/studie-f6-konfirmatorisch.test.js:356`, `:359`, `tests/studie-f6-vorfall.test.js:92`,
  `tests/studie-naht-vollendung.test.js:167`, `tests/studie-r1-register.test.js:175`,
  `tests/studie-register-single-appender-rule.test.js:270`.
- Stille Skips sichtbar UND gezaehlt machen (Konvention `tests/skip-honesty.test.js`: Statuszeile `  skip <name>` plus
  `skipped` in der Summenzeile; Exit bleibt 0). Fuer Standalone-Runner: `skip`-Zaehler in den lokalen `test()`-Helfer
  aufnehmen (Skip = geworfenes Sentinel-Objekt `{ skip: '<Grund>' }`, das der Helfer als Skip zaehlt), Summenzeile
  `<n> ok, <n> fail, <n> skipped`. Fuer `node:test`-Dateien `t.skip(grund)`. Stellen:
  `tests/studie-f6-berichtigung-bein2.test.js:139`, `tests/studie-f6-vorfall.test.js:155`,
  `tests/studie-t173-formtyp.test.js:77`, `tests/studie-regelwerk.test.js:153`,
  `tests/bk-sk-001-mitschnitt-stille-pannen.test.js:184`, `tests/pr-check-live-gate.test.js:66`.
  Nach dem Umbau muss `node tests/skip-honesty.test.js` weiterhin gruen sein.
- Paar-Datei `tests/_mutations-pairs/s-mut2.json` (Format aus S-MUT1) mit diesen Paaren anlegen und empirisch
  einordnen (`killed` oder begruendet `cli-only`): `scripts/watch-annual-spikes.js#baueNeuenBestand` ->
  `tests/annual-spikes.test.js`; `pull-yahoo.js#needsFullPull` -> `tests/pull-diet.test.js` (bewiesen killed);
  `lib/alter.js#alterTage` -> `tests/alter-tage-nonfinite-now-guard.test.js`; `discovery/mcap-prefilter.js#prefilterByMcap`
  -> `tests/mcap-prefilter.test.js`; `scripts/rank-ic.js#spearman` und `#benjaminiYekutieli` -> `tests/rank-ic.test.js`;
  `scripts/pipeline-status.js#validateMarker` -> `tests/pipeline-status-marker.test.js`;
  `lib/price-history-store.js#shardOf` -> `tests/price-history-store.test.js`; `scripts/update-ath-state.js#advanceEntry`
  -> `tests/ath-state.test.js`; das Modul, aus dem `tests/silent-errors.test.js` `runLamp` importiert, `#runLamp`;
  das Modul, aus dem `tests/p1-welle2-datenquellen-wahrheit.test.js` `smallcap` importiert, `#assertNonEmptyUniverse`.
ZIEL-DATEIEN: die 22 oben genannten Testdateien unter `tests/` (NICHT `tests/scoring/**`), `tests/_mutations-pairs/s-mut2.json`
(neu). Keine Produktivdatei.
FERTIG-WENN:
- 27/27 doesNotThrow-Stellen sind nach Regel (a)/(b) gehaertet oder als (c) „OK" begruendet; Zaehlung vorher/nachher
  im Commit-Body: `grep -c "assert.doesNotThrow(" <datei>` je Datei vorher, und nachher die Zahl der Stellen OHNE
  Wert-Assertion/Gegenprobe = 0.
- 6/6 Skip-Stellen erscheinen als `skip`-Zeile und in der Summenzeile; ohne die jeweilige Fixture endet die Datei
  weiterhin mit Exit 0, MIT Fixture laufen die Assertions.
- `node tests/_mutations-probe.js` meldet fuer alle s-mut2-Paare `killed` (oder `cli-only` mit einzeiliger Begruendung
  im JSON-Feld `"grund"`), Exit 0.
VERIFIKATION:
- Jede angefasste Datei einzeln: `node tests/<datei>` Exit 0.
- `node tests/skip-honesty.test.js` Exit 0; `node tests/_mutations-probe.js` Exit 0.
- Volle Gate-Suite wie in S-MUT1 (gleiche zwei erlaubte Rote).
- Stichprobe Gegenprobe: eine der neu ergaenzten `assert.throws`-Gegenproben voruebergehend auf die gueltige Eingabe
  umstellen -> Datei muss rot werden; zurueckdrehen.

## S16 · Lane B · Effort medium — screener-data tests/: Existenz-Assertions auf Datenobjekte -> Inhalt
ZIEL:
- Jede gelistete `assert.ok(<objekt>)`-Stelle so ersetzen/ergaenzen, dass der INHALT gegen den Fixture-Wert derselben
  Datei geprueft wird (`deepEqual`/`equal`/`match`), nie nur die Existenz:
  1. `tests/board-history-p99-sidecar.test.js:61` -> `deepEqual(Object.keys(sc.byDate['2026-08-01']), ['semiconductors'])`.
  2. `:82` -> zusaetzlich `deepEqual(Object.keys(sc.byDate).sort(), ['2026-08-01','2026-08-02'])`.
  3. `:90` -> Sidecar nach Tag 1 einlesen, Tag-1-Eintrag nach Tag 2 per `deepEqual` byte-gleich (Merge, kein Ueberschreiben).
  4. `tests/board-history.test.js:326` -> `deepEqual(v.cohort.profitable[0].pit, { beta: 1.5 })`.
  5. `tests/cn-jahresreihen.test.js:672` -> `deepEqual(store['FREMD.SS'], { source: 'alt', fys: [2024] })`.
  6. `:673` -> `store['688256.SS'].fys`/`source` gegen `FIX` deepEqual.
  7. `tests/e1-compression.test.js:107` -> `cand.ticker === 'CHEAP_HI'` PLUS das ausloesende Regel-/Grund-Feld des Kandidaten.
  8. `tests/in-nse-adapter.test.js:342` -> `fy2026` traegt das spaeteste Verbreitungsdatum der beiden Kandidaten (Feld
     aus der Fixture `listen('OFSS')`).
  9. `:655` und 10. `:702` -> `j['OFSS.NS'].nfy` (bzw. das GJ-Feld) gegen den Fixture-Wert.
  11.-14. `tests/p0-haertung3-builder-cache-cursor.test.js:97`, `:107`, `:156`, `:176` -> `deepEqual(danach['ALT.KS'], ALT_STORE['ALT.KS'])`.
  15. `tests/revenue-estimates-persistenz.test.js:143` -> `deepEqual(s.external.revenueEstimates['0y'], { avg: 500, growth: 0.1, numberOfAnalysts: 4 })`
      (exakte Form aus dem Fixture derselben Datei uebernehmen).
  16. `tests/rule40-guards.test.js:222` -> `res.bounds`: beide Schranken endlich, `lo < hi`, und `p.revGrowthPctUsed`
      ist exakt die obere Schranke.
  17. `tests/p1-welle9-quellen-wahrheit.test.js:386` -> `assert.match(e.failedAt, /^\d{4}-\d{2}-\d{2}T/)` und
      `Date.parse(e.failedAt) >= Date.parse(prev.fetchedAt)`.
  18. `tests/bk-sk-001-mitschnitt-stille-pannen.test.js:133` -> `wert` inhaltlich gegen `neu` (sortierte Schluessel
      `['A','C']`, Werte deepEqual).
  19. `tests/exit-event-resolver.test.js:429` -> `unresolvedReason` ist Element der im Modul exportierten Grund-Menge
      (falls keine Enum: nicht-leerer String, `assert.match(..., /\S/)`).
  20. `tests/studie-c0.test.js:122` -> `assert.match(eintrag.sha256, /^[0-9a-f]{64}$/)`.
  21. `tests/studie-f6-konfirmatorisch.test.js:317` -> `restrisiko[k]` Typ pruefen (String >= 10 Zeichen bzw. Objekt mit Textfeld).
  22. `tests/studie-e3-praereg.test.js:217` -> `fs.existsSync(P(regel.waechter.test))`.
  23. `tests/board-history-datenschub-zuschlag.test.js:213` -> `g.verfallsZeilen[0].feld === '<im Fixture verfallenes Feld>'`.
  24. `tests/aktienfinder-import-root-shape.test.js:53` und 25. `tests/tsx-total-failure-partial.test.js:42` ->
      Zustandsobjekt des Offline-Guards auf seine Felder pruefen (Modul ueber `offlineGuard`/`OFFLINE_STATE` im Test
      finden; z. B. `installed === true`, Zaehler `typeof 'number'`).
- Paar-Datei `tests/_mutations-pairs/s-mut3.json` (Format aus S-MUT1): `scripts/write-board-history.js#run` ->
  `tests/board-history-p99-sidecar.test.js` und `tests/board-history.test.js`; `scripts/build-krannual.js#main` ->
  `tests/p0-haertung3-builder-cache-cursor.test.js`; Modul `B` aus `tests/cn-jahresreihen.test.js` `#main` und
  `#pruefeSchema`; Modul `E` aus `tests/e1-compression.test.js` `#evaluateBoard`; Modul `M` aus
  `tests/in-nse-adapter.test.js` `#waehleMeldungen`; Modul `T` aus `tests/bk-sk-001-...` `#rotiereGrundbild`;
  Modul `R` aus `tests/exit-event-resolver.test.js` (die Resolver-Hauptfunktion). Jedes Paar empirisch einordnen.
ZIEL-DATEIEN: die 17 oben genannten Testdateien unter `tests/`, `tests/_mutations-pairs/s-mut3.json` (neu). Keine
Ueberschneidung mit S-MUT2 (disjunkte Dateien).
FERTIG-WENN:
- 25/25 Stellen pruefen Inhalt; im Commit-Body Zaehlung `vorher 25 Existenz-only -> nachher 0`, je Stelle eine Zeile
  „file:line -> <Assertion>".
- Alle s-mut3-Paare `killed` (oder begruendet `cli-only`); `node tests/_mutations-probe.js` Exit 0.
- Kein Produktivcode angefasst; Auffaelligkeiten (z. B. Feld existiert, traegt aber nie den Fixture-Wert) als `BEFUND:` im Commit-Body.
VERIFIKATION:
- Jede angefasste Datei: `node tests/<datei>` Exit 0. `node tests/_mutations-probe.js` Exit 0.
- Volle Gate-Suite wie in S-MUT1 (gleiche zwei erlaubte Rote).
- Stichprobe: in einer Datei den Fixture-Wert (z. B. `beta: 1.5` -> `1.6`) voruebergehend aendern -> die neue
  Assertion muss rot werden; zurueckdrehen.

## S17 · Lane B · Effort high — Scripts-Smoke: jedes `scripts/*.js` mit `--help`, ohne Argumente und `--dry-run` in einer Wegwerf-Kopie, Report + Fix der reinen Argument-Abstuerze
ZIEL: Bestand: 134 `scripts/*.js` (dazu 99 `.py` und 1 `.mjs` — NICHT Teil dieses Sweeps).
Kein einziges der 134 kennt `--help` (grep `--help` in `scripts/*.js`: 0 Treffer); 54 haben einen
Usage-Text, nur 2 nutzen die saubere Bauform von `scripts/gqs00-freeze.js:687-688`
(`console.error('Usage: ...'); process.exitCode = 2;`); 9 kennen `--dry-run`
(`archive-old-snapshots`, `b1-validate`, `backfill-prices-max`, `backfill-prices-research`,
`opinc-source-migrate`, `prune-watchlist`, `reconcile-smallcap`, `write-board-history`,
`write-newcomer-log`; Muster `scripts/archive-old-snapshots.js:53`); 111 haben einen
`require.main === module`-Guard, 23 nicht (Liste unten). Messlauf heute (Wegwerf-Kopie per
`git archive`, `snapshots/` LEER, 15 s Timeout, 114 Skripte gestartet, 18 netzfaehige uebersprungen):
`--help` -> 20 Stacktrace, 13 Usage-Exit, 46 sauberer Fehler-Exit ohne Stack, 29 Exit 0 (Skript hat
`--help` ignoriert und Default-Arbeit getan), 6 Timeout; ohne Argumente -> 17 / 14 / 43 / 30 / 9.
Aufgabe: (1) Runner `scripts/tag<n>a-scripts-smoke.js` schreiben (Einmal-Skript-Muster laut
`scripts/AGENTS.md`; `child_process.spawnSync(process.execPath, [skript, ...args], { cwd: kopie,
timeout: 20000, encoding: 'utf8', env: { ...process.env, HTTPS_PROXY: '', HTTP_PROXY: '' } })`),
der pro Skript die drei Modi faehrt und Exit-Code, Timeout-Flag, Anzahl Stacktrace-Zeilen
(`/^\s+at .*\.js:\d+/`) und erste stderr-Zeile (max. 110 Zeichen) sammelt.
(2) Wegwerf-Kopie AUSSERHALB von Repo und Worktree: `git archive --format=zip -o $env:TEMP\sd-smoke.zip HEAD`,
`Expand-Archive`, `Copy-Item -Recurse node_modules`, dann `snapshots\` loeschen und LEER neu anlegen
(die getrackte `snapshots/_manifest.json` darf NICHT drin sein). Nichts aus der Kopie wird committet.
(3) Netz-Sperrliste (27, NIE starten, nur `node --check` + Zeile „Netz" im Report):
`b1-instrument b1-validate backfill-form345 backfill-prices-max backfill-prices-research
backfill-prices build-cnannual build-inannual build-jpannual build-krannual build-secannual
build-twannual d2-submissions-bulk druckenmiller-13f enrich-q-revenue exit-event-resolver
fetch-secbulk plan-check probe-smallcap-coverage pull-13f-institutional pull-insider-form4-daily
pull-insider-form4 refresh-fx sec-pit-check snapshot-ticker-map test-gate value-spot-check`
(`enrich-q-revenue` hat im Messlauf trotz statisch unauffaelligem Kopf SEC erreicht: „[SEC] Failed:
HTTP 403"). Zeigt ein weiteres Skript im Lauf eine Netz-Meldung: sofort in die Liste, nicht zaehlen.
(4) Fix-Klassen — NUR diese, NUR im `require.main === module`-Block bzw. in der argv-Auswertung,
nie in exportierten Funktionen, keine Datenlogik: K1 `--help`/unbekanntes Argument wird ignoriert
und das Skript arbeitet los (29 Faelle bei `--help`, z. B. `heartbeat-preis-abdeckung`,
`data-quality-report`, `reconcile-smallcap`) -> `--help` erkennt Usage-Zeile (aus dem
Kopfkommentar `Run:`/`Usage:` der Datei, sonst aus der argv-Auswertung abgeleitet) und beendet mit
Exit 0 VOR jeder Datei-/Netz-Aktion. K2 Pflicht-Argument fehlt -> Stacktrace (TypeError auf
`undefined`, `ENOENT ... undefined`, `AssertionError` als Usage-Ersatz; z. B. `build-findash-name-map`,
`count-basic-coverage`, `formel-struktur-uebersicht`, `t-kdrift-signatur`, `t-veraltung-zwei-definitionen`,
`probe-smallcap-messlauf3`) -> `console.error('Usage: ...'); process.exitCode = 2; return;`.
K3 hart codierter fremder Absolutpfad als Default (`scripts/t-jahresausreisser-klassifikation.js:10`,
`scripts/t-kdrift-signatur.js:13`, `scripts/t322-ads-verhaeltnis-optionen.js:10` — alle
`C:/Users/Anwender/AppData/Local/Temp/claude/...`) -> Default entfernen, Pfad wird Pflicht-Argument
mit Usage. K4 unbehandelter `throw` einer Daten-Vorbedingung im main-Block (z. B.
`build-secannual-smallcap`: „kein snapshots-smallcap/ Universum") -> `try/catch` um den main-Aufruf:
`console.error(e.message); process.exitCode = 1;` (die geworfene Funktion selbst bleibt unveraendert,
Tests koennen den Wurf weiter erwarten). NICHT anfassen: Skripte, die ohne Argumente Exit 0 liefern
und Default-Arbeit tun (30) — das ist CI-Verhalten (daily-pull.yml ruft sie so auf); die 9
Timeouts (`k1-coverage-sim-a/b`, `k1-heterogen-sim`, `k1-reparatur-sim-a/b`, `rank-ic`,
`walk-forward-perf`, `test-offline-fixtures`, `build-twannual`) sind Simulationen/Netz, nur Klasse
„laeuft (>20 s)" im Report; die 6 weiteren hart codierten Pfade (`d2-submissions-bulk.js:67`,
`enrich-q-revenue.js:31`, `studie-f6-konfirmatorisch.js:145` u. a.) nur unter OFFEN melden (Netz-
oder Studien-Skripte). (5) Report `reports/codex-scripts-smoke-2026-09-24.md`: Tabelle
Skript | Netz? | `--help` (rc/Klasse) | ohne Args (rc/Klasse) | `--dry-run` (rc/Klasse, nur die 9) |
erste stderr-Zeile; Abschnitt „Gefixt" (Datei:Zeile, Klasse K1-K4, vorher -> nachher), Abschnitt
„Ohne require.main-Guard (23)": `anchor-regression-nullmcap b1-instrument backfill-prices-research
ccy-alarm-gate count-basic-coverage einmalertrag-trefferquote formel-struktur-uebersicht
k1-boardstruktur-mess k1-coverage-sim-a k1-reparatur-sim-a migrate-price-history-shards
pipeline-health-check probe-auflage1-messlauf probe-datenplausibilitaet probe-emittenten-zwillinge
probe-issuer-branchenkonflikt probe-issuer-strict-key-punct rank-ic-families sec-pit-check
t-jahresausreisser-klassifikation t135-paritaetsluecke tag229a-stale-snapshot-verify
test-gate-node-test-probe` (nur Liste, kein Umbau), Abschnitt OFFEN.
ZIEL-DATEIEN: `scripts/tag<n>a-scripts-smoke.js` (neu), `reports/codex-scripts-smoke-2026-09-24.md`
(neu), geaenderte `scripts/*.js` NUR aus der Netz-freien Menge und NUR in main-Block/argv-Auswertung.
Tabu: alles in `.codex-deny.txt`, `src/scoring/**`, `methods/**`, `README.md`, `docs/findash-export-v1*`,
die 27 Netz-Skripte (nur lesen), `scripts/test-gate.js` (ist Gate-Infrastruktur).
FERTIG-WENN: Report listet alle 134 Skripte (134 Tabellenzeilen, 27 davon „Netz"); zweiter
Messlauf nach den Fixes: Klasse „Stacktrace" bei `--help` 20 -> 0 und ohne Argumente 17 -> 0
(Daten-Vorbedingungen duerfen Exit 1 bleiben, aber ohne Stacktrace), Klasse „Exit 0 trotz --help"
29 -> 0; jedes geaenderte Skript antwortet auf `--help` mit einer `Usage:`-Zeile (stderr bei K2,
stdout bei K1) und Exit 0 (K1) bzw. 2 (K2/K3); `node scripts/test-gate.js --mode=blocking` gruen
(einzige erlaubte Rote: die zwei bekannt-lokalen aus QUEUE.md).
VERIFIKATION: `node scripts/tag<n>a-scripts-smoke.js --kopie $env:TEMP\sd-smoke --out reports/codex-scripts-smoke-2026-09-24.md`
vor und nach den Fixes (beide Klassen-Zaehler im PR-Body, vorher -> nachher); fuer jede
geaenderte Datei `node scripts/<datei>.js --help; echo $LASTEXITCODE` und `node --check`; fuer jede
geaenderte Datei die passenden Tests `Get-ChildItem tests/*<basename>*.test.js | % { node $_.FullName }`
(z. B. `tests/backfill-prices-arg-presence.test.js`, `tests/data-quality-report-arg-presence.test.js`,
`tests/archive-old-snapshots-keep-days-parser.test.js`); zum Schluss
`node scripts/test-gate.js --selftest` und `node scripts/test-gate.js --mode=blocking`.

## S18 · Lane A · Effort medium — JSDoc fuer alle Funktions-Exports in `lib/**` (115 -> 0) + Waechter `tests/jsdoc-exports.test.js`
ZIEL: Gemessen (statischer Parser ueber `module.exports = {...}`, `module.exports.x =`, `exports.x =`,
Definition per `function name(` / `const name = (async)? (function|(...) =>)`): 243 Funktions-Exports
in `lib/*.js` (25 Module, ohne `*.test.js`) + `lib/druckenmiller/*.js` (11 Module); 115 haben KEINEN
`/** ... */`-Block direkt ueber der Definition. Verteilung (fehlend/gesamt): `lib/early-detection.js`
33/33, `lib/studie-verfassung.js` 17/17, `lib/price-history-store.js` 11/11, `lib/sec-pit.js` 11/11,
`lib/e1-compression.js` 10/10, `lib/e2-earnings-blowout.js` 10/11, `lib/b1-detect.js` 6/6,
`lib/druckenmiller/ledger.js` 3/11, `lib/atomic-write.js` 2/2, `lib/druckenmiller/scoreboard.js` 2/25,
`lib/druckenmiller/thirteenf.js` 2/12, `lib/druckenmiller/universe.js` 2/9, `lib/read-json.js` 1/1,
`lib/sec-user-agent.js` 1/2, `lib/sub-profile.js` 1/1, `lib/zip-stream.js` 1/5,
`lib/druckenmiller/raw.js` 1/2, `lib/druckenmiller/registration.js` 1/2. Sicher: kein Test hasht
lib-Quelltext (`scripts/gqs00-freeze.js:255-261` pinnt nur `src/scoring/**`;
`tests/t204-welle7b-atomic-write.test.js` hasht Datenbytes), `tests/druckenmiller/import-graph.test.js`
prueft nur den Require-Graphen. Aufgabe: (1) Fuer jeden fehlenden Fall einen JSDoc-Block direkt
ueber der Definition: erste Zeile = Zweck in einem Satz; je Parameter `@param {Typ} name - Bedeutung`
(Typ aus dem Code ableiten: Destrukturierung -> `{{a: number, b?: string}}`, Arrays -> `{number[]}`,
kein `{*}` wo der Code den Typ zeigt); `@returns {Typ}`; `@throws {Error}` wenn im Rumpf `throw` steht.
Sprache = Sprache der Datei (deutsche Dateien wie `studie-verfassung.js` deutsch, englische wie
`early-detection.js` englisch). Bestehende Kommentare NICHT umformulieren, keine Zeile Code
aendern, keine Konstanten-Exports pflichtig (optional). (2) `tests/jsdoc-exports.test.js` als
Standalone-Runner nach `tests/AGENTS.md` (`node <datei>` -> Exit 0/1, `node:assert/strict`, kein
Framework, kein Netz): liest `lib/*.js` (ohne `*.test.js`) + `lib/druckenmiller/*.js`, ermittelt
Exportnamen (drei Formen oben, mehrzeilige `module.exports = {` inklusive), findet die Definition
(vier Formen oben), verlangt einen `/**`-Block, der nach dem Ueberspringen von Leerzeilen
unmittelbar vor der Definitionszeile endet, mit nicht-leerer erster Zeile und — wenn die Funktion
Parameter hat — mindestens einem `@param`; druckt `funktions-exports=<N> fehlend=<M>` und listet jede
Fehlstelle als `datei:zeile name`. Nicht-Funktions-Exports (Konstanten, Klassen, Re-Exports) werden
uebersprungen, nicht gezaehlt. (3) Beweis „nur Kommentare": Wegwerf-Skript (nicht committen), das
aus `git show origin/main:<datei>` und der Arbeitskopie alle `/* */`- und `//`-Kommentare entfernt,
Whitespace normalisiert und Byte-Gleichheit meldet — je Datei `IDENT` im PR-Body.
ZIEL-DATEIEN: `lib/*.js` (nur Kommentarzeilen), `lib/druckenmiller/*.js` (nur Kommentarzeilen),
`tests/jsdoc-exports.test.js` (neu). Nicht: `lib/*.test.js`, `src/**`, `scripts/**`.
FERTIG-WENN: `node tests/jsdoc-exports.test.js` -> Exit 0 mit `funktions-exports=243 fehlend=0`
(vorher: `fehlend=115`; weicht die Gesamtzahl um mehr als +-5 ab, Parser-Differenz im PR erklaeren);
Kommentar-Strip-Vergleich fuer alle 18 geaenderten Dateien `IDENT`; alle `lib/*test.js` (8 Dateien)
und `tests/druckenmiller/*test.js` Exit 0; `node --check` fuer jede geaenderte Datei Exit 0.
VERIFIKATION: `node tests/jsdoc-exports.test.js; echo $LASTEXITCODE` (Zaehler vorher -> nachher im
PR-Body); `Get-ChildItem lib/*test.js,tests/druckenmiller/*test.js | % { node $_.FullName; if ($LASTEXITCODE) { "FAIL: $($_.Name)" } }`;
`git diff --stat origin/main -- lib/` (nur Zeilen-Zugaenge, keine Loeschungen ausser Leerzeilen-Verschiebung);
Strip-Vergleich je Datei.

## S19 · Lane A · Effort medium — Syntax-Smoke als Gate-Test: `node --check` ueber alle getrackten JS/MJS, `JSON.parse` ueber getrackte JSON, engines/Lockfile-Konsistenz
ZIEL: Gemessen: `git ls-files '*.js' '*.mjs'` = 812 Dateien (811 `.js`: 567 unter `tests/`, 134 unter
`scripts/`, Rest `lib/`, `src/`, Root-CLIs, `entwurf/` usw.; 1 `.mjs`:
`scripts/early-detection-store-inventory.mjs`); `node --check` sequentiell: 0 Fehler in 52 s.
`git ls-files '*.json'` = 1.611 Dateien, `JSON.parse` 0 Fehler in 27,6 s (der Loewenanteil sind
History-Ordner). `package.json` engines `node >=22` == `package-lock.json` `packages[""].engines`
`>=22`, lockfileVersion 3, 1 Dependency (`yahoo-finance2 ^3.15.4`) in beiden identisch; alle 5
`node-version: '22'`-Stellen in `.github/workflows/*.yml` passen zum engines-Major. Heute prueft das
KEIN Test — eine Datei mit Syntaxfehler, die kein Test importiert (z. B. ein Einmal-Skript), faellt
erst im Nachtlauf auf. Aufgabe: `tests/syntax-smoke.test.js` (Standalone-Runner wie in
`tests/AGENTS.md`, CommonJS, `node:assert/strict`, kein Netz): (a) Dateiliste per
`execFileSync('git', ['ls-files', '-z', '*.js', '*.mjs'])` (nie `find`, damit node_modules/Scratch
draussen bleiben); je Datei `spawnSync(process.execPath, ['--check', datei])`, mit 8 parallelen
Kindprozessen (Promise-Pool ueber `child_process.spawn`), Ziel < 15 s; jede Fehlermeldung mit
Datei + erster stderr-Zeile sammeln. (b) JSON: `git ls-files -z '*.json'` MINUS Praefixe
`score-history/`, `board-history/`, `picks-history/`, `methods-history/`, `r40rx-history/`,
`druckenmiller-history/`, `board-history-archive/` (gross, zudem TABU) -> `JSON.parse`, Anzahl im
Output. (c) `package.json`: `engines.node` gleich `package-lock.json.packages[""].engines.node`;
jede `dependencies`-Angabe identisch im Lock-Root und als `packages["node_modules/<name>"]`
vorhanden; `lockfileVersion === 3`. (d) Nur LESEN (`.github/` bleibt TABU fuer Aenderungen): jede
`node-version:`-Angabe in `.github/workflows/*.yml` hat denselben Major wie `engines.node`.
Ausgabe: `js=<n> json=<m> lock=ok workflows=<k> fehler=0 ms=<t>`. Windows-tauglich (`process.execPath`,
`path.join`, `-z`-Trennung, keine Shell-Globs).
ZIEL-DATEIEN: `tests/syntax-smoke.test.js` (neu). Sonst nichts — findet der Test einen echten
Syntax-/JSON-Fehler in einer Datei ausserhalb der TABU-Pfade, den Fix im selben PR mit Datei:Zeile;
innerhalb TABU nur OFFEN melden.
FERTIG-WENN: `node tests/syntax-smoke.test.js` -> Exit 0, Ausgabe `js=812` (+-die im Worktree
getrackte Zahl), `json` = Anzahl der getrackten JSON ausserhalb der History-Praefixe (im PR-Body
nennen), `fehler=0`, Laufzeit < 60 s auf Karls Maschine (ms im PR-Body); Gegenprobe: eine temporaer
kaputte Datei (`echo "const = ;" > entwurf/_probe.js`, NICHT committen, danach loeschen) macht den
Test rot mit Datei + Meldung; `node scripts/test-gate.js --selftest` gruen (der Waechter zaehlt die
neue Datei ueber `tests/*test.js` automatisch mit).
VERIFIKATION: `node tests/syntax-smoke.test.js; echo $LASTEXITCODE` (Zaehler im PR-Body);
Gegenprobe wie oben mit Exit 1; `node scripts/test-gate.js --selftest; echo $LASTEXITCODE`;
`git ls-files '*.js' '*.mjs' | Measure-Object -Line` zum Abgleich der Dateizahl.

## S20 · Lane C · Effort high — backfill-form345: externes `unzip` durch lib/zip-stream.js ersetzen (Windows ohne unzip)
ZIEL: scripts/backfill-form345.js bricht heute ohne `unzip`-Binary auf dem PATH ab (Windows-Standard): Zeile 49 `const { execFileSync } = require('child_process');`, Zeile 136-139 `hasUnzip()` = `execFileSync('unzip', ['-v'], ...)`, Zeile 287 `execFileSync('unzip', ['-o', '-q', zipPath, '-d', extractDir], { stdio: 'ignore' });`, Zeile 413-420 harter Abbruch `BACKFILL REQUIRES \`unzip\`` mit `process.exit(2)`, Kopf-Kommentar Zeile 30-34 ("Node has no built-in zip reader. We shell out to `unzip`"). Das Repo hat aber einen reinen Node-ZIP-Leser: lib/zip-stream.js exportiert (Zeile 161-165) `findeEocd, leseVerzeichnisZeiger(schwanz, dateiGroesse), leseVerzeichnis(cd, anzahlErwartet), entpackeEintrag(block, eintrag), bytesFuer(eintrag), EOCD_MAX_SUCHE` (Methode 0 = store und 8 = deflate, ZIP64-faehig, Block beginnt am lokalen Kopf `lfhOffset`). Nutzungsmuster steht in scripts/fetch-secbulk.js:198-206 (Schwanz -> Zeiger -> Zentralverzeichnis) und :226 `zip.entpackeEintrag(block.subarray(e.lfhOffset - b.von), e)`. Nach dem Entpacken liest der Rest nur drei Dateien per `findTsv(extractDir, ...)` (Zeile 294-296: SUBMISSION.tsv, NONDERIV_TRANS.tsv, REPORTINGOWNER.tsv; findTsv Zeile 215-227 sucht rekursiv und case-insensitiv) und `parseTsv(<pfad>, ...)` von Platte (Zeile 303, 326, 350ff); Aufraeumen Zeile 402-403 (`fs.rmSync(extractDir)`/`rmSync(zipPath)`). Aufgabe: Funktion `entpackeZipLokal(zipPath, extractDir, gewuenschteNamen)` im Skript bauen — `fs.openSync/fstatSync/readSync`: Schwanz `Math.min(groesse, EOCD_MAX_SUCHE)` Bytes vom Ende lesen, `leseVerzeichnisZeiger(schwanz, groesse)`, Zentralverzeichnis (`cdOffset..cdOffset+cdGroesse`) lesen, `leseVerzeichnis(cd, anzahl)`, dann NUR Eintraege, deren Basisname (letztes Segment nach '/') case-insensitiv in `gewuenschteNamen` liegt, mit `Math.min(bytesFuer(e), groesse - e.lfhOffset)` Bytes ab `e.lfhOffset` lesen, `entpackeEintrag(block, e)` und unter `path.join(extractDir, ...e.name.split('/'))` schreiben (Unterordner per mkdirSync recursive anlegen; Eintragsnamen mit `..`-Segment oder fuehrendem `/` -> Error). Rueckgabe: Liste der geschriebenen Pfade. Zeile 287 durch den Aufruf mit den drei TSV-Namen ersetzen (der catch-Zweig Zeile 288-291 bleibt: Warnung + `{ skipped: true, error: 'zip' }`), `hasUnzip` und den Abbruch Zeile 413-420 entfernen (kein Test referenziert `hasUnzip`: grep in tests/ und lib/ = 0 Treffer; einziger Nutzer ist Zeile 413), `require('child_process')` entfernen, Kopf-Kommentar Zeile 30-34 anpassen, `_internals` (Zeile 481) um `entpackeZipLokal` ergaenzen. Keine neue Dependency, keine Aenderung an parseTsv/findTsv/Merge/Cleanup. Byte-Identitaet zum bisherigen `unzip -o`-Ergebnis wird per Test gepinnt: der Inhalt jeder geschriebenen TSV muss byte-gleich dem Quell-Inhalt sein. tests/helpers/zip-fixture.js:30 `baueZip(dateien, opt)` baut portable Fixtures (opt.gespeichert = store fuer das GANZE Archiv, sonst deflate; `zusatzLen` pro Datei = Zusatzfeld nur im lokalen Kopf, Zeile 40).
ZIEL-DATEIEN: scripts/backfill-form345.js, tests/backfill-form345-zip-portable.test.js
FERTIG-WENN: (1) `Select-String -Path scripts/backfill-form345.js -Pattern 'execFileSync|child_process'` liefert 0 Treffer (vorher 3: Zeile 49, 137, 287). (2) tests/backfill-form345-zip-portable.test.js (Standalone-Runner, node:assert, Exit 0/1) baut mit `baueZip` ZWEI Archive — eines deflate (default), eines `{ gespeichert: true }` — mit je: `SUBMISSION.tsv` (mit `zusatzLen: 12`), `sub/NONDERIV_TRANS.tsv` (Unterordner), `Reportingowner.TSV` (Mischschreibung, muss gefunden und unter genau diesem Namen geschrieben werden), `NOTES.txt` (Ballast, darf NICHT auf Platte landen); schreibt sie in mkdtemp, ruft `_internals.entpackeZipLokal` auf und prueft: genau 3 geschriebene Dateien, jede byte-gleich (`Buffer.equals`) zum Fixture-Inhalt, `findTsv(extractDir, 'REPORTINGOWNER.tsv')` findet die Mischschreibung, `NOTES.txt` existiert nicht, ein Eintrag `../evil.tsv` fuehrt zum Error (nichts geschrieben). (3) `node tests/backfill-form345-history-integrity.test.js` bleibt Exit 0 (heute Exit 0). (4) `require('./scripts/backfill-form345.js')` startet weiterhin nichts (require.main-Guard Zeile 475 bleibt).
VERIFIKATION: `node tests/backfill-form345-zip-portable.test.js; $LASTEXITCODE` (erwartet 0) ; `node tests/backfill-form345-history-integrity.test.js; $LASTEXITCODE` (erwartet 0) ; `(Select-String -Path scripts/backfill-form345.js -Pattern 'execFileSync|child_process' | Measure-Object).Count` (erwartet 0, vorher 3) ; `(Select-String -Path scripts/backfill-form345.js -Pattern 'entpackeZipLokal' | Measure-Object).Count` (erwartet >= 3: Definition, Aufruf, Export)

## S21 · Lane B · Effort medium — restore-test-prices-max nach Node portieren (Vollvergleich statt 20er-Stichprobe, F-CGPT-18)
ZIEL: scripts/restore-test-prices-max.ps1 ist PowerShell-only und traegt den bestaetigten Befund F-CGPT-18 (harter-pruef-sweep-8433206.md:113-118, schwere=hoch; harter-pruef-sweep-8433206-verifikation.md:304-312 verdikt=BESTAETIGT mit Repro: 21 Dateien, `f20.json` laengengleich veraendert -> "GRUEN ... exit=0"): Zeile 23-25 hasht nur `Select-Object -Index (0..19 ...)` = 20 Dateien, Zeile 37 `$ok = ($o.Count -eq $b.Count) -and ($o.Bytes -eq $b.Bytes) -and ($o.Sample -eq $b.Sample)`, Zeile 39 sagt selbst "KEIN Vollbeweis". Kein Node-Pendant im Repo (grep `restore-test-prices-max` trifft nur die .ps1, die zwei Sweep-Dokumente und audit-reports/2026-08-09-p1-chunk5-meldung.md:6). Neues portables, rein lesendes Skript scripts/restore-test-prices-max.js: `node scripts/restore-test-prices-max.js --backup <dir> [--original <dir>]` (Default --original = `<repo>/prices-max`, gitignored per .gitignore:145 `prices-max/`). Beide Baeume rekursiv einlesen (fs.readdirSync withFileTypes, nur regulaere Dateien), Schluessel = Relativpfad posix-normalisiert (`path.relative(...).split(path.sep).join('/')`), pro Datei Groesse + sha256 (crypto.createHash ueber readFileSync). Ausgabe: Zaehler Original/Backup (Dateien, Bytes), dann Listen FEHLT-IM-BACKUP / ZUSAETZLICH-IM-BACKUP / ABWEICHEND (Groesse oder Hash), jede Liste maximal 50 Zeilen, danach `... und N weitere`. Exit 0 = identisch, 1 = Abweichung, 2 = Argument-/Pfadfehler (fehlendes --backup, nicht existierendes Verzeichnis). Nichts wird geschrieben oder geloescht, kein Shell-Aufruf, keine '/'-Stringkonkatenation (nur path.join/path.relative). Die .ps1 bleibt inhaltlich unangetastet (nicht loeschen, keine Logikaenderung); erlaubt ist einzig eine Kommentarzeile im Kopf, die auf das .js verweist.
ZIEL-DATEIEN: scripts/restore-test-prices-max.js, tests/restore-test-prices-max.test.js, scripts/restore-test-prices-max.ps1 (nur eine Kommentarzeile im Kopf, sonst nichts)
FERTIG-WENN: tests/restore-test-prices-max.test.js (Standalone-Runner, node:assert, spawnSync(process.execPath, [...]), Exit 0/1) baut in mkdtemp zwei Baeume mit mindestens 25 Dateien in 3 Unterordnern (mehr als die alte 20er-Stichprobe) und prueft: (a) identische Kopie -> Exit 0; (b) eine Datei im Backup mit GLEICHER Laenge und anderem Inhalt (der F-CGPT-18-Fall, Datei jenseits der ersten 20 in Sortierreihenfolge) -> Exit 1 und der posix-Relativpfad (mit '/') steht in stdout; (c) fehlende Datei -> Exit 1 mit Pfad unter FEHLT; (d) zusaetzliche Datei -> Exit 1 mit Pfad unter ZUSAETZLICH; (e) fehlendes --backup -> Exit 2; (f) --backup auf nicht existierenden Pfad -> Exit 2; (g) nach jedem Lauf ist der Original-Baum byte-gleich unveraendert (Hash-Vergleich vor/nach). Das Skript enthaelt kein `execSync`/`spawnSync`/`child_process` und kein `writeFileSync`/`rmSync`/`unlinkSync`.
VERIFIKATION: `node tests/restore-test-prices-max.test.js; $LASTEXITCODE` (erwartet 0) ; `(Select-String -Path scripts/restore-test-prices-max.js -Pattern 'child_process|writeFileSync|rmSync|unlinkSync' | Measure-Object).Count` (erwartet 0) ; `git diff --stat -- scripts/restore-test-prices-max.ps1` (erwartet: 0 oder genau 1 geaenderte Zeile)

## S22 · Lane A · Effort medium — CRLF-tolerante Textleser fuer Tests, die Workflow-YAML/Quelltext zeilenweise verarbeiten
ZIEL: .gitattributes pinnt `text eol=lf` nur fuer early-detection-, studie-, src/scoring-, protocol- und druckenmiller-Pfade — NICHT fuer .github/workflows/*.yml, .gitignore, refresh-universe.js oder tests/*.js. Auf einem Windows-Checkout mit core.autocrlf=true (CLAUDE.md "Fallen": eine bekannte Zeilenende-Klasse) liegen diese Dateien mit CRLF im Baum, und mehrere Tests reichen die Zeilen mit haengendem '\r' weiter: tests/rank-ic-publish.test.js:26 liest daily-pull.yml, :37 `for (const l of rest.split('\n')) { ... lines.push(l.slice(10)) ... }`, :42 `spawnSync('bash', ['-c', script], ...)` (bash bricht bei '\r' mit `$'\r': command not found` ab); tests/pr-check-live-gate.test.js:25-26 liest pr-check.yml + daily-pull.yml, :44 identische split('\n')-Logik, :54 `spawnSync('bash', ['-c', script], ...)`; tests/f12-merge-filter.test.js:360-361 liest .gitignore und prueft `assert.match(gi, /^snapshots-eingang\/$/m, ...)` — `$` im m-Modus steht vor '\n', nicht vor '\r', der Match faellt unter CRLF; :320 YML, :371 split('\n'). Weitere Leser derselben Klasse ohne Normalisierung: tests/zweig-wache.test.js:43 `lies` (WF_DIR), split('\n') an :76-77, :150, :169, :255, :310; tests/weg-e-waechter-verdrahtung.test.js:36 (daily-pull.yml), :107 split('\n'); tests/refresh-universe.test.js:114-115 und :133-134 (refresh-universe.js), :307 `SRC_RU.split('\n')`, :503-504 (daily-pull.yml). Gemessen in tests/*.js: 29 Dateien nutzen split('\n'), 38 split(/\r?\n/), 13 ein `.replace(/\r\n/g, '\n')` (z. B. tests/druckenmiller/ci-wiring.test.js:20) — drei Idiome fuer eine Sache. Aufgabe: Helfer tests/helpers/lies-text.js (tests/helpers/ existiert: bridge-pin-chain.js, offline-network-guard.js, pin-abdeckung.js, zip-fixture.js) mit `liesText(pfad)` = `fs.readFileSync(pfad, 'utf8').replace(/\r\n?/g, '\n')` und `zeilen(text)` = `text.split(/\r?\n/)`. In den sechs Testdateien JEDE readFileSync(...,'utf8')-Stelle auf .yml/.js/.gitignore-Dateien des Repos (nicht mkdtemp-Ausgaben, nicht JSON.parse-Aufrufe, nicht node_modules) durch `liesText` ersetzen — exakt die oben genannten Zeilen; alle Assertions, Marker und Sabotage-Mutationen unveraendert lassen. NICHT anfassen: tests/druckenmiller/**, tests/studie-*, tests/early-detection*, tests/scoring/**, .gitattributes.
ZIEL-DATEIEN: tests/helpers/lies-text.js, tests/lies-text-helper.test.js, tests/rank-ic-publish.test.js, tests/pr-check-live-gate.test.js, tests/f12-merge-filter.test.js, tests/zweig-wache.test.js, tests/weg-e-waechter-verdrahtung.test.js, tests/refresh-universe.test.js
FERTIG-WENN: (1) tests/lies-text-helper.test.js (Standalone-Runner, node:assert, Exit 0/1) schreibt in mkdtemp dieselbe Datei einmal mit CRLF, einmal mit LF, einmal mit nacktem CR und prueft: `liesText` liefert fuer alle drei denselben String ohne '\r'; ein aus einem CRLF-Workflow-Ausschnitt per runBlock-Logik (wie rank-ic-publish.test.js:31-38, mit `liesText` statt readFileSync) extrahierter run:-Block enthaelt kein '\r' und `spawnSync('bash', ['-c', block])` liefert Status 0 (`assert.ok(!r.error)` wie in den Vorbildern — laut, nie still); `/^snapshots-eingang\/$/m` matcht auf dem normalisierten CRLF-Inhalt, `zeilen('a\r\nb\nc')` ist `['a','b','c']`. (2) In den sechs Testdateien liefert `Select-String -Pattern "readFileSync\([^)]*'utf8'\)"` nur noch Treffer, die ein mkdtemp-/Ausgabe-Artefakt lesen oder direkt in JSON.parse stehen (die oben genannten Repo-Textleser: 0 Treffer). (3) Alle acht Dateien laufen weiter mit Exit 0 (Baseline heute: rank-ic-publish R1-R5 ok, pr-check-live-gate G1-G5 ok).
VERIFIKATION: `foreach ($f in 'tests/lies-text-helper.test.js','tests/rank-ic-publish.test.js','tests/pr-check-live-gate.test.js','tests/f12-merge-filter.test.js','tests/zweig-wache.test.js','tests/weg-e-waechter-verdrahtung.test.js','tests/refresh-universe.test.js') { node $f *> $null; if ($LASTEXITCODE) { "FAIL $f" } }; 'done'` (erwartet: nur `done`) ; `Select-String -Path tests/rank-ic-publish.test.js,tests/pr-check-live-gate.test.js,tests/f12-merge-filter.test.js,tests/zweig-wache.test.js,tests/weg-e-waechter-verdrahtung.test.js,tests/refresh-universe.test.js -Pattern "readFileSync\([^)]*\.(yml|js|gitignore)['\`"][^)]*'utf8'\)"` (erwartet 0 Treffer)

## S23 · Lane B · Effort medium — lib/path-within.js: Enthaltensein-Pruefung fuer Pfade plattformfest (Windows Laufwerksbuchstaben-Case)
ZIEL: Vier Sicherheitswaechter pruefen "liegt X innerhalb der Wurzel W" per String-Praefix `startsWith(W + path.sep)`: scripts/write-board-history.js:103-104 `const abs = path.resolve(dir); if (abs === REPO_ROOT || abs.startsWith(REPO_ROOT + path.sep))` (dir aus process.env.BOARD_HISTORY_ARCHIVE_DIR, Zeile 96, Schutz BH-154 "Archivziel liegt innerhalb des Checkouts"), scripts/write-board-history.js:1600 `if (resolvedDateDir !== resolvedHistoryDir && !resolvedDateDir.startsWith(resolvedHistoryDir + path.sep))` (BH-147), scripts/t322-ads-verhaeltnis-optionen.js:59-60 `const root = path.resolve(directory); if (root === path.join(ROOT, 'snapshots') || root.startsWith(path.join(ROOT, 'snapshots') + path.sep))` (directory aus argv/env), scripts/filter-snapshot-merge.js:934 `if (path.isAbsolute(berichtRel) || !berichtAbs.startsWith(path.resolve(repoWurzel) + path.sep))`. Auf Windows ist der String-Vergleich case-sensitiv, das Dateisystem nicht: kommt X per argv/env als `c:\...` (klein) und W aus __dirname als `C:\...`, faellt die Pruefung still durch — bei write-board-history.js:104 wird ein Archivziel INNERHALB des Checkouts nicht mehr erkannt. Node loest das bereits in path.relative (gemessen, Node 22): `path.win32.relative('C:\\repo', 'c:\\repo\\board-history-archive')` = `board-history-archive`, `path.win32.relative('C:\\repo', 'C:\\repo-andere\\x')` = `..\\repo-andere\\x`, anderes Laufwerk -> absoluter Pfad `D:\\x`, `path.posix.relative('/repo', '/Repo/x')` = `../Repo/x` (posix bleibt case-sensitiv). Vorbild im Repo: tests/fixtures/t204-write-reachability/fixture-runner.js:70-77 `insideRoot` (relative-basiert). Neues Modul lib/path-within.js mit `liegtInnerhalb(wurzel, kandidat, pathImpl = path)`: beide Seiten `pathImpl.resolve`, `rel = pathImpl.relative(w, k)`, Rueckgabe `rel === '' || (!rel.startsWith('..') && !pathImpl.isAbsolute(rel))` — dabei `..` nur als ganzes erstes Segment werten (`rel === '..' || rel.startsWith('..' + pathImpl.sep)`), damit ein Ordner `..foo` nicht faelschlich als ausserhalb gilt. Die vier Stellen auf `liegtInnerhalb` umstellen (Aufruf-Semantik: Zeile 104 wirft wenn innerhalb; :1600, t322:60 und fsm:934 wie bisher), Fehlermeldungen wortgleich lassen (tests/scoring/bh-b03-boardhist.test.js:289 prueft `/innerhalb des Checkouts/`; :296-298 verbietet das Wort `tmpdir` im Quelltext von write-board-history.js). NICHT anfassen: scripts/t178-reihen-zwillinge.js:50 und scripts/t331-fundamentaluhr-wellen.js:221 (toLowerCase-Variante, anderer Chunk), tests/scoring/**. Refactoring ohne Verhaltensaenderung auf Linux/CI.
ZIEL-DATEIEN: lib/path-within.js, lib/path-within.test.js, scripts/write-board-history.js, scripts/t322-ads-verhaeltnis-optionen.js, scripts/filter-snapshot-merge.js
FERTIG-WENN: lib/path-within.test.js (Standalone-Runner, node:assert, Exit 0/1, faellt unter das CI-Gate lib/*test.js) prueft mit explizitem `path.win32` UND `path.posix`, damit alle Faelle auf beiden Plattformen laufen: (a) win32 `C:\repo` vs `c:\repo\board-history-archive` -> innerhalb; (b) win32 `C:\repo` vs `C:\repo-andere\x` -> ausserhalb; (c) Wurzel selbst -> innerhalb (beide Impl); (d) posix `/repo` vs `/Repo/x` -> ausserhalb; (e) Kandidat `<wurzel>/a/../../x` -> ausserhalb; (f) win32 anderes Laufwerk `D:\x` -> ausserhalb; (g) posix `/repo` vs `/repo/..foo` -> innerhalb. `Select-String -Pattern '\+ path\.sep\)'` ueber die drei Skripte liefert 0 Treffer (vorher 4: wbh:104, wbh:1600, t322:60, fsm:934). tests/board-history.test.js, tests/t322-ads-verhaeltnis-optionen.test.js (Baseline "12 passed; 0 failed", laedt das Skript auch per vm + createRequire, Zeile 14-16), tests/f12-merge-filter.test.js und tests/scoring/bh-b03-boardhist.test.js (nur ausfuehren, Baseline Exit 0) bleiben Exit 0.
VERIFIKATION: `node lib/path-within.test.js; $LASTEXITCODE` (erwartet 0) ; `foreach ($f in 'tests/t322-ads-verhaeltnis-optionen.test.js','tests/f12-merge-filter.test.js','tests/board-history.test.js','tests/scoring/bh-b03-boardhist.test.js') { node $f *> $null; if ($LASTEXITCODE) { "FAIL $f" } }; 'done'` (erwartet: nur `done`) ; `(Select-String -Path scripts/write-board-history.js,scripts/t322-ads-verhaeltnis-optionen.js,scripts/filter-snapshot-merge.js -Pattern '\+ path\.sep\)' | Measure-Object).Count` (erwartet 0, vorher 4) ; `(Select-String -Path scripts/write-board-history.js -Pattern 'tmpdir' | Measure-Object).Count` (erwartet 0)

## S24 · Lane A · Effort low — Naive Ticker-Dateinamen in zwei Lesern durch safeSnapshotFilename ersetzen
ZIEL: pull-yahoo.js schreibt fundamentals-cache/ ueber lib/snapshot-fs.js (pull-yahoo.js:3815 `const cachePath = path.join(cacheDir, safeSnapshotFilename(stock.ticker));`): Grossschreibung (snapshot-fs.js:44), Sanitizer, '_'-Praefix fuer Windows-reservierte Staemme (:25 WINDOWS_RESERVED CON/PRN/AUX/NUL/COM1-9/LPT1-9). Gemessen: `safeSnapshotFilename('CON.DE')` = `_CON.DE.json`, `'AUX.L'` = `_AUX.L.json`, `'nflx'` = `NFLX.json`; universe-list.txt:724 und :1447 fuehren `CON.DE`. Zwei Leser bauen den Namen naiv und ueberspringen solche Dateien still: scripts/enrich-q-revenue.js:213 `const cacheFile = path.join(CACHE, \`${ticker}.json\`);` (:214 `[skip] ... no cache file`; CACHE = fundamentals-cache Zeile 32; Ticker aus argv Zeile 178, Kleinschreibung moeglich) und scripts/probe-datenplausibilitaet.js:52-53 `const p = path.join(ordner, z.ticker + '.json'); if (!fs.existsSync(p)) continue;`. lib/snapshot-fs.js:9-12 nennt genau dieses Muster als P1-Befund ("CON -> _CON.json was invisible to a naive path.join(SNAP_DIR, t + '.json')"). Gemessen heute: mkdtemp mit `_CON.DE.json` + `BRK.B.json` und Board `{rows:[{ticker:'CON.DE',...},{ticker:'BRK.B',...}]}` -> probe-datenplausibilitaet.js druckt `davon mit Rohdaten  : 1` (soll 2). Aufgabe: beide Stellen auf `path.join(<dir>, safeSnapshotFilename(<ticker>))` umstellen (`const { safeSnapshotFilename } = require('../lib/snapshot-fs.js');`), sonst nichts aendern. Dazu tests/snapshot-dateiname-leser-paritaet.test.js: (a) funktional: mkdtemp-Ordner mit `_CON.DE.json`, `BRK.B.json` (Minimal-Snapshots `{"metrics":{},"timeseries":{},"annual":{}}`) und Mini-Board-JSON, probe-datenplausibilitaet.js per `spawnSync(process.execPath, [skript, ordner, board])` starten, aus stdout die Zeile `davon mit Rohdaten  : N` parsen und `N === 2` pruefen (vorher 1); (b) Quelltext-Waechter ueber scripts/*.js (ohne *test*): das Muster `(ticker|symbol)\s*\+\s*'\.json'|\$\{(ticker|symbol)\}\.json` darf nur noch in der festen Allowlist vorkommen — scripts/early-detection-audit.js (1 Treffer, Zeile 575; LF-gepinnte early-detection-Zone, NICHT anfassen), scripts/gqs00-freeze.js (2 Treffer, Zeile 501+508; Freeze-Skript, NICHT anfassen), scripts/t-veraltung-zwei-definitionen.js (1, Zeile 178, schreibt synthetische Fixtures), scripts/t178-reihen-zwillinge.js (1, Zeile 137, Vergleich) — ausserhalb der Allowlist 0 Treffer (heute 2: enrich-q-revenue.js:213, probe-datenplausibilitaet.js:52). enrich-q-revenue.js bekommt keinen Funktionstest (main() ruft fetchSecTickers = Netz); tests/scoring/bh-w2-enricher.test.js:20 requiret dessen Exporte `isQuarterlyPoint, extractQuarterlyPoints` und mockt :60-70 fetchSecTickers/fs.openSync — muss gruen bleiben, darf nicht geaendert werden.
ZIEL-DATEIEN: scripts/enrich-q-revenue.js, scripts/probe-datenplausibilitaet.js, tests/snapshot-dateiname-leser-paritaet.test.js
FERTIG-WENN: `node tests/snapshot-dateiname-leser-paritaet.test.js` Exit 0; der Test schlaegt nachweislich fehl, wenn probe-datenplausibilitaet.js:52 auf `z.ticker + '.json'` zurueckgedreht wird (einmal manuell geprueft, im Test-Kommentar vermerkt); beide Skripte enthalten `require('../lib/snapshot-fs.js')`; `node tests/scoring/bh-w2-enricher.test.js` Exit 0; `node tests/p1-welle8-metadata-filter.test.js` Exit 0.
VERIFIKATION: `node tests/snapshot-dateiname-leser-paritaet.test.js; $LASTEXITCODE` (erwartet 0) ; `node tests/scoring/bh-w2-enricher.test.js; $LASTEXITCODE` (erwartet 0) ; `(Select-String -Path scripts/enrich-q-revenue.js,scripts/probe-datenplausibilitaet.js -Pattern "\`$\{ticker\}\.json|z\.ticker \+ '\.json'" | Measure-Object).Count` (erwartet 0, vorher 2) ; `(Select-String -Path scripts/enrich-q-revenue.js,scripts/probe-datenplausibilitaet.js -Pattern 'snapshot-fs' | Measure-Object).Count` (erwartet 2)

## S25 · Lane A · Effort low — lib/env-key.js: Umgebungsvariablen case-sicher setzen (PATH vs Path auf Windows)
ZIEL: Auf Windows heisst der Schluessel im Prozess-Environment `Path`, nicht `PATH`; `{ ...process.env }` kopiert ihn in dieser Schreibweise in ein Plain-Object. tests/pr-check-live-gate.test.js:50-51 legt einen `gh`-Stub in ein mkdtemp-Verzeichnis (Shebang + chmod 0o755) und :54 startet `spawnSync('bash', ['-c', script], { ..., env: { ...process.env, PATH: dir + path.delimiter + process.env.PATH, GITHUB_OUTPUT: out, GH_TOKEN: 't' } })` — auf Windows enthaelt dieses Objekt danach BEIDE Schluessel (`Path` original, `PATH` mit dem Stub-Verzeichnis davor); welcher davon im Kind sichtbar wird, sichert die Node-API nicht zu (libuv sortiert den Block case-insensitiv), der gh-Stub kann also unbemerkt nicht gefunden werden (G4/G5 wuerden gegen das echte `gh` laufen oder rot). Es ist die einzige Stelle im Testbaum, die PATH so setzt (grep `PATH:` in tests/**: nur :54). scripts/test-gate.js loest dieselbe Klasse fuer NODE_OPTIONS/NODE_TEST_CONTEXT bereits — aber als acht Inline-Kopien von `process.platform === 'win32' ? key.toUpperCase() === '<NAME>' : key === '<NAME>'` (Zeile 387, 390, 825, 837, 868, 890, 911, 929). Kein lib/env-*.js vorhanden. Neues Modul lib/env-key.js mit `findeEnvSchluessel(env, name, platform = process.platform)` (auf 'win32' case-insensitiver Vergleich, sonst exakt; liefert den vorhandenen Schluesselnamen oder `name`) und `mitEnv(env, name, wert, platform = process.platform)` (flache Kopie, entfernt auf win32 alle Case-Varianten von `name`, setzt genau EINEN Schluessel — die vorhandene Schreibweise, sonst `name`; Eingabe wird nicht mutiert). tests/pr-check-live-gate.test.js:54 darauf umstellen: `env: mitEnv({ ...process.env, GITHUB_OUTPUT: out, GH_TOKEN: 't' }, 'PATH', dir + path.delimiter + (process.env.PATH || ''))`. scripts/test-gate.js NICHT anfassen (CI-Gate-Runner mit eigenem Selbsttest).
ZIEL-DATEIEN: lib/env-key.js, lib/env-key.test.js, tests/pr-check-live-gate.test.js
FERTIG-WENN: lib/env-key.test.js (Standalone-Runner, node:assert, Exit 0/1, faellt unter das CI-Gate lib/*test.js) prueft ueber den injizierten `platform`-Parameter, NICHT ueber process.platform: win32: `mitEnv({ Path: 'a', X: '1' }, 'PATH', 'b')` -> genau die Schluessel `Path` und `X`, `Path === 'b'`, Object.keys-Laenge 2, Eingabe unveraendert; win32 mit Duplikaten `{ Path: 'a', PATH: 'c' }` -> genau EIN Schluessel fuer den Pfad; linux: `mitEnv({ Path: 'a' }, 'PATH', 'b')` -> zwei Schluessel `Path` und `PATH`; `findeEnvSchluessel({ Path: 'a' }, 'PATH', 'win32')` = `'Path'`, dasselbe mit 'linux' = `'PATH'`; `findeEnvSchluessel({}, 'PATH', 'win32')` = `'PATH'`. tests/pr-check-live-gate.test.js bleibt Exit 0 (Baseline heute: G1-G5 ok) und enthaelt kein `PATH:` Literal in einem env-Spread mehr.
VERIFIKATION: `node lib/env-key.test.js; $LASTEXITCODE` (erwartet 0) ; `node tests/pr-check-live-gate.test.js; $LASTEXITCODE` (erwartet 0) ; `(Select-String -Path tests/pr-check-live-gate.test.js -Pattern 'PATH: dir \+ path\.delimiter' | Measure-Object).Count` (erwartet 0, vorher 1) ; `(Select-String -Path tests/pr-check-live-gate.test.js -Pattern 'mitEnv' | Measure-Object).Count` (erwartet >= 2: require + Aufruf)

## S26 · Lane A · Effort medium — readJsonOrNull: 14 byte-gleiche Kopien des toleranten JSON-Lesers nach lib/read-json.js
ZIEL: lib/read-json.js exportiert heute exakt `FEHLT,readJsonExistingOrThrow` (Zeile 54: `module.exports = { readJsonExistingOrThrow, FEHLT };`). ADDITIV zwei Exporte anlegen: `readJsonOrNull(p)` mit exakt dem Body `try { return JSON.parse(fs.readFileSync(p, 'utf8')); } catch (_) { return null; }` und `readJsonOr(p, fallback)` (gleicher Body, `return fallback`). Doc-Kommentar davor: bewusst tolerant, NUR fuer optionale Nebendateien; Pflichtdateien bleiben bei readJsonExistingOrThrow (kein Call-Site wird strenger oder lockerer). Danach die 14 Kopien durch den Import ersetzen, Call-Sites UNVERAENDERT lassen (lokalen Namen per Destructuring-Alias behalten, z. B. `const { readJsonOrNull: readJsonSafe } = require('../lib/read-json.js');`). Sites (Definitionszeile, jeweils try/JSON.parse(fs.readFileSync(p,'utf8'))/catch->null): scripts/backfill-form345.js:73 `function readJsonSafe(p) {`; scripts/backfill-prices-max.js:56 `function readJsonOrNull(f) { try { ... } catch (_) { return null; } }`; scripts/coverage-gate.js:42 `function readJSON(p) { ... catch (e) { return null; } }`; scripts/merge-shard-manifests.js:37 `function readJSON(p) {...}`; scripts/probe-smallcap-coverage.js:274 `function readJson(file) {...}`; scripts/pull-13f-institutional.js:195 `function readJsonSafe(p) {`; scripts/pull-insider-form4-daily.js:94 `function readJsonSafe(p) {`; scripts/pull-insider-form4.js:114 `function readJsonSafe(p) {` (importiert lib/read-json.js bereits in Zeile 58 — Destructuring dort erweitern); scripts/reconcile-smallcap.js:82 `function readJson(p) {`; scripts/watch-exchange-coverage.js:33, scripts/watch-fx-sanity.js:45, scripts/watch-unrouted-quote.js:31 je `function loadJson(p, fallback) { try {...} catch (e) { return fallback; } }` (Alias auf readJsonOr; einzige Call-Sites Zeile 108/142/76 uebergeben `null`); scripts/write-findash-export.js:159 `function readJSONOrNull(p) { try { return readJSON(p); } catch (_) { return null; } }` und scripts/write-rule40-export.js:208 `function readJsonOrNull(p) { try { return readJson(p); } catch (_) { return null; } }` — dort NUR den OrNull-Wrapper ersetzen, der strikte `readJSON` (Zeile 158, 13 weitere Verwendungen) bzw. `readJson` (Zeile 204, 3 Verwendungen) bleibt. NICHT anfassen: scripts/check-pull-stats.js:79 `loadJson` (warnt bei Nicht-ENOENT, ist exportiert und wird von tests/daten-waechter-alarm.test.js:168 importiert), scripts/write-board-history.js:310 (dokumentiertes Paar mit eigenem readJsonExistingOrThrow ab Zeile 379), scripts/walk-forward-perf.js:102 (Fitness-Messung, tabu). Quelltext-Wache beachten: tests/backfill-form345-history-integrity.test.js:20 prueft `assert.doesNotMatch(source, /readJsonSafe\(HISTORY_CACHE_PATH\)\s*\|\|\s*\{\}/)` — der lokale Name `readJsonSafe` MUSS in backfill-form345.js erhalten bleiben. Pinning-Test tests/s-dup-01-read-json-or-null.test.js (Standalone-Runner nach Muster lib/read-json.test.js: node:assert/strict, pass/fail-Zaehler, `process.exit(fail ? 1 : 0)`, tmp-Verzeichnis via fs.mkdtempSync): fehlende Datei -> null bzw. fallback (Identitaet: `readJsonOr(p, obj) === obj`); gueltiges Objekt -> deepEqual; Syntaxmuell `{"a":` -> null, KEIN Wurf (Gegenprobe: readJsonExistingOrThrow wirft bei derselben Datei); Inhalt `null`/`[]`/`5` -> genau dieser Wert (tolerant, keine Objekt-Pruefung); Verzeichnis statt Datei -> null. Der Test enthaelt zusaetzlich eine 1:1-Kopie des ALTEN Bodys als `alt(p)` und asserted fuer jeden Fall `alt(p)` deepEqual `readJsonOrNull(p)` (Pinning-Nachweis identischen Verhaltens).
ZIEL-DATEIEN: lib/read-json.js, tests/s-dup-01-read-json-or-null.test.js (neu), scripts/backfill-form345.js, scripts/backfill-prices-max.js, scripts/coverage-gate.js, scripts/merge-shard-manifests.js, scripts/probe-smallcap-coverage.js, scripts/pull-13f-institutional.js, scripts/pull-insider-form4-daily.js, scripts/pull-insider-form4.js, scripts/reconcile-smallcap.js, scripts/watch-exchange-coverage.js, scripts/watch-fx-sanity.js, scripts/watch-unrouted-quote.js, scripts/write-findash-export.js, scripts/write-rule40-export.js
FERTIG-WENN: Definitionszaehler `^function (readJsonSafe|readJSON|readJson|loadJson|readJsonOrNull|readJSONOrNull)\(` ueber scripts\*.js faellt von 20 (jetzt gemessen) auf 6; lib/read-json.js exportiert `FEHLT,readJsonExistingOrThrow,readJsonOr,readJsonOrNull`; neuer Test, lib/read-json.test.js und die sechs genannten Bestandstests Exit 0; volle Gate-Suite gruen bis auf die zwei bekannten lokalen Roten (calibration-ref, waehrung-ausliefer-waechter).
VERIFIKATION: `(Select-String -Path scripts\*.js -Pattern '^function (readJsonSafe|readJSON|readJson|loadJson|readJsonOrNull|readJSONOrNull)\(' | Measure-Object).Count` -> 6 (vorher 20); `node -e "console.log(Object.keys(require('./lib/read-json.js')).sort().join(','))"` -> FEHLT,readJsonExistingOrThrow,readJsonOr,readJsonOrNull; `node tests/s-dup-01-read-json-or-null.test.js; $LASTEXITCODE` -> 0; `node lib/read-json.test.js; $LASTEXITCODE` -> 0; je Exit 0: `node tests/backfill-form345-history-integrity.test.js`, `node tests/pull-13f-cache-integrity.test.js`, `node tests/coverage-gate-truth-table.test.js`, `node tests/merge-shard-manifests-schema.test.js`, `node tests/watch-fx-sanity-baseline-shape.test.js`, `node tests/rule40-contract.test.js`; Abschluss: `Get-ChildItem tests/*test.js,tests/scoring/*test.js,lib/*test.js | ForEach-Object { node $_.FullName; if ($LASTEXITCODE) { "FAIL: $($_.Name)" } }` -> nur die zwei bekannten FAIL-Zeilen.

## S27 · Lane A · Effort low — sleep(ms): 15 Kopien in scripts/ und Root durch einen Export aus lib/fetch-retry.js ersetzen (seriell NACH S-DUP-01, gleiche Dateien)
ZIEL: lib/fetch-retry.js hat in Zeile 31 `const schlaf = (ms) => new Promise((r) => setTimeout(r, ms));` (nicht exportiert; Zeile 114: `module.exports = { fetchBuffer, fetchJson, DEFAULT_PAUSE_MS, DEFAULT_VERSUCHE, _internals: { warteAufSlot, letzterAbruf } };`). Export ADDITIV um `sleep: schlaf` erweitern; der Test-Seam `_schlaf` (Zeile 69) und alles andere bleibt unangetastet. Dann die 15 byte-gleichen Promise/setTimeout-Kopien durch `const { sleep } = require('../lib/fetch-retry.js')` (Root-Skripte: `./lib/fetch-retry.js`) ersetzen, Call-Sites unveraendert. Sites: scripts/backfill-form345.js:72 `function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }`; scripts/backfill-prices-max.js:55 `const sleep = (ms) => new Promise((r) => setTimeout(r, ms));`; scripts/backfill-prices-research.js:40 (gleich); scripts/backfill-prices.js:46 `async function sleep(ms) {...}`; scripts/build-secannual.js:84 `const sleep = ms => new Promise(r => setTimeout(r, ms));` — ACHTUNG Zeile 326 exportiert `sleep` weiter: Export-Eintrag behalten, Wert ist dann der lib-Import; scripts/fetch-secbulk.js:67; scripts/messung-entdeckungsband.js:34 `const schlaf = ...` (Alias `const { sleep: schlaf } = ...`, Call-Sites 277/307 bleiben); scripts/probe-smallcap-coverage.js:263; scripts/probe-smallcap-messlauf3.js:52; scripts/pull-13f-institutional.js:193 (Zeile 592 `const pause = deps.sleep || sleep;` bleibt); scripts/pull-insider-form4-daily.js:92; scripts/pull-insider-form4.js:112; pull-earnings-dates.js:22 `async function sleep(ms)`; pull-historical-prices.js:94 `async function sleep(ms)`; pull-sec-xbrl.js:96 `function sleep(ms)`. Verhalten: alle Varianten (auch die async-function-Form) liefern ein Promise, das nach >= ms mit undefined aufloest; die async-Form kostet nur einen zusaetzlichen Microtask, was kein Aufrufer beobachtet (jeder wartet per await). Pinning-Test tests/s-dup-02-sleep-pinning.test.js (Standalone, async main, `process.exit(fail ? 1 : 0)`): (a) `sleep(20)` ist `instanceof Promise` und loest mit undefined auf; (b) `Date.now()`-Differenz um `await sleep(20)` >= 19 ms; (c) `await sleep(0)` loest auf; (d) `require('./scripts/build-secannual.js').sleep === require('./lib/fetch-retry.js').sleep` (Re-Export bleibt); (e) fuer Vergleich die alten drei Schreibweisen (function / const-Arrow / async function) als lokale Kopien im Test: alle drei erfuellen (a)-(c) identisch. NICHT anfassen: lib/sec-rate-limit.js (tests/sec-rate-limit.test.js:13 pinnt per deepEqual exakt die zwei Konstanten-Keys) und die S02-Testdatei lib/fetch-retry.test.js (anderer Codex-Thread; eigener Test liegt unter tests/).
ZIEL-DATEIEN: lib/fetch-retry.js, tests/s-dup-02-sleep-pinning.test.js (neu), scripts/backfill-form345.js, scripts/backfill-prices-max.js, scripts/backfill-prices-research.js, scripts/backfill-prices.js, scripts/build-secannual.js, scripts/fetch-secbulk.js, scripts/messung-entdeckungsband.js, scripts/probe-smallcap-coverage.js, scripts/probe-smallcap-messlauf3.js, scripts/pull-13f-institutional.js, scripts/pull-insider-form4-daily.js, scripts/pull-insider-form4.js, pull-earnings-dates.js, pull-historical-prices.js, pull-sec-xbrl.js
FERTIG-WENN: Definitionszaehler `^(async )?function sleep\(|^const (sleep|schlaf) = ` ueber scripts\*.js plus Root-*.js faellt von 15 auf 0 (lib/fetch-retry.js:31 bleibt die einzige Definition); lib/fetch-retry.js exportiert zusaetzlich `sleep`; neuer Test, die drei fetch-retry-Tests und drei Bestandstests Exit 0; volle Gate-Suite gruen bis auf die zwei bekannten lokalen Roten.
VERIFIKATION: `(Select-String -Path scripts\*.js,*.js -Pattern '^(async )?function sleep\(|^const (sleep|schlaf) = ' | Measure-Object).Count` -> 0 (vorher 15); `node -e "console.log(Object.keys(require('./lib/fetch-retry.js')).sort().join(','))"` -> DEFAULT_PAUSE_MS,DEFAULT_VERSUCHE,_internals,fetchBuffer,fetchJson,sleep; `node tests/s-dup-02-sleep-pinning.test.js; $LASTEXITCODE` -> 0; je Exit 0: `node tests/fetch-retry-status-guard.test.js`, `node tests/fetch-retry-json-parse-guard.test.js`, `node tests/fetch-retry-redirect-status-guard.test.js`, `node tests/pull-sec-xbrl-manifest-integrity.test.js`, `node tests/monthly-sec-failloud.test.js`, `node tests/sec-form4-test.js`; Abschluss volle Gate-Suite (Einzeiler aus CLAUDE.md) -> nur die zwei bekannten FAIL-Zeilen.

## S28 · Lane A · Effort low — ensureDir: 5 byte-gleiche Kopien nach lib/atomic-write.js (seriell NACH S-DUP-01/02, gleiche Dateien)
ZIEL: lib/atomic-write.js (Zeile 241: `module.exports = { writeFileAtomic, writeJsonAtomic, atomicWriteStats };`) ADDITIV um `ensureDir(p)` mit exakt dem Body `if (!fs.existsSync(p)) fs.mkdirSync(p, { recursive: true });` erweitern. Doc-Kommentar: der existsSync-Guard ist Absicht — liegt unter p eine DATEI, wirft `mkdirSync(p, {recursive:true})` (EEXIST/ENOTDIR), der Guard schweigt; dieses Verhalten wird gepinnt, nicht "verbessert". Die 5 Kopien durch den Import ersetzen (alle fuenf Dateien importieren lib/atomic-write.js bereits: archive-old-snapshots.js:28, backfill-form345.js:51, pull-13f-institutional.js:65, pull-insider-form4-daily.js:51, pull-insider-form4.js:57 — dort das Destructuring um `ensureDir` erweitern), Call-Sites unveraendert. Sites: scripts/archive-old-snapshots.js:58 `function ensureDir(p) { if (!fs.existsSync(p)) fs.mkdirSync(p, { recursive: true }); }`; scripts/backfill-form345.js:134 (identisch, einzeilig); scripts/pull-13f-institutional.js:276-278 (mehrzeilig; Zeile 1029 `ensureDirFn = ensureDir,` bleibt als Default-Parameter); scripts/pull-insider-form4-daily.js:99 (einzeilig; tests/pull-insider-form4-daily-date-validation.test.js:116 sucht im Quelltext den String `ensureDir(EXTERNAL_DIR);` — Name bleibt); scripts/pull-insider-form4.js:135-137. NICHT anfassen: scripts/pipeline-health-check.js:38 `const ensureDir = !fs.existsSync(HEALTH_DIR);` ist ein Boolean, kein Helper. Pinning-Test tests/s-dup-03-ensure-dir.test.js (Standalone, tmp-Verzeichnis): (a) verschachtelter neuer Pfad `a/b/c` wird angelegt; (b) bestehendes Verzeichnis mit Inhalt -> kein Wurf, Inhalt unveraendert; (c) bestehende DATEI unter p -> kein Wurf, Datei bleibt Datei (Guard-Semantik; Gegenprobe: `fs.mkdirSync(p, {recursive:true})` wirft an derselben Stelle); (d) Rueckgabe undefined; (e) Vergleich mit einer 1:1-Kopie des alten Bodys als `alt(p)` ueber dieselben drei Faelle.
ZIEL-DATEIEN: lib/atomic-write.js, tests/s-dup-03-ensure-dir.test.js (neu), scripts/archive-old-snapshots.js, scripts/backfill-form345.js, scripts/pull-13f-institutional.js, scripts/pull-insider-form4-daily.js, scripts/pull-insider-form4.js
FERTIG-WENN: `^function ensureDir\(` in scripts\*.js faellt von 5 auf 0; lib/atomic-write.js exportiert `atomicWriteStats,ensureDir,writeFileAtomic,writeJsonAtomic`; neuer Test, lib/atomic-write.test.js und vier Bestandstests Exit 0; volle Gate-Suite gruen bis auf die zwei bekannten lokalen Roten.
VERIFIKATION: `(Select-String -Path scripts\*.js -Pattern '^function ensureDir\(' | Measure-Object).Count` -> 0 (vorher 5); `node -e "console.log(Object.keys(require('./lib/atomic-write.js')).sort().join(','))"` -> atomicWriteStats,ensureDir,writeFileAtomic,writeJsonAtomic; `node tests/s-dup-03-ensure-dir.test.js; $LASTEXITCODE` -> 0; `node lib/atomic-write.test.js; $LASTEXITCODE` -> 0; je Exit 0: `node tests/pull-insider-form4-daily-date-validation.test.js`, `node tests/pull-13f-cache-integrity.test.js`, `node tests/archive-old-snapshots-keep-days-parser.test.js`, `node tests/backfill-form345-history-integrity.test.js`; Abschluss volle Gate-Suite -> nur die zwei bekannten FAIL-Zeilen.

## S29 · Lane A · Effort low — isJsonObject / isPlainObject: zwei Praedikate mit je 3 byte-gleichen Kopien nach lib/read-json.js (seriell NACH S-DUP-01, gleiche Dateien)
ZIEL: Zwei semantisch VERSCHIEDENE Praedikate, je dreimal identisch kopiert. (V1) `isJsonObject(value)` = `return value !== null && typeof value === 'object' && !Array.isArray(value);` in scripts/backfill-form345.js:77, scripts/pull-13f-institutional.js:200, scripts/update-ath-state.js:42. (V2) `isPlainObject(value)` = `if (value === null || typeof value !== 'object' || Array.isArray(value)) return false; const prototype = Object.getPrototypeOf(value); return prototype === Object.prototype || prototype === null;` in scripts/merge-shard-manifests.js:113, scripts/plan-check.js:48, scripts/watch-unrouted-quote.js:41. Beide ADDITIV in lib/read-json.js exportieren (Zeile 48 prueft heute dieselbe V1-Bedingung inline; darf so bleiben — kein Verhaltensunterschied), die 6 Kopien durch Import ersetzen (`const { isJsonObject } = require('../lib/read-json.js')` bzw. `isPlainObject`; wo lib/read-json.js nach S-DUP-01 schon importiert ist, Destructuring erweitern), Call-Sites unveraendert. NICHT anfassen: scripts/watch-exchange-coverage.js:43 (dritte Variante, prueft NUR `Object.getPrototypeOf(value) === Object.prototype`, d. h. `Object.create(null)` -> false — abweichendes Verhalten, bleibt lokal) und scripts/rank-ic-families.js:21 (Fitness, tabu). Pinning-Test tests/s-dup-04-object-predicates.test.js mit Wahrheitstabelle ueber die Unterscheider, Spalten (V1, V2): `{}` (true,true); `Object.create(null)` (true,true); `new (class X {})()` (true,false); `new Map()` (true,false); `new Date()` (true,false); `[]` (false,false); `null` (false,false); `'x'`, `5`, `undefined`, `() => {}` (false,false). Der Test traegt beide alten Bodys 1:1 als `altV1`/`altV2` und asserted fuer jede Zeile `altV1(x) === isJsonObject(x)` und `altV2(x) === isPlainObject(x)` (Pinning-Nachweis).
ZIEL-DATEIEN: lib/read-json.js, tests/s-dup-04-object-predicates.test.js (neu), scripts/backfill-form345.js, scripts/pull-13f-institutional.js, scripts/update-ath-state.js, scripts/merge-shard-manifests.js, scripts/plan-check.js, scripts/watch-unrouted-quote.js
FERTIG-WENN: `^function isJsonObject\(` in scripts\*.js faellt von 3 auf 0; `^function isPlainObject\(` faellt von 5 auf 2 (watch-exchange-coverage.js, rank-ic-families.js); neuer Test, lib/read-json.test.js und fuenf Bestandstests Exit 0; volle Gate-Suite gruen bis auf die zwei bekannten lokalen Roten.
VERIFIKATION: `(Select-String -Path scripts\*.js -Pattern '^function isJsonObject\(' | Measure-Object).Count` -> 0 (vorher 3); `(Select-String -Path scripts\*.js -Pattern '^function isPlainObject\(' | Measure-Object).Count` -> 2 (vorher 5); `node tests/s-dup-04-object-predicates.test.js; $LASTEXITCODE` -> 0; `node lib/read-json.test.js; $LASTEXITCODE` -> 0; je Exit 0: `node tests/ath-state.test.js`, `node tests/plan-check-manifest-integrity.test.js`, `node tests/merge-shard-manifests-schema.test.js`, `node tests/watch-unrouted-baseline-integrity.test.js`, `node tests/pull-13f-cache-integrity.test.js`; Abschluss volle Gate-Suite -> nur die zwei bekannten FAIL-Zeilen.

## S30 · Lane B · Effort medium — Werkzeug scripts/dead-exports-inventar.js: exportiert, aber nirgends referenziert (Report, kein Gate)
ZIEL: Kleines CI-unabhaengiges Werkzeug, das fuer alle Module in lib/*.js, scripts/*.js und Root-*.js (ohne *test.js; heute 178 Dateien) die Export-Namen STATISCH liest (KEIN require der Module — viele Skripte haben Ladezeit-Seiteneffekte, z. B. `require('../lib/sec-user-agent').secUserAgent()` in pull-insider-form4.js:80) und jeden Namen als "tot" meldet, der in KEINER anderen Datei als Wort vorkommt. Export-Erkennung: `module.exports = { ... }`-Block per Klammerzaehlung, Keys auf Tiefe 1 (`name,` / `name: wert` / `name }`), zusaetzlich die Keys eines verschachtelten `_internals: { ... }` als `_internals.name`, sowie `module.exports.X =` / `exports.X =`. Referenzsuche: Wortgrenzen-Match (`\bname\b`) ueber alle Dateien mit Endung .js/.mjs/.cjs/.yml/.yaml/.ps1/.sh/.py im Repo, ausser der definierenden Datei und ausser den Verzeichnissen node_modules, .git, board-history, methods-history, picks-history, druckenmiller-history, r40rx-history, score-history, newcomer-log, prices, price-shards-in, external-data, reports, protocol, snapshots, outputs, state, pull-stats, data-health, findings, audit-reports, research, rgate, entwurf, assets, configs (Markdown-Erwaehnungen in Berichten sind keine Verwendung; .github/workflows zaehlen, weil dort `require('./lib/...')` per `node -e` vorkommt, z. B. heartbeat.yml:65). Ausgabe: eine Zeile `<datei> :: <name>` je Fund, sortiert, danach `Summe: N tote Exporte in M Dateien`; mit `--json` ein Array `[{file,name}]`. Exit immer 0 (Inventar, kein Gate). Bewusst konservativ: ein Wort-Treffer irgendwo gilt als lebendig (falsch-negativ ist erlaubt, falsch-positiv nicht). Heute verifizierte Wahrheit (per Wort-grep ueber das ganze Repo, 0 Treffer ausserhalb der definierenden Datei), die das Werkzeug MINDESTENS melden muss: scripts/check-pull-stats.js::validateStatsHistory; refresh-universe.js::toYahooClassShare, _looksUS, dedupKey, EXCHANGE_SEITE, MIN_PREDEFINED_NONEMPTY_ANTEIL; pull-yahoo.js::corruptYoung, ftsCacheParse; scripts/cadence-marker.js::FIELD_MAP; scripts/f24-streak.js::cleanDays, isPlannedRunDay, previousPlannedDay; scripts/backfill-form345.js::normDate, recentCompletedQuarters, _internals.findTsv; scripts/fetch-secbulk.js::BULK_URL; scripts/build-krannual.js::buildSeriesN; scripts/backfill-prices-max.js::readProgressManifestOrThrow; lib/fetch-retry.js::DEFAULT_PAUSE_MS, DEFAULT_VERSUCHE; lib/metrics.js::hitRate, _median (src/scoring/lamps.js:43 hat eine EIGENE `_median`-Kopie und importiert nicht). Es darf NICHT melden: lib/atomic-write.js::writeFileAtomic (106 importierende Dateien), lib/snapshot-fs.js::safeSnapshotFilename, scripts/check-pull-stats.js::loadJson (tests/daten-waechter-alarm.test.js:168). Test tests/dead-exports-inventar.test.js (Standalone): baut in einem tmp-Verzeichnis ein Mini-Repo (lib/a.js mit `module.exports = { live, dead, _internals: { innenLive, innenTot } }`, scripts/b.js mit `const { live } = require('../lib/a.js'); ... _internals.innenLive`, ein node_modules/x.js das `dead` erwaehnt und ignoriert werden muss, ein audit-reports/r.md das `dead` erwaehnt und ignoriert werden muss) und erwartet per `--json` exakt `[{file:'lib/a.js',name:'dead'},{file:'lib/a.js',name:'_internals.innenTot'}]` (Werkzeug muss dafuer `--root <pfad>` annehmen). Zweiter Block: echter Repo-Lauf per child_process, Exit 0, Ausgabe enthaelt `pull-yahoo.js :: corruptYoung`, `pull-yahoo.js :: ftsCacheParse` und `lib/metrics.js :: hitRate` (diese drei bleiben in jeder Reihenfolge der S-DUP-Briefs tot: pull-yahoo.js und lib/metrics.js werden bewusst nicht angefasst), enthaelt NICHT `lib/atomic-write.js :: writeFileAtomic`. Nicht auf dedupKey/cleanDays pruefen — S-DUP-06 entfernt den einen Export, S-DUP-07 macht den anderen per Test lebendig. Keine Dependency, nur node:fs/path.
ZIEL-DATEIEN: scripts/dead-exports-inventar.js (neu), tests/dead-exports-inventar.test.js (neu)
FERTIG-WENN: `node scripts/dead-exports-inventar.js` laeuft in < 60 s mit Exit 0 und meldet mindestens die 22 oben genannten Namen (Stand jetzt; laufen S-DUP-06/07 vorher, fehlen die dort entfernten bzw. per Test lebendig gemachten Namen — stabil bleiben mindestens corruptYoung, ftsCacheParse, hitRate) und keinen der drei Gegenbeispiele; `--json` liefert parsebares JSON; Test Exit 0.
VERIFIKATION: `node scripts/dead-exports-inventar.js | Select-String 'pull-yahoo.js :: corruptYoung|pull-yahoo.js :: ftsCacheParse|lib/metrics.js :: hitRate' | Measure-Object | % Count` -> 3; vor S-DUP-06/07 zusaetzlich `node scripts/dead-exports-inventar.js | Select-String 'dedupKey|cleanDays|readProgressManifestOrThrow|validateStatsHistory|FIELD_MAP|BULK_URL|buildSeriesN|DEFAULT_VERSUCHE' | Measure-Object | % Count` -> 8; `node scripts/dead-exports-inventar.js | Select-String 'writeFileAtomic|safeSnapshotFilename|check-pull-stats.js :: loadJson' | Measure-Object | % Count` -> 0; `node scripts/dead-exports-inventar.js --json | node -e "const a=JSON.parse(require('fs').readFileSync(0,'utf8'));console.log(Array.isArray(a)&&a.length>=3)"` -> true; `node tests/dead-exports-inventar.test.js; $LASTEXITCODE` -> 0.

## S31 · Lane A · Effort low — 9 tote Export-Eintraege (verifiziert 0 Referenzen) aus 5 Modulen entfernen, Export-Oberflaeche pinnen (seriell NACH S-DUP-03, backfill-form345.js ueberschneidet sich)
KONFLIKT-HINWEIS: S20 entfernt `hasUnzip` aus scripts/backfill-form345.js. Ist S20 bereits gemerged oder als PR offen, die Soll-Liste fuer `_internals` OHNE `hasUnzip` pinnen und vor dem Push auf den aktuellen `origin/main` rebasen. S26/S27/S28 und S20 fassen dieselbe Datei an — seriell arbeiten.
ZIEL: Nur der Eintrag in `module.exports` faellt weg; die Funktionen/Konstanten bleiben, sie werden intern genutzt. Verifiziert per Wort-grep ueber das ganze Repo (0 Treffer ausserhalb der definierenden Datei, auch nicht in tests/, .github/, docs/): scripts/cadence-marker.js:119 `module.exports = { stampMarker, FIELD_MAP, writeMarker };` -> `FIELD_MAP` raus (intern Zeilen 19/80/85); scripts/fetch-secbulk.js:273 `module.exports = { baueBloecke, cikKarte, usTicker, veroeffentliche, parseLimit, BULK_URL };` -> `BULK_URL` raus (intern 195/199/204); scripts/build-krannual.js:279 -> `buildSeriesN` raus (intern 134/225); scripts/backfill-form345.js:479-482 `module.exports = { recentCompletedQuarters, parseTsv, normDate, txnKey, _internals: { hasUnzip, num, findTsv, readHistoryFileOrThrow } };` -> `normDate` (Zeile 480, intern 312/359) und `findTsv` aus `_internals` (Zeile 481, intern 293-295) raus, alles andere bleibt (recentCompletedQuarters bekommt in S-DUP-07 einen Test); refresh-universe.js:2012-2042 -> `toYahooClassShare, _looksUS, dedupKey` (Zeile 2013), `MIN_PREDEFINED_NONEMPTY_ANTEIL` (Zeile 2022) und `EXCHANGE_SEITE` (Zeile 2032) raus (27 interne Verwendungen bleiben). NICHT anfassen: pull-yahoo.js (corruptYoung/ftsCacheParse ebenfalls tot, aber 4800-Zeilen-Kernpuller — bewusst ausgelassen), scripts/check-pull-stats.js::validateStatsHistory und scripts/backfill-prices-max.js::readProgressManifestOrThrow (bekommen in S-DUP-07 Tests), lib/fetch-retry.js DEFAULT_* und lib/snapshot-fs.js WINDOWS_RESERVED (S01/S02 schreiben gerade Tests dagegen), lib/metrics.js (Fitness, tabu). Pinning-Test tests/s-dup-06-export-surface.test.js (Standalone; alle fuenf Module sind ladeseitig seiteneffektfrei — tests/cadence-marker-state-integrity.test.js, tests/fetch-secbulk-limit-validation.test.js, tests/build-krannual-numof.test.js, tests/backfill-form345-history-integrity.test.js, tests/refresh-universe.test.js requiren sie bereits): `Object.keys(require(m)).sort()` deepEqual gegen die Soll-Listen NACH dem Umbau: cadence-marker `['stampMarker','writeMarker']`; fetch-secbulk `['baueBloecke','cikKarte','parseLimit','usTicker','veroeffentliche']`; build-krannual `['KR','KR_FELDER','KR_OHNE_STANDARDKONTEN','buildSeries','main','numOf','pick','yearsFor']` (vorher 9 Keys); backfill-form345 `['_internals','parseTsv','recentCompletedQuarters','txnKey']` plus `Object.keys(_internals).sort()` = `['hasUnzip','num','readHistoryFileOrThrow']`; refresh-universe: 37 Keys (vorher 42 — Soll-Liste aus `node -e` nach dem Umbau in den Test uebernehmen, die fuenf entfernten Namen duerfen nicht enthalten sein). Zusaetzlich eine Negativ-Zeile je entferntem Namen: `assert.equal(Object.hasOwn(mod, name), false)`.
ZIEL-DATEIEN: scripts/cadence-marker.js, scripts/fetch-secbulk.js, scripts/build-krannual.js, scripts/backfill-form345.js, refresh-universe.js, tests/s-dup-06-export-surface.test.js (neu)
FERTIG-WENN: Key-Zahlen nach `node -e "console.log(Object.keys(require('./<m>')).length)"`: cadence-marker 2 (vorher 3), fetch-secbulk 5 (6), build-krannual 8 (9), backfill-form345 4 (4, aber `_internals` 3 statt 4), refresh-universe 37 (42); neuer Test und die fuenf genannten Bestandstests Exit 0; volle Gate-Suite gruen bis auf die zwei bekannten lokalen Roten.
VERIFIKATION: `node -e "for (const m of ['./scripts/cadence-marker.js','./scripts/fetch-secbulk.js','./scripts/build-krannual.js','./scripts/backfill-form345.js','./refresh-universe.js']) console.log(m, Object.keys(require(m)).length)"` -> 2,5,8,4,37; `node -e "console.log(Object.keys(require('./scripts/backfill-form345.js')._internals).sort().join(','))"` -> hasUnzip,num,readHistoryFileOrThrow; `node tests/s-dup-06-export-surface.test.js; $LASTEXITCODE` -> 0; je Exit 0: `node tests/cadence-marker-state-integrity.test.js`, `node tests/fetch-secbulk-limit-validation.test.js`, `node tests/build-krannual-numof.test.js`, `node tests/backfill-form345-history-integrity.test.js`, `node tests/refresh-universe.test.js`; Abschluss volle Gate-Suite -> nur die zwei bekannten FAIL-Zeilen.

## S32 · Lane B · Effort medium — Seam-Tests fuer 6 exportierte, aber nirgends referenzierte reine Funktionen (statt Entfernen)
ZIEL: Diese Exporte sind heute tot (0 Referenzen ausserhalb der Datei), aber als Test-Seams wertvoll — sie bekommen je einen Standalone-Test, KEINE Aenderung am Produktionscode. (1) scripts/f24-streak.js: `isPlannedRunDay(value)` (Zeile 36-39: `weekday >= 2 && weekday <= 6`, UTC), `previousPlannedDay(day)` (41-45), `cleanDays(boardDates, runs)` (56-65). Test tests/s-dup-07-f24-streak-seams.test.js: isPlannedRunDay true fuer 2026-09-22 (Di, getUTCDay 2) und 2026-09-19 (Sa, 6), false fuer 2026-09-20 (So, 0) und 2026-09-21 (Mo, 1), akzeptiert Date und ISO-String, wirft bei `'kaputt'` (toDate, Zeile 23); previousPlannedDay('2026-09-22') -> '2026-09-19' (springt ueber So+Mo), previousPlannedDay('2026-09-24') -> '2026-09-23', wirft bei Nicht-ISO (`'24.09.2026'`, Zeile 32); cleanDays: nur Runs mit event 'schedule' UND status 'completed' UND conclusion 'success' zaehlen, deren `createdAt`-Tag in boardDates liegt; Nicht-ISO-Eintraege in boardDates werden ignoriert; Rueckgabe ist ein Set; leere Eingaben -> leeres Set. (2) scripts/check-pull-stats.js:211 `validateStatsHistory(history, source)`: gueltige Zeilen (Form aus tests/check-pull-stats-count-shape.test.js:33ff uebernehmen) kommen als Array gleicher Laenge zurueck; eine kaputte Zeile an Index 1 -> `TypeError` mit Message-Match `/<source> row 1 is malformed: /` und gesetztem `.cause`; leeres Array -> leeres Array. (3) scripts/backfill-prices-max.js:89 `readProgressManifestOrThrow(f)`: fehlende Datei -> deepEqual `{ done: {} }`; Datei mit `{"done":[]}` -> `{ done: {} }` (Legacy-Array kanonisiert, Zeile 70-72); `{"done":["X"]}` -> wirft `/done must be an object/`; Syntaxmuell -> wirft `/Corrupt progress manifest/`; Verzeichnis statt Datei -> wirft mit `code === 'EISDIR'` (Nicht-ENOENT wird durchgereicht, Zeile 92); gueltiges `{"done":{"AAPL":{}}}` kommt unveraendert zurueck. (4) scripts/backfill-form345.js:177 `recentCompletedQuarters(n)` (haengt an `new Date()`, nicht injizierbar): n=0 -> []; n=6 -> Laenge 6, jedes `q` in 1..4, jedes `y` Integer, Folge streng absteigend (Vergleich `y*4+q` faellt um genau 1 je Schritt), erstes Element ist das Quartal VOR dem laufenden (im Test dieselbe Rechnung aus `new Date()` spiegeln). Tests 2-4 in tests/s-dup-07-dead-seams.test.js. Alle Tests offline, tmp-Verzeichnisse via fs.mkdtempSync, kein Netz; Standalone-Runner (node:assert/strict, `process.exit(fail ? 1 : 0)`).
ZIEL-DATEIEN: tests/s-dup-07-f24-streak-seams.test.js (neu), tests/s-dup-07-dead-seams.test.js (neu)
FERTIG-WENN: beide neuen Tests Exit 0 mit zusammen >= 20 Assertions; Mutations-Gegenprobe dokumentiert (in einer lokalen, NICHT committeten Kopie `weekday >= 2` zu `>= 1` aendern -> Test rot; danach Original wiederherstellen, `git diff --stat scripts/` leer); die vier Produktionsdateien unveraendert; volle Gate-Suite gruen bis auf die zwei bekannten lokalen Roten.
VERIFIKATION: `node tests/s-dup-07-f24-streak-seams.test.js; $LASTEXITCODE` -> 0; `node tests/s-dup-07-dead-seams.test.js; $LASTEXITCODE` -> 0; `git status --short scripts/` -> leer; `git diff --stat -- scripts/f24-streak.js scripts/check-pull-stats.js scripts/backfill-prices-max.js scripts/backfill-form345.js` -> leer; Abschluss volle Gate-Suite (Einzeiler aus CLAUDE.md) -> nur die zwei bekannten FAIL-Zeilen.
