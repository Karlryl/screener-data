# Tagesplan 2026-09-25 — Codex-Dauerlauf mit 8 Lanes (GPT-6 Astra, Effort max)

## AUF EINEN BLICK
- Gestern (Master 24.09., Cloud-Session „Codex limit optimization", Branch `claude/codex-limit-optimization-2u1g6h`):
  Queues S01–S66 / F01–F86 mit Konflikt-Audits und Reihenfolge; 9 Codex-PRs offen (#354–#362, zwei rot);
  34 Claude-Branches fertig ohne PR. Abschlussbericht: `codex-queue/ABSCHLUSSBERICHT-2026-09-24.md` (gleicher Branch).
- Heute: 8 Codex-Chats als DAUERLAUF ohne Endzeit, disjunkte Dateiraeume (4 screener-data, 4 findash), jede Lane
  Koordinator mit >= 8 Subagenten auf `gpt-6-astra`/max, Log-Pflicht, Selbst-Nachschub-Protokoll. Steuer-Datei:
  `codex-queue/DAUERLAUF-2026-09-25.md` (screener-data: alle Lanen; findash: L5–L8). Prompts: `codex-queue/prompts/2026-09-25/L1..L8.md`.
- Claude (Cloud) heute: Codex ist im Container nicht installiert und hat keine Zugangsdaten -> keine Codex-
  Subagenten aus der Cloud moeglich; keine Claude-Subagenten (Karl-Vorgabe). Stattdessen: Stand verifiziert,
  Ursachen der zwei roten PRs reproduziert, Lane-Zuschnitt, Regeln, 8 Prompts, Push auf `claude/sweet-pascal-w2u2xa`.

## 1. Stand aus dem gestrigen Master (heute verifiziert, 06:30 UTC)

| PR | Task | Branch | CI `test-gate` | Bemerkung |
|---|---|---|---|---|
| #354 | S01 | codex/S01 | gruen | Nachkommit 87a5858 (Wachposten AT-SK-REGION-001) |
| #355 | S05 | codex/S05 | gruen | |
| #356 | S06 | codex/S06 | gruen | |
| #357 | S10 | codex/S10 (Draft) | gruen | laut Audit ~80 % Doppelarbeit mit S14/S37/S38/S53 |
| #358 | S07 | codex/S07 | gruen | |
| #359 | S14 | codex/S14 | gruen | Mutations-Infra; S15/S16 haengen daran |
| #360 | S18 | codex/S18 (Draft) | **rot** | zwei Siegel-/Freeze-Waechter (Abschnitt 2) |
| #361 | S26 | codex/S26 (Draft) | **rot** | reconcile-smallcap g1–g5 (Abschnitt 2) |
| #362 | S09 | codex/S09 (Draft) | gruen | Coverage-Werkzeug; S11–S13 nutzen es |

Claude-Branches (je 1 Commit vor dem Hauptzweig, kein PR): screener-data 12 (`claude/S22 S24 S25 S35 S36 S37 S38
S43 S49 S53 S59 S62`), findash 22 (`claude/F03 F06 F09 F21 F22 F27 F28 F30 F31 F33 F34 F38 F39 F43 F54 F57 F58 F60
F62 F64 F68 F72`). Hauptzweige unveraendert seit gestern: screener-data `main` @ e803a15, findash `master` @ 79efe01.
Der gestrige Branch ist NICHT gemerged; die Task-Texte bleiben dort und werden von den Lanes per `git show` gelesen.

## 2. Ursachen der zwei roten PRs (heute reproduziert)

**#361 / S26** — `tests/reconcile-smallcap.test.js` Faelle g1–g5: `"undefined" is not valid JSON` (lokal im Worktree
`origin/codex/S26` reproduziert, auf `main` gruen). Der Test ersetzt per `Module._load` das `fs` NUR fuer Requires,
deren `parent.filename` das Skript selbst ist (Z. 318–356). S26 verlagert `readJson` nach `lib/read-json.js`; der
Leser nutzt jetzt das echte `fs` der lib und umgeht den In-Memory-Stub, `memory-report.json` wird nie gelesen.
Fix (L1): `scripts/reconcile-smallcap.js` aus S26 herausnehmen (Zaehler 20 -> 7 statt 6, Grund im PR), oder Stub im
Test um `../lib/read-json.js` erweitern, wenn der Reviewer die unveraenderte Staerke der g-Faelle bestaetigt.

**#360 / S18** — CI: `Test failed: tests/early-detection-siegel-wachposten.test.js` und
`tests/studie-protokoll-freeze-wachen.test.js`. Beide hashen registrierte lib-Dateien (freeze-wachen Z. 119–131:
`lib/ledger-single-appender.js` + Probe; siegel-wachposten Z. 84: Eltern-Manifest des early-detection-Siegels).
S18 hat 32 lib-Dateien angefasst, darunter `lib/ledger-single-appender.js` und `lib/early-detection.js`; die
Brief-Praemisse „kein Test hasht lib-Quelltext" war falsch. Fix (L1): JSDoc in versiegelten Dateien zuruecknehmen,
`VERSIEGELT`-Liste im Waechter `tests/jsdoc-exports.test.js`, Sollzahlen anpassen. Re-Seal = Karl/Claude, nie Codex.
Lokaler Gate-Lauf im Cloud-Container zeigt zusaetzlich 11 rote `studie-*`/Register-Tests — Ursache dort ist der
flache Klon („Mit `git fetch --unshallow` holen"), kein S18-Defekt; auf Karls Rechner mit voller Historie pruefen.

Hinweis: Die Zeile `UEBERSPRUNGEN … 7 — tests/scoring/…` steht auch in gruenen CI-Laeufen (leeres `snapshots/`
im PR-Check, A2-Artefakt-Retention) und ist NICHT die Ursache.

## 3. Lane-Zuschnitt (disjunkt nach den Audits vom 24.09.)

| Lane | Repo | Zustaendigkeit / Dateiraum | Start | Reihenfolge-Regel |
|---|---|---|---|---|
| L1 | screener-data | PR-Pflege: 9 Codex-PRs, 12 Claude-Branch-PRs, Merge-Reihenfolge, Gate-Laeufe, S04 | #361, #360 fixen | Merge statt Rebase, nie Force-Push |
| L2 | screener-data | `lib/*.test.js`, `tests/*.test.js`, Mutations-/Coverage-Kette | S12, S16, S11 | S15 nach S16 + S22/S25; S13 zuletzt |
| L3 | screener-data | `scripts/` SEC-/Pull-/DUP-Kette, Root-Pull-Skripte | S50, S51, S42 | S27 spaet, dann S28 -> S29 -> S31 |
| L4 | screener-data | isolierte Skripte, Harness, `index.html`, Docs, Skills | S44, S48, S47 | S17 zuletzt mit Ausschlussliste |
| L5 | findash | `data-layer/`, `server.js` additiv, `test/` | F04, F10, F11 | F20 zuletzt; F59/F65 nur Draft |
| L6 | findash | `web/test`, `web/src/lib`, Komponenten (nie index.tsx/api.ts) | F07, F12, F17 | F07 vor allem |
| L7 | findash | `routes/index.tsx` + `lib/api.ts` strikt seriell | F13 B1–5 | F46 -> … -> F51, F15, F13 B6, F01 zuletzt |
| L8 | findash | PRs fuer 22 Claude-Branches, CI-Wache, Actions-Minuten, e2e | claude/F27 F03 F28 F68 F72 | F14 als letzter e2e-Task |

Abhaengigkeiten auf ungemergte Claude-Branches (S22/S25/S35/S24/S36/S43, F27/F03/F28/F68/F72) werden per
Stapel-Basis ueberbrueckt (im PR benannt); L1/L8 oeffnen die PRs dieser Branches als erstes, damit Karl frueh mergen kann.

## 4. Was heute anders ist als gestern
1. Kein Endzeitpunkt, kein Shutdown-Timer. Ende nur durch Karls `STOPP` im jeweiligen Chat.
2. Worktree-Pool (4 je Lane, wiederverwendet) statt Worktree je Task — Disk und OneDrive.
3. Log-Pflicht: Vault-Log je Lane (Eintrag je Task + Heartbeat alle 20 Minuten) und Log-Branch `codex/log/L<n>-20260925`
   (stuendlich), damit Claude in der Cloud mitlesen kann.
4. Selbst-Nachschub-Protokoll mit Reviewer-Freigabe, damit keine Lane je „fertig" ist.
5. Volle Suite lokal VOR jedem Push, ein Push je Task (findash ist privat -> Actions-Minuten).
6. Claim-Mechanik bleibt (`codex/<ID>`-Branch = vergeben); Lanes haben zusaetzlich disjunkte Listen.

## 5. Karl-Entscheidungen (offen — Lanes bauen das NICHT)
1. Merge des gestrigen Branches `claude/codex-limit-optimization-2u1g6h` (Queue-Dateien) in `main`/`master`
   — Claude konnte ihn heute nicht in den Arbeitsbranch mergen (Freigabe verweigert); die Lanes lesen ihn direkt.
2. Merge der 9 Codex-PRs und der 34 Claude-Branch-PRs (L1/L8 bereiten vor, Reihenfolge-Doku je Repo).
3. Re-Seal fuer S18 (JSDoc in versiegelten lib-Dateien) — Branch `claude/siegel-reseal-20260830` als Vorlage.
4. findash: Actions-Minuten des privaten Repos pruefen (Settings > Billing); ggf. Push-Frequenz der Lanes drosseln.
5. findash F59/F65: zaehlt geaenderte Fehler-/Proxy-Semantik als „Routen nur additiv"-Verstoss? (Drafts warten.)
6. Aus dem 24.09. unveraendert offen: Security-Header server.js; `ROUTE_METHODS` 404 statt 405; 9.836 prettier-Fehler;
   Anzeige-Inkonsistenzen; 44 tote shadcn-Dateien + 37 Dependencies; `lib/sub-profile.js` ohne Konsument;
   `config/decision-dates.json` 2027; CLIs mit Exit 0 bei unbekannter Option.

## 6. Risiken mit Zahlen
- **Actions-Minuten findash (privat):** CI-Log screener-data `test-gate` ~7 min je Lauf (public, unbegrenzt); findash
  Unit-Job (npm ci + web build + node --test + tsc + vitest) geschaetzt 6–10 min (~70 %). 4 findash-Lanen x ~10–15
  PRs/Tag + Nachzieh-Pushes -> 60–100 Laeufe -> 400–1.000 min/Tag; Free-Plan 2.000 min/Monat, Pro 3.000 (~85 %).
  Gegenmassnahme: ein Push je Task nach voller lokaler Suite, L8 misst stuendlich.
- **CPU/Disk auf Karls Rechner:** 8 Lanen x >= 8 Subagenten, jede Suite 4–10 min CPU-gebunden; findash `web/node_modules`
  je Worktree mehrere hundert MB. Worktree-Pool begrenzt das auf 40 Worktrees gesamt.
- **Codex-Wochenlimit:** Verbrauch ist Ziel (70 %). Bei Sperre: Lanes loggen Stand und versuchen stuendlich weiter.
- **Zeilenangaben in den Briefs veralten** nach jedem gemergten PR (vor allem index.tsx): Lanes messen neu (`grep -n`).
- **Stapel-PRs** zeigen bis zum Merge der Basis fremde Diffs; Karl merged in Reihenfolge der L1/L8-Doku.

## 7. Ausfuehrung durch Claude heute (erledigt)
- [x] Gestrigen Master identifiziert und ausgewertet (Session, Branch, Abschlussbericht, Audits, Queue-Stand, CI-Status aller 9 PRs).
- [x] #361/S26 lokal reproduziert (Worktree, `node tests/reconcile-smallcap.test.js`: 5 FAIL auf S26, gruen auf main), Ursache im Test-Stub belegt.
- [x] #360/S18: CI-Log ausgewertet (zwei Waechter), voller lokaler Gate-Lauf auf dem S18-Worktree (Umgebungs-Rote durch flachen Klon getrennt).
- [x] Lane-Zuschnitt aus den Audit-Kollisionstabellen abgeleitet; Dauerlauf-Protokoll geschrieben (beide Repos).
- [x] 8 Prompts fuer die Codex-App geschrieben (`codex-queue/prompts/2026-09-25/`).
- [x] Push auf `claude/sweet-pascal-w2u2xa` (beide Repos).
- [ ] NICHT moeglich: Codex-Subagenten aus der Cloud (kein `codex`, kein OpenAI-Zugang im Container). Wenn Karl das
      kuenftig will: Umgebungs-Variable mit dem Codex-/OpenAI-Zugang in den Cloud-Umgebungs-Einstellungen hinterlegen, nie im Chat.
- [ ] NICHT moeglich: Masterplan/WORKLOG im Vault (kein Zugriff aus der Cloud) — dieser Plan ist die Vorlage fuer den WORKLOG-Eintrag.

## 8. Akzeptanz fuer heute Abend (woran Karl es prueft)
- L1: #354–#362 gruen und auf aktuellem `main`; 12 PRs aus `claude/S*`; `MERGE-REIHENFOLGE-screener-data.md` aktuell.
- L8: 22 PRs aus `claude/F*`; `MERGE-REIHENFOLGE-findash.md`; Actions-Minuten-Stand im Log.
- L2–L7: je Lane Vault-Log mit Eintrag je Task, jeder PR im MELDEFORMAT mit REVIEW und Nachlauf-Kommentar,
  Wiedervorlage leer oder begruendet, keine Lane laenger als 5 Minuten untaetig.
- Alle: `gh pr list --state open` zeigt nur PRs, deren CI gruen ist oder die als DRAFT mit Diagnose markiert sind.
